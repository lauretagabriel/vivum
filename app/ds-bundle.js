/* @ds-bundle: {"format":4,"namespace":"VivumAIDesignSystem_b2be15","components":[{"name":"Badge","sourcePath":"components/core/Badge.jsx"},{"name":"Button","sourcePath":"components/core/Button.jsx"},{"name":"Divider","sourcePath":"components/core/Divider.jsx"},{"name":"Eyebrow","sourcePath":"components/core/Eyebrow.jsx"},{"name":"Icon","sourcePath":"components/core/Icon.jsx"},{"name":"IconButton","sourcePath":"components/core/IconButton.jsx"},{"name":"Panel","sourcePath":"components/core/Panel.jsx"},{"name":"DataTable","sourcePath":"components/data/DataTable.jsx"},{"name":"RatioBar","sourcePath":"components/data/RatioBar.jsx"},{"name":"Stat","sourcePath":"components/data/Stat.jsx"},{"name":"StatusDot","sourcePath":"components/data/StatusDot.jsx"},{"name":"EmptyState","sourcePath":"components/feedback/EmptyState.jsx"},{"name":"Modal","sourcePath":"components/feedback/Modal.jsx"},{"name":"Toast","sourcePath":"components/feedback/Toast.jsx"},{"name":"Checkbox","sourcePath":"components/forms/Checkbox.jsx"},{"name":"Input","sourcePath":"components/forms/Input.jsx"},{"name":"Select","sourcePath":"components/forms/Select.jsx"},{"name":"Switch","sourcePath":"components/forms/Switch.jsx"},{"name":"Footer","sourcePath":"components/navigation/Footer.jsx"},{"name":"NavBar","sourcePath":"components/navigation/NavBar.jsx"},{"name":"SideNav","sourcePath":"components/navigation/SideNav.jsx"},{"name":"Bezel","sourcePath":"components/surfaces/Bezel.jsx"},{"name":"GradientField","sourcePath":"components/surfaces/GradientField.jsx"},{"name":"SectionFrame","sourcePath":"components/surfaces/SectionFrame.jsx"},{"name":"Sheet","sourcePath":"components/surfaces/Sheet.jsx"},{"name":"VideoBackdrop","sourcePath":"components/surfaces/VideoBackdrop.jsx"},{"name":"HudOutline","sourcePath":"components/surfaces/hud-shapes.js"}],"sourceHashes":{"components/core/Badge.jsx":"d58a4e9117d7","components/core/Button.jsx":"77eefcaf2d46","components/core/Divider.jsx":"d2abe7b159a8","components/core/Eyebrow.jsx":"3ebef34f5fd5","components/core/Icon.jsx":"5d181c382960","components/core/IconButton.jsx":"6a2f3c6a1fe8","components/core/Panel.jsx":"33f39479f39e","components/core/icon-paths-thin.js":"b7e7f7ab7b73","components/core/icon-paths.js":"ede91acfc847","components/data/DataTable.jsx":"db9b96149c2f","components/data/RatioBar.jsx":"5976333e89f3","components/data/Stat.jsx":"170dc6f80da7","components/data/StatusDot.jsx":"d62219176ab7","components/feedback/EmptyState.jsx":"ed63d062016d","components/feedback/Modal.jsx":"ad6f09bef929","components/feedback/Toast.jsx":"67482c4cf0f7","components/forms/Checkbox.jsx":"d97b5efb5fa7","components/forms/Input.jsx":"afa47bc6aeb4","components/forms/Select.jsx":"43b3ad40cb47","components/forms/Switch.jsx":"bf5214bfa0c9","components/navigation/Footer.jsx":"0853c695ccf3","components/navigation/NavBar.jsx":"cb62094d9903","components/navigation/SideNav.jsx":"f5fc576169de","components/surfaces/Bezel.jsx":"8ef5cd3a51df","components/surfaces/GradientField.jsx":"f58788173cd5","components/surfaces/SectionFrame.jsx":"791fa5eaa6bc","components/surfaces/Sheet.jsx":"53a35045febe","components/surfaces/VideoBackdrop.jsx":"f5201b93b411","components/surfaces/hud-shapes.js":"a20a5430811a","components/surfaces/plate-loop.js":"a553dd865301","slides/Slides.jsx":"7e36f6c6cf48","ui_kits/console/Console.jsx":"ec4deba8db39","ui_kits/console/FleetScreen.jsx":"aa81deff38d8","ui_kits/console/LoginScreen.jsx":"a0cd0c9e2a16","ui_kits/console/ModelsScreen.jsx":"b81ac67ec319","ui_kits/console/UnitScreen.jsx":"c580a68d83b9","ui_kits/website/AboutScreen.jsx":"e95f82875741","ui_kits/website/AboutSections.jsx":"5f0d4c1e0e69","ui_kits/website/AutonomyDomains.jsx":"d78a3ae31d26","ui_kits/website/AutonomyScreen.jsx":"88e9fcf767b2","ui_kits/website/BriefingScreen.jsx":"1900e7cc364f","ui_kits/website/ContactForm.jsx":"dd795c5484ca","ui_kits/website/ContactScreen.jsx":"1917f8ff605a","ui_kits/website/DomainInstrument.jsx":"7d562e751b20","ui_kits/website/HomeCapabilities.jsx":"24bd0d47caf2","ui_kits/website/HomeComparison.jsx":"4d2107913c5b","ui_kits/website/HomeScience.jsx":"2a8fc93b3888","ui_kits/website/HomeScreen.jsx":"91a51824af52","ui_kits/website/PlatformScreen.jsx":"209b6c54ed4d","ui_kits/website/Preloader.jsx":"3c79359f9037","ui_kits/website/ResearchScreen.jsx":"2afcda82262b","ui_kits/website/SeamAnimation.jsx":"6378cee451da","ui_kits/website/Site.jsx":"b3169d667d05","ui_kits/website/contact-countries.js":"8ef60c4264e7","ui_kits/website/tweaks-panel.jsx":"d259e3a86f73","ui_kits/website/vv-motion.jsx":"5944cbafac0a"},"inlinedExternals":[],"unexposedExports":[{"name":"hudGeometry","sourcePath":"components/surfaces/hud-shapes.js"},{"name":"iconGlyphs","sourcePath":"components/core/icon-paths.js"},{"name":"iconGlyphsThin","sourcePath":"components/core/icon-paths-thin.js"},{"name":"useElementSize","sourcePath":"components/surfaces/hud-shapes.js"},{"name":"usePlateLoop","sourcePath":"components/surfaces/plate-loop.js"}]} */

(() => {

const __ds_ns = (window.VivumAIDesignSystem_b2be15 = window.VivumAIDesignSystem_b2be15 || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// components/core/Badge.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const TONES = {
  blue: 'var(--vv-blue)',
  amber: 'var(--vv-amber)',
  graphite: 'var(--vv-graphite)',
  ice: 'var(--vv-ice)'
};
function Badge({
  children,
  tone = 'blue',
  variant = 'outline',
  mono,
  style,
  ...rest
}) {
  const c = TONES[tone] || TONES.blue;
  const fills = {
    outline: {
      background: 'transparent',
      border: `1px solid ${c}`,
      color: c
    },
    solid: {
      background: c,
      border: `1px solid ${c}`,
      color: 'var(--vv-deep-navy)'
    },
    quiet: {
      background: 'var(--vv-ice-08)',
      border: '1px solid transparent',
      color: c
    }
  };
  return /*#__PURE__*/React.createElement("span", _extends({
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      height: 22,
      padding: '0 8px',
      borderRadius: 'var(--radius-sm)',
      fontFamily: mono ? 'var(--font-mono)' : 'var(--font-sans)',
      fontSize: 11,
      fontWeight: 500,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      whiteSpace: 'nowrap',
      ...(fills[variant] || fills.outline),
      ...style
    }
  }, rest), children);
}
Object.assign(__ds_scope, { Badge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Badge.jsx", error: String((e && e.message) || e) }); }

// components/core/Divider.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Divider({
  orientation = 'horizontal',
  tone = 'hairline',
  spacing = 0,
  style,
  ...rest
}) {
  const bg = tone === 'accent' ? 'var(--vv-amber)' : tone === 'fade' ? 'var(--grad-rule)' : tone === 'strong' ? 'var(--line-strong)' : 'var(--line-hairline)';
  const vertical = orientation === 'vertical';
  return /*#__PURE__*/React.createElement("span", _extends({
    role: "separator",
    style: {
      display: 'block',
      flex: 'none',
      width: vertical ? 1 : '100%',
      height: vertical ? '100%' : 1,
      background: bg,
      margin: vertical ? `0 ${spacing}px` : `${spacing}px 0`,
      ...style
    }
  }, rest));
}
Object.assign(__ds_scope, { Divider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Divider.jsx", error: String((e && e.message) || e) }); }

// components/core/Eyebrow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const SIZES = {
  sm: {
    font: 11,
    rule: 32,
    gap: 12
  },
  md: {
    font: 13,
    rule: 56,
    gap: 16
  },
  slide: {
    font: 24,
    rule: 56,
    gap: 20
  }
};
function Eyebrow({
  children,
  size = 'md',
  rule = true,
  ruleWeight,
  labelTone = 'graphite',
  style,
  ...rest
}) {
  const s = SIZES[size] || SIZES.md;
  /* The rule is always amber — it is the brand's marker, and one accent colour across every
     section header is what makes the eyebrow read as a system element rather than a label. */
  /* Graphite is right on the flat canvas and wrong on a lit translucent fill, where it
     falls to about 1.8:1 — labelTone="ice" is for those surfaces (GlassSlab). */
  const labelColor = labelTone === 'ice' ? 'var(--vv-ice-72)' : 'var(--text-label)';
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: s.gap,
      ...style
    }
  }, rest), rule ? /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      width: s.rule,
      height: ruleWeight != null ? ruleWeight : size === 'slide' ? 4 : 1,
      background: 'var(--vv-amber)',
      flex: 'none'
    }
  }) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-sans)',
      fontSize: s.font,
      fontWeight: 600,
      letterSpacing: 'var(--track-label)',
      textTransform: 'uppercase',
      color: labelColor,
      lineHeight: 1.2
    }
  }, children));
}
Object.assign(__ds_scope, { Eyebrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Eyebrow.jsx", error: String((e && e.message) || e) }); }

// components/core/Panel.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const VARIANTS = {
  panel: {
    background: 'var(--surface-panel)',
    border: '1px solid transparent',
    color: 'var(--text-body)'
  },
  quiet: {
    background: 'var(--surface-panel-quiet)',
    border: '1px solid var(--line-hairline)',
    color: 'var(--text-body)'
  },
  outline: {
    background: 'transparent',
    border: '1px solid var(--line-hairline)',
    color: 'var(--text-body)'
  },
  raised: {
    background: 'var(--surface-raised)',
    border: '1px solid var(--line-hairline)',
    color: 'var(--text-body)'
  },
  sheet: {
    background: 'var(--surface-sheet)',
    border: '1px solid transparent',
    color: 'var(--text-body-on-light)'
  }
};
function Panel({
  children,
  variant = 'panel',
  pad = 24,
  title,
  meta,
  actions,
  style,
  ...rest
}) {
  const v = VARIANTS[variant] || VARIANTS.panel;
  return /*#__PURE__*/React.createElement("section", _extends({
    style: {
      borderRadius: 'var(--radius-sm)',
      boxSizing: 'border-box',
      ...v,
      ...style
    }
  }, rest), title || actions ? /*#__PURE__*/React.createElement("header", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 16,
      padding: `${Math.max(14, pad - 6)}px ${pad}px`,
      borderBottom: `1px solid ${variant === 'sheet' ? 'var(--line-on-light)' : 'var(--line-hairline)'}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4,
      minWidth: 0
    }
  }, title ? /*#__PURE__*/React.createElement("h4", {
    style: {
      margin: 0,
      fontSize: 15,
      fontWeight: 600,
      letterSpacing: '-0.01em',
      color: variant === 'sheet' ? 'var(--text-heading-on-light)' : 'var(--text-heading)'
    }
  }, title) : null, meta ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--text-caption)',
      letterSpacing: '0.02em'
    }
  }, meta) : null), actions ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      flex: 'none'
    }
  }, actions) : null) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: pad
    }
  }, children));
}
Object.assign(__ds_scope, { Panel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Panel.jsx", error: String((e && e.message) || e) }); }

// components/core/icon-paths-thin.js
try { (() => {
/* FontAwesome Thin glyph data, inlined — same key set as icon-paths.js (the Light weight), so
   Icon.jsx can swap between the two by name with no call-site changes. Selected via the global
   icon-style tweak (see setIconWeight in Icon.jsx). */
const iconGlyphsThin = {
  'arrows-rotate': ['0 0 640 640', 'M560 72C560 67.6 563.6 64 568 64C572.4 64 576 67.6 576 72L576 217.3C576 219.4 575.2 221.5 573.7 223C572.2 224.5 570.2 225.3 568 225.3L424 225.3C419.6 225.3 416 221.7 416 217.3C416 212.9 419.6 209.3 424 209.3L548.7 209.3L489.7 150.3C396 56.6 244 56.6 150.3 150.3C105.4 195.2 82 253.4 80.1 312.3C80 316.7 76.3 320.2 71.8 320C67.3 319.8 64 316.2 64.1 311.8C66.1 249 91.1 186.9 139 139C239 39 401 39 501 139L560 198L560 72zM80 568C80 572.4 76.4 576 72 576C67.6 576 64 572.4 64 568L64 422.7C64 420.6 64.8 418.5 66.3 417C67.8 415.5 69.8 414.7 72 414.7L216 414.7C220.4 414.7 224 418.3 224 422.7C224 427.1 220.4 430.7 216 430.7L91.3 430.7L150.3 489.7C244 583.4 396 583.4 489.7 489.7C534.6 444.8 558 386.6 559.9 327.7C560 323.3 563.7 319.8 568.2 320C572.7 320.2 576.1 323.8 575.9 328.3C573.9 391 548.9 453.2 501.1 501.1C401.1 601.1 239 601.1 139.1 501.1L80.1 442.1L80.1 568.1z'],
  'clock': ['0 0 640 640', 'M560 320C560 452.5 452.5 560 320 560C187.5 560 80 452.5 80 320C80 187.5 187.5 80 320 80C452.5 80 560 187.5 560 320zM64 320C64 461.4 178.6 576 320 576C461.4 576 576 461.4 576 320C576 178.6 461.4 64 320 64C178.6 64 64 178.6 64 320zM312 168L312 320C312 322.7 313.3 325.2 315.6 326.7L411.6 390.7C415.3 393.2 420.2 392.2 422.7 388.5C425.2 384.8 424.2 379.9 420.5 377.4L328 315.7L328 168C328 163.6 324.4 160 320 160C315.6 160 312 163.6 312 168z'],
  'circle-nodes': ['0 0 640 640', 'M528 144C528 108.7 499.3 80 464 80C428.7 80 400 108.7 400 144C400 179.3 428.7 208 464 208C499.3 208 528 179.3 528 144zM544 144C544 188.2 508.2 224 464 224C461.9 224 459.7 223.9 457.6 223.8L422.1 419.1C455.5 428.7 480 459.5 480 496C480 540.2 444.2 576 400 576C355.8 576 320 540.2 320 496C320 478.7 325.5 462.7 334.8 449.6L198.6 330.4C184.3 343.8 165.1 352 144 352C99.8 352 64 316.2 64 272C64 227.8 99.8 192 144 192C174.8 192 201.6 209.4 214.9 235L387.1 166.1C385.1 159.1 384 151.6 384 144C384 99.8 419.8 64 464 64C508.2 64 544 99.8 544 144zM393 181L220.9 249.9C222.9 256.9 224 264.4 224 272C224 289.3 218.5 305.3 209.2 318.4L345.4 437.6C359.7 424.2 378.9 416 400 416C402.1 416 404.3 416.1 406.4 416.2L441.9 220.9C420.7 214.8 403.1 200.2 393.1 181zM464 496C464 460.7 435.3 432 400 432C364.7 432 336 460.7 336 496C336 531.3 364.7 560 400 560C435.3 560 464 531.3 464 496zM144 336C179.3 336 208 307.3 208 272C208 236.7 179.3 208 144 208C108.7 208 80 236.7 80 272C80 307.3 108.7 336 144 336z'],
  'memory': ['0 0 640 640', 'M64 184C64 153.1 89.1 128 120 128L520 128C550.9 128 576 153.1 576 184L576 200.6C576 203.5 574.5 206.1 572 207.5C555.2 217.2 544 235.3 544 256C544 276.7 555.2 294.8 572 304.5C574.5 305.9 576 308.6 576 311.4L576 480C576 497.7 561.7 512 544 512L96 512C78.3 512 64 497.7 64 480L64 311.4C64 308.5 65.5 305.9 68 304.5C84.8 294.8 96 276.7 96 256C96 235.3 84.8 217.2 68 207.5C65.5 206.1 64 203.4 64 200.6L64 184zM480 456L480 496L544 496C552.8 496 560 488.8 560 480L560 384L80 384L80 480C80 488.8 87.2 496 96 496L160 496L160 456C160 451.6 163.6 448 168 448C172.4 448 176 451.6 176 456L176 496L264 496L264 456C264 451.6 267.6 448 272 448C276.4 448 280 451.6 280 456L280 496L360 496L360 456C360 451.6 363.6 448 368 448C372.4 448 376 451.6 376 456L376 496L464 496L464 456C464 451.6 467.6 448 472 448C476.4 448 480 451.6 480 456zM560 315.9C540.7 303 528 281 528 256C528 231 540.7 209 560 196.1L560 184C560 161.9 542.1 144 520 144L120 144C97.9 144 80 161.9 80 184L80 196.1C99.3 209 112 231 112 256C112 281 99.3 303 80 315.9L80 368L560 368L560 315.9zM192 208C183.2 208 176 215.2 176 224L176 288C176 296.8 183.2 304 192 304C200.8 304 208 296.8 208 288L208 224C208 215.2 200.8 208 192 208zM320 208C311.2 208 304 215.2 304 224L304 288C304 296.8 311.2 304 320 304C328.8 304 336 296.8 336 288L336 224C336 215.2 328.8 208 320 208zM448 208C439.2 208 432 215.2 432 224L432 288C432 296.8 439.2 304 448 304C456.8 304 464 296.8 464 288L464 224C464 215.2 456.8 208 448 208zM192 192C209.7 192 224 206.3 224 224L224 288C224 305.7 209.7 320 192 320C174.3 320 160 305.7 160 288L160 224C160 206.3 174.3 192 192 192zM320 192C337.7 192 352 206.3 352 224L352 288C352 305.7 337.7 320 320 320C302.3 320 288 305.7 288 288L288 224C288 206.3 302.3 192 320 192zM448 192C465.7 192 480 206.3 480 224L480 288C480 305.7 465.7 320 448 320C430.3 320 416 305.7 416 288L416 224C416 206.3 430.3 192 448 192z'],
  'dna': ['0 0 640 640', 'M504 64C508.4 64 512 67.6 512 72C512 132.3 487.9 179.6 452.5 219.9C419.6 257.3 376.6 289.1 333.7 320C376.6 350.9 419.6 382.7 452.5 420.1C487.9 460.4 512 507.7 512 568C512 572.4 508.4 576 504 576C499.6 576 496 572.4 496 568C496 553.9 494.6 540.6 491.9 528L148.1 528C145.4 540.6 144 553.9 144 568C144 572.4 140.4 576 136 576C131.6 576 128 572.4 128 568C128 507.7 152.1 460.4 187.5 420.1C220.4 382.7 263.4 350.9 306.3 320C263.4 289.1 220.4 257.3 187.5 219.9C152.1 179.6 128 132.3 128 72C128 67.6 131.6 64 136 64C140.4 64 144 67.6 144 72C144 86.1 145.4 99.4 148.1 112L492 112C494.6 99.4 496.1 86.1 496.1 72C496.1 67.6 499.7 64 504.1 64zM152.2 128C161.3 157.9 177.6 184.1 198.5 208.1C199 208 199.5 208 200 208L440 208C440.5 208 441 208 441.5 208.1C462.4 184.1 478.7 157.9 487.8 128L152.2 128zM213.2 224C243.8 255 281.4 282.4 320 310.1C358.6 282.4 396.2 254.9 426.8 224L213.2 224zM320 329.9C281.4 357.6 243.8 385 213.2 416L426.8 416C396.2 385 358.6 357.6 320 329.9zM441.5 431.9C441 432 440.5 432 440 432L200 432C199.5 432 199 432 198.5 431.9C177.6 455.9 161.3 482.1 152.2 512L487.9 512C478.8 482.1 462.5 455.9 441.6 431.9z'],
  'gauge-high': ['0 0 640 640', 'M320 80C452.5 80 560 187.5 560 320C560 452.5 452.5 560 320 560C187.5 560 80 452.5 80 320C80 187.5 187.5 80 320 80zM320 576C461.4 576 576 461.4 576 320C576 178.6 461.4 64 320 64C178.6 64 64 178.6 64 320C64 461.4 178.6 576 320 576zM336 144C336 135.2 328.8 128 320 128C311.2 128 304 135.2 304 144C304 152.8 311.2 160 320 160C328.8 160 336 152.8 336 144zM272 416C272 389.5 293.5 368 320 368C346.5 368 368 389.5 368 416C368 442.5 346.5 464 320 464C293.5 464 272 442.5 272 416zM384 416C384 393.8 372.7 374.3 355.6 362.8L439.2 195.6C441.2 191.6 439.6 186.8 435.6 184.9C431.6 183 426.8 184.5 424.9 188.5L341.2 355.6C334.6 353.3 327.4 352 320 352C284.7 352 256 380.7 256 416C256 451.3 284.7 480 320 480C355.3 480 384 451.3 384 416zM224 192C224 183.2 216.8 176 208 176C199.2 176 192 183.2 192 192C192 200.8 199.2 208 208 208C216.8 208 224 200.8 224 192zM160 320C168.8 320 176 312.8 176 304C176 295.2 168.8 288 160 288C151.2 288 144 295.2 144 304C144 312.8 151.2 320 160 320zM496 304C496 295.2 488.8 288 480 288C471.2 288 464 295.2 464 304C464 312.8 471.2 320 480 320C488.8 320 496 312.8 496 304z'],
  'microchip': ['0 0 640 640', 'M224 72C224 67.6 220.4 64 216 64C211.6 64 208 67.6 208 72L208 128L192 128C156.7 128 128 156.7 128 192L128 208L72 208C67.6 208 64 211.6 64 216C64 220.4 67.6 224 72 224L128 224L128 312L72 312C67.6 312 64 315.6 64 320C64 324.4 67.6 328 72 328L128 328L128 416L72 416C67.6 416 64 419.6 64 424C64 428.4 67.6 432 72 432L128 432L128 448C128 483.3 156.7 512 192 512L208 512L208 568C208 572.4 211.6 576 216 576C220.4 576 224 572.4 224 568L224 512L312 512L312 568C312 572.4 315.6 576 320 576C324.4 576 328 572.4 328 568L328 512L416 512L416 568C416 572.4 419.6 576 424 576C428.4 576 432 572.4 432 568L432 512L448 512C483.3 512 512 483.3 512 448L512 432L568 432C572.4 432 576 428.4 576 424C576 419.6 572.4 416 568 416L512 416L512 328L568 328C572.4 328 576 324.4 576 320C576 315.6 572.4 312 568 312L512 312L512 224L568 224C572.4 224 576 220.4 576 216C576 211.6 572.4 208 568 208L512 208L512 192C512 156.7 483.3 128 448 128L432 128L432 72C432 67.6 428.4 64 424 64C419.6 64 416 67.6 416 72L416 128L328 128L328 72C328 67.6 324.4 64 320 64C315.6 64 312 67.6 312 72L312 128L224 128L224 72zM320 144L448 144C474.5 144 496 165.5 496 192L496 448C496 474.5 474.5 496 448 496L192 496C165.5 496 144 474.5 144 448L144 192C144 165.5 165.5 144 192 144L320 144zM416 240L416 400C416 408.8 408.8 416 400 416L240 416C231.2 416 224 408.8 224 400L224 240C224 231.2 231.2 224 240 224L400 224C408.8 224 416 231.2 416 240zM240 208C222.3 208 208 222.3 208 240L208 400C208 417.7 222.3 432 240 432L400 432C417.7 432 432 417.7 432 400L432 240C432 222.3 417.7 208 400 208L240 208z'],
  'drone': ['0 0 640 640', 'M560 464C560 411 517 368 464 368L462.9 368C463.6 362.8 464 357.4 464 352C525.9 352 576 402.1 576 464C576 525.9 525.9 576 464 576C402.1 576 352 525.9 352 464C357.4 464 362.8 463.6 368 462.9L368 464C368 517 411 560 464 560C517 560 560 517 560 464zM462.9 272L464 272C517 272 560 229 560 176C560 123 517 80 464 80C411 80 368 123 368 176L368 177.1C362.8 176.4 357.4 176 352 176C352 114.1 402.1 64 464 64C525.9 64 576 114.1 576 176C576 237.9 525.9 288 464 288C464 282.6 463.6 277.2 462.9 272zM288 176C282.6 176 277.2 176.4 272 177.1L272 176C272 123 229 80 176 80C123 80 80 123 80 176C80 229 123 272 176 272L177.1 272C176.4 277.2 176 282.6 176 288C114.1 288 64 237.9 64 176C64 114.1 114.1 64 176 64C237.9 64 288 114.1 288 176zM176 352C176 357.4 176.4 362.8 177.1 368L176 368C123 368 80 411 80 464C80 517 123 560 176 560C229 560 272 517 272 464L272 462.9C277.2 463.6 282.6 464 288 464C288 525.9 237.9 576 176 576C114.1 576 64 525.9 64 464C64 402.1 114.1 352 176 352zM288 240C261.5 240 240 261.5 240 288L240 352C240 378.5 261.5 400 288 400L352 400C378.5 400 400 378.5 400 352L400 288C400 261.5 378.5 240 352 240L288 240zM224 288C224 273.2 229 259.6 237.4 248.8L170.3 181.7C167.2 178.6 167.2 173.5 170.3 170.4C173.4 167.3 178.5 167.3 181.6 170.4L248.7 237.5C259.6 229 273.2 224 288 224L352 224C366.8 224 380.4 229 391.2 237.4L458.3 170.3C461.4 167.2 466.5 167.2 469.6 170.3C472.7 173.4 472.7 178.5 469.6 181.6L402.5 248.7C411 259.6 416 273.2 416 288L416 352C416 366.8 411 380.4 402.6 391.2L469.7 458.3C472.8 461.4 472.8 466.5 469.7 469.6C466.6 472.7 461.5 472.7 458.4 469.6L391.3 402.5C380.4 411 366.8 416 352 416L288 416C273.2 416 259.6 411 248.8 402.6L181.7 469.7C178.6 472.8 173.5 472.8 170.4 469.7C167.3 466.6 167.3 461.5 170.4 458.4L237.5 391.3C229 380.4 224 366.8 224 352L224 288z'],
  'arrow-right': ['0 0 640 640', 'M573.7 325.7C576.8 322.6 576.8 317.5 573.7 314.4L397.7 138.4C394.6 135.3 389.5 135.3 386.4 138.4C383.3 141.5 383.3 146.6 386.4 149.7L548.7 312L72 312C67.6 312 64 315.6 64 320C64 324.4 67.6 328 72 328L548.7 328L386.4 490.3C383.3 493.4 383.3 498.5 386.4 501.6C389.5 504.7 394.6 504.7 397.7 501.6L573.7 325.6z'],
  'arrow-left': ['0 0 640 640', 'M66.3 314.3C63.2 317.4 63.2 322.5 66.3 325.6L242.3 501.6C245.4 504.7 250.5 504.7 253.6 501.6C256.7 498.5 256.7 493.4 253.6 490.3L91.3 328L568 328C572.4 328 576 324.4 576 320C576 315.6 572.4 312 568 312L91.3 312L253.7 149.7C256.8 146.6 256.8 141.5 253.7 138.4C250.6 135.3 245.5 135.3 242.4 138.4L66.4 314.4z'],
  'arrow-up-right': ['0 0 640 640', 'M504 128C508.4 128 512 131.6 512 136L512 408C512 412.4 508.4 416 504 416C499.6 416 496 412.4 496 408L496 155.3L141.7 509.7C138.6 512.8 133.5 512.8 130.4 509.7C127.3 506.6 127.3 501.5 130.4 498.4L484.7 144L232 144C227.6 144 224 140.4 224 136C224 131.6 227.6 128 232 128L504 128z'],
  'arrow-down-to-line': ['0 0 640 640', 'M504 576C508.4 576 512 572.4 512 568C512 563.6 508.4 560 504 560L136 560C131.6 560 128 563.6 128 568C128 572.4 131.6 576 136 576L504 576zM314.4 445.7C317.5 448.8 322.6 448.8 325.7 445.7L477.7 293.7C480.8 290.6 480.8 285.5 477.7 282.4C474.6 279.3 469.5 279.3 466.4 282.4L328 420.7L328 72C328 67.6 324.4 64 320 64C315.6 64 312 67.6 312 72L312 420.7L173.7 282.4C170.6 279.3 165.5 279.3 162.4 282.4C159.3 285.5 159.3 290.6 162.4 293.7L314.4 445.7z'],
  'arrow-up-from-line': ['0 0 640 640', 'M325.7 66.3C322.6 63.2 317.5 63.2 314.4 66.3L162.4 218.3C159.3 221.4 159.3 226.5 162.4 229.6C165.5 232.7 170.6 232.7 173.7 229.6L312 91.3L312 440C312 444.4 315.6 448 320 448C324.4 448 328 444.4 328 440L328 91.3L466.3 229.6C469.4 232.7 474.5 232.7 477.6 229.6C480.7 226.5 480.7 221.4 477.6 218.3L325.6 66.3zM136 560C131.6 560 128 563.6 128 568C128 572.4 131.6 576 136 576L504 576C508.4 576 512 572.4 512 568C512 563.6 508.4 560 504 560L136 560z'],
  'pause': ['0 0 640 640', 'M176 112C158.3 112 144 126.3 144 144L144 496C144 513.7 158.3 528 176 528L240 528C257.7 528 272 513.7 272 496L272 144C272 126.3 257.7 112 240 112L176 112zM128 144C128 117.5 149.5 96 176 96L240 96C266.5 96 288 117.5 288 144L288 496C288 522.5 266.5 544 240 544L176 544C149.5 544 128 522.5 128 496L128 144zM400 112C382.3 112 368 126.3 368 144L368 496C368 513.7 382.3 528 400 528L464 528C481.7 528 496 513.7 496 496L496 144C496 126.3 481.7 112 464 112L400 112zM352 144C352 117.5 373.5 96 400 96L464 96C490.5 96 512 117.5 512 144L512 496C512 522.5 490.5 544 464 544L400 544C373.5 544 352 522.5 352 496L352 144z'],
  'expand': ['0 0 640 640', 'M248 96C252.4 96 256 99.6 256 104C256 108.4 252.4 112 248 112L112 112L112 248C112 252.4 108.4 256 104 256C99.6 256 96 252.4 96 248L96 104C96 99.6 99.6 96 104 96L248 96zM96 392C96 387.6 99.6 384 104 384C108.4 384 112 387.6 112 392L112 528L248 528C252.4 528 256 531.6 256 536C256 540.4 252.4 544 248 544L104 544C99.6 544 96 540.4 96 536L96 392zM392 96L536 96C540.4 96 544 99.6 544 104L544 248C544 252.4 540.4 256 536 256C531.6 256 528 252.4 528 248L528 112L392 112C387.6 112 384 108.4 384 104C384 99.6 387.6 96 392 96zM544 392L544 536C544 540.4 540.4 544 536 544L392 544C387.6 544 384 540.4 384 536C384 531.6 387.6 528 392 528L528 528L528 392C528 387.6 531.6 384 536 384C540.4 384 544 387.6 544 392z'],
  'triangle-exclamation': ['0 0 640 640', 'M320.5 64C335.2 64 348.7 72.1 355.7 85L571.7 485C578.4 497.4 578.1 512.4 570.9 524.5C563.7 536.6 550.6 544 536.5 544L104.5 544C90.4 544 77.4 536.6 70.2 524.5C63 512.4 62.7 497.4 69.4 485L285.4 85C292.4 72.1 305.9 64 320.6 64zM320.5 81.6C312.3 81.6 304.7 86.1 300.8 93.4L84.8 493.4C81.1 500.3 81.2 508.8 85.3 515.5C89.4 522.2 96.7 526.4 104.5 526.4L536.5 526.4C544.4 526.4 551.7 522.2 555.7 515.5C559.7 508.8 559.9 500.3 556.2 493.4L340.2 93.4C336.3 86.2 328.7 81.7 320.5 81.6zM320.5 472C311.7 472 304.5 464.8 304.5 456C304.5 447.2 311.7 440 320.5 440C329.3 440 336.5 447.2 336.5 456C336.5 464.8 329.3 472 320.5 472zM323.4 232.3C330 233.7 334.9 239.7 334.6 246.7L328.5 392.3C328.3 396.6 324.8 400 320.5 400C316.2 400 312.7 396.6 312.5 392.3L306.4 246.7C306.1 238.7 312.5 232 320.5 232L323.4 232.3z'],
  'arrow-rotate-left': ['0 0 640 640', 'M320 64C461.4 64 576 178.6 576 320C576 461.4 461.4 576 320 576C217 576 128.2 515.2 87.6 427.5C85.7 423.5 87.5 418.7 91.5 416.9C95.5 415.1 100.3 416.8 102.1 420.8C140.2 503 223.5 560 320 560C452.5 560 560 452.5 560 320C560 187.5 452.5 80 320 80C255.5 80 193.7 105.6 148.1 151.2L91.3 208L216 208C220.4 208 224 211.6 224 216C224 220.4 220.4 224 216 224L72 224C67.6 224 64 220.4 64 216L64 72C64 67.6 67.6 64 72 64C76.4 64 80 67.6 80 72L80 196.7L136.8 139.9C185.4 91.3 251.3 64 320 64z'],
  'radar': ['0 0 640 640', 'M404.7 224C382.1 204.1 352.5 192 320 192C249.3 192 192 249.3 192 320C192 390.7 249.3 448 320 448C390.7 448 448 390.7 448 320L464 320C464 399.5 399.5 464 320 464C240.5 464 176 399.5 176 320C176 240.5 240.5 176 320 176C356.9 176 390.5 189.9 416 212.7L484 144.7C441.1 104.6 383.4 80 320 80C187.5 80 80 187.5 80 320C80 452.5 187.5 560 320 560C452.5 560 560 452.5 560 320L576 320C576 461.4 461.4 576 320 576C178.6 576 64 461.4 64 320C64 178.6 178.6 64 320 64C387.8 64 449.5 90.4 495.3 133.4L538.4 90.3C541.5 87.2 546.6 87.2 549.7 90.3C552.8 93.4 552.8 98.5 549.7 101.6L325.7 325.6C322.6 328.7 317.5 328.7 314.4 325.6C311.3 322.5 311.3 317.4 314.4 314.3L404.7 224z'],
  'sitemap': ['0 0 640 640', 'M352 112C360.8 112 368 119.2 368 128L368 192C368 200.8 360.8 208 352 208L288 208C279.2 208 272 200.8 272 192L272 128C272 119.2 279.2 112 288 112L352 112zM288 96C270.3 96 256 110.3 256 128L256 192C256 209.7 270.3 224 288 224L312 224L312 304L176 304C145.1 304 120 329.1 120 360L120 416L96 416C78.3 416 64 430.3 64 448L64 512C64 529.7 78.3 544 96 544L160 544C177.7 544 192 529.7 192 512L192 448C192 430.3 177.7 416 160 416L136 416L136 360C136 337.9 153.9 320 176 320L312 320L312 416L288 416C270.3 416 256 430.3 256 448L256 512C256 529.7 270.3 544 288 544L352 544C369.7 544 384 529.7 384 512L384 448C384 430.3 369.7 416 352 416L328 416L328 320L464 320C486.1 320 504 337.9 504 360L504 416L480 416C462.3 416 448 430.3 448 448L448 512C448 529.7 462.3 544 480 544L544 544C561.7 544 576 529.7 576 512L576 448C576 430.3 561.7 416 544 416L520 416L520 360C520 329.1 494.9 304 464 304L328 304L328 224L352 224C369.7 224 384 209.7 384 192L384 128C384 110.3 369.7 96 352 96L288 96zM96 432L160 432C168.8 432 176 439.2 176 448L176 512C176 520.8 168.8 528 160 528L96 528C87.2 528 80 520.8 80 512L80 448C80 439.2 87.2 432 96 432zM288 432L352 432C360.8 432 368 439.2 368 448L368 512C368 520.8 360.8 528 352 528L288 528C279.2 528 272 520.8 272 512L272 448C272 439.2 279.2 432 288 432zM480 432L544 432C552.8 432 560 439.2 560 448L560 512C560 520.8 552.8 528 544 528L480 528C471.2 528 464 520.8 464 512L464 448C464 439.2 471.2 432 480 432z'],
  'chart-line': ['0 0 640 640', 'M80 104C80 99.6 76.4 96 72 96C67.6 96 64 99.6 64 104L64 464C64 508.2 99.8 544 144 544L568 544C572.4 544 576 540.4 576 536C576 531.6 572.4 528 568 528L144 528C108.7 528 80 499.3 80 464L80 104zM541.7 205.7C544.8 202.6 544.8 197.5 541.7 194.4C538.6 191.3 533.5 191.3 530.4 194.4L384 340.7L293.7 250.3C290.6 247.2 285.5 247.2 282.4 250.3L154.4 378.3C151.3 381.4 151.3 386.5 154.4 389.6C157.5 392.7 162.6 392.7 165.7 389.6L288 267.3L378.3 357.7C381.4 360.8 386.5 360.8 389.6 357.7L541.6 205.7z'],
  'bell': ['0 0 640 640', 'M320 64C315.6 64 312 67.6 312 72L312 96.2C227.4 100.4 160 170.3 160 256L160 273.4C160 314.2 147.9 354.2 125.2 388.2L102.2 422.7C98.2 428.8 96 435.9 96 443.2C96 463.5 112.5 480 132.8 480L507.1 480C527.4 480 543.9 463.5 543.9 443.2C543.9 435.9 541.7 428.8 537.7 422.8L514.6 388.2C491.9 354.2 479.8 314.3 479.8 273.4L479.8 256C479.8 170.3 412.4 100.4 327.8 96.2L328 72C328 67.6 324.4 64 320 64zM176 256C176 176.5 240.5 112 320 112C399.5 112 464 176.5 464 256L464 273.4C464 317.4 477 360.4 501.4 397L524.5 431.6C526.8 435 528 439 528 443.2C528 454.7 518.7 464 507.2 464L132.8 464C121.3 464 112 454.7 112 443.2C112 439.1 113.2 435.1 115.5 431.6L138.6 397C163 360.4 176 317.4 176 273.4L176 256zM252.1 528C262 556 288.7 576 320 576C351.3 576 378 556 387.9 528L370.6 528C361.6 546.9 342.3 560 320 560C297.7 560 278.4 546.9 269.4 528L252.1 528z'],
  'gear': ['0 0 640 640', 'M251 158.2C255.8 156.1 259.3 151.8 260.4 146.7L274.8 76.8C276.3 69.3 282.8 64 290.4 64L350.2 64C357.8 64 364.3 69.3 365.9 76.8L380.3 146.7C381.4 151.8 384.9 156.1 389.7 158.2C402.6 163.7 414.7 170.8 425.8 179.1C430 182.2 435.5 183.1 440.4 181.5L508.2 159C515.4 156.6 523.3 159.6 527.1 166.2L557 217.9C560.8 224.5 559.4 232.8 553.8 237.9L500.4 285.4C496.5 288.9 494.5 294 495.1 299.2C495.9 306 496.3 313 496.3 320C496.3 327 495.9 334 495.1 340.8C494.5 346 496.5 351.2 500.4 354.6L553.8 402.1C559.5 407.1 560.8 415.5 557 422.1L527.1 473.9C523.3 480.5 515.4 483.5 508.2 481.1L440.4 458.6C435.4 456.9 430 457.8 425.8 461C414.7 469.3 402.6 476.3 389.7 481.9C384.9 484 381.4 488.3 380.3 493.4L365.9 563.3C364.4 570.7 357.8 576.1 350.2 576.1L290.4 576.1C282.8 576.1 276.3 570.8 274.7 563.3L260.3 493.4C259.2 488.3 255.7 484 250.9 481.9C238 476.4 225.9 469.3 214.8 461C210.6 457.9 205.1 457 200.2 458.6L132.5 481.1C125.3 483.5 117.4 480.5 113.6 473.9L83.7 422.1C79.9 415.5 81.3 407.2 86.9 402.1L140.3 354.6C144.2 351.1 146.2 346 145.6 340.8C144.8 334 144.4 327 144.4 320C144.4 313 144.8 306 145.6 299.2C146.2 294 144.2 288.8 140.3 285.4L86.9 237.9C81.2 232.9 79.9 224.5 83.7 217.9L113.6 166.1C117.4 159.5 125.3 156.5 132.5 158.9L200.3 181.4C205.3 183.1 210.7 182.2 214.9 179C226 170.7 238.1 163.7 251 158.1zM290.4 48C275.2 48 262.1 58.7 259.1 73.5L244.7 143.5C230.6 149.5 217.5 157.2 205.4 166.3L137.5 143.7C123.1 138.9 107.3 144.9 99.7 158.1L69.8 209.9C62.2 223.1 64.9 239.7 76.3 249.8L129.7 297.3C128.8 304.7 128.4 312.3 128.4 320C128.4 327.7 128.9 335.3 129.7 342.7L76.3 390.2C64.9 400.3 62.3 417 69.8 430.1L99.7 481.9C107.3 495 123.1 501.1 137.5 496.3L205.3 473.8C217.4 482.9 230.6 490.5 244.6 496.6L259 566.5C262.1 581.4 275.2 592 290.3 592L350.1 592C365.3 592 378.4 581.3 381.4 566.5L396 496.5C410.1 490.5 423.2 482.8 435.3 473.7L503.1 496.2C517.5 501 533.3 495 540.9 481.8L570.8 430C578.4 416.8 575.7 400.2 564.3 390.1L511 342.7C511.9 335.3 512.3 327.7 512.3 320C512.3 312.3 511.8 304.7 511 297.3L564.4 249.8C575.8 239.7 578.4 223 570.9 209.9L540.9 158.1C533.3 144.9 517.5 138.9 503.1 143.7L435.3 166.2C423.2 157.2 410 149.5 396 143.5L381.6 73.5C378.5 58.7 365.4 48 350.2 48L290.4 48zM255.7 320C255.7 284.5 284.5 255.7 320 255.7C355.5 255.7 384.3 284.5 384.3 320C384.3 355.5 355.5 384.3 320 384.3C284.5 384.3 255.7 355.5 255.7 320zM400.3 320C400.3 275.7 364.3 239.7 320 239.7C275.7 239.7 239.7 275.7 239.7 320C239.7 364.3 275.7 400.3 320 400.3C364.3 400.3 400.3 364.3 400.3 320z'],
  'arrow-right-from-bracket': ['0 0 640 640', 'M248 112C252.4 112 256 108.4 256 104C256 99.6 252.4 96 248 96L160 96C107 96 64 139 64 192L64 448C64 501 107 544 160 544L248 544C252.4 544 256 540.4 256 536C256 531.6 252.4 528 248 528L160 528C115.8 528 80 492.2 80 448L80 192C80 147.8 115.8 112 160 112L248 112zM573.7 325.7C576.8 322.6 576.8 317.5 573.7 314.4L421.7 162.4C418.6 159.3 413.5 159.3 410.4 162.4C407.3 165.5 407.3 170.6 410.4 173.7L548.7 312L232 312C227.6 312 224 315.6 224 320C224 324.4 227.6 328 232 328L548.7 328L410.4 466.3C407.3 469.4 407.3 474.5 410.4 477.6C413.5 480.7 418.6 480.7 421.7 477.6L573.7 325.6z'],
  'user': ['0 0 640 640', 'M208 192C208 130.1 258.1 80 320 80C381.9 80 432 130.1 432 192C432 253.9 381.9 304 320 304C258.1 304 208 253.9 208 192zM448 192C448 121.3 390.7 64 320 64C249.3 64 192 121.3 192 192C192 262.7 249.3 320 320 320C390.7 320 448 262.7 448 192zM112 544C112 455.6 183.6 384 272 384L368 384C456.4 384 528 455.6 528 544L528 568C528 572.4 531.6 576 536 576C540.4 576 544 572.4 544 568L544 544C544 446.8 465.2 368 368 368L272 368C174.8 368 96 446.8 96 544L96 568C96 572.4 99.6 576 104 576C108.4 576 112 572.4 112 568L112 544z'],
  'lock': ['0 0 640 640', 'M208 160L208 224L432 224L432 160C432 98.1 381.9 48 320 48C258.1 48 208 98.2 208 160zM192 224L192 160C192 89.3 249.3 32 320 32C390.7 32 448 89.3 448 160L448 224C483.3 224 512 252.7 512 288L512 512C512 547.3 483.3 576 448 576L192 576C156.7 576 128 547.3 128 512L128 288C128 252.7 156.7 224 192 224zM192 240C165.5 240 144 261.5 144 288L144 512C144 538.5 165.5 560 192 560L448 560C474.5 560 496 538.5 496 512L496 288C496 261.5 474.5 240 448 240L192 240z'],
  'magnifying-glass': ['0 0 640 640', 'M464 272C464 166 378 80 272 80C166 80 80 166 80 272C80 378 166 464 272 464C378 464 464 378 464 272zM413.3 424.6C376.2 459 326.6 480 272 480C157.1 480 64 386.9 64 272C64 157.1 157.1 64 272 64C386.9 64 480 157.1 480 272C480 326.6 459 376.2 424.6 413.3L573.6 562.3C576.7 565.4 576.7 570.5 573.6 573.6C570.5 576.7 565.4 576.7 562.3 573.6L413.3 424.6z'],
  'xmark': ['0 0 640 640', 'M509.7 141.7C512.8 138.6 512.8 133.5 509.7 130.4C506.6 127.3 501.5 127.3 498.4 130.4L320 308.7L141.7 130.3C138.6 127.2 133.5 127.2 130.4 130.3C127.3 133.4 127.3 138.5 130.4 141.6L308.7 320L130.3 498.3C127.2 501.4 127.2 506.5 130.3 509.6C133.4 512.7 138.5 512.7 141.6 509.6L320 331.3L498.3 509.7C501.4 512.8 506.5 512.8 509.6 509.7C512.7 506.6 512.7 501.5 509.6 498.4L331.3 320L509.7 141.7z'],
  'brain-circuit': ['0 0 640 640', 'M264 80C290.5 80 312 101.5 312 128L312 232L279.2 232C275.5 213.7 259.4 200 240 200C217.9 200 200 217.9 200 240C200 262.1 217.9 280 240 280C259.4 280 275.5 266.3 279.2 248L312 248L312 320L256 320C233.9 320 216 337.9 216 360L216 376.8C197.7 380.5 184 396.6 184 416C184 438.1 201.9 456 224 456C246.1 456 264 438.1 264 416C264 396.6 250.3 380.5 232 376.8L232 360C232 346.7 242.7 336 256 336L312 336L312 504C312 534.9 286.9 560 256 560C229.9 560 208 542.2 201.8 518C200.9 514.4 197.6 511.9 193.9 512C193.3 512 192.7 512 192.1 512C152.3 512 120.1 479.8 120.1 440C120.1 424.7 124.8 410.6 132.9 399C135.2 395.6 134.7 391.1 131.6 388.4C114.7 373.7 104.1 352.1 104.1 328C104.1 296.7 122.1 269.5 148.4 256.4C152.3 254.4 153.9 249.6 152 245.7C147 235.6 144.1 224.1 144.1 212C144.1 173.7 172.4 142 209.2 136.8C213.1 136.2 216.1 132.9 216.1 128.9L216.1 128C216.1 101.5 237.6 80 264.1 80zM328 248L360.8 248C364.5 266.3 380.6 280 400 280C422.1 280 440 262.1 440 240C440 217.9 422.1 200 400 200C380.6 200 364.5 213.7 360.8 232L328 232L328 128C328 101.5 349.5 80 376 80C402.5 80 424 101.5 424 128L424 128.9C424 132.9 426.9 136.3 430.9 136.8C467.7 142.1 496 173.7 496 212C496 224.1 493.2 235.5 488.1 245.7C486.1 249.6 487.7 254.4 491.7 256.4C518 269.5 536 296.7 536 328C536 350.7 526.6 371.1 511.4 385.7C508.7 388.3 508.2 392.3 510 395.5C516.3 406.2 519.9 418.7 519.9 432C519.9 471.8 487.7 504 447.9 504L447.5 504C443.4 504 440 507 439.5 511C436.1 538.6 412.5 560 383.9 560C353 560 327.9 534.9 327.9 504L327.9 480L383.9 480C406 480 423.9 462.1 423.9 440L423.9 423.2C442.2 419.5 455.9 403.4 455.9 384C455.9 361.9 438 344 415.9 344C393.8 344 375.9 361.9 375.9 384C375.9 403.4 389.6 419.5 407.9 423.2L407.9 440C407.9 453.3 397.2 464 383.9 464L327.9 464L327.9 248zM256 576C283.9 576 308 560.2 320 537C332 560.2 356.1 576 384 576C418.3 576 447.1 552 454.3 519.8C500 516.6 536 478.5 536 432C536 417.9 532.7 404.6 526.8 392.8C542.4 375.7 552 353 552 328C552 293.1 533.4 262.6 505.6 245.8C509.7 235.3 512 223.9 512 212C512 168 481.1 131.2 439.7 122.1C436.7 89.5 409.3 64 376 64C351.9 64 330.9 77.3 320 97C309.1 77.3 288.1 64 264 64C230.6 64 203.2 89.5 200.3 122.1C159 131.1 128 167.9 128 212C128 223.9 130.3 235.3 134.4 245.8C106.6 262.6 88 293.1 88 328C88 354.4 98.7 378.4 115.9 395.7C108.3 408.7 104 423.9 104 440C104 487.3 141.3 525.9 188.1 527.9C198 555.9 224.6 576 256 576zM400 264C386.7 264 376 253.3 376 240C376 226.7 386.7 216 400 216C413.3 216 424 226.7 424 240C424 253.3 413.3 264 400 264zM440 384C440 397.3 429.3 408 416 408C402.7 408 392 397.3 392 384C392 370.7 402.7 360 416 360C429.3 360 440 370.7 440 384zM248 416C248 429.3 237.3 440 224 440C210.7 440 200 429.3 200 416C200 402.7 210.7 392 224 392C237.3 392 248 402.7 248 416zM264 240C264 253.3 253.3 264 240 264C226.7 264 216 253.3 216 240C216 226.7 226.7 216 240 216C253.3 216 264 226.7 264 240z'],
  'network-wired': ['0 0 640 640', 'M368 112C385.7 112 400 126.3 400 144L400 208C400 225.7 385.7 240 368 240L272 240C254.3 240 240 225.7 240 208L240 144C240 126.3 254.3 112 272 112L368 112zM272 96C245.5 96 224 117.5 224 144L224 208C224 234.5 245.5 256 272 256L312 256L312 312L40 312C35.6 312 32 315.6 32 320C32 324.4 35.6 328 40 328L184 328L184 384L144 384C117.5 384 96 405.5 96 432L96 496C96 522.5 117.5 544 144 544L240 544C266.5 544 288 522.5 288 496L288 432C288 405.5 266.5 384 240 384L200 384L200 328L440 328L440 384L400 384C373.5 384 352 405.5 352 432L352 496C352 522.5 373.5 544 400 544L496 544C522.5 544 544 522.5 544 496L544 432C544 405.5 522.5 384 496 384L456 384L456 328L600 328C604.4 328 608 324.4 608 320C608 315.6 604.4 312 600 312L328 312L328 256L368 256C394.5 256 416 234.5 416 208L416 144C416 117.5 394.5 96 368 96L272 96zM144 400L240 400C257.7 400 272 414.3 272 432L272 496C272 513.7 257.7 528 240 528L144 528C126.3 528 112 513.7 112 496L112 432C112 414.3 126.3 400 144 400zM496 400C513.7 400 528 414.3 528 432L528 496C528 513.7 513.7 528 496 528L400 528C382.3 528 368 513.7 368 496L368 432C368 414.3 382.3 400 400 400L496 400z'],
  'eye': ['0 0 640 640', 'M320 112C244.4 112 183.4 146.4 138.3 188.3C93.4 230 63.3 280 49.2 313.9C47.6 317.8 47.6 322.3 49.2 326.2C63.3 360.1 93.3 410 138.3 451.8C183.4 493.7 244.4 528.1 320 528.1C395.6 528.1 456.6 493.7 501.7 451.8C546.6 410.1 576.7 360.1 590.8 326.2C592.4 322.3 592.4 317.8 590.8 313.9C576.7 280 546.7 230.1 501.7 188.3C456.6 146.4 395.6 112 320 112zM127.4 176.6C174.5 132.8 239.2 96 320 96C400.8 96 465.5 132.8 512.6 176.6C559.4 220.1 590.7 272 605.6 307.7C608.9 315.6 608.9 324.4 605.6 332.3C590.7 368 559.4 420 512.6 463.4C465.5 507.1 400.8 544 320 544C239.2 544 174.5 507.2 127.4 463.4C80.6 419.9 49.3 368 34.4 332.3C31.1 324.4 31.1 315.6 34.4 307.7C49.3 272 80.6 220 127.4 176.6zM320 432C381.9 432 432 381.9 432 320C432 261.8 387.5 213.9 330.7 208.5C334.1 218.4 336 229 336 240C336 293 293 336 240 336C229 336 218.4 334.1 208.5 330.7C213.9 387.5 261.8 432 320 432zM192 320C192 315 192.3 310.1 192.8 305.3L192.9 304.7C199.9 246.3 246.2 199.9 304.7 192.9C309.7 192.3 314.8 192 320 192L320.4 192C321.3 192 322.3 192 323.2 192C392.4 193.7 448 250.3 448 320C448 390.7 390.7 448 320 448C249.3 448 192 390.7 192 320zM208.2 313.4C217.9 317.6 228.7 320 240 320C284.2 320 320 284.2 320 240C320 228.7 317.7 217.9 313.4 208.2C256.8 211.5 211.4 256.8 208.2 313.4z'],
  'diagram-project': ['0 0 640 640', 'M112 112C94.3 112 80 126.3 80 144L80 240C80 257.7 94.3 272 112 272L208 272C225.7 272 240 257.7 240 240L240 144C240 126.3 225.7 112 208 112L112 112zM256 200L256 240C256 256.3 247.9 270.7 235.5 279.3L291.3 353.7C295.4 352.6 299.6 352 304 352L400 352C426.5 352 448 373.5 448 400L448 496C448 522.5 426.5 544 400 544L304 544C277.5 544 256 522.5 256 496L256 400C256 383.7 264.1 369.3 276.5 360.7L220.7 286.3C216.6 287.4 212.4 288 208 288L112 288C85.5 288 64 266.5 64 240L64 144C64 117.5 85.5 96 112 96L208 96C234.5 96 256 117.5 256 144L256 184L384 184L384 144C384 117.5 405.5 96 432 96L528 96C554.5 96 576 117.5 576 144L576 240C576 266.5 554.5 288 528 288L432 288C405.5 288 384 266.5 384 240L384 200L256 200zM400 200L400 240C400 257.7 414.3 272 432 272L528 272C545.7 272 560 257.7 560 240L560 144C560 126.3 545.7 112 528 112L432 112C414.3 112 400 126.3 400 144L400 200zM304 368C286.3 368 272 382.3 272 400L272 496C272 513.7 286.3 528 304 528L400 528C417.7 528 432 513.7 432 496L432 400C432 382.3 417.7 368 400 368L304 368z'],
  'bolt': ['0 0 640 640', 'M425.4 67.1C419.5 62.8 411.4 63.1 405.8 67.8L117.8 307.8C112.6 312.1 110.7 319.2 113 325.5C115.3 331.8 121.3 336 128 336L272.7 336C284 336 291.8 347.4 287.6 357.9L209.1 554.1C206.4 560.9 208.6 568.7 214.6 573C220.6 577.3 228.6 577 234.2 572.3L522.2 332.3C527.4 328 529.3 320.9 527 314.6C524.7 308.3 518.7 304 512 304L367.3 304C356 304 348.2 292.6 352.4 282.1L430.9 85.9C433.6 79.1 431.4 71.3 425.5 67zM395.5 55.4C406.8 46 422.9 45.5 434.8 54.1C446.7 62.7 451.1 78.3 445.7 91.9L367.3 288L512 288C525.5 288 537.5 296.4 542.1 309.1C546.7 321.8 542.8 336 532.5 344.6L244.5 584.6C233.2 594 217.1 594.5 205.2 585.9C193.3 577.3 188.9 561.7 194.3 548.1L272.7 352L128 352C114.5 352 102.5 343.6 97.9 330.9L97.9 330.9C93.3 318.2 97.2 304 107.5 295.4L395.5 55.4z'],
  'angle-left': ['0 0 640 640', 'M210.3 325.7C207.2 322.6 207.2 317.5 210.3 314.4L386.3 138.4C389.4 135.3 394.5 135.3 397.6 138.4C400.7 141.5 400.7 146.6 397.6 149.7L227.3 320L397.7 490.3C400.8 493.4 400.8 498.5 397.7 501.6C394.6 504.7 389.5 504.7 386.4 501.6L210.4 325.6z'],
  'angle-right': ['0 0 640 640', 'M429.7 314.3C432.8 317.4 432.8 322.5 429.7 325.6L253.7 501.6C250.6 504.7 245.5 504.7 242.4 501.6C239.3 498.5 239.3 493.4 242.4 490.3L412.7 320L242.3 149.7C239.2 146.6 239.2 141.5 242.3 138.4C245.4 135.3 250.5 135.3 253.6 138.4L429.6 314.4z'],
  'angle-down': ['0 0 640 640', 'M314.3 429.7C317.4 432.8 322.5 432.8 325.6 429.7L501.6 253.7C504.7 250.6 504.7 245.5 501.6 242.4C498.5 239.3 493.4 239.3 490.3 242.4L320 412.7L149.7 242.3C146.6 239.2 141.5 239.2 138.4 242.3C135.3 245.4 135.3 250.5 138.4 253.6L314.4 429.6z'],
  'check': ['0 0 640 640', 'M509.7 141.7C512.8 138.6 512.8 133.5 509.7 130.4C506.6 127.3 501.5 127.3 498.4 130.4L320 308.7L141.7 130.3C138.6 127.2 133.5 127.2 130.4 130.3C127.3 133.4 127.3 138.5 130.4 141.6L308.7 320L130.3 498.3C127.2 501.4 127.2 506.5 130.3 509.6C133.4 512.7 138.5 512.7 141.6 509.6L320 331.3L498.3 509.7C501.4 512.8 506.5 512.8 509.6 509.7C512.7 506.6 512.7 501.5 509.6 498.4L331.3 320L509.7 141.7z'],
  'circle-check': ['0 0 640 640', 'M320 576C178.6 576 64 461.4 64 320C64 178.6 178.6 64 320 64C461.4 64 576 178.6 576 320C576 461.4 461.4 576 320 576zM320 80C187.5 80 80 187.5 80 320C80 452.5 187.5 560 320 560C452.5 560 560 452.5 560 320C560 187.5 452.5 80 320 80zM417.4 224.4C420 220.8 425 220.1 428.6 222.6C432.2 225.1 433 230.2 430.4 233.8L294.5 420.7C293.1 422.6 291 423.8 288.7 424C286.4 424.2 284.1 423.3 282.4 421.7L210.4 349.7C207.3 346.6 207.3 341.5 210.4 338.4C213.5 335.3 218.6 335.3 221.7 338.4L287 403.7L417.4 224.4z'],
  'badge-check': ['0 0 640 640', 'M320 64C356.4 64 388.3 83.3 406.1 112.2C439.1 104.3 475.3 113.3 501 139C526.7 164.7 535.7 201 527.8 233.9C556.7 251.7 576 283.6 576 320C576 356.4 556.7 388.3 527.8 406.1C535.7 439.1 526.7 475.3 501 501C475.3 526.7 439 535.7 406.1 527.8C388.3 556.7 356.4 576 320 576C283.6 576 251.7 556.7 233.9 527.8C200.9 535.7 164.7 526.7 139 501C113.3 475.3 104.3 439 112.2 406.1C83.3 388.3 64 356.4 64 320C64 283.6 83.3 251.7 112.2 233.9C104.3 200.9 113.3 164.7 139 139C164.7 113.3 201 104.3 233.9 112.2C251.7 83.3 283.6 64 320 64zM320 80C287.4 80 259.1 98.3 244.8 125.2C243 128.6 239.1 130.2 235.4 129.1C206.3 120.2 173.3 127.2 150.2 150.2C127.1 173.2 120.1 206.2 129.1 235.4C130.2 239.1 128.6 243 125.2 244.8C98.3 259.1 80 287.4 80 320C80 352.6 98.3 380.9 125.2 395.2C128.6 397 130.2 400.9 129.1 404.6C120.2 433.7 127.2 466.7 150.2 489.8C173.2 512.9 206.2 519.9 235.4 510.9C239.1 509.8 243 511.4 244.8 514.8C259.1 541.7 287.4 560.1 320 560.1C352.6 560.1 380.9 541.8 395.2 514.8C397 511.4 400.9 509.8 404.6 510.9C433.7 519.8 466.7 512.8 489.8 489.8C512.9 466.8 519.9 433.8 510.9 404.6C509.8 400.9 511.4 397 514.8 395.2C541.7 380.9 560.1 352.6 560.1 320C560.1 287.4 541.8 259.1 514.8 244.8C511.4 243 509.8 239.1 510.9 235.4C519.8 206.3 512.8 173.3 489.8 150.2C466.8 127.1 433.8 120.1 404.6 129.1C400.9 130.2 397 128.6 395.2 125.2C380.9 98.3 352.6 80 320 80zM401.7 227.7C404.3 224.1 409.3 223.4 412.9 226C416.5 228.6 417.2 233.6 414.7 237.2L300.9 393.5C299.5 395.4 297.3 396.6 295 396.8C292.7 397 290.3 396.1 288.7 394.4L226.4 329.9C223.3 326.7 223.4 321.7 226.6 318.6C229.8 315.5 234.8 315.6 237.9 318.8L293.6 376.4L401.7 227.7z'],
  'bars': ['0 0 640 640', 'M104 168H536V192H104ZM104 308H536V332H104ZM104 448H536V472H104Z']
};
Object.assign(__ds_scope, { iconGlyphsThin });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/icon-paths-thin.js", error: String((e && e.message) || e) }); }

// components/core/icon-paths.js
try { (() => {
/* FontAwesome Light glyph data, inlined.

   Inlined rather than loaded from assets/icons/*.svg on purpose: this design system is consumed
   from pages at many different depths (cards, ui kits, templates, bundled single files), and a
   relative sprite URL cannot resolve correctly for all of them. Path data in the bundle always
   resolves. Keys are the SEMANTIC names the UI asks for — see ICON_ALIAS in Icon.jsx for the
   Material-era names that still map onto these. */
const iconGlyphs = {
  'arrows-rotate': ['0 0 640 640', 'M96.5 305.1C100 252.8 121.7 201.6 161.6 161.6C205.3 117.9 262.6 96 320 96C382 96 441.2 121.5 483.8 166.6L522.9 208L416 208C407.2 208 400 215.2 400 224C400 232.8 407.2 240 416 240L560 240C568.8 240 576 232.8 576 224L576 80C576 71.2 568.8 64 560 64C551.2 64 544 71.2 544 80L544 183.8L507 144.7C458.4 93.2 390.8 64 320 64C254.5 64 189 89 139 139C93.3 184.6 68.5 243.2 64.6 302.9C64 311.8 70.7 319.4 79.5 320C88.3 320.6 95.9 313.9 96.5 305.1zM543.5 335C540 387.3 518.3 438.5 478.4 478.4C434.7 522.1 377.4 544 320 544C258 544 198.8 518.5 156.2 473.4L117.1 432L224 432C232.8 432 240 424.8 240 416C240 407.2 232.8 400 224 400L80 400C71.2 400 64 407.2 64 416L64 560C64 568.8 71.2 576 80 576C88.8 576 96 568.8 96 560L96 456.2L133 495.3C181.6 546.8 249.3 575.9 320 575.9C385.5 575.9 451 550.9 501 500.9C546.6 455.3 571.4 396.6 575.4 336.9C576 328.1 569.3 320.5 560.5 319.9C551.7 319.3 544.1 326 543.5 334.8z'],
  'clock': ['0 0 640 640', 'M544 320C544 443.7 443.7 544 320 544C196.3 544 96 443.7 96 320C96 196.3 196.3 96 320 96C443.7 96 544 196.3 544 320zM64 320C64 461.4 178.6 576 320 576C461.4 576 576 461.4 576 320C576 178.6 461.4 64 320 64C178.6 64 64 178.6 64 320zM304 176L304 320C304 325.4 306.7 330.3 311.1 333.3L407.1 397.3C414.5 402.2 424.4 400.2 429.3 392.9C434.2 385.6 432.2 375.6 424.9 370.7L336 311.4L336 176C336 167.2 328.8 160 320 160C311.2 160 304 167.2 304 176z'],
  'circle-nodes': ['0 0 640 640', 'M440.2 185.7L440.2 185.7C447.2 189.7 455.4 192 464 192C466.6 192 469.2 191.8 471.6 191.4L471.6 191.4C494.5 187.7 512 167.9 512 144C512 117.5 490.5 96 464 96C437.5 96 416 117.5 416 144C416 144.7 416 145.3 416 146L416 146C416.5 157.4 420.9 167.7 427.9 175.7L427.9 175.7C431.4 179.7 435.5 183.1 440.1 185.7zM434.3 218.3C419.1 212.2 406.1 201.5 397.2 188L222.7 257.7C223.5 262.3 224 267.1 224 272C224 286.4 220.2 299.9 213.5 311.6L351.5 432.4C364.5 422.5 380.7 416.4 398.2 416.1L434.2 218.4zM429.8 421.7C459.2 433.5 480 462.3 480 496C480 540.2 444.2 576 400 576C355.8 576 320 540.2 320 496C320 481.6 323.8 468.1 330.5 456.4L192.5 335.6C179 345.9 162.2 352 144 352C99.8 352 64 316.2 64 272C64 227.8 99.8 192 144 192C171.9 192 196.5 206.3 210.8 228L385.3 158.3C384.5 153.7 384 148.9 384 144C384 99.8 419.8 64 464 64C508.2 64 544 99.8 544 144C544 187.6 509.1 223.1 465.7 224L429.7 421.7zM183.8 298.8C184.5 297.8 185.1 296.8 185.7 295.7C189.7 288.7 192 280.6 192 271.9C192 245.4 170.5 223.9 144 223.9C117.5 223.9 96 245.4 96 271.9C96 298.4 117.5 319.9 144 319.9C152.5 319.9 160.6 317.7 167.5 313.7L167.5 313.7C174 310 179.6 304.9 183.8 298.7zM355.4 478.2C353.2 483.7 352 489.7 352 496C352 522.5 373.5 544 400 544C426.5 544 448 522.5 448 496C448 469.5 426.5 448 400 448C386 448 373.4 454 364.7 463.5C362 466.4 359.7 469.6 357.8 473.1C356.9 474.7 356.1 476.5 355.4 478.2L355.4 478.2z'],
  'memory': ['0 0 640 640', 'M64 184C64 153.1 89.1 128 120 128L520 128C550.9 128 576 153.1 576 184L576 200.6C576 206.3 573 211.6 568 214.4C553.6 222.7 544 238.2 544 256C544 273.8 553.6 289.3 568 297.6C572.9 300.5 576 305.7 576 311.4L576 480C576 497.7 561.7 512 544 512L96 512C78.3 512 64 497.7 64 480L64 311.4C64 305.7 67 300.4 72 297.6C86.4 289.3 96 273.8 96 256C96 238.2 86.4 222.7 72 214.4C67.1 211.5 64 206.3 64 200.6L64 184zM96 384L96 480L160 480L160 448C160 439.2 167.2 432 176 432C184.8 432 192 439.2 192 448L192 480L256 480L256 448C256 439.2 263.2 432 272 432C280.8 432 288 439.2 288 448L288 480L352 480L352 448C352 439.2 359.2 432 368 432C376.8 432 384 439.2 384 448L384 480L448 480L448 448C448 439.2 455.2 432 464 432C472.8 432 480 439.2 480 448L480 480L544 480L544 384L96 384zM544 352L544 320C524.6 305.4 512 282.2 512 256C512 229.8 524.6 206.6 544 192L544 184C544 170.7 533.3 160 520 160L120 160C106.7 160 96 170.7 96 184L96 192C115.4 206.6 128 229.8 128 256C128 282.2 115.4 305.4 96 320L96 352L544 352zM208 192C216.8 192 224 199.2 224 208L224 304C224 312.8 216.8 320 208 320C199.2 320 192 312.8 192 304L192 208C192 199.2 199.2 192 208 192zM320 192C328.8 192 336 199.2 336 208L336 304C336 312.8 328.8 320 320 320C311.2 320 304 312.8 304 304L304 208C304 199.2 311.2 192 320 192zM432 192C440.8 192 448 199.2 448 208L448 304C448 312.8 440.8 320 432 320C423.2 320 416 312.8 416 304L416 208C416 199.2 423.2 192 432 192z'],
  'dna': ['0 0 640 640', 'M496 64C504.8 64 512 71.2 512 80C512 193.2 428.4 260.5 347.2 320C428.4 379.5 512 446.8 512 560C512 568.8 504.8 576 496 576C487.2 576 480 568.8 480 560C480 554.5 479.8 549.2 479.3 544L160.7 544C160.3 549.2 160 554.5 160 560C160 568.8 152.8 576 144 576C135.2 576 128 568.8 128 560C128 446.8 211.6 379.5 292.8 320C211.6 260.5 128 193.2 128 80C128 71.2 135.2 64 144 64C152.8 64 160 71.2 160 80C160 85.5 160.2 90.8 160.7 96L479.4 96C479.8 90.8 480.1 85.5 480.1 80C480.1 71.2 487.3 64 496.1 64zM166.4 128C172.9 151.4 184 172.5 198.6 192L441.5 192C456.1 172.5 467.2 151.4 473.7 128L166.4 128zM226.5 224C253.1 250.7 285.6 275.1 320 300.2C354.4 275.1 386.9 250.7 413.5 224L226.5 224zM320 339.8C285.6 364.9 253.1 389.3 226.5 416L413.5 416C386.9 389.3 354.4 364.9 320 339.8zM441.4 447.9C440.9 447.9 440.5 448 440 448L200 448C199.5 448 199.1 448 198.6 447.9C184 467.4 172.8 488.5 166.4 512L473.7 512C467.2 488.5 456.1 467.5 441.5 447.9z'],
  'gauge-high': ['0 0 640 640', 'M320 96C443.7 96 544 196.3 544 320C544 443.7 443.7 544 320 544C196.3 544 96 443.7 96 320C96 196.3 196.3 96 320 96zM320 576C461.4 576 576 461.4 576 320C576 178.6 461.4 64 320 64C178.6 64 64 178.6 64 320C64 461.4 178.6 576 320 576zM344 160C344 146.7 333.3 136 320 136C306.7 136 296 146.7 296 160C296 173.3 306.7 184 320 184C333.3 184 344 173.3 344 160zM288 416C288 398.3 302.3 384 320 384C337.7 384 352 398.3 352 416C352 433.7 337.7 448 320 448C302.3 448 288 433.7 288 416zM384 416C384 397.4 376.1 380.7 363.5 369L446.1 215.6C450.3 207.8 447.4 198.1 439.6 193.9C431.8 189.7 422.1 192.6 417.9 200.4L335.3 353.8C330.4 352.6 325.3 352 320 352C284.7 352 256 380.7 256 416C256 451.3 284.7 480 320 480C355.3 480 384 451.3 384 416zM232 208C232 194.7 221.3 184 208 184C194.7 184 184 194.7 184 208C184 221.3 194.7 232 208 232C221.3 232 232 221.3 232 208zM160 344C173.3 344 184 333.3 184 320C184 306.7 173.3 296 160 296C146.7 296 136 306.7 136 320C136 333.3 146.7 344 160 344zM504 320C504 306.7 493.3 296 480 296C466.7 296 456 306.7 456 320C456 333.3 466.7 344 480 344C493.3 344 504 333.3 504 320z'],
  'microchip': ['0 0 640 640', 'M240 80C240 71.2 232.8 64 224 64C215.2 64 208 71.2 208 80L208 128L192 128C156.7 128 128 156.7 128 192L128 208L80 208C71.2 208 64 215.2 64 224C64 232.8 71.2 240 80 240L128 240L128 304L80 304C71.2 304 64 311.2 64 320C64 328.8 71.2 336 80 336L128 336L128 400L80 400C71.2 400 64 407.2 64 416C64 424.8 71.2 432 80 432L128 432L128 448C128 483.3 156.7 512 192 512L208 512L208 560C208 568.8 215.2 576 224 576C232.8 576 240 568.8 240 560L240 512L304 512L304 560C304 568.8 311.2 576 320 576C328.8 576 336 568.8 336 560L336 512L400 512L400 560C400 568.8 407.2 576 416 576C424.8 576 432 568.8 432 560L432 512L448 512C483.3 512 512 483.3 512 448L512 432L560 432C568.8 432 576 424.8 576 416C576 407.2 568.8 400 560 400L512 400L512 336L560 336C568.8 336 576 328.8 576 320C576 311.2 568.8 304 560 304L512 304L512 240L560 240C568.8 240 576 232.8 576 224C576 215.2 568.8 208 560 208L512 208L512 192C512 156.7 483.3 128 448 128L432 128L432 80C432 71.2 424.8 64 416 64C407.2 64 400 71.2 400 80L400 128L336 128L336 80C336 71.2 328.8 64 320 64C311.2 64 304 71.2 304 80L304 128L240 128L240 80zM480 192L480 448C480 465.7 465.7 480 448 480L192 480C174.3 480 160 465.7 160 448L160 192C160 174.3 174.3 160 192 160L448 160C465.7 160 480 174.3 480 192zM256 224C238.3 224 224 238.3 224 256L224 384C224 401.7 238.3 416 256 416L384 416C401.7 416 416 401.7 416 384L416 256C416 238.3 401.7 224 384 224L256 224zM384 256L384 384L256 384L256 256L384 256z'],
  'drone': ['0 0 640 640', 'M464 384C462.4 384 460.9 384 459.3 384.1C462.3 373.9 464 363.1 464 352C525.9 352 576 402.1 576 464C576 525.9 525.9 576 464 576C402.1 576 352 525.9 352 464C363.2 464 374 462.4 384.1 459.3C384 460.8 384 462.4 384 464C384 508.2 419.8 544 464 544C508.2 544 544 508.2 544 464C544 419.8 508.2 384 464 384zM384.5 407.1C378.1 410.9 370.9 413.6 363.2 415C359.5 415.6 355.8 416 351.9 416L287.9 416C284.1 416 280.3 415.7 276.6 415C269 413.6 261.8 410.9 255.3 407.1L187.1 475.3C180.9 481.5 170.7 481.5 164.5 475.3C158.3 469.1 158.3 458.9 164.5 452.7L232.7 384.5C228.9 378.1 226.2 370.9 224.8 363.2C224.2 359.5 223.8 355.8 223.8 351.9L223.8 287.9C223.8 284.1 224.1 280.3 224.8 276.6C226.2 269 228.9 261.8 232.7 255.3L164.5 187.1C158.3 180.9 158.3 170.7 164.5 164.5C170.7 158.3 180.9 158.3 187.1 164.5L255.3 232.7C261.7 228.9 268.9 226.2 276.6 224.8C280.3 224.2 284 223.8 287.9 223.8L351.9 223.8C355.7 223.8 359.5 224.1 363.2 224.8C370.8 226.2 378 228.9 384.5 232.7L452.7 164.5C458.9 158.3 469.1 158.3 475.3 164.5C481.5 170.7 481.5 180.9 475.3 187.1L407.1 255.3C410.9 261.7 413.6 268.9 415 276.6C415.6 280.3 416 284 416 287.9L416 351.9C416 355.7 415.7 359.5 415 363.2C413.6 370.8 410.9 378 407.1 384.5L475.3 452.7C481.5 458.9 481.5 469.1 475.3 475.3C469.1 481.5 458.9 481.5 452.7 475.3L384.5 407.1zM176 64C237.9 64 288 114.1 288 176C276.8 176 266 177.6 255.9 180.7C256 179.2 256 177.6 256 176C256 131.8 220.2 96 176 96C131.8 96 96 131.8 96 176C96 220.2 131.8 256 176 256C177.6 256 179.1 256 180.7 255.9C177.7 266.1 176 276.9 176 288C114.1 288 64 237.9 64 176C64 114.1 114.1 64 176 64zM64 464C64 402.1 114.1 352 176 352C176 363.2 177.6 374 180.7 384.1C179.2 384 177.6 384 176 384C131.8 384 96 419.8 96 464C96 508.2 131.8 544 176 544C220.2 544 256 508.2 256 464C256 462.4 256 460.9 255.9 459.3C266.1 462.3 276.9 464 288 464C288 525.9 237.9 576 176 576C114.1 576 64 525.9 64 464zM384.1 180.7C373.9 177.7 363.1 176 352 176C352 114.1 402.1 64 464 64C525.9 64 576 114.1 576 176C576 237.9 525.9 288 464 288C464 276.8 462.4 266 459.3 255.9C460.8 256 462.4 256 464 256C508.2 256 544 220.2 544 176C544 131.8 508.2 96 464 96C419.8 96 384 131.8 384 176C384 177.6 384 179.1 384.1 180.7zM288 256C270.3 256 256 270.3 256 288L256 352C256 369.7 270.3 384 288 384L352 384C369.7 384 384 369.7 384 352L384 288C384 270.3 369.7 256 352 256L288 256z'],
  'arrow-right': ['0 0 640 640', 'M571.3 331.3C577.5 325.1 577.5 314.9 571.3 308.7L395.3 132.7C389.1 126.5 378.9 126.5 372.7 132.7C366.5 138.9 366.5 149.1 372.7 155.3L521.4 304L80 304C71.2 304 64 311.2 64 320C64 328.8 71.2 336 80 336L521.4 336L372.7 484.7C366.5 490.9 366.5 501.1 372.7 507.3C378.9 513.5 389.1 513.5 395.3 507.3L571.3 331.3z'],
  'arrow-left': ['0 0 640 640', 'M68.7 308.7C62.5 314.9 62.5 325.1 68.7 331.3L244.7 507.3C250.9 513.5 261.1 513.5 267.3 507.3C273.5 501.1 273.5 490.9 267.3 484.7L118.6 336L560 336C568.8 336 576 328.8 576 320C576 311.2 568.8 304 560 304L118.6 304L267.3 155.3C273.5 149.1 273.5 138.9 267.3 132.7C261.1 126.5 250.9 126.5 244.7 132.7L68.7 308.7z'],
  'arrow-up-right': ['0 0 640 640', 'M496 128C504.8 128 512 135.2 512 144L512 400C512 408.8 504.8 416 496 416C487.2 416 480 408.8 480 400L480 182.6L155.3 507.3C149.1 513.5 138.9 513.5 132.7 507.3C126.5 501.1 126.5 490.9 132.7 484.7L457.4 160L240 160C231.2 160 224 152.8 224 144C224 135.2 231.2 128 240 128L496 128z'],
  'arrow-down-to-line': ['0 0 640 640', 'M496 576C504.8 576 512 568.8 512 560C512 551.2 504.8 544 496 544L144 544C135.2 544 128 551.2 128 560C128 568.8 135.2 576 144 576L496 576zM308.7 443.3C314.9 449.5 325.1 449.5 331.3 443.3L475.3 299.3C481.5 293.1 481.5 282.9 475.3 276.7C469.1 270.5 458.9 270.5 452.7 276.7L336 393.4L336 80C336 71.2 328.8 64 320 64C311.2 64 304 71.2 304 80L304 393.4L187.3 276.7C181.1 270.5 170.9 270.5 164.7 276.7C158.5 282.9 158.5 293.1 164.7 299.3L308.7 443.3z'],
  'arrow-up-from-line': ['0 0 640 640', 'M331.3 68.7C325.1 62.5 314.9 62.5 308.7 68.7L164.7 212.7C158.5 218.9 158.5 229.1 164.7 235.3C170.9 241.5 181.1 241.5 187.3 235.3L304 118.6L304 432C304 440.8 311.2 448 320 448C328.8 448 336 440.8 336 432L336 118.6L452.7 235.3C458.9 241.5 469.1 241.5 475.3 235.3C481.5 229.1 481.5 218.9 475.3 212.7L331.3 68.7zM144 544C135.2 544 128 551.2 128 560C128 568.8 135.2 576 144 576L496 576C504.8 576 512 568.8 512 560C512 551.2 504.8 544 496 544L144 544z'],
  'pause': ['0 0 640 640', 'M176 128C167.2 128 160 135.2 160 144L160 496C160 504.8 167.2 512 176 512L240 512C248.8 512 256 504.8 256 496L256 144C256 135.2 248.8 128 240 128L176 128zM128 144C128 117.5 149.5 96 176 96L240 96C266.5 96 288 117.5 288 144L288 496C288 522.5 266.5 544 240 544L176 544C149.5 544 128 522.5 128 496L128 144zM400 128C391.2 128 384 135.2 384 144L384 496C384 504.8 391.2 512 400 512L464 512C472.8 512 480 504.8 480 496L480 144C480 135.2 472.8 128 464 128L400 128zM352 144C352 117.5 373.5 96 400 96L464 96C490.5 96 512 117.5 512 144L512 496C512 522.5 490.5 544 464 544L400 544C373.5 544 352 522.5 352 496L352 144z'],
  'expand': ['0 0 640 640', 'M240 96C248.8 96 256 103.2 256 112C256 120.8 248.8 128 240 128L128 128L128 240C128 248.8 120.8 256 112 256C103.2 256 96 248.8 96 240L96 112C96 103.2 103.2 96 112 96L240 96zM96 400C96 391.2 103.2 384 112 384C120.8 384 128 391.2 128 400L128 512L240 512C248.8 512 256 519.2 256 528C256 536.8 248.8 544 240 544L112 544C103.2 544 96 536.8 96 528L96 400zM528 96C536.8 96 544 103.2 544 112L544 240C544 248.8 536.8 256 528 256C519.2 256 512 248.8 512 240L512 128L400 128C391.2 128 384 120.8 384 112C384 103.2 391.2 96 400 96L528 96zM512 400C512 391.2 519.2 384 528 384C536.8 384 544 391.2 544 400L544 528C544 536.8 536.8 544 528 544L400 544C391.2 544 384 536.8 384 528C384 519.2 391.2 512 400 512L512 512L512 400z'],
  'triangle-exclamation': ['0 0 640 640', 'M320.5 64C335.2 64 348.7 72.1 355.7 85L571.7 485C578.4 497.4 578.1 512.4 570.9 524.5C563.7 536.6 550.6 544 536.5 544L104.5 544C90.4 544 77.4 536.6 70.2 524.5C63 512.4 62.7 497.4 69.4 485L285.4 85C292.4 72.1 305.9 64 320.6 64zM320.5 96C317.6 96 314.9 97.6 313.5 100.2L97.5 500.2C96.2 502.7 96.2 505.7 97.7 508.1C99.2 510.5 101.8 512 104.6 512L536.6 512C539.4 512 542 510.5 543.5 508.1C545 505.7 545 502.7 543.7 500.2L327.7 100.2C326.3 97.6 323.6 96 320.7 96zM320.5 464C307.2 464 296.5 453.3 296.5 440C296.5 426.7 307.2 416 320.5 416C333.8 416 344.5 426.7 344.5 440C344.5 453.3 333.8 464 320.5 464zM320.5 240C334.1 240 344.8 251.6 343.9 265.1L336.5 369.1C335.9 377.5 328.9 384 320.5 384C312.1 384 305.1 377.5 304.5 369.1L297.1 265.1C296.1 251.5 306.9 240 320.5 240z'],
  'arrow-rotate-left': ['0 0 640 640', 'M80 64C88.8 64 96 71.2 96 80L96 183.8L133 144.7C181.6 93.2 249.2 64 320 64C461.4 64 576 178.6 576 320C576 461.4 461.4 576 320 576C221.7 576 136.4 520.6 93.5 439.5C89.4 431.7 92.4 422 100.2 417.9C108 413.8 117.7 416.8 121.8 424.6C159.4 495.6 234.1 544 320 544C443.7 544 544 443.7 544 320C544 196.3 443.7 96 320 96C258 96 198.8 121.5 156.2 166.6L117.1 208L224 208C232.8 208 240 215.2 240 224C240 232.8 232.8 240 224 240L80 240C71.2 240 64 232.8 64 224L64 80C64 71.2 71.2 64 80 64z'],
  'radar': ['0 0 640 640', 'M387.1 230.3C368.4 216.3 345.2 208 320 208C258.1 208 208 258.1 208 320C208 381.9 258.1 432 320 432C381.9 432 432 381.9 432 320L464 320C464 399.5 399.5 464 320 464C240.5 464 176 399.5 176 320C176 240.5 240.5 176 320 176C354 176 385.2 187.8 409.9 207.5L466.7 150.7C427.4 116.6 376.1 96 320 96C196.3 96 96 196.3 96 320C96 443.7 196.3 544 320 544C443.7 544 544 443.7 544 320L576 320C576 461.4 461.4 576 320 576C178.6 576 64 461.4 64 320C64 178.6 178.6 64 320 64C384.9 64 444.2 88.2 489.4 128L532.7 84.7C538.9 78.5 549.1 78.5 555.3 84.7C561.5 90.9 561.5 101.1 555.3 107.3L331.3 331.3C325.1 337.5 314.9 337.5 308.7 331.3C302.5 325.1 302.5 314.9 308.7 308.7L387.1 230.3z'],
  'sitemap': ['0 0 640 640', 'M352 128L352 192L288 192L288 128L352 128zM288 224L304 224L304 288L176 288C140.7 288 112 316.7 112 352L112 416L96 416C78.3 416 64 430.3 64 448L64 512C64 529.7 78.3 544 96 544L160 544C177.7 544 192 529.7 192 512L192 448C192 430.3 177.7 416 160 416L144 416L144 352C144 334.3 158.3 320 176 320L304 320L304 416L288 416C270.3 416 256 430.3 256 448L256 512C256 529.7 270.3 544 288 544L352 544C369.7 544 384 529.7 384 512L384 448C384 430.3 369.7 416 352 416L336 416L336 320L464 320C481.7 320 496 334.3 496 352L496 416L480 416C462.3 416 448 430.3 448 448L448 512C448 529.7 462.3 544 480 544L544 544C561.7 544 576 529.7 576 512L576 448C576 430.3 561.7 416 544 416L528 416L528 352C528 316.7 499.3 288 464 288L336 288L336 224L352 224C369.7 224 384 209.7 384 192L384 128C384 110.3 369.7 96 352 96L288 96C270.3 96 256 110.3 256 128L256 192C256 209.7 270.3 224 288 224zM96 448L160 448L160 512L96 512L96 448zM288 448L352 448L352 512L288 512L288 448zM480 448L544 448L544 512L480 512L480 448z'],
  'chart-line': ['0 0 640 640', 'M96 112C96 103.2 88.8 96 80 96C71.2 96 64 103.2 64 112L64 464C64 508.2 99.8 544 144 544L560 544C568.8 544 576 536.8 576 528C576 519.2 568.8 512 560 512L144 512C117.5 512 96 490.5 96 464L96 112zM539.3 219.3C545.5 213.1 545.5 202.9 539.3 196.7C533.1 190.5 522.9 190.5 516.7 196.7L384 329.4L299.3 244.7C293.1 238.5 282.9 238.5 276.7 244.7L164.7 356.7C158.5 362.9 158.5 373.1 164.7 379.3C170.9 385.5 181.1 385.5 187.3 379.3L288 278.6L372.7 363.3C378.9 369.5 389.1 369.5 395.3 363.3L539.3 219.3z'],
  'bell': ['0 0 640 640', 'M320 64C311.2 64 304 71.2 304 80L304 96.8C223.1 104.8 160 173 160 256L160 266.3C160 311.4 144.6 355.2 116.5 390.4L106.4 403C99.7 411.4 96 421.9 96 432.6C96 458.8 117.2 480 143.4 480L496.6 480C522.8 480 544 458.8 544 432.6C544 421.8 540.3 411.4 533.6 403L523.5 390.4C495.3 355.2 480 311.4 480 266.3L480 256C480 173 416.9 104.8 336 96.8L336 80C336 71.2 328.8 64 320 64zM320 128C390.7 128 448 185.3 448 256L448 266.3C448 318.7 465.8 369.5 498.6 410.4L508.6 423C510.8 425.7 512 429.1 512 432.6C512 441.1 505.1 448 496.6 448L143.4 448C134.9 448 128 441.1 128 432.6C128 429.1 129.2 425.7 131.4 423L141.5 410.4C174.2 369.5 192 318.7 192 266.3L192 256C192 185.3 249.3 128 320 128zM252.1 528C262 556 288.7 576 320 576C351.3 576 378 556 387.9 528L352 528C344.7 537.7 333.1 544 320 544C306.9 544 295.3 537.7 288 528L252.1 528z'],
  'gear': ['0 0 640 640', 'M249.9 176.3C243.4 179.5 237.1 183.1 231.1 187.2C222.9 192.7 212.6 194.1 203.1 191L131.4 167.2L93.5 232.8L150 283.1C157.4 289.7 161.3 299.3 160.7 309.2C160.2 316.4 160.2 323.8 160.7 331C161.4 340.9 157.4 350.5 150 357.1L93.5 407.3L131.4 473L203.1 449.2C212.5 446.1 222.8 447.5 231.1 453C237.1 457 243.4 460.7 249.9 463.9C258.8 468.3 265.1 476.5 267.1 486.2L282.3 560.2L358.1 560.2L373.3 486.2C375.3 476.5 381.7 468.3 390.5 463.9C397 460.7 403.3 457.1 409.3 453C417.5 447.5 427.8 446.1 437.3 449.2L509 473L546.9 407.3L490.4 357.1C483 350.5 479.1 340.9 479.7 331C479.9 327.4 480.1 323.8 480.1 320.1C480.1 316.4 480 312.8 479.7 309.2C479 299.3 483 289.7 490.4 283.1L546.9 232.9L509 167.2L437.3 191C427.9 194.1 417.6 192.7 409.3 187.2C403.3 183.2 397 179.5 390.5 176.3C381.6 171.9 375.3 163.7 373.3 154L358.1 80L282.3 80L267.1 154C265.1 163.7 258.7 171.9 249.9 176.3zM358.2 48C373.4 48 386.5 58.7 389.5 73.5L404.7 147.5C412.5 151.3 420.1 155.7 427.3 160.6L499 136.8C513.4 132 529.2 138 536.8 151.2L574.7 216.9C582.3 230.1 579.6 246.7 568.2 256.8L511.9 307C512.5 315.6 512.5 324.5 511.9 333L568.4 383.2C579.8 393.3 582.4 410 574.9 423.1L537 488.8C529.4 502 513.6 508 499.2 503.2L427.5 479.4C420.3 484.2 412.8 488.6 404.9 492.5L389.7 566.5C386.6 581.4 373.5 592 358.4 592L282.6 592C267.4 592 254.3 581.3 251.3 566.5L236.1 492.5C228.3 488.7 220.7 484.3 213.5 479.4L141.5 503.2C127.1 508 111.3 502 103.7 488.8L65.8 423.2C58.2 410.1 60.9 393.4 72.3 383.3L128.7 333C128.1 324.4 128.1 315.5 128.7 307L72.2 256.8C60.8 246.7 58.2 230 65.7 216.9L103.7 151.2C111.3 138 127.1 132 141.5 136.8L213.2 160.6C220.4 155.8 227.9 151.4 235.8 147.5L251 73.5C254.1 58.7 267.2 48 282.4 48L358.2 48zM264.3 320C264.3 350.8 289.2 375.7 320 375.7C350.8 375.7 375.7 350.8 375.7 320C375.7 289.2 350.8 264.3 320 264.3C289.2 264.3 264.3 289.2 264.3 320zM319.7 408C271.1 407.8 231.8 368.3 232 319.7C232.2 271.1 271.7 231.8 320.3 232C368.9 232.2 408.2 271.7 408 320.3C407.8 368.9 368.3 408.2 319.7 408z'],
  'arrow-right-from-bracket': ['0 0 640 640', 'M240 128C248.8 128 256 120.8 256 112C256 103.2 248.8 96 240 96L160 96C107 96 64 139 64 192L64 448C64 501 107 544 160 544L240 544C248.8 544 256 536.8 256 528C256 519.2 248.8 512 240 512L160 512C124.7 512 96 483.3 96 448L96 192C96 156.7 124.7 128 160 128L240 128zM571.3 331.3C577.5 325.1 577.5 314.9 571.3 308.7L427.3 164.7C421.1 158.5 410.9 158.5 404.7 164.7C398.5 170.9 398.5 181.1 404.7 187.3L521.4 304L240 304C231.2 304 224 311.2 224 320C224 328.8 231.2 336 240 336L521.4 336L404.7 452.7C398.5 458.9 398.5 469.1 404.7 475.3C410.9 481.5 421.1 481.5 427.3 475.3L571.3 331.3z'],
  'user': ['0 0 640 640', 'M224 192C224 139 267 96 320 96C373 96 416 139 416 192C416 245 373 288 320 288C267 288 224 245 224 192zM448 192C448 121.3 390.7 64 320 64C249.3 64 192 121.3 192 192C192 262.7 249.3 320 320 320C390.7 320 448 262.7 448 192zM128 544C128 464.5 192.5 400 272 400L368 400C447.5 400 512 464.5 512 544L512 560C512 568.8 519.2 576 528 576C536.8 576 544 568.8 544 560L544 544C544 446.8 465.2 368 368 368L272 368C174.8 368 96 446.8 96 544L96 560C96 568.8 103.2 576 112 576C120.8 576 128 568.8 128 560L128 544z'],
  'lock': ['0 0 640 640', 'M224 160L224 224L416 224L416 160C416 107 373 64 320 64C267 64 224 107 224 160zM192 224L192 160C192 89.3 249.3 32 320 32C390.7 32 448 89.3 448 160L448 224C483.3 224 512 252.7 512 288L512 512C512 547.3 483.3 576 448 576L192 576C156.7 576 128 547.3 128 512L128 288C128 252.7 156.7 224 192 224zM160 288L160 512C160 529.7 174.3 544 192 544L448 544C465.7 544 480 529.7 480 512L480 288C480 270.3 465.7 256 448 256L192 256C174.3 256 160 270.3 160 288z'],
  'magnifying-glass': ['0 0 640 640', 'M448 272C448 174.8 369.2 96 272 96C174.8 96 96 174.8 96 272C96 369.2 174.8 448 272 448C369.2 448 448 369.2 448 272zM407.3 430C371 461.2 323.7 480 272 480C157.1 480 64 386.9 64 272C64 157.1 157.1 64 272 64C386.9 64 480 157.1 480 272C480 323.7 461.2 371 430 407.3L571.3 548.7C577.5 554.9 577.5 565.1 571.3 571.3C565.1 577.5 554.9 577.5 548.7 571.3L407.3 430z'],
  'xmark': ['0 0 640 640', 'M507.3 155.3C513.5 149.1 513.5 138.9 507.3 132.7C501.1 126.5 490.9 126.5 484.7 132.7L320 297.4L155.3 132.7C149.1 126.5 138.9 126.5 132.7 132.7C126.5 138.9 126.5 149.1 132.7 155.3L297.4 320L132.7 484.7C126.5 490.9 126.5 501.1 132.7 507.3C138.9 513.5 149.1 513.5 155.3 507.3L320 342.6L484.7 507.3C490.9 513.5 501.1 513.5 507.3 507.3C513.5 501.1 513.5 490.9 507.3 484.7L342.6 320L507.3 155.3z'],
  'brain-circuit': ['0 0 640 640', 'M264 96C286 96 303.8 113.7 304 135.7L304 136L304 224L285.3 224C278.7 205.4 260.9 192 240 192C213.5 192 192 213.5 192 240C192 266.5 213.5 288 240 288C260.9 288 278.7 274.6 285.3 256L304 256L304 320L256 320C229.5 320 208 341.5 208 368L208 370.7C189.4 377.3 176 395.1 176 416C176 442.5 197.5 464 224 464C250.5 464 272 442.5 272 416C272 395.1 258.6 377.3 240 370.7L240 368C240 359.2 247.2 352 256 352L304 352L304 492C304 520.7 280.7 544 252 544C228.6 544 208.7 528.5 202.2 507.2C200.3 500.8 194.6 496.3 187.9 495.9C154.5 493.8 128 466 128 432C128 420.1 131.2 409.1 136.8 399.6C140.6 393.2 139.4 385.1 134.1 379.9C120.5 366.8 112 348.4 112 328C112 299.8 128.2 275.4 151.8 263.6C159.7 259.7 162.9 250.1 159 242.2C154.5 233.1 152 222.9 152 212.1C152 177.7 177.5 149.3 210.6 144.7C217.8 143.7 223.4 138.1 224.3 130.9C226.8 111.3 243.6 96.1 264 96.1zM336 256L354.7 256C361.3 274.6 379.1 288 400 288C426.5 288 448 266.5 448 240C448 213.5 426.5 192 400 192C379.1 192 361.3 205.4 354.7 224L336 224L336 135.7C336.2 113.7 354 96 376 96C396.3 96 413.1 111.2 415.7 130.8C416.6 138 422.2 143.6 429.4 144.6C462.5 149.2 488 177.6 488 212C488 222.9 485.5 233.1 481 242.1C477.1 250 480.3 259.6 488.2 263.5C511.9 275.3 528 299.8 528 327.9C528 348.3 519.5 366.7 505.9 379.8C500.6 384.9 499.4 393.1 503.2 399.5C508.8 409 512 420.1 512 431.9C512 465.9 485.5 493.7 452.1 495.8C445.4 496.2 439.7 500.7 437.8 507.1C431.3 528.4 411.5 543.9 388 543.9C359.3 543.9 336 520.6 336 491.9L336 463.9L384 463.9C410.5 463.9 432 442.4 432 415.9L432 413.2C450.6 406.6 464 388.8 464 367.9C464 341.4 442.5 319.9 416 319.9C389.5 319.9 368 341.4 368 367.9C368 388.8 381.4 406.6 400 413.2L400 415.9C400 424.7 392.8 431.9 384 431.9L336 431.9L336 255.9zM252 576C280 576 304.7 562.3 320 541.3C335.3 562.3 360 576 388 576C422.1 576 451.4 555.7 464.6 526.6C509.7 518.7 544 479.4 544 432C544 418.6 541.2 405.8 536.2 394.2C551 376.2 560 353.2 560 328C560 292.6 542.3 261.3 515.3 242.5C518.4 232.9 520 222.6 520 212C520 165.4 488.1 126.2 444.9 115.1C436 85.5 408.5 64 376 64C353.4 64 333.2 74.4 320 90.7C306.8 74.4 286.6 64 264 64C231.5 64 204 85.5 195.1 115.1C151.9 126.2 120 165.3 120 212C120 222.6 121.7 232.9 124.7 242.5C97.7 261.3 80 292.5 80 328C80 353.1 88.9 376.2 103.8 394.2C98.8 405.8 96 418.6 96 432C96 479.4 130.3 518.7 175.4 526.6C188.6 555.7 217.9 576 252 576zM400 256C391.2 256 384 248.8 384 240C384 231.2 391.2 224 400 224C408.8 224 416 231.2 416 240C416 248.8 408.8 256 400 256zM432 368C432 376.8 424.8 384 416 384C407.2 384 400 376.8 400 368C400 359.2 407.2 352 416 352C424.8 352 432 359.2 432 368zM240 416C240 424.8 232.8 432 224 432C215.2 432 208 424.8 208 416C208 407.2 215.2 400 224 400C232.8 400 240 407.2 240 416zM256 240C256 248.8 248.8 256 240 256C231.2 256 224 248.8 224 240C224 231.2 231.2 224 240 224C248.8 224 256 231.2 256 240z'],
  'network-wired': ['0 0 640 640', 'M368 128C376.8 128 384 135.2 384 144L384 208C384 216.8 376.8 224 368 224L272 224C263.2 224 256 216.8 256 208L256 144C256 135.2 263.2 128 272 128L368 128zM272 96C245.5 96 224 117.5 224 144L224 208C224 234.5 245.5 256 272 256L304 256L304 304L48 304C39.2 304 32 311.2 32 320C32 328.8 39.2 336 48 336L176 336L176 384L144 384C117.5 384 96 405.5 96 432L96 496C96 522.5 117.5 544 144 544L240 544C266.5 544 288 522.5 288 496L288 432C288 405.5 266.5 384 240 384L208 384L208 336L432 336L432 384L400 384C373.5 384 352 405.5 352 432L352 496C352 522.5 373.5 544 400 544L496 544C522.5 544 544 522.5 544 496L544 432C544 405.5 522.5 384 496 384L464 384L464 336L592 336C600.8 336 608 328.8 608 320C608 311.2 600.8 304 592 304L336 304L336 256L368 256C394.5 256 416 234.5 416 208L416 144C416 117.5 394.5 96 368 96L272 96zM144 416L240 416C248.8 416 256 423.2 256 432L256 496C256 504.8 248.8 512 240 512L144 512C135.2 512 128 504.8 128 496L128 432C128 423.2 135.2 416 144 416zM496 416C504.8 416 512 423.2 512 432L512 496C512 504.8 504.8 512 496 512L400 512C391.2 512 384 504.8 384 496L384 432C384 423.2 391.2 416 400 416L496 416z'],
  'eye': ['0 0 640 640', 'M320 128C179.2 128 90.7 256 64 320C90.7 384 179.2 512 320 512C460.8 512 549.3 384 576 320C549.3 256 460.8 128 320 128zM127.4 176.6C174.5 132.8 239.2 96 320 96C400.8 96 465.5 132.8 512.6 176.6C559.4 220.1 590.7 272 605.6 307.7C608.9 315.6 608.9 324.4 605.6 332.3C590.7 368 559.4 420 512.6 463.4C465.5 507.1 400.8 544 320 544C239.2 544 174.5 507.2 127.4 463.4C80.6 419.9 49.3 368 34.4 332.3C31.1 324.4 31.1 315.6 34.4 307.7C49.3 272 80.6 220 127.4 176.6zM320 416C373 416 416 373 416 320C416 276.7 387.3 240.1 347.9 228.1C350.6 236.9 352 246.3 352 256C352 309 309 352 256 352C246.3 352 236.9 350.6 228.1 347.9C240 387.3 276.7 416 320 416zM192.2 327.8C192 325.2 192 322.6 192 320C192 307.8 193.7 296.1 196.9 285C197.2 284.1 197.4 283.2 197.7 282.3C210.1 241.9 242 210.1 282.4 197.6C294.3 193.9 307 192 320.1 192C322.6 192 325.1 192.1 327.5 192.2L327.9 192.2C395 196.2 448.1 251.9 448.1 320C448.1 390.7 390.8 448 320.1 448C252 448 196.3 394.8 192.3 327.8zM224.3 311.7C233.6 317 244.4 320.1 255.9 320.1C291.2 320.1 319.9 291.4 319.9 256.1C319.9 244.6 316.9 233.8 311.5 224.5C265.1 228.5 228.2 265.4 224.2 311.8z'],
  'diagram-project': ['0 0 640 640', 'M112 128C103.2 128 96 135.2 96 144L96 240C96 248.8 103.2 256 112 256L208 256C216.8 256 224 248.8 224 240L224 144C224 135.2 216.8 128 208 128L112 128zM211.9 287.8C210.6 287.9 209.3 288 208 288L112 288C85.5 288 64 266.5 64 240L64 144C64 117.5 85.5 96 112 96L208 96C234.5 96 256 117.5 256 144L256 176L384 176L384 144C384 117.5 405.5 96 432 96L528 96C554.5 96 576 117.5 576 144L576 240C576 266.5 554.5 288 528 288L432 288C405.5 288 384 266.5 384 240L384 208L256 208L256 240C256 253.4 250.5 265.5 241.7 274.2L300.2 352.1C301.5 352 302.8 351.9 304.1 351.9L400.1 351.9C426.6 351.9 448.1 373.4 448.1 399.9L448.1 495.9C448.1 522.4 426.6 543.9 400.1 543.9L304.1 543.9C277.6 543.9 256.1 522.4 256.1 495.9L256.1 399.9C256.1 386.5 261.6 374.4 270.4 365.7L211.9 287.8zM304 384C295.2 384 288 391.2 288 400L288 496C288 504.8 295.2 512 304 512L400 512C408.8 512 416 504.8 416 496L416 400C416 391.2 408.8 384 400 384L304 384zM416 208L416 240C416 248.8 423.2 256 432 256L528 256C536.8 256 544 248.8 544 240L544 144C544 135.2 536.8 128 528 128L432 128C423.2 128 416 135.2 416 144L416 208z'],
  'bolt': ['0 0 640 640', 'M380.1 256L445.7 91.9C451.1 78.3 446.7 62.7 434.8 54.1C422.9 45.5 406.8 46 395.5 55.4L107.5 295.4C97.2 304 93.3 318.2 97.9 330.9C102.5 343.6 114.5 352 128 352L272.7 352L259.9 384L194.3 548.1C188.9 561.7 193.4 577.3 205.2 585.9C217 594.5 233.2 594 244.5 584.6L532.5 344.6C542.8 336 546.7 321.8 542.1 309.1C537.5 296.4 525.5 288 512 288L367.3 288L380.1 256zM299.3 334.1C293.3 325.3 283.4 320 272.8 320L128 320L416 80L337.6 276.1C333.7 286 334.9 297.1 340.8 305.9C346.7 314.7 356.6 320 367.3 320L512 320L224 560L302.4 363.9C306.3 354 305.1 342.9 299.2 334.1z'],
  'angle-left': ['0 0 640 640', 'M210.3 325.7C207.2 322.6 207.2 317.5 210.3 314.4L386.3 138.4C389.4 135.3 394.5 135.3 397.6 138.4C400.7 141.5 400.7 146.6 397.6 149.7L227.3 320L397.7 490.3C400.8 493.4 400.8 498.5 397.7 501.6C394.6 504.7 389.5 504.7 386.4 501.6L210.4 325.6z'],
  'angle-right': ['0 0 640 640', 'M429.7 314.3C432.8 317.4 432.8 322.5 429.7 325.6L253.7 501.6C250.6 504.7 245.5 504.7 242.4 501.6C239.3 498.5 239.3 493.4 242.4 490.3L412.7 320L242.3 149.7C239.2 146.6 239.2 141.5 242.3 138.4C245.4 135.3 250.5 135.3 253.6 138.4L429.6 314.4z'],
  'angle-down': ['0 0 640 640', 'M314.3 429.7C317.4 432.8 322.5 432.8 325.6 429.7L501.6 253.7C504.7 250.6 504.7 245.5 501.6 242.4C498.5 239.3 493.4 239.3 490.3 242.4L320 412.7L149.7 242.3C146.6 239.2 141.5 239.2 138.4 242.3C135.3 245.4 135.3 250.5 138.4 253.6L314.4 429.6z'],
  'check': ['0 0 640 640', 'M540.7 129.5C544.3 132.1 545 137.1 542.4 140.7L254.4 532.7C253 534.6 250.9 535.8 248.6 535.9C246.3 536 244 535.2 242.3 533.6L98.3 389.6C95.2 386.5 95.2 381.4 98.3 378.3C101.4 375.2 106.5 375.2 109.6 378.3L247.1 515.8L529.6 131.3C532.2 127.7 537.2 127 540.8 129.6z'],
  'circle-check': ['0 0 640 640', 'M320 576C178.6 576 64 461.4 64 320C64 178.6 178.6 64 320 64C461.4 64 576 178.6 576 320C576 461.4 461.4 576 320 576zM320 80C187.5 80 80 187.5 80 320C80 452.5 187.5 560 320 560C452.5 560 560 452.5 560 320C560 187.5 452.5 80 320 80zM417.4 224.4C420 220.8 425 220.1 428.6 222.6C432.2 225.1 433 230.2 430.4 233.8L294.5 420.7C293.1 422.6 291 423.8 288.7 424C286.4 424.2 284.1 423.3 282.4 421.7L210.4 349.7C207.3 346.6 207.3 341.5 210.4 338.4C213.5 335.3 218.6 335.3 221.7 338.4L287 403.7L417.4 224.4z'],
  'badge-check': ['0 0 640 640', 'M320 64C356.4 64 388.3 83.3 406.1 112.2C439.1 104.3 475.3 113.3 501 139C526.7 164.7 535.7 201 527.8 233.9C556.7 251.7 576 283.6 576 320C576 356.4 556.7 388.3 527.8 406.1C535.7 439.1 526.7 475.3 501 501C475.3 526.7 439 535.7 406.1 527.8C388.3 556.7 356.4 576 320 576C283.6 576 251.7 556.7 233.9 527.8C200.9 535.7 164.7 526.7 139 501C113.3 475.3 104.3 439 112.2 406.1C83.3 388.3 64 356.4 64 320C64 283.6 83.3 251.7 112.2 233.9C104.3 200.9 113.3 164.7 139 139C164.7 113.3 201 104.3 233.9 112.2C251.7 83.3 283.6 64 320 64zM320 80C287.4 80 259.1 98.3 244.8 125.2C243 128.6 239.1 130.2 235.4 129.1C206.3 120.2 173.3 127.2 150.2 150.2C127.1 173.2 120.1 206.2 129.1 235.4C130.2 239.1 128.6 243 125.2 244.8C98.3 259.1 80 287.4 80 320C80 352.6 98.3 380.9 125.2 395.2C128.6 397 130.2 400.9 129.1 404.6C120.2 433.7 127.2 466.7 150.2 489.8C173.2 512.9 206.2 519.9 235.4 510.9C239.1 509.8 243 511.4 244.8 514.8C259.1 541.7 287.4 560.1 320 560.1C352.6 560.1 380.9 541.8 395.2 514.8C397 511.4 400.9 509.8 404.6 510.9C433.7 519.8 466.7 512.8 489.8 489.8C512.9 466.8 519.9 433.8 510.9 404.6C509.8 400.9 511.4 397 514.8 395.2C541.7 380.9 560.1 352.6 560.1 320C560.1 287.4 541.8 259.1 514.8 244.8C511.4 243 509.8 239.1 510.9 235.4C519.8 206.3 512.8 173.3 489.8 150.2C466.8 127.1 433.8 120.1 404.6 129.1C400.9 130.2 397 128.6 395.2 125.2C380.9 98.3 352.6 80 320 80zM401.7 227.7C404.3 224.1 409.3 223.4 412.9 226C416.5 228.6 417.2 233.6 414.7 237.2L300.9 393.5C299.5 395.4 297.3 396.6 295 396.8C292.7 397 290.3 396.1 288.7 394.4L226.4 329.9C223.3 326.7 223.4 321.7 226.6 318.6C229.8 315.5 234.8 315.6 237.9 318.8L293.6 376.4L401.7 227.7z'],
  'bars': ['0 0 640 640', 'M104 160H536V200H104ZM104 300H536V340H104ZM104 440H536V480H104Z'],
  'rectangle-history': ['0 0 640 640', 'M544 336C544 318.3 529.7 304 512 304L128 304C110.3 304 96 318.3 96 336L96 480C96 497.7 110.3 512 128 512L512 512C529.7 512 544 497.7 544 480L544 336zM512 272C547.3 272 576 300.7 576 336L576 480C576 515.3 547.3 544 512 544L128 544C92.7 544 64 515.3 64 480L64 336C64 300.7 92.7 272 128 272L512 272zM512 168C520.8 168 528 175.2 528 184C528 192.8 520.8 200 512 200L128 200C119.2 200 112 192.8 112 184C112 175.2 119.2 168 128 168L512 168zM464 64C472.8 64 480 71.2 480 80C480 88.8 472.8 96 464 96L176 96C167.2 96 160 88.8 160 80C160 71.2 167.2 64 176 64L464 64z'],
  'shield-check': ['0 0 640 640', 'M295.1 71.9C311.1 65.1 329.1 65.1 345.1 71.9L521.8 146.8C543.8 156.1 560.2 177.8 560.1 204C559.6 303.2 518.8 484.7 346.4 567.2C329.7 575.2 310.3 575.2 293.7 567.2C121.3 484.7 80.6 303.2 80.1 204C80 177.8 96.4 156.1 118.4 146.8L295.1 71.9zM332.6 101.3C324.6 97.9 315.6 97.9 307.6 101.3L130.9 176.3C119.6 181.1 112.1 191.8 112.1 203.9C112.6 297.9 151.4 463.7 307.5 538.4C315.4 542.2 324.7 542.2 332.6 538.4C488.7 463.7 527.6 298 528.1 203.9C528.2 191.8 520.6 181.1 509.3 176.3L332.6 101.3zM387.1 234.2C392.3 227.1 402.3 225.5 409.4 230.7C416.5 235.9 418.1 245.9 412.9 253L307.4 398.2C304.6 402 300.3 404.4 295.6 404.7C290.9 405 286.3 403.3 283 399.9L228.6 343.6C222.5 337.2 222.6 327.1 229 321C235.4 314.9 245.5 315.1 251.6 321.4L292.8 364L387.2 234.2z']
};
Object.assign(__ds_scope, { iconGlyphs });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/icon-paths.js", error: String((e && e.message) || e) }); }

// components/core/Icon.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* FontAwesome Light — the system's icon face. The hairline stroke matches the 1px borders and
   the thin type used across the brand far better than a font-based icon set, and rendering the
   glyphs as inline SVG (rather than an icon font) means they scale and take `color` without a
   webfont load.

   A Thin weight is also available (icon-paths-thin.js), switched globally rather than per call
   site — a page mixing weights reads as a bug, not a choice. setIconWeight()/getIconWeight() are
   attached to `window` (not exported) so the Tweaks panel of ANY page loaded alongside this
   module can flip it; useIconWeight() is the subscription every <Icon> re-renders on.

   Names are semantic. ICON_ALIAS keeps the Material-era names that the kits were written
   against pointing at their chosen FA replacement, so both spellings work:
       <Icon name="dna" />  ===  <Icon name="biotech" />
   `weight` on a single <Icon> is accepted and ignored — weight is a whole-site setting, not a
   per-icon prop; see setIconWeight. */
const ICON_ALIAS = {
  autorenew: 'arrows-rotate',
  schedule: 'clock',
  hub: 'circle-nodes',
  biotech: 'dna',
  speed: 'gauge-high',
  developer_board: 'microchip',
  flight: 'drone',
  arrow_forward: 'arrow-right',
  arrow_back: 'arrow-left',
  north_east: 'arrow-up-right',
  download: 'arrow-down-to-line',
  upload: 'arrow-up-from-line',
  fullscreen: 'expand',
  warning: 'triangle-exclamation',
  undo: 'arrow-rotate-left',
  account_tree: 'sitemap',
  monitoring: 'chart-line',
  notifications: 'bell',
  settings: 'gear',
  logout: 'arrow-right-from-bracket',
  person: 'user',
  search: 'magnifying-glass',
  search_off: 'magnifying-glass',
  close: 'xmark',
  visibility: 'eye',
  memory_alt: 'memory',
  menu: 'bars'
};
let _iconWeight = typeof window !== 'undefined' && window.__vvIconWeight || 'light';
const _iconWeightSubs = new Set();
function setIconWeight(w) {
  _iconWeight = w === 'thin' ? 'thin' : 'light';
  if (typeof window !== 'undefined') window.__vvIconWeight = _iconWeight;
  _iconWeightSubs.forEach(fn => fn());
}
function getIconWeight() {
  return _iconWeight;
}
function useIconWeight() {
  const [, force] = React.useState(0);
  React.useEffect(() => {
    const fn = () => force(x => x + 1);
    _iconWeightSubs.add(fn);
    return () => _iconWeightSubs.delete(fn);
  }, []);
  return _iconWeight;
}
if (typeof window !== 'undefined') {
  window.vvSetIconWeight = setIconWeight;
  window.vvGetIconWeight = getIconWeight;
}
function Icon({
  name,
  size = 20,
  color = 'currentColor',
  filled,
  weight,
  style,
  ...rest
}) {
  const globalWeight = useIconWeight();
  const set = globalWeight === 'thin' ? __ds_scope.iconGlyphsThin : __ds_scope.iconGlyphs;
  const key = set[name] ? name : ICON_ALIAS[name] && set[ICON_ALIAS[name]] ? ICON_ALIAS[name] : null;
  // A thin glyph not yet drawn for a given name falls back to light rather than rendering blank.
  const fallbackKey = __ds_scope.iconGlyphs[name] ? name : ICON_ALIAS[name];
  const glyph = key ? set[key] : fallbackKey ? __ds_scope.iconGlyphs[fallbackKey] : null;
  if (!glyph) return null;
  const [viewBox, d] = glyph;
  return /*#__PURE__*/React.createElement("svg", _extends({
    "aria-hidden": "true",
    viewBox: viewBox,
    width: size,
    height: size,
    focusable: "false",
    style: {
      display: 'inline-block',
      flex: 'none',
      color,
      verticalAlign: 'middle',
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("path", {
    d: d,
    fill: "currentColor"
  }));
}
Object.assign(__ds_scope, { Icon });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Icon.jsx", error: String((e && e.message) || e) }); }

// components/core/Button.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const SIZES = {
  sm: {
    height: 32,
    padX: 14,
    font: 13,
    icon: 16,
    gap: 7
  },
  md: {
    height: 40,
    padX: 20,
    font: 14,
    icon: 18,
    gap: 9
  },
  lg: {
    height: 48,
    padX: 26,
    font: 16,
    icon: 20,
    gap: 10
  }
};

/* One button, four colourways. Every variant is the same object — a square 1px rule, a 7%
   wash of its own colour behind it, and a label in that same colour — so primary and
   secondary always read as a matched pair, and amber stays a lit edge rather than a slab.
   Hover lifts the wash to 14%; nothing else moves. */
const VARIANTS = {
  primary: {
    line: 'var(--vv-amber)',
    wash: 'var(--vv-amber-07)',
    washHover: 'var(--vv-amber-14)',
    fg: 'var(--vv-amber)'
  },
  secondary: {
    line: 'var(--vv-ice)',
    wash: 'var(--vv-ice-07)',
    washHover: 'var(--vv-ice-14)',
    fg: 'var(--vv-ice)'
  },
  /* Text only — no rule, no wash at rest. For tertiary actions in a row. */
  quiet: {
    line: 'transparent',
    wash: 'transparent',
    washHover: 'var(--vv-ice-04)',
    fg: 'var(--action-quiet-fg)'
  },
  onLight: {
    line: 'var(--vv-deep-navy)',
    wash: 'var(--vv-navy-07)',
    washHover: 'var(--vv-navy-24)',
    fg: 'var(--vv-deep-navy)'
  }
};
function Button({
  children,
  variant = 'primary',
  size = 'md',
  icon,
  iconAfter,
  fullWidth,
  disabled,
  href,
  onClick,
  style,
  ...rest
}) {
  const s = SIZES[size] || SIZES.md;
  const v = VARIANTS[variant] || VARIANTS.primary;
  const [hover, setHover] = React.useState(false);
  const [press, setPress] = React.useState(false);
  const Tag = href ? 'a' : 'button';
  return /*#__PURE__*/React.createElement(Tag, _extends({
    href: href,
    onClick: disabled ? undefined : onClick,
    disabled: href ? undefined : disabled,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => {
      setHover(false);
      setPress(false);
    },
    onMouseDown: () => setPress(true),
    onMouseUp: () => setPress(false),
    style: {
      display: fullWidth ? 'flex' : 'inline-flex',
      width: fullWidth ? '100%' : undefined,
      alignItems: 'center',
      justifyContent: 'center',
      gap: s.gap,
      boxSizing: 'border-box',
      height: s.height,
      padding: `0 ${s.padX}px`,
      borderRadius: 0,
      fontFamily: 'var(--font-sans)',
      fontSize: s.font,
      fontWeight: 400,
      lineHeight: 1,
      letterSpacing: '0.01em',
      textDecoration: 'none',
      cursor: disabled ? 'not-allowed' : 'pointer',
      border: `1px solid ${v.line}`,
      color: v.fg,
      background: hover && !disabled ? v.washHover : v.wash,
      opacity: disabled ? 0.38 : 1,
      transform: press && !disabled ? 'scale(var(--press-scale))' : 'none',
      transition: 'background var(--dur-fast) var(--ease-out), transform var(--dur-instant) var(--ease-out)',
      ...style
    }
  }, rest), icon ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: s.icon
  }) : null, children, iconAfter ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: iconAfter,
    size: s.icon
  }) : null);
}
Object.assign(__ds_scope, { Button });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Button.jsx", error: String((e && e.message) || e) }); }

// components/core/IconButton.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const SIZES = {
  sm: {
    box: 28,
    icon: 16
  },
  md: {
    box: 36,
    icon: 18
  },
  lg: {
    box: 44,
    icon: 20
  }
};
function IconButton({
  icon,
  label,
  size = 'md',
  variant = 'outline',
  active,
  disabled,
  onClick,
  style,
  ...rest
}) {
  const s = SIZES[size] || SIZES.md;
  const [hover, setHover] = React.useState(false);
  const base = variant === 'outline' ? {
    background: 'transparent',
    border: '1px solid var(--action-secondary-line)'
  } : {
    background: 'transparent',
    border: '1px solid transparent'
  };
  return /*#__PURE__*/React.createElement("button", _extends({
    "aria-label": label,
    title: label,
    onClick: disabled ? undefined : onClick,
    disabled: disabled,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: s.box,
      height: s.box,
      borderRadius: 'var(--radius-sm)',
      cursor: disabled ? 'not-allowed' : 'pointer',
      color: active ? 'var(--vv-amber)' : 'var(--text-secondary)',
      opacity: disabled ? 0.38 : 1,
      transition: 'background var(--dur-fast) var(--ease-out), color var(--dur-fast) var(--ease-out), border-color var(--dur-fast) var(--ease-out)',
      ...base,
      ...(hover && !disabled ? {
        background: 'var(--action-secondary-bg-hover)',
        color: 'var(--vv-ice)'
      } : null),
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: s.icon
  }));
}
Object.assign(__ds_scope, { IconButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/IconButton.jsx", error: String((e && e.message) || e) }); }

// components/data/DataTable.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function DataTable({
  columns = [],
  rows = [],
  dense,
  onLight,
  style,
  ...rest
}) {
  const line = onLight ? 'var(--line-on-light)' : 'var(--line-hairline)';
  const padY = dense ? 9 : 13;
  return /*#__PURE__*/React.createElement("table", _extends({
    style: {
      width: '100%',
      borderCollapse: 'collapse',
      fontFamily: 'var(--font-sans)',
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map(c => /*#__PURE__*/React.createElement("th", {
    key: c.key,
    style: {
      textAlign: c.align || 'left',
      padding: `10px 16px`,
      background: onLight ? 'transparent' : 'var(--vv-ice-04)',
      borderBottom: `1px solid ${line}`,
      fontSize: 11,
      fontWeight: 600,
      letterSpacing: 'var(--track-label)',
      textTransform: 'uppercase',
      color: 'var(--text-label)',
      whiteSpace: 'nowrap'
    }
  }, c.label)))), /*#__PURE__*/React.createElement("tbody", null, rows.map((r, i) => /*#__PURE__*/React.createElement("tr", {
    key: r.id || i
  }, columns.map(c => /*#__PURE__*/React.createElement("td", {
    key: c.key,
    style: {
      textAlign: c.align || 'left',
      padding: `${padY}px 16px`,
      borderBottom: `1px solid ${line}`,
      fontSize: 13,
      lineHeight: 1.4,
      whiteSpace: c.wrap ? 'normal' : 'nowrap',
      fontFamily: c.mono ? 'var(--font-mono)' : 'var(--font-sans)',
      letterSpacing: c.mono ? 'var(--track-mono)' : '0',
      color: c.tone === 'muted' ? 'var(--text-caption)' : c.tone === 'data' ? 'var(--text-data)' : onLight ? 'var(--text-body-on-light)' : 'var(--text-heading)'
    }
  }, r[c.key]))))));
}
Object.assign(__ds_scope, { DataTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/DataTable.jsx", error: String((e && e.message) || e) }); }

// components/data/RatioBar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function RatioBar({
  segments = [],
  height = 12,
  legend = true,
  style,
  ...rest
}) {
  const total = segments.reduce((s, x) => s + (x.value || 0), 0) || 1;
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      fontFamily: 'var(--font-sans)',
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      height,
      width: '100%'
    }
  }, segments.map((s, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    title: s.label,
    style: {
      flex: s.value,
      background: s.color,
      border: s.outline ? '1px solid var(--line-strong)' : 'none',
      boxSizing: 'border-box'
    }
  }))), legend ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 20
    }
  }, segments.map((s, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      fontSize: 12,
      color: 'var(--text-caption)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      background: s.color,
      flex: 'none',
      border: s.outline ? '1px solid var(--line-strong)' : 'none',
      boxSizing: 'border-box'
    }
  }), s.label, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      color: 'var(--text-secondary)'
    }
  }, Math.round(s.value / total * 100), "%")))) : null);
}
Object.assign(__ds_scope, { RatioBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/RatioBar.jsx", error: String((e && e.message) || e) }); }

// components/data/Stat.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const SIZES = {
  sm: 24,
  md: 40,
  lg: 56,
  slide: 44
};
function Stat({
  value,
  label,
  unit,
  delta,
  deltaTone = 'blue',
  size = 'md',
  mono,
  style,
  ...rest
}) {
  const fs = SIZES[size] || SIZES.md;
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8,
      fontFamily: 'var(--font-sans)',
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: mono ? 'var(--font-mono)' : 'var(--font-sans)',
      fontSize: fs,
      fontWeight: 700,
      letterSpacing: 'var(--track-title)',
      lineHeight: 1,
      color: 'var(--text-heading)'
    }
  }, value), unit ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: Math.round(fs * 0.36),
      fontWeight: 500,
      color: 'var(--text-caption)'
    }
  }, unit) : null), label ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: size === 'slide' ? 24 : 11,
      fontWeight: 600,
      letterSpacing: 'var(--track-label)',
      textTransform: 'uppercase',
      color: 'var(--text-label)'
    }
  }, label) : null, delta ? /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 5,
      fontSize: 12,
      color: deltaTone === 'amber' ? 'var(--vv-amber)' : deltaTone === 'graphite' ? 'var(--vv-graphite)' : 'var(--vv-blue)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: String(delta).trim().startsWith('-') ? 'trending_down' : 'trending_up',
    size: 13
  }), delta) : null);
}
Object.assign(__ds_scope, { Stat });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Stat.jsx", error: String((e && e.message) || e) }); }

// components/data/StatusDot.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const TONES = {
  live: 'var(--status-live)',
  attention: 'var(--status-attention)',
  idle: 'var(--status-idle)',
  ok: 'var(--status-ok)'
};
function StatusDot({
  children,
  tone = 'live',
  pulse,
  mono,
  style,
  ...rest
}) {
  const c = TONES[tone] || TONES.live;
  return /*#__PURE__*/React.createElement("span", _extends({
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      fontFamily: mono ? 'var(--font-mono)' : 'var(--font-sans)',
      fontSize: 13,
      color: 'var(--text-secondary)',
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'relative',
      display: 'inline-flex',
      width: 7,
      height: 7,
      flex: 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      borderRadius: 'var(--radius-full)',
      background: c
    }
  }), pulse ? /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: -3,
      borderRadius: 'var(--radius-full)',
      border: `1px solid ${c}`,
      opacity: 0.5,
      animation: 'vv-pulse 2s var(--ease-field) infinite'
    }
  }) : null), children, /*#__PURE__*/React.createElement("style", null, '@keyframes vv-pulse{0%{transform:scale(0.7);opacity:0.7}70%{transform:scale(1.25);opacity:0}100%{opacity:0}}'));
}
Object.assign(__ds_scope, { StatusDot });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/StatusDot.jsx", error: String((e && e.message) || e) }); }

// components/feedback/EmptyState.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function EmptyState({
  icon = 'radar',
  title,
  children,
  action,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      justifyContent: 'center',
      gap: 14,
      padding: '56px 32px',
      textAlign: 'center',
      background: 'var(--surface-panel-quiet)',
      border: '1px solid var(--line-hairline)',
      borderRadius: 'var(--radius-sm)',
      boxSizing: 'border-box',
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 26,
    color: "var(--vv-graphite)"
  }), title ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 15,
      fontWeight: 600,
      color: 'var(--text-heading)'
    }
  }, title) : null, children ? /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      maxWidth: 380,
      fontSize: 13,
      lineHeight: 1.5,
      color: 'var(--text-caption)'
    }
  }, children) : null, action ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 6
    }
  }, action) : null);
}
Object.assign(__ds_scope, { EmptyState });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/EmptyState.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Modal.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Modal({
  open = true,
  title,
  meta,
  children,
  actions,
  onClose,
  width = 520,
  style,
  ...rest
}) {
  if (!open) return null;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      background: 'var(--vv-navy-72)',
      backdropFilter: 'blur(3px)',
      WebkitBackdropFilter: 'blur(3px)',
      padding: 24,
      zIndex: 40
    },
    onClick: onClose
  }, /*#__PURE__*/React.createElement("div", _extends({
    role: "dialog",
    "aria-modal": "true",
    onClick: e => e.stopPropagation(),
    style: {
      width,
      maxWidth: '100%',
      background: 'var(--surface-panel)',
      border: '1px solid var(--line-strong)',
      borderRadius: 'var(--radius-sm)',
      boxSizing: 'border-box',
      animation: 'vv-modal-in var(--dur-base) var(--ease-out) both',
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("header", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      justifyContent: 'space-between',
      gap: 16,
      padding: '20px 24px',
      borderBottom: '1px solid var(--line-hairline)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4
    }
  }, title ? /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: 18,
      fontWeight: 600,
      letterSpacing: '-0.01em',
      color: 'var(--text-heading)'
    }
  }, title) : null, meta ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--text-caption)'
    }
  }, meta) : null), onClose ? /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    icon: "close",
    label: "Close",
    size: "sm",
    variant: "ghost",
    onClick: onClose
  }) : null), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 24,
      fontSize: 14,
      lineHeight: 1.55,
      color: 'var(--text-body)'
    }
  }, children), actions ? /*#__PURE__*/React.createElement("footer", {
    style: {
      display: 'flex',
      justifyContent: 'flex-end',
      gap: 10,
      padding: '16px 24px',
      borderTop: '1px solid var(--line-hairline)'
    }
  }, actions) : null, /*#__PURE__*/React.createElement("style", null, '@keyframes vv-modal-in{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}')));
}
Object.assign(__ds_scope, { Modal });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Modal.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Toast.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Toast({
  children,
  title,
  tone = 'live',
  onClose,
  mono,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    role: "status",
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 12,
      width: 360,
      maxWidth: '100%',
      boxSizing: 'border-box',
      padding: '14px 14px 14px 16px',
      background: 'var(--surface-panel)',
      border: '1px solid var(--line-strong)',
      borderRadius: 'var(--radius-sm)',
      fontFamily: 'var(--font-sans)',
      animation: 'vv-toast-in var(--dur-base) var(--ease-out) both',
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement(__ds_scope.StatusDot, {
    tone: tone,
    style: {
      marginTop: 5
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      gap: 3,
      minWidth: 0
    }
  }, title ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13.5,
      fontWeight: 600,
      color: 'var(--text-heading)'
    }
  }, title) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12.5,
      lineHeight: 1.45,
      color: 'var(--text-secondary)',
      fontFamily: mono ? 'var(--font-mono)' : 'inherit'
    }
  }, children)), onClose ? /*#__PURE__*/React.createElement(__ds_scope.IconButton, {
    icon: "close",
    label: "Dismiss",
    size: "sm",
    variant: "ghost",
    onClick: onClose
  }) : null, /*#__PURE__*/React.createElement("style", null, '@keyframes vv-toast-in{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}'));
}
Object.assign(__ds_scope, { Toast });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Toast.jsx", error: String((e && e.message) || e) }); }

// components/forms/Checkbox.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Checkbox({
  label,
  hint,
  checked,
  defaultChecked,
  disabled,
  onChange,
  style,
  ...rest
}) {
  const [on, setOn] = React.useState(defaultChecked || false);
  const isOn = checked === undefined ? on : checked;
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 10,
      cursor: disabled ? 'not-allowed' : 'pointer',
      opacity: disabled ? 0.4 : 1,
      fontFamily: 'var(--font-sans)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("input", _extends({
    type: "checkbox",
    checked: isOn,
    disabled: disabled,
    onChange: e => {
      if (checked === undefined) setOn(e.target.checked);
      onChange && onChange(e);
    },
    style: {
      position: 'absolute',
      opacity: 0,
      width: 0,
      height: 0
    }
  }, rest)), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      flex: 'none',
      width: 16,
      height: 16,
      marginTop: 2,
      borderRadius: 'var(--radius-sm)',
      background: isOn ? 'var(--vv-amber)' : 'var(--vv-ice-04)',
      border: `1px solid ${isOn ? 'var(--vv-amber)' : 'var(--line-strong)'}`,
      transition: 'background var(--dur-fast) var(--ease-out), border-color var(--dur-fast) var(--ease-out)'
    }
  }, isOn ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "check",
    size: 12,
    color: "var(--vv-deep-navy)"
  }) : null), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 3
    }
  }, label ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 14,
      color: 'var(--text-heading)',
      lineHeight: 1.35
    }
  }, label) : null, hint ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--text-caption)',
      lineHeight: 1.4
    }
  }, hint) : null));
}
Object.assign(__ds_scope, { Checkbox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Checkbox.jsx", error: String((e && e.message) || e) }); }

// components/forms/Input.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Input({
  label,
  hint,
  error,
  icon,
  value,
  defaultValue,
  placeholder,
  type = 'text',
  disabled,
  onChange,
  style,
  ...rest
}) {
  const [focus, setFocus] = React.useState(false);
  const line = error ? 'var(--vv-amber)' : focus ? 'var(--vv-blue)' : 'var(--line-hairline)';
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8,
      fontFamily: 'var(--font-sans)',
      ...style
    }
  }, label ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      fontWeight: 600,
      letterSpacing: 'var(--track-label)',
      textTransform: 'uppercase',
      color: 'var(--text-label)'
    }
  }, label) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      height: 40,
      padding: '0 12px',
      boxSizing: 'border-box',
      background: 'var(--vv-ice-04)',
      border: `1px solid ${line}`,
      borderRadius: 'var(--radius-sm)',
      boxShadow: focus ? 'inset 0 0 0 1px var(--vv-blue-24)' : 'none',
      opacity: disabled ? 0.4 : 1,
      transition: 'border-color var(--dur-fast) var(--ease-out), box-shadow var(--dur-fast) var(--ease-out)'
    }
  }, icon ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 16,
    color: "var(--text-caption)"
  }) : null, /*#__PURE__*/React.createElement("input", _extends({
    type: type,
    value: value,
    defaultValue: defaultValue,
    placeholder: placeholder,
    disabled: disabled,
    onChange: onChange,
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      flex: 1,
      minWidth: 0,
      background: 'transparent',
      border: 0,
      outline: 'none',
      padding: 0,
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      color: 'var(--text-heading)',
      letterSpacing: '0.01em'
    }
  }, rest))), error || hint ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: error ? 'var(--vv-amber)' : 'var(--text-caption)',
      lineHeight: 1.4
    }
  }, error || hint) : null);
}
Object.assign(__ds_scope, { Input });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Input.jsx", error: String((e && e.message) || e) }); }

// components/forms/Select.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Select({
  label,
  hint,
  options = [],
  value,
  defaultValue,
  disabled,
  onChange,
  style,
  ...rest
}) {
  const [focus, setFocus] = React.useState(false);
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8,
      fontFamily: 'var(--font-sans)',
      ...style
    }
  }, label ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      fontWeight: 600,
      letterSpacing: 'var(--track-label)',
      textTransform: 'uppercase',
      color: 'var(--text-label)'
    }
  }, label) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'relative',
      display: 'flex',
      alignItems: 'center',
      height: 40,
      boxSizing: 'border-box',
      background: 'var(--vv-ice-04)',
      border: `1px solid ${focus ? 'var(--vv-blue)' : 'var(--line-hairline)'}`,
      borderRadius: 'var(--radius-sm)',
      opacity: disabled ? 0.4 : 1,
      transition: 'border-color var(--dur-fast) var(--ease-out)'
    }
  }, /*#__PURE__*/React.createElement("select", _extends({
    value: value,
    defaultValue: defaultValue,
    disabled: disabled,
    onChange: onChange,
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      appearance: 'none',
      WebkitAppearance: 'none',
      flex: 1,
      minWidth: 0,
      height: '100%',
      background: 'transparent',
      border: 0,
      outline: 'none',
      padding: '0 34px 0 12px',
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      color: 'var(--text-heading)',
      cursor: 'pointer'
    }
  }, rest), options.map(o => {
    const opt = typeof o === 'string' ? {
      value: o,
      label: o
    } : o;
    return /*#__PURE__*/React.createElement("option", {
      key: opt.value,
      value: opt.value,
      style: {
        background: 'var(--vv-slate)',
        color: 'var(--vv-ice)'
      }
    }, opt.label);
  })), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "keyboard_arrow_down",
    size: 16,
    color: "var(--text-caption)",
    style: {
      position: 'absolute',
      right: 11,
      pointerEvents: 'none'
    }
  })), hint ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--text-caption)'
    }
  }, hint) : null);
}
Object.assign(__ds_scope, { Select });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Select.jsx", error: String((e && e.message) || e) }); }

// components/forms/Switch.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Switch({
  label,
  hint,
  checked,
  defaultChecked,
  disabled,
  onChange,
  style,
  ...rest
}) {
  const [on, setOn] = React.useState(defaultChecked || false);
  const isOn = checked === undefined ? on : checked;
  const toggle = () => {
    if (disabled) return;
    if (checked === undefined) setOn(!isOn);
    onChange && onChange(!isOn);
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 16,
      fontFamily: 'var(--font-sans)',
      opacity: disabled ? 0.4 : 1,
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 3,
      minWidth: 0
    }
  }, label ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 14,
      color: 'var(--text-heading)'
    }
  }, label) : null, hint ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--text-caption)',
      lineHeight: 1.4
    }
  }, hint) : null), /*#__PURE__*/React.createElement("button", {
    role: "switch",
    "aria-checked": isOn,
    "aria-label": typeof label === 'string' ? label : 'Toggle',
    onClick: toggle,
    disabled: disabled,
    style: {
      position: 'relative',
      flex: 'none',
      width: 40,
      height: 20,
      padding: 2,
      boxSizing: 'border-box',
      borderRadius: 'var(--radius-sm)',
      cursor: disabled ? 'not-allowed' : 'pointer',
      background: isOn ? 'var(--vv-blue)' : 'var(--vv-ice-08)',
      border: `1px solid ${isOn ? 'var(--vv-blue)' : 'var(--line-strong)'}`,
      transition: 'background var(--dur-base) var(--ease-out), border-color var(--dur-base) var(--ease-out)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      width: 14,
      height: 14,
      borderRadius: 1,
      background: isOn ? 'var(--vv-ice)' : 'var(--vv-graphite)',
      transform: `translateX(${isOn ? 20 : 0}px)`,
      transition: 'transform var(--dur-base) var(--ease-out), background var(--dur-base) var(--ease-out)'
    }
  })));
}
Object.assign(__ds_scope, { Switch });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Switch.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Footer.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function useMaxWidth(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}

/* Three brand glyphs, inlined as currentColor path data (same device as components/core/
   icon-paths.js) so each social link can be colored and sized from CSS with no extra asset
   request. Add more by dropping another {viewBox, path} entry. */
const SOCIAL_GLYPHS = {
  x: ['0 0 640 640', 'M453.2 112L523.8 112L369.6 288.2L551 528L409 528L297.7 382.6L170.5 528L99.8 528L264.7 339.5L90.8 112L236.4 112L336.9 244.9L453.2 112zM428.4 485.8L467.5 485.8L215.1 152L173.1 152L428.4 485.8z'],
  linkedin: ['0 0 640 640', 'M196.3 512L103.4 512L103.4 212.9L196.3 212.9L196.3 512zM149.8 172.1C120.1 172.1 96 147.5 96 117.8C96 103.5 101.7 89.9 111.8 79.8C121.9 69.7 135.6 64 149.8 64C164 64 177.7 69.7 187.8 79.8C197.9 89.9 203.6 103.6 203.6 117.8C203.6 147.5 179.5 172.1 149.8 172.1zM543.9 512L451.2 512L451.2 366.4C451.2 331.7 450.5 287.2 402.9 287.2C354.6 287.2 347.2 324.9 347.2 363.9L347.2 512L254.4 512L254.4 212.9L343.5 212.9L343.5 253.7L344.8 253.7C357.2 230.2 387.5 205.4 432.7 205.4C526.7 205.4 544 267.3 544 347.7L544 512L543.9 512z'],
  facebook: ['0 0 640 640', 'M240 363.3L240 576L356 576L356 363.3L442.5 363.3L460.5 265.5L356 265.5L356 230.9C356 179.2 376.3 159.4 428.7 159.4C445 159.4 458.1 159.8 465.7 160.6L465.7 71.9C451.4 68 416.4 64 396.2 64C289.3 64 240 114.5 240 223.4L240 265.5L174 265.5L174 363.3L240 363.3z']
};
function SocialLink({
  name,
  href
}) {
  const g = SOCIAL_GLYPHS[name];
  const [hover, setHover] = React.useState(false);
  if (!g) return null;
  return /*#__PURE__*/React.createElement("a", {
    href: href || '#',
    "aria-label": name,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: 34,
      height: 34,
      border: `1px solid ${hover ? 'var(--vv-amber-24)' : 'var(--line-hairline)'}`,
      color: hover ? 'var(--vv-amber)' : 'var(--text-label)',
      background: hover ? 'var(--vv-amber-07)' : 'transparent',
      transition: 'background var(--dur-fast) var(--ease-out), color var(--dur-fast) var(--ease-out), border-color var(--dur-fast) var(--ease-out)'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: g[0],
    width: 15,
    height: 15,
    fill: "currentColor"
  }, /*#__PURE__*/React.createElement("path", {
    d: g[1]
  })));
}
function NavLink({
  href,
  children
}) {
  const [hover, setHover] = React.useState(false);
  return /*#__PURE__*/React.createElement("a", {
    href: href || '#',
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      fontSize: 15,
      color: hover ? 'var(--vv-amber)' : 'var(--text-secondary)',
      transition: 'color var(--dur-fast) var(--ease-out)',
      overflowWrap: 'anywhere'
    }
  }, children);
}
function LegalLink({
  href,
  children
}) {
  const [hover, setHover] = React.useState(false);
  return /*#__PURE__*/React.createElement("a", {
    href: href || '#',
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      fontSize: 12,
      color: hover ? 'var(--vv-amber)' : 'var(--text-caption)',
      transition: 'color var(--dur-fast) var(--ease-out)'
    }
  }, children);
}
const LABEL = {
  fontSize: 11,
  fontWeight: 600,
  letterSpacing: 'var(--track-label)',
  textTransform: 'uppercase',
  color: 'var(--text-label)'
};

/* Boot-offset gate. Identical contract to NavBar's: the offsets below only ever apply once a
   real animation frame has been served, so any context that does not service rAF renders the
   footer at its resting design values rather than invisible. */
if (typeof document !== 'undefined' && !document.getElementById('vv-footer-motion')) {
  const _s = document.createElement('style');
  _s.id = 'vv-footer-motion';
  _s.textContent = 'html:not(.vv-motion-live) [data-vv-boot]{opacity:1!important;transform:none!important;transition:none!important}@media (prefers-reduced-motion: reduce){[data-vv-boot]{opacity:1!important;transform:none!important;transition:none!important}}';
  document.head.appendChild(_s);
}

/* Re-arming proof of life — see vv-motion.jsx. A single rAF at load latches shut on any load
   that begins unpainted (background tab, hidden iframe, session restore), which would keep the
   gate above disabling this component's entrance for the whole session. */
function vvArmMotionGate() {
  if (typeof window === 'undefined' || typeof document === 'undefined') return;
  if (typeof requestAnimationFrame !== 'function') return;
  if (document.documentElement.classList.contains('vv-motion-live')) return;
  let pending = false;
  const settle = () => {
    if (document.documentElement.classList.contains('vv-motion-live')) {
      detach();
      return;
    }
    document.documentElement.classList.add('vv-motion-live');
    window.dispatchEvent(new Event('scroll'));
    detach();
  };
  const arm = () => {
    if (pending || document.documentElement.classList.contains('vv-motion-live')) return;
    pending = true;
    requestAnimationFrame(() => {
      pending = false;
      settle();
    });
  };
  const onVis = () => {
    if (!document.hidden) arm();
  };
  function detach() {
    document.removeEventListener('visibilitychange', onVis);
    window.removeEventListener('scroll', arm, true);
    window.removeEventListener('pointerdown', arm, true);
    window.removeEventListener('pointermove', arm, true);
    window.removeEventListener('keydown', arm, true);
    window.removeEventListener('focus', arm);
  }
  document.addEventListener('visibilitychange', onVis);
  window.addEventListener('scroll', arm, {
    passive: true,
    capture: true
  });
  window.addEventListener('pointerdown', arm, {
    passive: true,
    capture: true
  });
  window.addEventListener('pointermove', arm, {
    passive: true,
    capture: true
  });
  window.addEventListener('keydown', arm, {
    capture: true
  });
  window.addEventListener('focus', arm);
  arm();
}
if (typeof document !== 'undefined') vvArmMotionGate();
function Footer({
  markSrc,
  tagline,
  nav = [],
  legalLinks = [],
  social = [],
  location,
  emails = [],
  legal = '© 2026 Vivum Computing. All rights reserved.',
  style,
  ...rest
}) {
  const stacked = useMaxWidth(600);
  const narrow1440 = useMaxWidth(1440);
  /* Footer arrives as one HUD panel: the top rule draws, then the columns rise in sequence.
     Self-contained observer — this component ships alone, so it owns its own trigger.
     Every column ends at the design's exact resting style; `col(i)` adds nothing else. */
  const ref = React.useRef(null);
  const [lit, setLit] = React.useState(() => typeof window !== 'undefined' && window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  React.useEffect(() => {
    if (lit) return undefined;
    const node = ref.current;
    const check = () => {
      if (!node) return true;
      const r = node.getBoundingClientRect();
      const h = window.innerHeight || document.documentElement.clientHeight || 0;
      /* One-directional — true in view AND once scrolled past. A two-sided test strands the
         whole footer when the user jumps to the bottom of the page in one motion. */
      return r.top < h * 0.9;
    };
    /* Synchronous test first, then an observer, then a scroll listener — all three
       position-correct. No unconditional timer: one would light the footer while the user is
       still at the top of the page. The CSS gate below covers contexts where none of them run
       by showing the resting design outright. */
    if (check()) {
      setLit(true);
      return undefined;
    }
    let io = null;
    const onScroll = () => {
      if (check()) setLit(true);
    };
    if (typeof IntersectionObserver !== 'undefined') {
      io = new IntersectionObserver(es => {
        if (es.some(e => e.isIntersecting)) setLit(true);
      }, {
        rootMargin: '0px 0px -10% 0px',
        threshold: 0.05
      });
      io.observe(node);
    }
    window.addEventListener('scroll', onScroll, {
      passive: true,
      capture: true
    });
    window.addEventListener('resize', onScroll);
    return () => {
      if (io) io.disconnect();
      window.removeEventListener('scroll', onScroll, true);
      window.removeEventListener('resize', onScroll);
    };
  }, [lit]);
  const col = i => ({
    opacity: lit ? 1 : 0,
    transform: lit ? 'none' : 'translate3d(0,18px,0)',
    transition: `opacity 640ms cubic-bezier(.16,1,.3,1) ${i * 110}ms, transform 640ms cubic-bezier(.16,1,.3,1) ${i * 110}ms`
  });
  const bootAttr = {
    'data-vv-boot': ''
  };
  return /*#__PURE__*/React.createElement("footer", _extends({
    ref: ref,
    style: {
      position: 'relative',
      background: 'var(--surface-canvas)',
      overflow: 'hidden',
      borderTop: '1px solid var(--line-hairline)',
      marginTop: 'clamp(48px, 6vw, 96px)',
      padding: 'clamp(56px, 6vw, 72px) var(--gutter-site)',
      boxSizing: 'border-box',
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      inset: 0,
      background: 'radial-gradient(circle farthest-corner at 20% -60%, #082036 0%, #001121 76%)',
      opacity: 0.4,
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: '40px 24px',
      paddingBottom: 'clamp(56px, 6vw, 80px)',
      borderBottom: '1px solid var(--line-hairline)',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 20,
      flex: stacked ? '1 1 100%' : '0 0 30%',
      minWidth: stacked ? 0 : 200,
      maxWidth: stacked ? '100%' : '30%',
      ...col(0)
    }
  }, bootAttr), markSrc ? /*#__PURE__*/React.createElement("img", {
    src: markSrc,
    alt: "Vivum",
    style: {
      height: 48,
      width: 'auto',
      maxWidth: 'max-content',
      alignSelf: 'flex-start',
      display: 'block'
    }
  }) : /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 22,
      fontWeight: 700,
      letterSpacing: '0.08em',
      color: 'var(--vv-ice)'
    }
  }, "VIVUM"), tagline ? /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 14,
      lineHeight: 1.5,
      color: 'var(--text-caption)',
      maxWidth: 220
    }
  }, tagline) : null), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      flexDirection: stacked ? 'column' : 'row',
      justifyContent: stacked || narrow1440 ? 'flex-start' : 'flex-end',
      columnGap: 'clamp(32px, 6vw, 112px)',
      rowGap: 32,
      flex: '1 1 480px',
      minWidth: 0
    }
  }, nav.length ? /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      flex: '0 1 auto',
      minWidth: 90,
      ...col(1)
    }
  }, bootAttr), /*#__PURE__*/React.createElement("span", {
    style: LABEL
  }, "Navigate"), nav.map(l => /*#__PURE__*/React.createElement(NavLink, {
    key: l.label,
    href: l.href
  }, l.label))) : null, emails.length ? /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16,
      flex: '0 1 auto',
      minWidth: 160,
      ...col(2)
    }
  }, bootAttr), /*#__PURE__*/React.createElement("span", {
    style: LABEL
  }, "Contact"), emails.map(e => /*#__PURE__*/React.createElement("div", {
    key: e.email,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      color: 'var(--text-caption)'
    }
  }, e.label), /*#__PURE__*/React.createElement(NavLink, {
    href: `mailto:${e.email}`
  }, e.email)))) : null, location ? /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      flex: '0 1 auto',
      minWidth: 140,
      ...col(3)
    }
  }, bootAttr), /*#__PURE__*/React.createElement("span", {
    style: LABEL
  }, "Location"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, location.map((line, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      fontSize: 15,
      lineHeight: 1.5,
      color: 'var(--text-secondary)'
    }
  }, line)))) : null, social.length ? /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      flex: '0 0 auto',
      ...col(4)
    }
  }, bootAttr), /*#__PURE__*/React.createElement("span", {
    style: LABEL
  }, "Follow us"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 10
    }
  }, social.map(s => /*#__PURE__*/React.createElement(SocialLink, {
    key: s.name,
    name: s.name,
    href: s.href
  })))) : null)), /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      flexWrap: 'wrap',
      gap: 16,
      marginTop: 28,
      ...col(5)
    }
  }, bootAttr), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--text-caption)'
    }
  }, legal), legalLinks.length ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 20,
      alignItems: 'center'
    }
  }, legalLinks.map((l, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: l.label
  }, i > 0 ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--text-caption)'
    }
  }, "\u2022") : null, /*#__PURE__*/React.createElement(LegalLink, {
    href: l.href
  }, l.label)))) : null)));
}
Object.assign(__ds_scope, { Footer });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Footer.jsx", error: String((e && e.message) || e) }); }

// components/navigation/NavBar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Glass appears one header-height down (96px): the moment the first content actually
   passes under the bar, so the plate is a response to overlap, not a scroll counter.
   A 24px hysteresis band keeps it from flickering on trackpad micro-scrolls. */
const GLASS_AT = 96;

/* Self-contained: the bar's own motion must not depend on any page-level animation kit,
   since this component ships to consumers on its own. */
const NAV_MOTION_CSS = `
/* Same master gate as the site's motion kit: the boot offsets only apply once a real frame
   has been served, so print, PDF export, image capture and background tabs render the bar at
   its resting design values instead of an invisible one. */
html:not(.vv-motion-live) [data-vv-boot]{opacity:1!important;transform:none!important;transition:none!important}
html:not(.vv-motion-live) [data-vv-hud-fx]{display:none!important}
@media (prefers-reduced-motion: reduce){[data-vv-boot]{opacity:1!important;transform:none!important;transition:none!important}[data-vv-hud-fx]{display:none!important}}`;
if (typeof document !== 'undefined' && !document.getElementById('vv-nav-motion')) {
  const _s = document.createElement('style');
  _s.id = 'vv-nav-motion';
  _s.textContent = NAV_MOTION_CSS;
  document.head.appendChild(_s);
}

/* Re-arming proof of life — see vv-motion.jsx. A single rAF at load latches shut on any load
   that begins unpainted (background tab, hidden iframe, session restore), which would keep the
   gate above disabling this component's entrance for the whole session. */
function vvArmMotionGate() {
  if (typeof window === 'undefined' || typeof document === 'undefined') return;
  if (typeof requestAnimationFrame !== 'function') return;
  if (document.documentElement.classList.contains('vv-motion-live')) return;
  let pending = false;
  const settle = () => {
    if (document.documentElement.classList.contains('vv-motion-live')) {
      detach();
      return;
    }
    document.documentElement.classList.add('vv-motion-live');
    window.dispatchEvent(new Event('scroll'));
    detach();
  };
  const arm = () => {
    if (pending || document.documentElement.classList.contains('vv-motion-live')) return;
    pending = true;
    requestAnimationFrame(() => {
      pending = false;
      settle();
    });
  };
  const onVis = () => {
    if (!document.hidden) arm();
  };
  function detach() {
    document.removeEventListener('visibilitychange', onVis);
    window.removeEventListener('scroll', arm, true);
    window.removeEventListener('pointerdown', arm, true);
    window.removeEventListener('pointermove', arm, true);
    window.removeEventListener('keydown', arm, true);
    window.removeEventListener('focus', arm);
  }
  document.addEventListener('visibilitychange', onVis);
  window.addEventListener('scroll', arm, {
    passive: true,
    capture: true
  });
  window.addEventListener('pointerdown', arm, {
    passive: true,
    capture: true
  });
  window.addEventListener('pointermove', arm, {
    passive: true,
    capture: true
  });
  window.addEventListener('keydown', arm, {
    capture: true
  });
  window.addEventListener('focus', arm);
  arm();
}
if (typeof document !== 'undefined') vvArmMotionGate();
function useScrolledPast(threshold, enabled) {
  const [past, setPast] = React.useState(false);
  React.useEffect(() => {
    if (!enabled) {
      setPast(false);
      return undefined;
    }
    let frame = 0;
    const read = () => {
      frame = 0;
      const y = window.scrollY || document.documentElement.scrollTop || 0;
      setPast(prev => prev ? y > threshold - 24 : y > threshold);
    };
    const onScroll = () => {
      if (!frame) frame = requestAnimationFrame(read);
    };
    read();
    window.addEventListener('scroll', onScroll, {
      passive: true
    });
    return () => {
      window.removeEventListener('scroll', onScroll);
      if (frame) cancelAnimationFrame(frame);
    };
  }, [threshold, enabled]);
  return past;
}
function useMaxWidth(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}

/* Mobile menu presentation is a whole-site setting, not a per-call-site prop — same device as
   Icon.jsx's icon weight. Exposed via a tweak in the site's shared TweaksPanel so it can be
   explored without touching code, and read here through a subscription hook. */
let _menuStyle = typeof window !== 'undefined' && window.__vvMenuStyle || 'dropdown';
const _menuStyleSubs = new Set();
function setMobileMenuStyle(v) {
  _menuStyle = v;
  if (typeof window !== 'undefined') window.__vvMenuStyle = v;
  _menuStyleSubs.forEach(fn => fn());
}
function useMobileMenuStyle() {
  const [, force] = React.useState(0);
  React.useEffect(() => {
    const fn = () => force(x => x + 1);
    _menuStyleSubs.add(fn);
    return () => _menuStyleSubs.delete(fn);
  }, []);
  return _menuStyle;
}
if (typeof window !== 'undefined') {
  window.vvSetMobileMenuStyle = setMobileMenuStyle;
  window.vvGetMobileMenuStyle = () => _menuStyle;
}
function NavBar({
  logoSrc,
  logoAlt = 'Vivum',
  brand = 'VIVUM',
  links = [],
  activeHref,
  cta,
  transparent,
  glassAt = GLASS_AT,
  onNavigate,
  style,
  ...rest
}) {
  /* `transparent` means "start bare and earn the plate on scroll"; an opaque bar keeps its
     plate at rest, so pages that need a solid chrome are unchanged. */
  const scrolled = useScrolledPast(glassAt, !!transparent);
  const glass = transparent ? scrolled : true;
  const mobile = useMaxWidth(760);
  const menuStyle = useMobileMenuStyle();
  /* Boot stagger: the bar assembles left to right on first paint — logo, then each link,
     then the CTA — so the chrome arrives like a HUD coming online rather than being there
     already. `boot(i)` contributes only opacity/transform/transition, never a design value,
     and the resting values are the design's own. */
  const [booted, setBooted] = React.useState(() => typeof window !== 'undefined' && window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  React.useEffect(() => {
    const r = typeof requestAnimationFrame === 'function' ? requestAnimationFrame(() => setBooted(true)) : 0;
    /* Companion timer: rAF is not serviced in every context, and the bar must never be left
       hidden waiting for one. The CSS gate above covers the case where neither fires. */
    const t = setTimeout(() => setBooted(true), 900);
    return () => {
      if (r) cancelAnimationFrame(r);
      clearTimeout(t);
    };
  }, []);
  const boot = (i, extra) => ({
    opacity: booted ? 1 : 0,
    transform: booted ? 'none' : 'translate3d(0,-9px,0)',
    transition: `${extra ? extra + ', ' : ''}opacity 560ms cubic-bezier(.16,1,.3,1) ${i * 90}ms, transform 560ms cubic-bezier(.16,1,.3,1) ${i * 90}ms`
  });
  /* No acquire tick on the bottom edge: the plate arrives on blur alone. The bar carries no
     amber at any scroll position — deliberately, do not reintroduce a sweep here. */
  const [open, setOpen] = React.useState(false);
  React.useEffect(() => {
    if (!mobile) setOpen(false);
  }, [mobile]);
  /* One frame of opacity 0 so the overlay fades in instead of appearing hard. It is a paint-only
     transition — the overlay is already at its final full-viewport geometry on the first frame. */
  const [entered, setEntered] = React.useState(false);
  React.useEffect(() => {
    if (!(mobile && open)) {
      setEntered(false);
      return undefined;
    }
    const r = requestAnimationFrame(() => setEntered(true));
    return () => cancelAnimationFrame(r);
  }, [mobile, open]);
  const navigate = href => {
    setOpen(false);
    if (onNavigate) onNavigate(href);
  };
  const menuIsOpen = mobile && open;
  return /*#__PURE__*/React.createElement("header", _extends({
    style: {
      position: 'relative',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 32,
      flexWrap: 'wrap',
      /* At rest the bar has extra top room to breathe against the hero, so its padding is
         deliberately top-heavy. Scrolled, the plate becomes a compact band with symmetric
         padding — lopsided padding inside a centred flex row reads as a misaligned bar. */
      padding: glass ? '14px var(--gutter-site)' : mobile ? '18px var(--gutter-site) 10px' : '46px var(--gutter-site) 10px',
      minHeight: glass ? 68 : mobile ? 60 : 96,
      boxSizing: 'border-box',
      /* No plate fill in either state — the bar is pure blur over the page, so it never
         reads as a band against the flat #001121 body. */
      /* backdrop-filter other than `none` makes the bar a containing block for its fixed-
         position mobile menu overlay, so the overlay's inset:0 resolves against the bar
         instead of the viewport and its overflow-y:auto clips everything below it. Dropping
         the blur while the menu is open avoids that; the overlay paints its own full-viewport
         blur over the same area, so nothing is lost visually. This condition looks redundant
         — do not "simplify" it back to `glass ? 'blur(20px)' : 'none'`. */
      backdropFilter: glass && !menuIsOpen ? 'blur(20px)' : 'none',
      WebkitBackdropFilter: glass && !menuIsOpen ? 'blur(20px)' : 'none',
      /* No rule under the bar in either state. */
      borderBottom: '1px solid transparent',
      /* backdrop-filter must drop to `none` in the SAME frame the menu opens. Transitioning it
         leaves the bar a containing block for the whole animation, so the overlay's inset:0
         resolves against the short bar first (visible squeeze + a scrollbar) and only snaps to
         the viewport when the transition ends — the open flicker. Off scroll-top only, because
         at rest there is no blur to remove. Keep the property un-transitioned while open. */
      transition: menuIsOpen ? 'background var(--dur-base) var(--ease-out), padding var(--dur-base) var(--ease-out), min-height var(--dur-base) var(--ease-out), border-color var(--dur-base) var(--ease-out)' : 'background var(--dur-base) var(--ease-out), padding var(--dur-base) var(--ease-out), min-height var(--dur-base) var(--ease-out), border-color var(--dur-base) var(--ease-out), backdrop-filter var(--dur-base) var(--ease-out)',
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("a", {
    href: "#",
    "data-vv-boot": "",
    onClick: e => {
      e.preventDefault();
      navigate(links[0] && links[0].href);
    },
    style: {
      display: 'flex',
      alignItems: 'center',
      flex: 'none',
      ...boot(0)
    }
  }, logoSrc ? /*#__PURE__*/React.createElement("img", {
    src: logoSrc,
    alt: logoAlt,
    style: {
      height: mobile ? 42 : glass ? 40 : 50,
      display: 'block',
      transition: 'height var(--dur-base) var(--ease-out)'
    }
  }) : /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: mobile ? 17 : 20,
      fontWeight: 700,
      letterSpacing: '0.06em',
      color: 'var(--vv-ice)'
    }
  }, brand)), !mobile ? /*#__PURE__*/React.createElement("nav", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 32,
      flex: 1,
      justifyContent: 'flex-end'
    }
  }, links.map((l, i) => {
    const active = l.href === activeHref;
    return /*#__PURE__*/React.createElement("a", {
      key: l.href,
      href: l.href,
      "data-vv-boot": "",
      onClick: e => {
        if (onNavigate) {
          e.preventDefault();
          onNavigate(l.href);
        }
      },
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 6,
        fontSize: 14,
        fontWeight: 500,
        color: active ? 'var(--vv-ice)' : 'var(--text-secondary)',
        borderBottom: `2px solid ${active ? 'var(--vv-amber)' : 'transparent'}`,
        paddingBottom: 2,
        paddingTop: 4,
        ...boot(i + 1, 'color var(--dur-fast) var(--ease-out), border-color var(--dur-base) var(--ease-out)')
      }
    }, l.label, l.external ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: "north_east",
      size: 13
    }) : null);
  })) : null, /*#__PURE__*/React.createElement("div", {
    "data-vv-boot": "",
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      flex: mobile ? 'none' : 'none',
      marginLeft: mobile ? 'auto' : 0,
      ...boot(mobile ? 1 : links.length + 1)
    }
  }, !mobile && cta ? /*#__PURE__*/React.createElement(__ds_scope.Button, {
    variant: "primary",
    size: "sm",
    onClick: cta.onClick,
    href: cta.href
  }, cta.label) : null, mobile ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => setOpen(v => !v),
    "aria-label": open ? 'Close menu' : 'Open menu',
    "aria-expanded": open,
    style: {
      position: 'relative',
      width: 30,
      height: 20,
      zIndex: 31,
      padding: 0,
      border: 0,
      background: 'transparent',
      cursor: 'pointer',
      WebkitTapHighlightColor: 'transparent'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: 0,
      top: '50%',
      marginTop: -1,
      width: 30,
      height: 2,
      background: '#999EA3',
      transition: 'transform .3s',
      transform: open ? 'rotate(45deg)' : 'translateY(-4px)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: 0,
      top: '50%',
      marginTop: -1,
      width: 30,
      height: 2,
      background: '#999EA3',
      transition: 'transform .3s',
      transform: open ? 'rotate(-45deg)' : 'translateY(4px)'
    }
  })) : null), menuIsOpen ? /*#__PURE__*/React.createElement("div", {
    style: {
      opacity: entered ? 1 : 0,
      transition: 'opacity .2s var(--ease-out)',
      willChange: 'opacity',
      ...(menuStyle === 'fullscreen' ? {
        position: 'fixed',
        inset: 0,
        background: 'rgba(0,17,33,0.82)',
        backdropFilter: 'blur(20px)',
        WebkitBackdropFilter: 'blur(20px)',
        display: 'flex',
        flexDirection: 'column',
        padding: '96px var(--gutter-site) 32px',
        gap: 8,
        zIndex: 30,
        overflowY: 'auto'
      } : menuStyle === 'drawer' ? {
        position: 'fixed',
        top: 0,
        right: 0,
        bottom: 0,
        width: 'min(84vw, 340px)',
        background: '#001121',
        borderLeft: '1px solid var(--line-hairline)',
        boxShadow: '-24px 0 48px rgba(0,0,0,0.4)',
        display: 'flex',
        flexDirection: 'column',
        padding: '96px 28px 32px',
        gap: 8,
        zIndex: 30,
        overflowY: 'auto'
      } : {
        position: 'fixed',
        top: 0,
        left: 0,
        right: 0,
        background: 'rgba(0,17,33,0.82)',
        borderBottom: '1px solid var(--line-hairline)',
        backdropFilter: 'blur(20px)',
        WebkitBackdropFilter: 'blur(20px)',
        display: 'flex',
        flexDirection: 'column',
        padding: '56px var(--gutter-site) 24px',
        gap: 4,
        zIndex: 30
      })
    }
  }, links.map(l => {
    const active = l.href === activeHref;
    return /*#__PURE__*/React.createElement("a", {
      key: l.href,
      href: l.href,
      onClick: e => {
        if (onNavigate) {
          e.preventDefault();
          navigate(l.href);
        } else {
          setOpen(false);
        }
      },
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 6,
        fontSize: menuStyle === 'fullscreen' ? 28 : 16,
        fontWeight: 500,
        color: active ? 'var(--vv-ice)' : 'var(--text-secondary)',
        padding: menuStyle === 'fullscreen' ? '10px 0' : '14px 0',
        borderBottom: menuStyle === 'fullscreen' ? 'none' : '1px solid var(--line-hairline)'
      }
    }, l.label, l.external ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: "north_east",
      size: 14
    }) : null);
  }), cta ? /*#__PURE__*/React.createElement(__ds_scope.Button, {
    variant: "primary",
    size: "lg",
    onClick: () => {
      setOpen(false);
      cta.onClick && cta.onClick();
    },
    href: cta.href,
    style: {
      marginTop: 16,
      width: '100%'
    }
  }, cta.label) : null) : null);
}
Object.assign(__ds_scope, { NavBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/NavBar.jsx", error: String((e && e.message) || e) }); }

// components/navigation/SideNav.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function SideNav({
  logoSrc,
  sections = [],
  activeId,
  onSelect,
  footer,
  width = 244,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("aside", _extends({
    style: {
      width,
      flex: 'none',
      boxSizing: 'border-box',
      display: 'flex',
      flexDirection: 'column',
      background: 'var(--surface-canvas)',
      borderRight: '1px solid var(--line-hairline)',
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 64,
      display: 'flex',
      alignItems: 'center',
      padding: '0 20px',
      borderBottom: '1px solid var(--line-hairline)',
      flex: 'none'
    }
  }, logoSrc ? /*#__PURE__*/React.createElement("img", {
    src: logoSrc,
    alt: "Vivum",
    style: {
      height: 20,
      display: 'block'
    }
  }) : /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 15,
      fontWeight: 700,
      letterSpacing: '0.08em',
      color: 'var(--vv-ice)'
    }
  }, "VIVUM")), /*#__PURE__*/React.createElement("nav", {
    style: {
      flex: 1,
      overflowY: 'auto',
      padding: '20px 0'
    }
  }, sections.map((s, si) => /*#__PURE__*/React.createElement("div", {
    key: s.label || si,
    style: {
      marginBottom: 24
    }
  }, s.label ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 20px 10px',
      fontSize: 10,
      fontWeight: 600,
      letterSpacing: 'var(--track-label)',
      textTransform: 'uppercase',
      color: 'var(--text-label)'
    }
  }, s.label) : null, (s.items || []).map(it => {
    const active = it.id === activeId;
    return /*#__PURE__*/React.createElement("button", {
      key: it.id,
      onClick: () => onSelect && onSelect(it.id),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 11,
        width: '100%',
        boxSizing: 'border-box',
        padding: '0 20px',
        height: 38,
        background: active ? 'var(--vv-ice-04)' : 'transparent',
        border: 0,
        borderLeft: `2px solid ${active ? 'var(--vv-amber)' : 'transparent'}`,
        color: active ? 'var(--vv-ice)' : 'var(--text-secondary)',
        cursor: 'pointer',
        fontFamily: 'var(--font-sans)',
        fontSize: 13.5,
        fontWeight: active ? 500 : 400,
        textAlign: 'left',
        transition: 'background var(--dur-fast) var(--ease-out), color var(--dur-fast) var(--ease-out)'
      }
    }, it.icon ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: it.icon,
      size: 16
    }) : null, /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'more_horiz'
      }
    }, it.label), it.trailing);
  })))), footer ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 16,
      borderTop: '1px solid var(--line-hairline)',
      flex: 'none'
    }
  }, footer) : null);
}
Object.assign(__ds_scope, { SideNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/SideNav.jsx", error: String((e && e.message) || e) }); }

// components/surfaces/GradientField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const FIELDS = {
  /* `none` is the off switch: call sites keep their `field` name in a comment so a bloom can be
     put back by deleting one word. Every entry below is still live and still supported. */
  none: 'none',
  signature: 'var(--grad-signature)',
  horizon: 'var(--grad-horizon)',
  sweep: 'var(--grad-sweep)',
  well: 'var(--grad-well)',
  aperture: 'var(--grad-aperture)',
  ember: 'var(--grad-ember)',
  twin: 'var(--grad-twin)',
  strata: 'var(--grad-strata)',
  abyss: 'var(--grad-abyss)',
  /* Blooms end at transparent: they compose over the canvas so sections never hard-cut. */
  crest: 'var(--bloom-crest)',
  base: 'var(--bloom-base)',
  left: 'var(--bloom-left)',
  right: 'var(--bloom-right)',
  core: 'var(--bloom-core)',
  data: 'var(--bloom-data)',
  signal: 'var(--bloom-signal)'
};
const BLOOMS = ['crest', 'base', 'left', 'right', 'core', 'data', 'signal'];
function GradientField({
  children,
  field = 'signature',
  pad,
  minHeight,
  as = 'section',
  style,
  ...rest
}) {
  const Tag = as;
  return /*#__PURE__*/React.createElement(Tag, _extends({
    style: {
      position: 'relative',
      background: FIELDS[field] || FIELDS.signature,
      backgroundColor: BLOOMS.includes(field) || field === 'none' ? 'transparent' : undefined,
      color: 'var(--text-body)',
      padding: pad,
      minHeight,
      boxSizing: 'border-box',
      ...style
    }
  }, rest), children);
}
Object.assign(__ds_scope, { GradientField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/surfaces/GradientField.jsx", error: String((e && e.message) || e) }); }

// components/surfaces/Sheet.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Sheet({
  children,
  pad = 64,
  maxWidth = 'var(--container-prose)',
  gradient,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", _extends({
    style: {
      background: gradient ? 'var(--grad-sheet)' : 'var(--surface-sheet)',
      color: 'var(--text-body-on-light)',
      padding: pad,
      boxSizing: 'border-box',
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth,
      margin: '0 auto'
    }
  }, children));
}
Object.assign(__ds_scope, { Sheet });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/surfaces/Sheet.jsx", error: String((e && e.message) || e) }); }

// components/surfaces/hud-shapes.js
try { (() => {
/* The chamfered silhouette behind Bezel: top-right and bottom-left corners cut.

   Why SVG rather than CSS: an outline made of rotated 1px divs cannot close cleanly — a
   rotated div's ends land half a pixel off the straight edge it meets, so the diagonals
   show as gaps or overhangs — and a plain `border` is sliced off wherever `clip-path` cuts
   a corner. One continuous path with miter joins is exact at any size, and insetting it by
   half the stroke width keeps the line fully inside the element box. */

const n = v => Math.round(v * 100) / 100;

/* Corner points, clockwise from the top-left, with two corners cut. */
function corners(w, h, c) {
  return [[0, 0], [w - c, 0], [w, c], [w, h], [c, h], [0, h - c]];
}
const toPath = (pts, o) => 'M' + pts.map(([x, y]) => `${n(x + o)} ${n(y + o)}`).join(' L') + ' Z';

/* `fill` and `clip` cover the element exactly; `stroke` is inset half a line so the
   1px outline lands inside the box rather than straddling its edge. */
function hudGeometry(w, h, cut) {
  const cap = (bw, bh) => Math.max(0, Math.min(cut, Math.min(bw, bh) / 2 - 1));
  const outer = corners(w, h, cap(w, h));
  return {
    fill: toPath(outer, 0),
    stroke: toPath(corners(w - 1, h - 1, cap(w - 1, h - 1)), 0.5),
    clip: 'polygon(' + outer.map(([x, y]) => `${n(x)}px ${n(y)}px`).join(', ') + ')'
  };
}

/* The geometry is in real pixels, so it needs the rendered box. Returns null until the
   first measurement, which callers use to skip drawing for one frame. */
function useElementSize(ref) {
  const [size, setSize] = React.useState(null);
  React.useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return undefined;
    const measure = () => {
      const w = el.offsetWidth,
        h = el.offsetHeight;
      setSize(prev => prev && prev.w === w && prev.h === h ? prev : {
        w,
        h
      });
    };
    measure();
    if (typeof ResizeObserver === 'undefined') return undefined;
    const ro = new ResizeObserver(measure);
    ro.observe(el);
    return () => ro.disconnect();
  }, [ref]);
  return size;
}

/* The frame overlay: an optional translucent plate, then the outline on top. */
function HudOutline({
  w,
  h,
  cut,
  fill,
  line,
  weight = 1
}) {
  const g = hudGeometry(w, h, cut);
  return /*#__PURE__*/React.createElement("svg", {
    width: w,
    height: h,
    viewBox: `0 0 ${w} ${h}`,
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      left: 0,
      top: 0,
      pointerEvents: 'none'
    }
  }, fill ? /*#__PURE__*/React.createElement("path", {
    d: g.fill,
    style: {
      fill
    }
  }) : null, /*#__PURE__*/React.createElement("path", {
    d: g.stroke,
    fill: "none",
    stroke: line,
    strokeWidth: weight,
    pathLength: "1",
    strokeLinejoin: "miter",
    strokeLinecap: "butt",
    shapeRendering: "geometricPrecision"
  }));
}
Object.assign(__ds_scope, { hudGeometry, useElementSize, HudOutline });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/surfaces/hud-shapes.js", error: String((e && e.message) || e) }); }

// components/surfaces/plate-loop.js
try { (() => {
/* Plate loop behaviour, shared by VideoBackdrop and Bezel. Both modes end at the same place —
   no visible cut when the footage runs out — but they cost very different amounts.

   'crossfade' (the one to use). TWO copies of the same file, stacked. The visible copy plays
   straight through; a beat before it ends the other starts at frame 0 and the pair dissolves.
   Nothing ever seeks and nothing ever plays backwards, so the decoder only does the one thing
   it is fast at: play forward. The idle copy is PAUSED outside the dissolve window, so the
   steady state is a single decode, and the only JavaScript running while the plate plays is a
   `timeupdate` check — four times a second, no work — with CSS doing the fade.

   'pingpong'. Plays forward, then walks `currentTime` back to 0 by seeking. Honest reverse
   motion, but browsers decode forward only: every backward seek re-decodes from the preceding
   keyframe, so a 1080p plate does a burst of work once per cycle. That burst is exactly the
   periodic hitch it looks like. Keep it for short, small, low-bitrate plates only.

   Both modes pause the plate while it is off screen — a hero plate still decoding three
   sections down is stealing frames from the plate you are actually looking at.

   Everything here is written to survive a HIDDEN document, which is where a naive crossfade
   dies: `requestAnimationFrame` and CSS transitions do not run when the page is hidden, while
   media playback, `timeupdate` and timers do. So the dissolve never depends on a frame
   callback, it hard-cuts when hidden, and `ended` is always wired as the backstop — a missed
   swap window costs one visible cut, never a plate frozen on its last frame. */
function usePlateLoop(mode, {
  fade = 0.9,
  hold = 1
} = {}) {
  const aRef = React.useRef(null);
  const bRef = React.useRef(null);
  React.useEffect(() => {
    if (!mode) return;
    const a = aRef.current;
    if (!a) return;
    const b = mode === 'crossfade' ? bRef.current : null;
    if (mode === 'crossfade' && !b) return;
    const els = b ? [a, b] : [a];
    let visible = true;
    let front = a;
    let back = b;
    let swapping = false;
    let swapTimer = null;
    let rewinding = false;
    let watchdog = null;
    let seekAt = 0;
    const play = el => {
      const p = el && el.play();
      if (p && p.catch) p.catch(() => {});
    };
    const seek = (el, t) => {
      try {
        el.currentTime = t;
      } catch (e) {}
    };

    /* ── crossfade ─────────────────────────────────────────────────────────────────────── */
    const TRIGGER = fade + 0.35; // timeupdate only fires ~4×/s, so arm early
    // The resting state: front at its hold, back invisible. Also the recovery state — any
    // time the pair might have desynced, this puts opacity back in step with playback.
    const rest = () => {
      front.style.transition = '';
      back.style.transition = '';
      front.style.opacity = String(hold);
      back.style.opacity = '0';
    };
    const dissolve = () => {
      if (document.hidden) {
        // No transitions in a hidden document, and nothing to see either: hard-cut.
        front.style.transition = '';
        back.style.transition = '';
        back.style.opacity = String(hold);
        front.style.opacity = '0';
        return;
      }
      front.style.transition = `opacity ${fade}s linear`;
      back.style.transition = `opacity ${fade}s linear`;
      void back.offsetWidth; // flush the start value so the transition has somewhere to run from
      back.style.opacity = String(hold);
      front.style.opacity = '0';
    };
    const finishSwap = () => {
      if (swapTimer) {
        clearTimeout(swapTimer);
        swapTimer = null;
      }
      const t = front;
      front = back;
      back = t;
      back.pause();
      seek(back, 0);
      swapping = false;
    };
    const startSwap = () => {
      if (swapping) return;
      swapping = true;
      seek(back, 0);
      play(back);
      dissolve();
      swapTimer = setTimeout(finishSwap, (document.hidden ? 0 : fade * 1000) + 90);
    };
    /* Deliberately NOT gated on `visible`: visibility decides whether the plate plays, not
       whether it is allowed to loop. Gating the hand-off on it meant one stale observer
       record could skip every window and strand the plate on its final frame. */
    const onTime = e => {
      if (swapping || e.target !== front) return;
      const d = front.duration;
      if (!isFinite(d) || d <= TRIGGER * 2 || d - front.currentTime > TRIGGER) return;
      startSwap();
    };
    // Backstop: if a window was missed the file just ends. Take the cut and carry on.
    const onEndedX = e => {
      if (e.target !== front) {
        e.target.pause();
        seek(e.target, 0);
        return;
      }
      if (swapping) {
        finishSwap();
        rest();
        play(front);
        return;
      }
      seek(front, 0);
      rest();
      play(front);
    };

    /* ── ping-pong ─────────────────────────────────────────────────────────────────────── */
    const MAX_STEP = 0.2;
    const stopRewind = () => {
      rewinding = false;
      if (watchdog) clearTimeout(watchdog);
      watchdog = null;
      a.removeEventListener('seeked', step);
    };
    function step() {
      if (!rewinding) return;
      if (watchdog) clearTimeout(watchdog);
      const now = performance.now();
      const dt = Math.min(MAX_STEP, (now - seekAt) / 1000 || 0.05);
      seekAt = now;
      const next = Math.max(0, a.currentTime - dt);
      if (next <= 0.02) {
        stopRewind();
        seek(a, 0);
        play(a);
        return;
      }
      seek(a, next);
      watchdog = setTimeout(step, 900);
    }
    const onEnded = () => {
      a.pause();
      rewinding = true;
      seekAt = performance.now();
      a.addEventListener('seeked', step);
      step();
    };
    if (b) {
      a.style.willChange = 'opacity';
      b.style.willChange = 'opacity';
      rest();
      b.pause();
      seek(b, 0);
      a.addEventListener('timeupdate', onTime);
      b.addEventListener('timeupdate', onTime);
      a.addEventListener('ended', onEndedX);
      b.addEventListener('ended', onEndedX);
    } else {
      a.addEventListener('ended', onEnded);
    }
    const resume = () => {
      if (!visible || document.hidden) return;
      if (b) {
        if (swapping) els.forEach(play);else {
          rest();
          play(front);
        }
      } else if (!rewinding) play(a);
    };
    /* No equality guard on the callback: it fires rarely, and treating every report as the
       truth is what lets the plate recover from anything that paused it while it was out of
       sight instead of staying frozen on one frame. */
    const io = new IntersectionObserver(entries => {
      visible = entries.some(x => x.isIntersecting);
      if (!visible) {
        els.forEach(el => el.pause());
        return;
      }
      resume();
    }, {
      rootMargin: '160px'
    });
    io.observe(a);
    const onVis = () => {
      if (!document.hidden) resume();
    };
    document.addEventListener('visibilitychange', onVis);
    return () => {
      io.disconnect();
      document.removeEventListener('visibilitychange', onVis);
      if (swapTimer) clearTimeout(swapTimer);
      if (b) {
        a.removeEventListener('timeupdate', onTime);
        b.removeEventListener('timeupdate', onTime);
        a.removeEventListener('ended', onEndedX);
        b.removeEventListener('ended', onEndedX);
      } else {
        a.removeEventListener('ended', onEnded);
        stopRewind();
      }
    };
  }, [mode, fade, hold]);
  return {
    aRef,
    bRef
  };
}
Object.assign(__ds_scope, { usePlateLoop });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/surfaces/plate-loop.js", error: String((e && e.message) || e) }); }

// components/surfaces/Bezel.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* A chamfered outline frame — the instrument-bezel silhouette with nothing inside it:
   top-right and bottom-left cut, and whatever sits behind, a video plate or a gradient
   field, shows straight through.

   The outline is a single SVG path rather than a border or a stack of divs: `clip-path`
   slices a real border off at the cuts, and rotated 1px divs cannot close cleanly against
   the straight edges they meet. One path with miter joins is exact at any size.

   `plateSrc` puts a video inside the frame: the frame's own polygon clips it, and it still
   carries the `--plate-feather` alpha mask, so the footage fades out before it reaches the
   outline rather than stopping against it. The plate keeps its own size —
   `plateScale`/`plateShift` place it, and anything past the cut edge is clipped.
   `platePoster` is the same plate as a still image — identical filter, mask and placement, so
   a build that cannot ship 10 MB of footage looks like the video's first frame rather than
   losing the plate. `plateSrc` wins when both are given (it is the richer treatment); pass
   `platePoster` alone for the still.

   `decor` is the same idea for arbitrary art: whatever you pass is absolutely positioned
   against the frame and clipped by the polygon, so a mark can be deliberately hung past an
   edge and the chamfer crops it — no overflow, no separate wrapper at the call site. */
function Bezel({
  children,
  chamfer = 36,
  pad = '52px 56px',
  weight = 1,
  color = 'var(--line-strong)',
  accent = true,
  minHeight,
  plateSrc,
  platePoster,
  plateOpacity = 0.45,
  plateShift,
  plateScale,
  seamless,
  pingPong,
  fill,
  blur,
  decor,
  stretch,
  style,
  ...rest
}) {
  const hostRef = React.useRef(null);
  // How the plate returns to its first frame; see plate-loop.js. `seamless` dissolves two
  // copies of the file into each other (cheap, no seeking), `pingPong` rewinds by seeking.
  // `loop` has to come off the element either way, or it cuts back to 0 first.
  const loopMode = seamless ? 'crossfade' : pingPong ? 'pingpong' : null;
  const {
    aRef,
    bRef
  } = __ds_scope.usePlateLoop(loopMode, {
    hold: plateOpacity
  });
  const box = __ds_scope.useElementSize(hostRef);
  const clip = box ? __ds_scope.hudGeometry(box.w, box.h, chamfer).clip : undefined;
  const transforms = [plateShift ? `translateX(${plateShift})` : null, plateScale && plateScale !== 1 ? `scale(${plateScale})` : null].filter(Boolean).join(' ');
  // One style object for both plate forms, so the still cannot drift from the footage.
  const plateStyle = {
    position: 'absolute',
    inset: 0,
    width: '100%',
    height: '100%',
    objectFit: 'cover',
    opacity: plateOpacity,
    filter: 'var(--plate-filter-base)',
    maskImage: 'var(--plate-feather)',
    WebkitMaskImage: 'var(--plate-feather)',
    ...(transforms ? {
      transform: transforms
    } : null)
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    ref: hostRef,
    style: {
      position: 'relative',
      minHeight,
      boxSizing: 'border-box',
      ...style
    }
  }, rest), clip && (fill || blur) ? /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      inset: 0,
      clipPath: clip,
      pointerEvents: 'none',
      ...(fill ? {
        background: fill
      } : null),
      ...(blur ? {
        backdropFilter: `blur(${blur}px)`,
        WebkitBackdropFilter: `blur(${blur}px)`
      } : null)
    }
  }) : null, (plateSrc || platePoster) && clip ? /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      clipPath: clip,
      overflow: 'hidden',
      pointerEvents: 'none'
    }
  }, plateSrc ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("video", {
    ref: aRef,
    src: plateSrc,
    autoPlay: true,
    muted: true,
    loop: !loopMode,
    playsInline: true,
    preload: "auto",
    disableRemotePlayback: true,
    style: seamless ? {
      ...plateStyle,
      opacity: undefined
    } : plateStyle
  }), seamless ? /*#__PURE__*/React.createElement("video", {
    ref: bRef,
    src: plateSrc,
    muted: true,
    playsInline: true,
    preload: "auto",
    disableRemotePlayback: true,
    "aria-hidden": "true",
    style: {
      ...plateStyle,
      opacity: undefined
    }
  }) : null) : /*#__PURE__*/React.createElement("img", {
    src: platePoster,
    alt: "",
    style: plateStyle
  })) : null, decor && clip ? /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      inset: 0,
      clipPath: clip,
      overflow: 'hidden',
      pointerEvents: 'none'
    }
  }, decor) : null, box ? /*#__PURE__*/React.createElement(__ds_scope.HudOutline, {
    w: box.w,
    h: box.h,
    cut: chamfer,
    line: color,
    weight: weight
  }) : null, accent ? /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: 0,
      top: 0,
      width: 56,
      height: 3,
      background: 'var(--vv-amber)',
      pointerEvents: 'none'
    }
  }) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      padding: pad,
      boxSizing: 'border-box',
      ...(stretch ? {
        height: '100%',
        display: 'flex',
        flexDirection: 'column'
      } : null)
    }
  }, children));
}
Object.assign(__ds_scope, { Bezel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/surfaces/Bezel.jsx", error: String((e && e.message) || e) }); }

// components/surfaces/SectionFrame.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The chamfered frame every marketing section sits in — Bezel plus the one alignment recipe
   they all have to agree on.

   Each section used to hand-roll its own padding, copy cap and inset math, and the two that
   sit above each other on the home page drifted apart: one derived a left inset from its own
   slack space, the other kept balanced padding, so the headlines started at different x. The
   rule here is the settled one: THE COPY IS ALWAYS FLUSH TO THE FRAME'S LEFT PADDING. Slack
   is never spent on a left inset — a left-only margin inside a frame just reads as lopsided
   padding — it is spent pulling `reserve`d decoration inward instead.

   `reserve` is the width of art hung inside the frame (the bio tree). It shortens the copy's
   measure without moving its left edge.

   Once the copy hits its cap the frame has width to spare, and that slack goes into the SIDE
   PADDINGS — not into a wider gutter or dead space behind the art. Two rules make that safe
   across sibling sections:

   1. The grown padding is derived from the SHARED page container and the copy cap alone —
      never from the frame's own width, and never from `reserve`. Sections are different
      widths (this one is full-bleed, the mission copy sits in a 2-col grid) and carry
      different art, so anything self-referential gives each frame a different inset and the
      headlines drift apart again — the exact bug this component exists to kill.
   2. The expression is percentage-free. `var()` substitution is textual, so a `%` inside a
      custom property resolves against whatever element finally uses it; only vw/px units
      resolve to the same number in every frame.

   So the copy stays flush to the left padding, hung art stays flush to the right padding, and
   every framed section grows by the same amount at the same moment — which also means the
   padding must not be gated on `stacked`, or any other per-section state (see below). Below
   the crossover the clamp floor wins and nothing moves. Growth is capped (`padGrowMax`)
   because the narrower grid-column frames share the value and still need a usable measure.

   Published as custom properties so callers position decoration and corner links off the
   frame instead of re-deriving it:
     --sf-pad-x / --sf-pad-y   the frame's resolved padding
     --sf-gap                  the copy column's row gap
     --sf-copy-max             the resolved copy measure
     --sf-decor-right          where reserved art should sit, in FRAME coordinates */
// The one padding token. It used to have a shorter vertical partner, which read as lopsided
// once the horizontal side grew past it on a wide screen — so vertical padding now defaults to
// whatever the horizontal side resolves to, and the frame stays square on all four sides at
// every width.
const PAD_X = 'clamp(28px, 5vw, 72px)';
// The shared content column, in viewport units — mirrors `maxWidth: var(--container-site)`
// inside `padding: var(--gutter-site)`. `100vw` counts a classic scrollbar that the container
// does not, so this can read a shade wide; it reads wide by the SAME amount in every frame,
// which is what matters here.
const CONTENT_W = 'min(var(--container-site-narrow, 1280px), 100vw - 2 * var(--gutter-site, 80px))';
// The measure the shared padding is computed against. Deliberately NOT each caller's
// `copyMax`: a section with a wider cap would resolve a different inset and drift out of line
// with its siblings. One reference for the whole page, `copyMax` free to vary per section.
const PAD_REF = '610px';
// The container hits its 1400px cap once the site gutter has stopped growing: gutter tops out
// at 80px, so 1400 + 2*80 = 1560px of viewport. Past that the frame stops widening and the
// two cut corners are pure blank space — so that is exactly where the cut opens up.
const WIDE_MQ = 1560;
function useMinWidth(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(min-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}
function SectionFrame({
  children,
  before,
  after,
  copyMax = 610,
  reserve,
  reserveInset = 'clamp(8px, 2vw, 40px)',
  gutter = 8,
  gap = 20,
  stacked = false,
  columnMinHeight,
  padX = PAD_X,
  padY,
  padRef = PAD_REF,
  padGrowMax = 'clamp(72px, 7vw, 112px)',
  chamfer = 44,
  chamferWide = 104,
  color = 'var(--vv-ice-08)',
  blur = 12,
  accent = false,
  contentStyle,
  style,
  ...rest
}) {
  const cap = typeof copyMax === 'number' ? `${copyMax}px` : copyMax;
  // The cut is geometry, not CSS — it feeds the outline's path math, so it steps on a media
  // query rather than interpolating. One switch here covers every big frame on the page; the
  // small chamfer-16 cards keep their own tight corner.
  const wide = useMinWidth(WIDE_MQ);
  const cut = wide ? chamferWide : chamfer;
  // Half the slack the SHARED column has past the copy cap, floored at the clamp token and
  // ceilinged so narrow sibling frames keep a usable measure. Same number in EVERY frame —
  // note there is deliberately no `stacked` branch here. `stacked` is a per-section decision
  // (each section picks the threshold its own content needs), so gating the padding on it
  // makes sections disagree in the band where one has stacked and the other has not. A
  // stacked single-column section is not harmed by the wider inset: it is already floored and
  // ceilinged. If a section ever genuinely needs a tighter floor, pass `padX` — explicitly, and
  // identically to its siblings — rather than deriving it from a breakpoint.
  const padXFit = `max(${padX}, min(${padGrowMax}, calc((${CONTENT_W} - ${padRef}) / 2)))`;
  // Balanced by default: the grown horizontal value on all four sides.
  const padYFit = padY ?? padXFit;
  // Art hangs flush to the right padding, mirroring the copy on the left; `reserveInset` is
  // just the floor for narrow frames, where padding has stopped growing.
  const decorRight = `max(${reserveInset}, ${padXFit})`;
  // Here `100%` is this frame's own padded content box — correct, and the reason the measure
  // is a `min()` against the cap: a narrower frame simply gets a shorter line, not a shifted
  // one. `reserve` is charged here only, never against the padding.
  //
  // At max width the cap is dropped and the copy fills to the right padding. The cap is a
  // readability device for a growing frame, but once the frame has stopped growing it just
  // parks dead space inside the right padding — the copy's right edge ended up ~275px off the
  // frame while its left edge sat on the 112px padding, which reads as lopsided padding even
  // though the padding itself is symmetric.
  const copyWidth = stacked || wide && !reserve ? '100%' : reserve ? `min(${cap}, calc(100% - ${reserve} - ${gutter}px))` : `min(${cap}, 100%)`;
  return /*#__PURE__*/React.createElement(__ds_scope.Bezel, _extends({
    accent: accent,
    chamfer: cut,
    color: color,
    blur: blur,
    pad: `${padYFit} ${padXFit}`,
    style: {
      '--sf-pad-x': padXFit,
      '--sf-pad-y': padYFit,
      '--sf-gap': `${gap}px`,
      '--sf-copy-max': copyWidth,
      '--sf-decor-right': decorRight,
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'center',
      flex: '1 1 auto',
      minHeight: stacked ? 0 : columnMinHeight
    }
  }, before, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap,
      maxWidth: copyWidth,
      ...contentStyle
    }
  }, children), after));
}
Object.assign(__ds_scope, { SectionFrame });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/surfaces/SectionFrame.jsx", error: String((e && e.message) || e) }); }

// components/surfaces/VideoBackdrop.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The feather lives in tokens/gradients.css as --plate-feather; Bezel masks its own
   plate with the same token, so the two treatments cannot drift apart. */
const FEATHER = 'var(--plate-feather)';
const SCRIMS = {
  bottom: 'var(--scrim-bottom)',
  left: 'var(--scrim-left)',
  vignette: 'var(--scrim-vignette)',
  flood: 'var(--scrim-flood)',
  cinema: 'var(--scrim-cinema)'
};

/* The signature Vivum surface: a muted looping plate under a navy scrim, type on top.
   Pass `src` for video, or `poster` alone for a still plate.
   scrim="none" drops the overlay entirely — only for a plate already feathered by
   circleMask, where the mask alone shapes the footage against the flat canvas. */
function VideoBackdrop({
  children,
  src,
  poster,
  scrim = 'bottom',
  minHeight = 560,
  align = 'end',
  pad = '80px',
  grain,
  edgeFade = true,
  foot,
  circleMask,
  plateOpacity,
  plateAlpha,
  plateShift,
  plateSide = 'right',
  plateScale,
  plateDrop,
  plateFit = 'cover',
  bleed,
  seamless,
  pingPong,
  style,
  ...rest
}) {
  /* How the plate gets back to its first frame. `seamless` dissolves two copies of the file
     into each other; `pingPong` literally rewinds. See plate-loop.js — seamless is the cheap
     one, and the only one that holds up on a full-bleed 1080p plate. Either way `loop` comes
     off the element, or it cuts to frame 0 before the hand-off can run. */
  const loopMode = seamless ? 'crossfade' : pingPong ? 'pingpong' : null;
  const {
    aRef,
    bRef
  } = __ds_scope.usePlateLoop(loopMode, {
    hold: seamless && plateAlpha != null ? plateAlpha : 1
  });
  /* A feathered plate must keep every falloff inside the section: a transform would
     push the right-hand falloff past the edge, where overflow clips it into a straight
     cut. So a masked plate is inset instead — its box starts at `plateShift` and stops
     short of the right edge, leaving the ellipse room to reach zero on all four sides.
     Width is explicit: `auto` on an absolutely positioned video resolves to the file's
     intrinsic width and ignores `right`. */
  /* plateScale grows the plate around its own centre. A feathered plate can overflow
     the section safely: the ellipse reaches zero alpha at 68% of each radius, so the
     visible footage still ends inside the section even when the box does not. */
  const RIGHT_GAP = '4%';
  const box = plateShift ? circleMask ? plateSide === 'center' ? {
    left: plateShift,
    right: plateShift,
    width: `calc(100% - ${plateShift} - ${plateShift})`
  } : plateSide === 'left' ? {
    left: RIGHT_GAP,
    right: plateShift,
    width: `calc(100% - ${plateShift} - ${RIGHT_GAP})`
  } : {
    left: plateShift,
    right: RIGHT_GAP,
    width: `calc(100% - ${plateShift} - ${RIGHT_GAP})`
  } : {
    transform: `translateX(${plateSide === 'left' ? '-' : ''}${plateShift})`
  } : null;
  const transforms = [box && box.transform, plateScale && plateScale !== 1 ? `scale(${plateScale})` : null].filter(Boolean).join(' ');
  const shift = transforms ? {
    ...box,
    transform: transforms
  } : box;
  /* plateDrop pushes a feathered plate down its section without clipping: the box keeps
     its full height and starts lower, so the top falloff clears whatever sits above. */
  const drop = plateDrop ? {
    top: plateDrop,
    bottom: 'auto'
  } : null;
  const plate = {
    position: 'absolute',
    inset: 0,
    width: '100%',
    height: '100%',
    objectFit: plateFit,
    filter: plateOpacity != null ? `var(--plate-filter-base) opacity(${plateOpacity})` : plateAlpha != null ? 'var(--plate-filter-base)' : 'var(--plate-filter)',
    /* In crossfade mode the hook owns element opacity (it IS the dissolve), so the resting
       hold is handed to it instead of declared here — a re-render would clobber it. */
    ...(plateAlpha != null && !seamless ? {
      opacity: plateAlpha
    } : null),
    ...(circleMask ? {
      maskImage: FEATHER,
      WebkitMaskImage: FEATHER
    } : null),
    ...shift,
    ...drop,
    ...(bleed ? {
      pointerEvents: 'none'
    } : null)
  };
  return /*#__PURE__*/React.createElement("section", _extends({
    style: {
      position: 'relative',
      minHeight,
      display: 'flex',
      flexDirection: 'column',
      justifyContent: align === 'center' ? 'center' : align === 'start' ? 'flex-start' : 'flex-end',
      overflow: bleed ? 'visible' : 'hidden',
      background: 'var(--surface-canvas)',
      boxSizing: 'border-box',
      /* bleed lets a feathered plate finish past the section edge instead of meeting a
         hard cut. The raised z-index keeps it painting over the next section's canvas;
         it is pointer-transparent, and its falloff is near zero by the time it gets there. */
      ...(bleed ? {
        zIndex: 1
      } : null),
      ...style
    }
  }, rest), src ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("video", {
    ref: aRef,
    src: src,
    poster: poster,
    autoPlay: true,
    muted: true,
    loop: !loopMode,
    playsInline: true,
    preload: "auto",
    disableRemotePlayback: true,
    style: plate
  }), seamless ? /*#__PURE__*/React.createElement("video", {
    ref: bRef,
    src: src,
    muted: true,
    playsInline: true,
    preload: "auto",
    disableRemotePlayback: true,
    "aria-hidden": "true",
    style: plate
  }) : null) : poster ? /*#__PURE__*/React.createElement("img", {
    src: poster,
    alt: "",
    style: plate
  }) : /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'var(--grad-aperture)'
    }
  }), scrim === 'none' ? null : /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: SCRIMS[scrim] || SCRIMS.bottom,
      pointerEvents: 'none'
    }
  }), edgeFade ? /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      pointerEvents: 'none',
      background: 'linear-gradient(180deg, var(--surface-canvas) 0%, rgba(0, 17, 33, 0) 15%, rgba(0, 17, 33, 0) 85%, var(--surface-canvas) 100%)'
    }
  }) : null, foot ? /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      bottom: 0,
      height: foot === true ? '58%' : foot,
      pointerEvents: 'none',
      background: 'var(--scrim-foot)'
    }
  }) : null, grain ? /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'var(--vv-ice-04)',
      mixBlendMode: 'overlay',
      pointerEvents: 'none'
    }
  }) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1,
      padding: pad,
      width: '100%',
      boxSizing: 'border-box'
    }
  }, children));
}
Object.assign(__ds_scope, { VideoBackdrop });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/surfaces/VideoBackdrop.jsx", error: String((e && e.message) || e) }); }

// slides/Slides.jsx
try { (() => {
const DS = () => window.VivumAIDesignSystem_b2be15;
const PAD = {
  padding: '84px 92px 72px',
  boxSizing: 'border-box',
  height: '100%',
  display: 'flex',
  flexDirection: 'column'
};

/* Every slide is authored at 1280×720 and scales with the frame. */

function TitleSlide() {
  const {
    GradientField,
    Eyebrow
  } = DS();
  return /*#__PURE__*/React.createElement(GradientField, {
    field: "signature",
    style: {
      ...PAD,
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "../assets/vivum-logo-gray.svg",
    alt: "Vivum",
    style: {
      width: 260,
      display: 'block'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 24
    }
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontSize: 62,
      fontWeight: 700,
      letterSpacing: '-0.035em',
      lineHeight: 1.02,
      color: 'var(--vv-ice)',
      maxWidth: 940
    }
  }, "Redefining legitimate autonomy"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 27,
      lineHeight: 1.35,
      color: 'var(--vv-ice-82)',
      maxWidth: 820
    }
  }, "Evolutionary Intelligence for air, land, sea, space, and edge applications.")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'flex-end',
      borderTop: '1px solid var(--line-hairline)',
      paddingTop: 22
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 15,
      fontWeight: 600,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: 'var(--vv-graphite)'
    }
  }, "Vivum AI \xB7 Capability overview"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 15,
      fontWeight: 600,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: 'var(--vv-graphite)'
    }
  }, "August 2026")));
}
function SectionSlide() {
  const {
    GradientField,
    Eyebrow
  } = DS();
  return /*#__PURE__*/React.createElement(GradientField, {
    field: "twin",
    style: {
      ...PAD,
      justifyContent: 'center',
      gap: 30
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, {
    size: "md",
    style: {
      transform: 'scale(1.3)',
      transformOrigin: 'left center'
    }
  }, "Section 02"), /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontSize: 76,
      fontWeight: 700,
      letterSpacing: '-0.03em',
      lineHeight: 1,
      color: 'var(--vv-ice)',
      maxWidth: 900
    }
  }, "The platform"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 24,
      lineHeight: 1.4,
      color: 'var(--vv-ice-72)',
      maxWidth: 700
    }
  }, "Runtime, adaptation loop, ground view \u2014 and what each one refuses to do."));
}
function ContentSlide() {
  const {
    GradientField,
    Eyebrow,
    Divider
  } = DS();
  const items = [['The problem', 'Current autonomy is limited by real-world resource constraints.'], ['Blue ocean market', 'Defense, government, and B2B partnerships.'], ['Solution · tech stack', 'Adaptive systems that surpass trained control and resource-intensive designs.'], ['Operating plans', 'Path to profitability with optional external financing to scale.'], ['Product · performance', 'Affordable, efficient, faster speeds, and superior adaptability.'], ['Team', 'Leadership with proven success in deep tech and defense.']];
  return /*#__PURE__*/React.createElement(GradientField, {
    field: "horizon",
    style: PAD
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Overview"), /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: '30px 0 0',
      fontSize: 44,
      fontWeight: 700,
      letterSpacing: '-0.025em',
      lineHeight: 1.12,
      color: 'var(--vv-ice)',
      maxWidth: 1000
    }
  }, "Redefining ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "legitimate autonomy"), " for the most challenging environments"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '20px 0 0',
      fontSize: 21,
      lineHeight: 1.45,
      color: 'var(--vv-ice-82)',
      maxWidth: 860
    }
  }, "Evolutionary Intelligence designed to enhance performance and adaptability across air, land, sea, space, and edge applications."), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      columnGap: 64,
      rowGap: 26,
      marginTop: 'auto',
      paddingTop: 34,
      borderTop: '1px solid var(--line-hairline)'
    }
  }, items.map(([k, v]) => /*#__PURE__*/React.createElement("div", {
    key: k,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 7
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 19,
      fontWeight: 600,
      color: 'var(--vv-blue)'
    }
  }, k), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 17,
      lineHeight: 1.4,
      color: 'var(--vv-ice-82)'
    }
  }, v)))));
}
function DataSlide() {
  const {
    GradientField,
    Eyebrow,
    Stat,
    DataTable,
    Panel
  } = DS();
  return /*#__PURE__*/React.createElement(GradientField, {
    field: "strata",
    style: PAD
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Performance"), /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: '26px 0 0',
      fontSize: 44,
      fontWeight: 700,
      letterSpacing: '-0.025em',
      color: 'var(--vv-ice)'
    }
  }, "Reference envelope"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 64,
      marginTop: 34
    }
  }, /*#__PURE__*/React.createElement(Stat, {
    value: "4.2",
    unit: "ms",
    label: "On-device inference",
    size: "lg",
    mono: true
  }), /*#__PURE__*/React.createElement(Stat, {
    value: "11.4",
    unit: "W",
    label: "Draw at peak",
    size: "lg",
    mono: true
  }), /*#__PURE__*/React.createElement(Stat, {
    value: "0",
    label: "Cloud dependencies",
    size: "lg",
    mono: true
  }), /*#__PURE__*/React.createElement(Stat, {
    value: "118",
    label: "Units in trial",
    size: "lg",
    mono: true
  })), /*#__PURE__*/React.createElement(Panel, {
    pad: 0,
    style: {
      marginTop: 'auto'
    }
  }, /*#__PURE__*/React.createElement(DataTable, {
    columns: [{
      key: 'k',
      label: 'Metric'
    }, {
      key: 'air',
      label: 'Air',
      mono: true,
      align: 'right',
      tone: 'data'
    }, {
      key: 'land',
      label: 'Land',
      mono: true,
      align: 'right',
      tone: 'data'
    }, {
      key: 'sea',
      label: 'Sea',
      mono: true,
      align: 'right',
      tone: 'data'
    }, {
      key: 'edge',
      label: 'Edge',
      mono: true,
      align: 'right',
      tone: 'data'
    }],
    rows: [{
      k: 'Inference latency',
      air: '4.2 ms',
      land: '4.6 ms',
      sea: '5.1 ms',
      edge: '3.8 ms'
    }, {
      k: 'Power draw, peak',
      air: '11.4 W',
      land: '14.0 W',
      sea: '9.8 W',
      edge: '6.2 W'
    }, {
      k: 'Adaptation window',
      air: '90 s',
      land: '120 s',
      sea: '240 s',
      edge: '60 s'
    }]
  })));
}
function RatioSlide() {
  const {
    GradientField,
    Eyebrow,
    RatioBar,
    Divider
  } = DS();
  return /*#__PURE__*/React.createElement(GradientField, {
    field: "horizon",
    style: PAD
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Architecture"), /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: '26px 0 0',
      fontSize: 44,
      fontWeight: 700,
      letterSpacing: '-0.025em',
      color: 'var(--vv-ice)'
    }
  }, "Where the compute sits"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '18px 0 0',
      fontSize: 21,
      lineHeight: 1.45,
      color: 'var(--vv-ice-82)',
      maxWidth: 800
    }
  }, "Decisions stay on the device. The uplink carries what the operator needs to know \u2014 never what the vehicle needs to act."), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 46
    }
  }, /*#__PURE__*/React.createElement(RatioBar, {
    height: 92,
    segments: [{
      label: 'On-device inference',
      value: 62,
      color: 'var(--vv-blue)'
    }, {
      label: 'Edge relay',
      value: 26,
      color: 'var(--vv-slate)'
    }, {
      label: 'Ground',
      value: 12,
      color: 'var(--vv-amber)'
    }]
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(3, 1fr)',
      gap: 48,
      marginTop: 'auto',
      paddingTop: 30,
      borderTop: '1px solid var(--line-hairline)'
    }
  }, ['No cloud round-trip in the control path.', 'Uplink loss degrades reporting, never control.', 'Every field adaptation is versioned and reversible.'].map(t => /*#__PURE__*/React.createElement("p", {
    key: t,
    style: {
      margin: 0,
      fontSize: 17,
      lineHeight: 1.45,
      color: 'var(--vv-ice-72)'
    }
  }, t))));
}
function QuoteSlide() {
  const {
    GradientField
  } = DS();
  return /*#__PURE__*/React.createElement(GradientField, {
    field: "aperture",
    style: {
      ...PAD,
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 72,
      height: 4,
      background: 'var(--vv-amber)',
      display: 'block',
      marginBottom: 40
    }
  }), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 46,
      fontWeight: 600,
      letterSpacing: '-0.02em',
      lineHeight: 1.22,
      color: 'var(--vv-ice)',
      maxWidth: 1000
    }
  }, "\u201CAutonomy that only works inside the conditions it was trained on isn't autonomy. It's a demo.\u201D"), /*#__PURE__*/React.createElement("span", {
    style: {
      marginTop: 42,
      fontSize: 17,
      fontWeight: 600,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: 'var(--vv-graphite)'
    }
  }, "Vivum Research \xB7 field notes, July 2026"));
}
function ClosingSlide() {
  const {
    GradientField,
    Eyebrow,
    Divider
  } = DS();
  return /*#__PURE__*/React.createElement(GradientField, {
    field: "well",
    style: {
      ...PAD,
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Next steps"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(3, 1fr)',
      gap: 64
    }
  }, [['Briefing', '45 minutes, remote or on site. Bring an engineer.'], ['Evaluation', 'Runtime on your hardware, inside your envelope.'], ['Deployment', 'Staged waves, 24-hour rollback, signed provenance.']].map(([k, v], i) => /*#__PURE__*/React.createElement("div", {
    key: k,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 30,
      fontWeight: 700,
      color: 'var(--vv-blue)',
      letterSpacing: '-0.02em'
    }
  }, '0' + (i + 1)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 24,
      fontWeight: 600,
      color: 'var(--vv-ice)',
      letterSpacing: '-0.015em'
    }
  }, k), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 17,
      lineHeight: 1.45,
      color: 'var(--vv-ice-72)'
    }
  }, v)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'flex-end',
      borderTop: '1px solid var(--line-hairline)',
      paddingTop: 22
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 17,
      color: 'var(--vv-ice-72)'
    }
  }, "brand@vivum.ai"), /*#__PURE__*/React.createElement("img", {
    src: "../assets/vivum-mark-gray.svg",
    alt: "Vivum",
    style: {
      height: 40,
      display: 'block'
    }
  })));
}
Object.assign(window, {
  TitleSlide,
  SectionSlide,
  ContentSlide,
  DataSlide,
  RatioSlide,
  QuoteSlide,
  ClosingSlide
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "slides/Slides.jsx", error: String((e && e.message) || e) }); }

// ui_kits/console/Console.jsx
try { (() => {
const DS = () => window.VivumAIDesignSystem_b2be15;
function Console() {
  const {
    SideNav,
    IconButton,
    StatusDot,
    Toast,
    Badge,
    Icon,
    Divider
  } = DS();
  const [signedIn, setSignedIn] = React.useState(false);
  const [view, setView] = React.useState('fleet');
  const [unit, setUnit] = React.useState(null);
  const [toast, setToast] = React.useState(null);
  React.useEffect(() => {
    if (!toast) return;
    const t = setTimeout(() => setToast(null), 4200);
    return () => clearTimeout(t);
  }, [toast]);
  if (!signedIn) return /*#__PURE__*/React.createElement(LoginScreen, {
    onEnter: () => setSignedIn(true)
  });
  const title = unit ? 'Unit detail' : {
    fleet: 'Fleet',
    models: 'Models',
    runs: 'Training runs'
  }[view] || 'Fleet';
  const body = unit ? /*#__PURE__*/React.createElement(UnitScreen, {
    unitId: unit,
    onBack: () => setUnit(null),
    onToast: setToast
  }) : view === 'models' ? /*#__PURE__*/React.createElement(ModelsScreen, {
    onToast: setToast
  }) : view === 'runs' ? /*#__PURE__*/React.createElement(RunsPlaceholder, null) : /*#__PURE__*/React.createElement(FleetScreen, {
    onOpenUnit: id => setUnit(id),
    onToast: setToast
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      display: 'flex',
      minHeight: '100vh',
      background: 'var(--surface-canvas)'
    }
  }, /*#__PURE__*/React.createElement(SideNav, {
    logoSrc: "../../assets/vivum-logo-gray.svg",
    activeId: unit ? 'fleet' : view,
    onSelect: id => {
      setUnit(null);
      setView(id);
    },
    footer: /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 10
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 3,
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 12.5,
        color: 'var(--text-heading)'
      }
    }, "A. Reyes"), /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 11,
        color: 'var(--text-caption)'
      }
    }, "Flight ops \xB7 Mojave")), /*#__PURE__*/React.createElement(IconButton, {
      icon: "logout",
      label: "Sign out",
      size: "sm",
      variant: "ghost",
      onClick: () => {
        setSignedIn(false);
        setUnit(null);
      }
    })),
    sections: [{
      label: 'Operate',
      items: [{
        id: 'fleet',
        label: 'Fleet',
        icon: 'radar'
      }, {
        id: 'models',
        label: 'Models',
        icon: 'account_tree',
        trailing: /*#__PURE__*/React.createElement(Badge, {
          tone: "amber",
          style: {
            height: 18,
            fontSize: 10
          }
        }, "1")
      }]
    }, {
      label: 'Evolve',
      items: [{
        id: 'runs',
        label: 'Training runs',
        icon: 'monitoring'
      }]
    }]
  }), /*#__PURE__*/React.createElement("main", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      height: 64,
      flex: 'none',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 var(--gutter-page)',
      borderBottom: '1px solid var(--line-hairline)',
      gap: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 15,
      fontWeight: 600,
      color: 'var(--text-heading)'
    }
  }, title), /*#__PURE__*/React.createElement(Divider, {
    orientation: "vertical",
    style: {
      height: 20
    }
  }), /*#__PURE__*/React.createElement(StatusDot, {
    tone: "live",
    pulse: true,
    mono: true
  }, "ground link \xB7 nominal")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11.5,
      color: 'var(--text-caption)',
      marginRight: 6
    }
  }, "08:14:02 PT"), /*#__PURE__*/React.createElement(IconButton, {
    icon: "notifications",
    label: "Alerts",
    size: "sm",
    active: true
  }), /*#__PURE__*/React.createElement(IconButton, {
    icon: "settings",
    label: "Settings",
    size: "sm"
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 'var(--gutter-page)',
      flex: 1,
      minWidth: 0
    }
  }, body)), toast ? /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'fixed',
      right: 24,
      bottom: 24,
      zIndex: 60
    }
  }, /*#__PURE__*/React.createElement(Toast, {
    title: toast.title,
    tone: toast.tone || 'live',
    onClose: () => setToast(null)
  }, toast.body)) : null);
}
function RunsPlaceholder() {
  const {
    EmptyState,
    Button
  } = DS();
  return /*#__PURE__*/React.createElement(EmptyState, {
    icon: "monitoring",
    title: "Training runs are not part of the supplied sources",
    action: /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      size: "sm"
    }, "Read the platform note")
  }, "No screens for the training pipeline existed in the material provided for this design system, so this view is intentionally left blank rather than invented.");
}
Object.assign(window, {
  Console,
  RunsPlaceholder
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/console/Console.jsx", error: String((e && e.message) || e) }); }

// ui_kits/console/FleetScreen.jsx
try { (() => {
const DS = () => window.VivumAIDesignSystem_b2be15;
const UNITS = [{
  id: 'vv-edge-041',
  domain: 'Land',
  site: 'Mojave',
  model: 'v2.14.0',
  lat: '4.2 ms',
  pwr: '11.4 W',
  state: 'live'
}, {
  id: 'vv-edge-042',
  domain: 'Land',
  site: 'Mojave',
  model: 'v2.14.0',
  lat: '4.6 ms',
  pwr: '12.1 W',
  state: 'attention'
}, {
  id: 'vv-air-007',
  domain: 'Air',
  site: 'Kern',
  model: 'v2.13.4',
  lat: '5.1 ms',
  pwr: '9.8 W',
  state: 'live'
}, {
  id: 'vv-air-008',
  domain: 'Air',
  site: 'Kern',
  model: 'v2.13.4',
  lat: '5.4 ms',
  pwr: '10.2 W',
  state: 'idle'
}, {
  id: 'vv-sea-002',
  domain: 'Sea',
  site: 'Point Mugu',
  model: 'v2.12.9',
  lat: '6.0 ms',
  pwr: '8.4 W',
  state: 'live'
}, {
  id: 'vv-edge-055',
  domain: 'Edge',
  site: 'Bench 3',
  model: 'v2.14.0',
  lat: '3.8 ms',
  pwr: '6.2 W',
  state: 'live'
}];
function FleetScreen({
  onOpenUnit,
  onToast
}) {
  const {
    Panel,
    DataTable,
    Stat,
    StatusDot,
    RatioBar,
    Select,
    Input,
    Badge,
    Button,
    IconButton,
    Divider,
    EmptyState,
    Eyebrow
  } = DS();
  const [domain, setDomain] = React.useState('All domains');
  const [q, setQ] = React.useState('');
  const rows = UNITS.filter(u => (domain === 'All domains' || u.domain === domain) && u.id.includes(q));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(4, 1fr)',
      gap: 1,
      background: 'var(--line-hairline)'
    }
  }, [{
    value: '118',
    label: 'Units live',
    delta: '+6 this week'
  }, {
    value: '4.4',
    unit: 'ms',
    label: 'Median inference',
    delta: '-8% vs. last build'
  }, {
    value: '11.0',
    unit: 'W',
    label: 'Median draw',
    delta: '+2%',
    tone: 'amber'
  }, {
    value: '3',
    label: 'Needs attention',
    tone: 'amber'
  }].map(s => /*#__PURE__*/React.createElement("div", {
    key: s.label,
    style: {
      background: 'var(--surface-canvas)',
      padding: '22px 24px'
    }
  }, /*#__PURE__*/React.createElement(Stat, {
    value: s.value,
    unit: s.unit,
    label: s.label,
    delta: s.delta,
    deltaTone: s.tone || 'blue'
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 320px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(Panel, {
    pad: 0,
    title: "Fleet",
    meta: `${rows.length} units · updated 12s ago`,
    actions: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Input, {
      placeholder: "Filter by ID",
      icon: "search",
      value: q,
      onChange: e => setQ(e.target.value),
      style: {
        width: 200
      }
    }), /*#__PURE__*/React.createElement(Select, {
      options: ['All domains', 'Air', 'Land', 'Sea', 'Edge'],
      value: domain,
      onChange: e => setDomain(e.target.value),
      style: {
        width: 160
      }
    }), /*#__PURE__*/React.createElement(IconButton, {
      icon: "refresh",
      label: "Refresh",
      size: "sm",
      onClick: () => onToast({
        title: 'Fleet refreshed',
        body: '118 units · 12s ago'
      })
    }))
  }, rows.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 24
    }
  }, /*#__PURE__*/React.createElement(EmptyState, {
    icon: "settings_input_antenna",
    title: "No units match those filters",
    action: /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      size: "sm",
      onClick: () => {
        setQ('');
        setDomain('All domains');
      }
    }, "Clear filters")
  }, "Units appear here once they check in from the field.")) : /*#__PURE__*/React.createElement(DataTable, {
    dense: true,
    columns: [{
      key: 'unit',
      label: 'Unit',
      mono: true
    }, {
      key: 'domain',
      label: 'Domain',
      tone: 'muted'
    }, {
      key: 'site',
      label: 'Site',
      tone: 'muted'
    }, {
      key: 'model',
      label: 'Model',
      mono: true
    }, {
      key: 'lat',
      label: 'Latency',
      mono: true,
      align: 'right',
      tone: 'data'
    }, {
      key: 'pwr',
      label: 'Draw',
      mono: true,
      align: 'right',
      tone: 'data'
    }, {
      key: 'state',
      label: 'State'
    }, {
      key: 'go',
      label: '',
      align: 'right'
    }],
    rows: rows.map(u => ({
      id: u.id,
      unit: /*#__PURE__*/React.createElement("a", {
        href: "#",
        onClick: e => {
          e.preventDefault();
          onOpenUnit(u.id);
        }
      }, u.id),
      domain: u.domain,
      site: u.site,
      model: u.model,
      lat: u.lat,
      pwr: u.pwr,
      state: /*#__PURE__*/React.createElement(StatusDot, {
        tone: u.state,
        pulse: u.state === 'live'
      }, u.state === 'live' ? 'Streaming' : u.state === 'attention' ? 'Degraded link' : 'Dormant'),
      go: /*#__PURE__*/React.createElement(IconButton, {
        icon: "chevron_right",
        label: `Open ${u.id}`,
        size: "sm",
        variant: "ghost",
        onClick: () => onOpenUnit(u.id)
      })
    }))
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 24
    }
  }, /*#__PURE__*/React.createElement(Panel, {
    title: "Compute placement",
    pad: 20
  }, /*#__PURE__*/React.createElement(RatioBar, {
    segments: [{
      label: 'On-device',
      value: 62,
      color: 'var(--vv-blue)'
    }, {
      label: 'Edge relay',
      value: 26,
      color: 'var(--vv-slate)'
    }, {
      label: 'Ground',
      value: 12,
      color: 'var(--vv-amber)'
    }]
  })), /*#__PURE__*/React.createElement(Panel, {
    title: "Model rollout",
    meta: "v2.14.0 \xB7 wave 2 of 3",
    pad: 20
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, [['Wave 1 · Mojave', '42 units', 'live'], ['Wave 2 · Kern', '38 units', 'live'], ['Wave 3 · Point Mugu', '38 units', 'idle']].map(([k, v, tone]) => /*#__PURE__*/React.createElement("div", {
    key: k,
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(StatusDot, {
    tone: tone
  }, k), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 12,
      color: 'var(--text-caption)'
    }
  }, v))), /*#__PURE__*/React.createElement(Divider, {
    spacing: 2
  }), /*#__PURE__*/React.createElement(Badge, {
    tone: "blue",
    style: {
      alignSelf: 'flex-start'
    }
  }, "Rollback available 22h"))), /*#__PURE__*/React.createElement(Panel, {
    variant: "quiet",
    title: "Field log",
    pad: 20
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      fontFamily: 'var(--font-mono)',
      fontSize: 11.5,
      color: 'var(--text-secondary)',
      lineHeight: 1.5
    }
  }, /*#__PURE__*/React.createElement("span", null, "08:14:02 \xB7 vv-edge-042 uplink 61% loss"), /*#__PURE__*/React.createElement("span", null, "08:12:47 \xB7 vv-air-007 adapt window closed 88s"), /*#__PURE__*/React.createElement("span", null, "08:09:31 \xB7 v2.14.0 wave 2 complete"), /*#__PURE__*/React.createElement("span", null, "07:58:10 \xB7 vv-sea-002 thermal margin 4.1\xB0C"))))));
}
Object.assign(window, {
  FleetScreen,
  UNITS
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/console/FleetScreen.jsx", error: String((e && e.message) || e) }); }

// ui_kits/console/LoginScreen.jsx
try { (() => {
const DS = () => window.VivumAIDesignSystem_b2be15;
function LoginScreen({
  onEnter
}) {
  const {
    GradientField,
    Panel,
    Input,
    Button,
    Eyebrow,
    Checkbox,
    StatusDot,
    Divider
  } = DS();
  return /*#__PURE__*/React.createElement(GradientField, {
    field: "signature",
    style: {
      minHeight: '100vh',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: 40
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 420,
      display: 'flex',
      flexDirection: 'column',
      gap: 32
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/vivum-logo-gray.svg",
    alt: "Vivum",
    style: {
      width: 200,
      display: 'block'
    }
  }), /*#__PURE__*/React.createElement(Panel, {
    pad: 28,
    title: "Fleet Console",
    meta: "Operator sign-in"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement(Input, {
    label: "Operator ID",
    defaultValue: "a.reyes@vivum.ai",
    icon: "person"
  }), /*#__PURE__*/React.createElement(Input, {
    label: "Passphrase",
    type: "password",
    defaultValue: "\xB7\xB7\xB7\xB7\xB7\xB7\xB7\xB7\xB7\xB7\xB7\xB7",
    icon: "lock"
  }), /*#__PURE__*/React.createElement(Checkbox, {
    label: "Remember this workstation"
  }), /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    fullWidth: true,
    icon: "arrow_forward",
    onClick: onEnter
  }, "Sign in"), /*#__PURE__*/React.createElement(Divider, {
    spacing: 2
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement(StatusDot, {
    tone: "live",
    mono: true
  }, "ground link \xB7 nominal"), /*#__PURE__*/React.createElement("a", {
    href: "#",
    style: {
      fontSize: 12.5
    }
  }, "Recover access")))), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--text-caption)',
      textAlign: 'center'
    }
  }, "Authorized use only \xB7 \xA9 2026 Vivum Computing")));
}
Object.assign(window, {
  LoginScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/console/LoginScreen.jsx", error: String((e && e.message) || e) }); }

// ui_kits/console/ModelsScreen.jsx
try { (() => {
const DS = () => window.VivumAIDesignSystem_b2be15;
function ModelsScreen({
  onToast
}) {
  const {
    Panel,
    DataTable,
    Badge,
    Button,
    Stat,
    Divider,
    Eyebrow,
    StatusDot,
    Select,
    IconButton
  } = DS();
  const [scope, setScope] = React.useState('All domains');
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr 1fr',
      gap: 24
    }
  }, /*#__PURE__*/React.createElement(Panel, {
    title: "Current build",
    meta: "Promoted 6 Aug 2026",
    pad: 20
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(Stat, {
    value: "v2.14.0",
    label: "Fleet default",
    mono: true,
    size: "sm"
  }), /*#__PURE__*/React.createElement(StatusDot, {
    tone: "live"
  }, "118 of 118 units"))), /*#__PURE__*/React.createElement(Panel, {
    title: "Candidate",
    meta: "Staged in wave 2",
    pad: 20
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(Stat, {
    value: "v2.14.1",
    label: "Awaiting promotion",
    mono: true,
    size: "sm"
  }), /*#__PURE__*/React.createElement(StatusDot, {
    tone: "attention"
  }, "38 units \xB7 12h observed"))), /*#__PURE__*/React.createElement(Panel, {
    variant: "quiet",
    title: "Rollback",
    meta: "Always one build back",
    pad: 20
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(Stat, {
    value: "v2.13.4",
    label: "Held ready",
    mono: true,
    size: "sm"
  }), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    size: "sm",
    icon: "undo",
    onClick: () => onToast({
      title: 'Rollback armed',
      body: 'v2.13.4 · 22h window',
      tone: 'attention'
    })
  }, "Arm rollback")))), /*#__PURE__*/React.createElement(Panel, {
    pad: 0,
    title: "Builds",
    meta: "Signed, versioned, reversible",
    actions: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Select, {
      options: ['All domains', 'Air', 'Land', 'Sea', 'Edge'],
      value: scope,
      onChange: e => setScope(e.target.value),
      style: {
        width: 160
      }
    }), /*#__PURE__*/React.createElement(IconButton, {
      icon: "download",
      label: "Export manifest",
      size: "sm"
    }))
  }, /*#__PURE__*/React.createElement(DataTable, {
    columns: [{
      key: 'v',
      label: 'Build',
      mono: true
    }, {
      key: 'date',
      label: 'Promoted',
      mono: true,
      tone: 'muted'
    }, {
      key: 'lat',
      label: 'Median latency',
      mono: true,
      align: 'right',
      tone: 'data'
    }, {
      key: 'pwr',
      label: 'Median draw',
      mono: true,
      align: 'right',
      tone: 'data'
    }, {
      key: 'units',
      label: 'Units',
      mono: true,
      align: 'right'
    }, {
      key: 'state',
      label: 'State'
    }],
    rows: [{
      v: 'v2.14.1',
      date: '— staged',
      lat: '4.1 ms',
      pwr: '11.2 W',
      units: '38',
      state: /*#__PURE__*/React.createElement(Badge, {
        tone: "amber"
      }, "Candidate")
    }, {
      v: 'v2.14.0',
      date: '2026-08-06',
      lat: '4.4 ms',
      pwr: '11.0 W',
      units: '118',
      state: /*#__PURE__*/React.createElement(Badge, {
        tone: "blue"
      }, "Default")
    }, {
      v: 'v2.13.4',
      date: '2026-07-18',
      lat: '4.9 ms',
      pwr: '11.6 W',
      units: '0',
      state: /*#__PURE__*/React.createElement(Badge, {
        tone: "graphite"
      }, "Held")
    }, {
      v: 'v2.12.9',
      date: '2026-06-02',
      lat: '5.6 ms',
      pwr: '12.4 W',
      units: '0',
      state: /*#__PURE__*/React.createElement(Badge, {
        tone: "graphite"
      }, "Archived")
    }]
  })), /*#__PURE__*/React.createElement(Panel, {
    variant: "outline",
    pad: 24
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 32,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10,
      maxWidth: 620
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, {
    size: "sm"
  }, "Provenance"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 14,
      lineHeight: 1.55,
      color: 'var(--text-secondary)'
    }
  }, "Every build carries the training set hash, the envelope it was signed against, and the adaptation deltas accepted in the field.")), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    iconAfter: "north_east"
  }, "Open provenance record"))));
}
Object.assign(window, {
  ModelsScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/console/ModelsScreen.jsx", error: String((e && e.message) || e) }); }

// ui_kits/console/UnitScreen.jsx
try { (() => {
const DS = () => window.VivumAIDesignSystem_b2be15;
function UnitScreen({
  unitId,
  onBack,
  onToast
}) {
  const {
    Panel,
    Stat,
    DataTable,
    StatusDot,
    Badge,
    Button,
    IconButton,
    Divider,
    Switch,
    Modal,
    GradientField,
    Icon,
    RatioBar
  } = DS();
  const [deploy, setDeploy] = React.useState(false);
  const [shadow, setShadow] = React.useState(false);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      display: 'flex',
      flexDirection: 'column',
      gap: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 24,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(IconButton, {
    icon: "arrow_back",
    label: "Back to fleet",
    onClick: onBack
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 22,
      color: 'var(--text-heading)',
      letterSpacing: '0.01em'
    }
  }, unitId), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 12,
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement(StatusDot, {
    tone: "live",
    pulse: true
  }, "Streaming \xB7 240 Hz"), /*#__PURE__*/React.createElement(Badge, {
    tone: "graphite",
    mono: true
  }, "v2.14.0"), /*#__PURE__*/React.createElement(Badge, {
    tone: "blue"
  }, "Land \xB7 Mojave")))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    icon: "download"
  }, "Pull telemetry"), /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    icon: "upload",
    onClick: () => setDeploy(true)
  }, "Deploy model"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr 1fr 1fr',
      gap: 1,
      background: 'var(--line-hairline)'
    }
  }, [['4.2', 'ms', 'Inference latency', '-31% vs. baseline', 'blue'], ['11.4', 'W', 'Power draw', '+4%', 'amber'], ['88', 's', 'Last adapt window', '', 'blue'], ['4.1', '°C', 'Thermal margin', '', 'graphite']].map(([v, u, l, d, t]) => /*#__PURE__*/React.createElement("div", {
    key: l,
    style: {
      background: 'var(--surface-canvas)',
      padding: '22px 24px'
    }
  }, /*#__PURE__*/React.createElement(Stat, {
    value: v,
    unit: u,
    label: l,
    delta: d || undefined,
    deltaTone: t,
    mono: true
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.4fr 1fr',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 24
    }
  }, /*#__PURE__*/React.createElement(Panel, {
    pad: 0,
    title: "Adaptation history",
    meta: "Bounded envelope \xB7 every change reversible"
  }, /*#__PURE__*/React.createElement(DataTable, {
    dense: true,
    columns: [{
      key: 't',
      label: 'Timestamp',
      mono: true,
      tone: 'muted'
    }, {
      key: 'ev',
      label: 'Event'
    }, {
      key: 'delta',
      label: 'Δ latency',
      mono: true,
      align: 'right',
      tone: 'data'
    }, {
      key: 'state',
      label: 'State'
    }],
    rows: [{
      t: '08:12:47',
      ev: 'Adapt window closed',
      delta: '-0.3 ms',
      state: /*#__PURE__*/React.createElement(Badge, {
        tone: "blue"
      }, "Kept")
    }, {
      t: '07:41:02',
      ev: 'Adapt window closed',
      delta: '-0.1 ms',
      state: /*#__PURE__*/React.createElement(Badge, {
        tone: "blue"
      }, "Kept")
    }, {
      t: '06:58:33',
      ev: 'Envelope breach — power',
      delta: '+0.6 ms',
      state: /*#__PURE__*/React.createElement(Badge, {
        tone: "amber"
      }, "Reverted")
    }, {
      t: '06:12:20',
      ev: 'Model v2.14.0 applied',
      delta: '-1.8 ms',
      state: /*#__PURE__*/React.createElement(Badge, {
        tone: "blue"
      }, "Kept")
    }]
  })), /*#__PURE__*/React.createElement(Panel, {
    title: "Sensor plate",
    meta: "Live feed \xB7 scrim applied for legibility",
    pad: 0
  }, /*#__PURE__*/React.createElement(GradientField, {
    field: "twin",
    minHeight: 190,
    style: {
      position: 'relative',
      display: 'flex',
      alignItems: 'flex-end'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'var(--scrim-bottom)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      padding: 18,
      display: 'flex',
      justifyContent: 'space-between',
      width: '100%',
      alignItems: 'flex-end'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      color: 'var(--vv-ice-72)'
    }
  }, "CAM 01 \xB7 34.0522\xB0 N, 118.2437\xB0 W"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 15,
      fontWeight: 600,
      color: 'var(--vv-ice)'
    }
  }, "Terrain classifier \xB7 9 tracks")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(IconButton, {
    icon: "pause",
    label: "Pause feed",
    size: "sm"
  }), /*#__PURE__*/React.createElement(IconButton, {
    icon: "fullscreen",
    label: "Expand",
    size: "sm"
  })))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 24
    }
  }, /*#__PURE__*/React.createElement(Panel, {
    title: "Runtime settings",
    pad: 20
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(Switch, {
    label: "Autonomous fallback",
    hint: "Run on-device when the link drops",
    defaultChecked: true
  }), /*#__PURE__*/React.createElement(Divider, null), /*#__PURE__*/React.createElement(Switch, {
    label: "Shadow mode",
    hint: "Evaluate without acting",
    checked: shadow,
    onChange: setShadow
  }), /*#__PURE__*/React.createElement(Divider, null), /*#__PURE__*/React.createElement(Switch, {
    label: "Stream telemetry to ground",
    hint: "Adds 40 kbps uplink",
    defaultChecked: true
  }))), /*#__PURE__*/React.createElement(Panel, {
    title: "Budget vs. measured",
    pad: 20
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement(RatioBar, {
    height: 10,
    legend: false,
    segments: [{
      label: 'Used',
      value: 76,
      color: 'var(--vv-blue)'
    }, {
      label: 'Headroom',
      value: 24,
      color: 'var(--vv-ice-14)'
    }]
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      fontFamily: 'var(--font-mono)',
      fontSize: 11.5,
      color: 'var(--text-caption)'
    }
  }, /*#__PURE__*/React.createElement("span", null, "11.4 W of 15.0 W"), /*#__PURE__*/React.createElement("span", null, "76%")), /*#__PURE__*/React.createElement(Divider, null), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 10,
      alignItems: 'flex-start'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "warning",
    size: 17,
    color: "var(--vv-amber)",
    style: {
      marginTop: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12.5,
      lineHeight: 1.45,
      color: 'var(--text-secondary)'
    }
  }, "One envelope breach in the last 24 hours. Reverted automatically.")))))), /*#__PURE__*/React.createElement(Modal, {
    open: deploy,
    width: 440,
    title: "Deploy model v2.14.1",
    meta: `Target · ${unitId}`,
    onClose: () => setDeploy(false),
    actions: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      size: "sm",
      onClick: () => setDeploy(false)
    }, "Cancel"), /*#__PURE__*/React.createElement(Button, {
      variant: "primary",
      size: "sm",
      onClick: () => {
        setDeploy(false);
        onToast({
          title: 'Deployment queued',
          body: `${unitId} · wave 2 · rollback 24h`,
          tone: 'live'
        });
      }
    }, "Deploy"))
  }, "The unit keeps the current model until the new one clears its first adaptation window. Rollback stays available for 24 hours."));
}
Object.assign(window, {
  UnitScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/console/UnitScreen.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/AboutScreen.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Website › About. Same vocabulary as the homepage and Autonomy — navy canvas, chamfered
   frames, one amber phrase per view, HUD reveals — but arranged as a manifesto rather than a
   front door:

   · the hero is CENTRED on a vignette scrim, the one hero shape no other page uses, because
     About opens on a statement rather than on a route to somewhere else;
   · the thesis is two columns of prose under a single mono label — no second headline
     competing with the hero's;
   · the principles carry display-scale ghost numerals (Autonomy numbers at label scale);
   · Where we're going closes on the page's one graphic: a hairline axis, ice at Now and amber
     at Next, drawn from the sentence above it.

   Every claim, principle and name comes from the supplied About copy. No figures appear on
   this page — none were supplied, and the brand ships no placeholder numbers on a real page. */

const DS_A2 = () => window.VivumAIDesignSystem_b2be15;
const AB_TWEAKS = /*EDITMODE-BEGIN*/{
  "mobileMenuStyle": "fullscreen",
  "heroAlign": "base",
  "heroSub": "mission",
  "heroRail": true,
  "heroHold": 0.15,
  "thesis": "statement",
  "principles": "spine",
  "leadership": "masthead",
  "horizon": "plain",
  "cta": "plate",
  "ctaCase": "sentence",
  "seamAnim": "ribbons",
  "seamSize": 480,
  "seamSpeed": 2,
  "seamX": 0,
  "seamY": 0,
  "seamOpacity": 0.2,
  "mFrame": "rise",
  "mSpeed": 1.2,
  "mDelay": 0,
  "mReplay": false
} /*EDITMODE-END*/;
const AB_PLATE = '../../assets/plates/biological-intelligence.mp4';

/* Three subtitles, all built the way Autonomy's and Contact's are: a short declarative that
   says what the page contains, no adjectives doing the selling. */
const AB_SUBS = {
  plain: 'Who we are, what we hold to, and where edge autonomy goes next.',
  mission: 'Adaptive, explainable models built for the field \u2014 and the people building them.',
  field: "Autonomy for the places the cloud can't reach and conditions never stop changing."
};
const AB_CHAPTERS = [['Vivum AI', 'company'], ['Principles', 'principles'], ['Leadership', 'leadership'], ['Where we are going', 'horizon']];
function useAbMax(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}

/* Walks the page on an interval eased off the wall clock rather than asking for
   `behavior: 'smooth'` — native smooth scrolling is silently a no-op in embedded contexts
   (previews, webviews), and the failure mode there is the worst one available: a jump-link
   that appears not to respond at all. Instant when the user asked for less motion, and it
   yields the moment they scroll for themselves. */
function abScrollTo(y, dur = 620) {
  const from = window.pageYOffset || document.documentElement.scrollTop || 0;
  const to = Math.max(0, y);
  if (Math.abs(to - from) < 2) return;
  if (vvReduced()) {
    window.scrollTo(0, to);
    return;
  }
  const t0 = Date.now();
  let id = 0;
  const stop = () => {
    clearInterval(id);
    window.removeEventListener('wheel', stop);
    window.removeEventListener('touchstart', stop);
    window.removeEventListener('keydown', stop);
  };
  window.addEventListener('wheel', stop, {
    passive: true,
    once: true
  });
  window.addEventListener('touchstart', stop, {
    passive: true,
    once: true
  });
  window.addEventListener('keydown', stop, {
    once: true
  });
  id = setInterval(() => {
    const p = Math.min(1, (Date.now() - t0) / dur);
    window.scrollTo(0, Math.round(from + (to - from) * (1 - Math.pow(1 - p, 4))));
    if (p >= 1) {
      window.scrollTo(0, to);
      stop();
    }
  }, 16);
}

/* The hero's chapter index. A long page states its own shape in the first screen, and mono
   numerals plus the brand's middle dot need no pill or frosted plate to stay legible over
   video — the scrim carries that. */
function AbChapterRail({
  mobile,
  center
}) {
  const [hot, setHot] = React.useState(null);
  const jump = (e, id) => {
    e.preventDefault();
    const host = document.getElementById(id);
    if (!host) return;
    if (window.history && window.history.replaceState) window.history.replaceState(null, '', `#${id}`);
    abScrollTo(host.getBoundingClientRect().top + (window.pageYOffset || 0) - 88);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: mobile ? 'column' : 'row',
      alignItems: mobile ? center ? 'center' : 'flex-start' : 'baseline',
      justifyContent: center ? 'center' : 'flex-start',
      gap: mobile ? 12 : 'clamp(12px, 1.4vw, 20px)',
      paddingTop: 'clamp(20px, 2.2vw, 30px)',
      marginTop: 'clamp(6px, 1vw, 12px)',
      borderTop: '1px solid var(--vv-ice-14)',
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      ...window.AB_LBL,
      flex: 'none'
    }
  }, "On this page"), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: center ? 'center' : 'flex-start',
      gap: 'clamp(8px, 1vw, 14px)',
      flexWrap: 'wrap'
    }
  }, AB_CHAPTERS.map(([label, id], i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: id
  }, i ? /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      color: 'var(--vv-ice-56)',
      fontSize: 13
    }
  }, "\xB7") : null, /*#__PURE__*/React.createElement("a", {
    href: `#${id}`,
    onClick: e => jump(e, id),
    onMouseEnter: () => setHot(id),
    onMouseLeave: () => setHot(null),
    style: {
      display: 'inline-flex',
      alignItems: 'baseline',
      gap: 7,
      fontFamily: 'var(--font-mono)',
      fontSize: mobile ? 12.5 : 13.5,
      fontWeight: 500,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      textDecoration: 'none',
      color: hot === id ? 'var(--vv-amber)' : 'var(--vv-ice-82)',
      transition: 'color 160ms ease'
    }
  }, label)))));
}
function AboutScreen({
  onNavigate
}) {
  const {
    VideoBackdrop,
    GradientField,
    Eyebrow,
    Button,
    SectionFrame
  } = DS_A2();
  const [t, setTweak] = useTweaks(AB_TWEAKS);
  React.useEffect(() => {
    window.vvSetMobileMenuStyle && window.vvSetMobileMenuStyle(t.mobileMenuStyle);
  }, [t.mobileMenuStyle]);
  React.useLayoutEffect(() => {
    window.vvSetMotion({
      frame: t.mFrame,
      speed: t.mSpeed,
      delay: t.mDelay,
      replay: t.mReplay
    });
  }, [t.mFrame, t.mSpeed, t.mDelay, t.mReplay]);
  const stacked = useAbMax(940);
  const mobile = useAbMax(640);
  const tablet = useAbMax(1024) && !mobile;
  const navMobile = useAbMax(760);
  const center = t.heroAlign === 'center' && !mobile;
  const split = t.heroAlign === 'split' && !stacked;
  const heroCopy = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: center ? 'center' : 'flex-start',
      textAlign: center ? 'center' : 'left',
      gap: 'clamp(14px, 1.1vw, 18px)',
      maxWidth: center ? 980 : undefined,
      margin: center ? '0 auto' : undefined,
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    delay: 140
  }, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "About",
    speed: 34
  }))), /*#__PURE__*/React.createElement(Reveal, {
    as: "h1",
    variant: "text",
    dur: 880,
    delay: 260,
    style: {
      margin: 0,
      fontSize: 'clamp(30px, 3.2vw, 46px)',
      fontWeight: 700,
      letterSpacing: '-0.03em',
      lineHeight: 1.08,
      color: 'var(--vv-ice)',
      maxWidth: '22em',
      textTransform: 'capitalize',
      textWrap: 'balance'
    }
  }, "We pioneer ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Evolutionary AI"), ", inspired by the resilience of biological intelligence."), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 620,
    style: {
      margin: 0,
      fontSize: 'clamp(17px, 1.35vw, 20px)',
      lineHeight: 1.5,
      color: 'var(--vv-ice-82)',
      maxWidth: center ? 640 : '100%',
      textWrap: 'pretty'
    }
  }, AB_SUBS[t.heroSub] || AB_SUBS.plain), t.heroRail !== false && !split ? /*#__PURE__*/React.createElement(Reveal, {
    variant: "up",
    delay: 880,
    dur: 520,
    style: {
      width: '100%',
      marginTop: 'clamp(10px, 1vw, 18px)'
    }
  }, /*#__PURE__*/React.createElement(AbChapterRail, {
    mobile: mobile,
    center: center
  })) : null);

  /* `split` puts the statement left and the subtitle plus the index right, on one baseline —
     the shape for a wide screen where a centred block leaves the two halves unbalanced. */
  const heroSplit = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'minmax(0, 1.15fr) minmax(0, 0.85fr)',
      columnGap: 'clamp(40px, 5vw, 96px)',
      alignItems: 'end'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(14px, 1.1vw, 18px)'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    delay: 140
  }, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "About",
    speed: 34
  }))), /*#__PURE__*/React.createElement(Reveal, {
    as: "h1",
    variant: "text",
    dur: 880,
    delay: 260,
    style: {
      margin: 0,
      fontSize: 'clamp(30px, 3.2vw, 46px)',
      fontWeight: 700,
      letterSpacing: '-0.03em',
      lineHeight: 1.08,
      color: 'var(--vv-ice)',
      textTransform: 'capitalize',
      textWrap: 'balance'
    }
  }, "We pioneer ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Evolutionary AI"), ", inspired by the resilience of biological intelligence.")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(14px, 1.6vw, 22px)'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 620,
    style: {
      margin: 0,
      fontSize: 'clamp(17px, 1.35vw, 20px)',
      lineHeight: 1.5,
      color: 'var(--vv-ice-82)',
      textWrap: 'pretty'
    }
  }, AB_SUBS[t.heroSub] || AB_SUBS.plain), t.heroRail !== false ? /*#__PURE__*/React.createElement(Reveal, {
    variant: "up",
    delay: 880,
    dur: 520
  }, /*#__PURE__*/React.createElement(AbChapterRail, {
    mobile: mobile
  })) : null));

  /* ── Thesis ──────────────────────────────────────────────────────────────────────────────
     Two columns of prose, no headline: the hero already made the statement, and a second H2
     four lines under it would argue with it. The lede holds ice-92 at lead scale and the
     detail steps down to secondary, so the eye reads thesis → detail rather than left → right. */
  const lede = /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 200,
    style: {
      margin: 0,
      fontSize: 'clamp(1.4rem, 2vw, 2.8rem)',
      fontWeight: 400,
      lineHeight: 1.35,
      letterSpacing: '-0.015em',
      color: 'var(--vv-ice-92)',
      textWrap: 'pretty'
    }
  }, "Vivum pioneers ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Evolutionary AI"), ", harnessing biological intelligence to give autonomous vehicles, advanced robotics, and edge devices the ability to", ' ', /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "sense"), ", ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "decide"), ", and ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "act"), " in the real world \u2014 where the cloud cannot reach and conditions never stop changing.");
  const detail = /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 380,
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.62,
      color: 'var(--text-secondary)',
      textWrap: 'pretty'
    }
  }, "Our adaptive, explainable Dynamic Neural Models deliver legitimate autonomy while drastically cutting energy use and compute demands. They run natively on edge devices, degrade gracefully in contested and comms-degraded environments, and stay decoupled from any single hardware platform \u2014 autonomy you can trust, because you can understand it.");
  const thesisBody = t.thesis === 'lede' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(20px, 2.2vw, 30px)',
      maxWidth: 820
    }
  }, lede, detail) : t.thesis === 'columns' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : 'minmax(0, 1fr) minmax(0, 1fr)',
      columnGap: 'clamp(36px, 4.6vw, 80px)',
      rowGap: 'clamp(22px, 5vw, 32px)',
      alignItems: 'start'
    }
  }, lede, /*#__PURE__*/React.createElement("div", {
    style: stacked ? null : {
      paddingLeft: 'clamp(24px, 3vw, 48px)',
      borderLeft: '1px solid var(--line-hairline)'
    }
  }, detail)) : t.thesis === 'ledger' ?
  /*#__PURE__*/
  /* ledger — a mono index column holds the section's name and its two beats, the prose runs
     in one measure beside it. The label stops being a hat on the copy and becomes a spine. */
  React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : 'minmax(0, 0.26fr) minmax(0, 1fr)',
      columnGap: 'clamp(32px, 4vw, 72px)',
      rowGap: 'clamp(22px, 5vw, 30px)',
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    delay: 120,
    style: {
      display: 'flex',
      flexDirection: stacked ? 'row' : 'column',
      alignItems: stacked ? 'center' : 'flex-start',
      gap: stacked ? 12 : 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      ...window.AB_LBL,
      color: 'var(--vv-amber)'
    }
  }, "Thesis"), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      flex: stacked ? 1 : 'none',
      width: stacked ? undefined : 44,
      height: 1,
      background: 'var(--vv-ice-24)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: window.AB_LBL
  }, "01 \u2014 02")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(20px, 2.2vw, 30px)',
      maxWidth: 860
    }
  }, lede, /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 'clamp(18px, 2vw, 28px)',
      borderTop: '1px solid var(--line-hairline)'
    }
  }, detail))) :
  /*#__PURE__*/
  /* statement — the recommended one. The claim runs the full measure at lead scale, then a
     hairline drops and the qualifying detail steps in from the right at body scale, so the
     block reads as one sentence and its footnote rather than as two equal columns. */
  React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(22px, 2.4vw, 34px)'
    }
  }, lede, /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 'clamp(20px, 2.2vw, 30px)',
      borderTop: '1px solid var(--line-hairline)'
    }
  }, detail));
  const thesis = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(20px, 2.4vw, 34px)'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    delay: 80
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "VIVUM AI")), thesisBody);
  const ctaHead = t.ctaCase === 'caps' ? /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontSize: mobile ? 'clamp(19px, 5vw, 24px)' : 'clamp(22px, 2.2vw, 32px)',
      fontWeight: 600,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      lineHeight: 1.25,
      color: 'var(--vv-ice)'
    }
  }, "We are paving the way for a ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "better tomorrow")) : /*#__PURE__*/React.createElement(React.Fragment, null, "We are paving the way for a ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "better tomorrow"));
  return /*#__PURE__*/React.createElement("div", {
    id: "about",
    style: {
      isolation: 'isolate'
    }
  }, /*#__PURE__*/React.createElement(VideoBackdrop, {
    src: AB_PLATE,
    scrim: center ? 'vignette' : 'bottom',
    seamless: true,
    minHeight: mobile ? 'max(560px, 78svh)' : 'max(560px, 74svh)',
    align: center ? 'center' : 'end',
    edgeFade: false,
    foot: center ? false : '14%',
    pad: mobile ? '170px var(--gutter-site) clamp(76px, 18vw, 108px)' : navMobile ? '176px var(--gutter-site) 48px' : center ? '180px var(--gutter-site) clamp(96px, 10vw, 148px)' : '180px var(--gutter-site) clamp(72px, 7vw, 112px)',
    plateOpacity: typeof t.heroHold === 'number' ? t.heroHold : 0.4
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site)',
      margin: '0 auto',
      width: '100%'
    }
  }, split ? heroSplit : heroCopy)), /*#__PURE__*/React.createElement(GradientField, {
    field: "none",
    as: "section",
    id: "company",
    pad: mobile ? 'clamp(56px, 13vw, 88px) var(--gutter-site)' : 'clamp(52px, 5.4vw, 92px) var(--gutter-site) clamp(40px, 4.2vw, 72px)'
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: mobile ? 'up' : 'frame',
    dur: 680
  }, /*#__PURE__*/React.createElement(SectionFrame, _extends({
    stacked: true,
    stretch: true,
    copyMax: "100%",
    contentStyle: {
      maxWidth: '100%'
    },
    gap: 0,
    chamfer: mobile ? 28 : 44,
    chamferWide: 72,
    weight: 1,
    color: "var(--vv-ice-14)",
    accent: false,
    fill: "transparent"
  }, mobile ? {
    chamfer: 0,
    color: 'transparent',
    blur: 0,
    padX: '0px',
    padY: '0px'
  } : tablet ? {
    padX: 'clamp(40px, 6vw, 80px)'
  } : null), thesis)))), /*#__PURE__*/React.createElement(AbPrinciples, {
    variant: t.principles
  }), /*#__PURE__*/React.createElement(SeamAnimation, {
    variant: t.seamAnim,
    size: t.seamSize,
    speed: t.seamSpeed,
    x: t.seamX,
    y: t.seamY,
    opacity: t.seamOpacity
  }), /*#__PURE__*/React.createElement(AbLeadership, {
    variant: t.leadership
  }), /*#__PURE__*/React.createElement(AbHorizon, {
    variant: t.horizon
  }), /*#__PURE__*/React.createElement(GradientField, {
    field: "none",
    as: "section",
    id: "join",
    pad: mobile ? 'clamp(64px, 15vw, 96px) var(--gutter-site) clamp(96px, 22vw, 140px)' : 'clamp(40px, 4.2vw, 72px) var(--gutter-site) clamp(48px, 5vw, 88px)'
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, t.cta === 'bare' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 'clamp(28px, 4vw, 64px)',
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 800,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)',
      maxWidth: 780,
      textWrap: 'balance'
    }
  }, ctaHead), /*#__PURE__*/React.createElement(Reveal, {
    variant: "rise",
    delay: 280,
    dur: 520,
    style: mobile ? {
      width: '100%'
    } : undefined
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    size: "lg",
    onClick: () => onNavigate('/contact'),
    style: mobile ? {
      width: '100%'
    } : undefined
  }, "Contact us"))) : /*#__PURE__*/React.createElement(Reveal, {
    variant: "frame",
    dur: 760
  }, /*#__PURE__*/React.createElement(SectionFrame, {
    accent: true,
    chamfer: 44,
    chamferWide: 104,
    weight: 1,
    color: "var(--vv-ice-14)",
    stacked: stacked,
    contentStyle: {
      maxWidth: '100%'
    },
    padX: "clamp(64px, 7vw, 120px)",
    plateSrc: t.cta === 'plate' ? '../../assets/plates/home-banner-video.mp4' : undefined,
    plateOpacity: 0.2,
    plateShift: "clamp(30px, 20vw, 300px)",
    plateScale: 1.7,
    fill: "transparent"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: stacked ? 'column' : 'row',
      justifyContent: 'center',
      columnGap: 'clamp(40px, 6vw, 96px)',
      rowGap: 'clamp(28px, 3.4vw, 40px)',
      alignItems: stacked ? 'flex-start' : 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'flex-start',
      gap: 16,
      maxWidth: tablet ? '100%' : 680,
      flex: '0 1 auto'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 800,
    delay: 280,
    style: {
      margin: 0,
      fontSize: mobile ? 'clamp(26px, 2.5vw, 36px)' : 'clamp(30px, 3.2vw, 46px)',
      fontWeight: 700,
      letterSpacing: mobile ? '-0.02em' : '-0.03em',
      lineHeight: mobile ? 1.15 : 1.08,
      color: 'var(--vv-ice)',
      textTransform: 'capitalize',
      textWrap: tablet ? 'wrap' : 'balance'
    }
  }, ctaHead), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 460,
    style: {
      margin: 0,
      fontSize: 'clamp(17px, 1.35vw, 20px)',
      lineHeight: 1.5,
      color: 'var(--vv-ice-82)',
      maxWidth: tablet ? '100%' : 600,
      textWrap: 'pretty'
    }
  }, "Tell us the platform you field and the constraint that hurts most \u2014 we\u2019ll show you what Evolutionary AI does with it.")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'center',
      flex: mobile || tablet ? '1 1 100%' : '0 0 auto',
      width: mobile || tablet ? '100%' : undefined
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "rise",
    delay: 740,
    dur: 520,
    style: {
      width: mobile || tablet ? '100%' : undefined
    }
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    size: "lg",
    onClick: () => onNavigate('/contact'),
    style: {
      height: 58,
      padding: '0 38px',
      fontSize: 17,
      ...(mobile || tablet ? {
        width: '100%'
      } : null)
    }
  }, "Contact us")))))))), /*#__PURE__*/React.createElement(TweaksPanel, null, /*#__PURE__*/React.createElement(TweakSection, {
    label: "Hero"
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Layout",
    value: t.heroAlign,
    options: ['center', 'base', 'split'],
    onChange: v => setTweak('heroAlign', v)
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Subtitle",
    value: t.heroSub,
    options: ['plain', 'mission', 'field'],
    onChange: v => setTweak('heroSub', v)
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Chapter index",
    value: t.heroRail !== false,
    onChange: v => setTweak('heroRail', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Plate hold",
    value: typeof t.heroHold === 'number' ? t.heroHold : 0.4,
    min: 0.15,
    max: 0.7,
    step: 0.05,
    onChange: v => setTweak('heroHold', v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Sections"
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Thesis",
    value: t.thesis,
    options: ['statement', 'ledger', 'columns', 'lede'],
    onChange: v => setTweak('thesis', v)
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Principles",
    value: t.principles,
    options: ['spine', 'cells', 'ledger'],
    onChange: v => setTweak('principles', v)
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Leadership",
    value: t.leadership,
    options: ['masthead', 'frame', 'stacked', 'plain'],
    onChange: v => setTweak('leadership', v)
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Horizon",
    value: t.horizon,
    options: ['plain', 'quote'],
    onChange: v => setTweak('horizon', v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Closing band"
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Frame",
    value: t.cta,
    options: ['banner', 'plate', 'bare'],
    onChange: v => setTweak('cta', v)
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Headline case",
    value: t.ctaCase,
    options: ['sentence', 'caps'],
    onChange: v => setTweak('ctaCase', v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Motion"
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Chamfered frames",
    value: t.mFrame,
    options: window.VV_FRAME_OPTIONS,
    onChange: v => setTweak('mFrame', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Speed",
    value: t.mSpeed,
    min: 0.25,
    max: 2.5,
    step: 0.05,
    unit: "\xD7",
    onChange: v => setTweak('mSpeed', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Delay",
    value: t.mDelay,
    min: 0,
    max: 800,
    step: 20,
    unit: "ms",
    onChange: v => setTweak('mDelay', v)
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Replay on re-entry",
    value: t.mReplay,
    onChange: v => setTweak('mReplay', v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Seam background"
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Animation",
    value: t.seamAnim,
    options: [{
      value: 'texture',
      label: 'Texture — film grain'
    }, {
      value: 'nebula',
      label: 'Nebula — turbulence cloud'
    }, {
      value: 'ash',
      label: 'Ash — falling grit'
    }, {
      value: 'sheen',
      label: 'Sheen — satin sweep'
    }, {
      value: 'weave',
      label: 'Weave — combed fibre'
    }, {
      value: 'smoke',
      label: 'Smoke — climbing plumes'
    }, {
      value: 'marble',
      label: 'Marble — breathing veins'
    }, {
      value: 'static',
      label: 'Static — signal loss'
    }, {
      value: 'ribbons',
      label: 'Ribbons — sliding bands'
    }, {
      value: 'starfield',
      label: 'Starfield — twinkling depths'
    }, {
      value: 'drift',
      label: 'Drift — crossing blooms'
    }, {
      value: 'scan',
      label: 'Scan — sweeping hairlines'
    }, {
      value: 'pulse',
      label: 'Pulse — propagating rings'
    }, {
      value: 'lattice',
      label: 'Lattice — drifting mesh'
    }, {
      value: 'orbit',
      label: 'Orbit — tracking dots'
    }, {
      value: 'contours',
      label: 'Contours — breathing terrain'
    }, {
      value: 'motes',
      label: 'Motes — rising particles'
    }, {
      value: 'waves',
      label: 'Waves — swaying crests'
    }, {
      value: 'shafts',
      label: 'Shafts — angled light'
    }, {
      value: 'off',
      label: 'Off'
    }],
    onChange: v => setTweak('seamAnim', v)
  }), t.seamAnim !== 'off' && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Size",
    value: t.seamSize,
    min: 120,
    max: 640,
    step: 10,
    unit: "px",
    onChange: v => setTweak('seamSize', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Speed",
    value: t.seamSpeed,
    min: 0.25,
    max: 3,
    step: 0.05,
    unit: "\xD7",
    onChange: v => setTweak('seamSpeed', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "X position",
    value: t.seamX,
    min: -50,
    max: 50,
    step: 1,
    unit: "%",
    onChange: v => setTweak('seamX', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Y position",
    value: t.seamY,
    min: -100,
    max: 100,
    step: 2,
    unit: "%",
    onChange: v => setTweak('seamY', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Opacity",
    value: t.seamOpacity,
    min: 0.02,
    max: 1,
    step: 0.01,
    onChange: v => setTweak('seamOpacity', v)
  }))));
}
Object.assign(window, {
  AboutScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/AboutScreen.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/AboutSections.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Website › About — the three middle sections: Principles, Leadership, Horizon.
   Split out of AboutScreen.jsx to keep both files readable. Every headline, claim and name
   here comes from the supplied About copy; no figures appear anywhere on this page because
   none were supplied. */

const DS_AB = () => window.VivumAIDesignSystem_b2be15;
function useAbsMax(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}
function useAbsMin(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(min-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}

/* The page's one micro-label style — mono, tracked, graphite. Shared with AboutScreen through
   window, since each babel script owns its own scope. */
const AB_LBL = {
  fontFamily: 'var(--font-mono)',
  fontSize: 11,
  letterSpacing: '0.18em',
  textTransform: 'uppercase',
  color: 'var(--vv-graphite)'
};

/* Titles held to three or four words and bodies to a single balanced line each, so the three
   read as one set rather than one long entry and two short ones. Meaning is unchanged. */
const AB_PRINCIPLES = [{
  n: '01',
  icon: 'rectangle-history',
  title: 'Decoupled from any platform',
  body: 'Our intelligence layer sits on top of physical systems, never locked to a single one.'
}, {
  n: '02',
  icon: 'radar',
  title: 'Built for contested environments',
  body: 'No cloud dependence. Built to degrade gracefully under pressure rather than fail outright.'
}, {
  n: '03',
  icon: 'shield-check',
  title: 'Explainable by design',
  body: 'Autonomy you can trust, because every decision the model makes can be read and understood.'
}];

/* The founder's name is the section's headline, so it is deliberately NOT repeated here — the
   ledger complements the prose rather than restating it. */
const AB_LEDGER = [['Team', 'Operators, engineers, researchers'], ['Advisors', 'National security and intelligence communities'], ['Backing', "Leading institutional investors"]];
const AB_HORIZON = [{
  k: 'Now',
  title: 'Foundational autonomy',
  note: 'Systems that sense, decide, and act on the device.'
}, {
  k: 'Next',
  title: 'Distributed, coordinated intelligence',
  note: 'Across fleets and domains, compounding with every deployment.'
}];

/* ── Principles ───────────────────────────────────────────────────────────────────────────
   A ghost numeral in the gutter is this page's own device: Autonomy numbers its advantages at
   label scale, so About numbers its principles at display scale instead — the same cue, a
   different voice, and the numerals carry the section's rhythm without a second heading. */
function AbPrincipleRow({
  item,
  i,
  mobile
}) {
  const [hot, setHot] = React.useState(false);
  return /*#__PURE__*/React.createElement(Reveal, {
    variant: "up",
    delay: i * 130,
    dur: 560,
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      display: 'grid',
      gridTemplateColumns: mobile ? 'minmax(0, 1fr)' : 'auto minmax(0, 1fr)',
      columnGap: 'clamp(22px, 2.8vw, 48px)',
      rowGap: 10,
      alignItems: 'start',
      padding: mobile ? '22px 0 24px' : 'clamp(24px, 2.6vw, 36px) 0',
      borderTop: i ? '1px solid var(--line-hairline)' : 'none',
      transition: 'background var(--dur-fast) var(--ease-out)',
      background: hot ? 'rgba(218,232,242,0.03)' : 'transparent'
    }
  }, /*#__PURE__*/React.createElement(HudText, {
    text: item.n,
    speed: 22,
    delay: 140 + i * 130,
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: mobile ? 20 : 'clamp(26px, 2.5vw, 38px)',
      fontWeight: 400,
      lineHeight: 1,
      letterSpacing: '-0.02em',
      fontVariantNumeric: 'tabular-nums',
      color: hot ? 'var(--vv-amber)' : 'var(--vv-ice-24)',
      transition: 'color var(--dur-fast) var(--ease-out)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexDirection: mobile ? 'column' : 'row',
      alignItems: mobile ? 'flex-start' : 'baseline',
      gap: mobile ? 8 : 'clamp(22px, 2.6vw, 44px)'
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      flex: mobile ? undefined : '0 0 clamp(210px, 22vw, 320px)',
      fontSize: mobile ? 18 : 'clamp(19px, 1.7vw, 23px)',
      fontWeight: 600,
      letterSpacing: '-0.01em',
      lineHeight: 1.25,
      color: 'var(--vv-ice)',
      textWrap: 'balance'
    }
  }, item.title), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 15,
      lineHeight: 1.6,
      color: 'var(--text-secondary)',
      textWrap: 'pretty',
      maxWidth: '58ch'
    }
  }, item.body)));
}
function AbPrincipleCell({
  item,
  i,
  mobile
}) {
  const {
    Icon
  } = DS_AB();
  const [hot, setHot] = React.useState(false);
  return /*#__PURE__*/React.createElement(Reveal, {
    variant: "up",
    delay: i * 130,
    dur: 560,
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      background: hot ? 'rgba(218,232,242,0.04)' : 'var(--surface-canvas)',
      padding: mobile ? '26px 22px 30px' : 'clamp(26px, 2.8vw, 38px)',
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(16px, 1.6vw, 22px)',
      transition: 'background var(--dur-fast) var(--ease-out)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: item.icon,
    size: mobile ? 28 : 34,
    color: "var(--vv-ice)"
  }), /*#__PURE__*/React.createElement(HudText, {
    text: item.n,
    speed: 22,
    delay: 200 + i * 130,
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: mobile ? 20 : 28,
      fontWeight: 400,
      lineHeight: 1,
      fontVariantNumeric: 'tabular-nums',
      color: hot ? 'var(--vv-amber)' : 'var(--vv-ice-24)',
      transition: 'color var(--dur-fast) var(--ease-out)'
    }
  })), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: mobile ? 18 : 'clamp(19px, 1.6vw, 22px)',
      fontWeight: 600,
      letterSpacing: '-0.01em',
      lineHeight: 1.25,
      color: 'var(--vv-ice)',
      textWrap: 'balance'
    }
  }, item.title), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 14.5,
      lineHeight: 1.6,
      color: 'var(--text-secondary)',
      textWrap: 'pretty'
    }
  }, item.body));
}

/* Right column of Principles — the same device as Autonomy's Advantages spine: one hairline
   rail with icon boxes sitting on it, OUTSIDE the chamfered frame that holds the copy, so the
   two read as peer objects rather than one frame containing everything. */
function AbPrinciplesSpine({
  mobile
}) {
  const {
    Icon
  } = DS_AB();
  const wide = useAbsMin(1560);
  const box = wide ? 96 : 64,
    rail = wide ? 132 : 92;
  if (mobile) {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 28
      }
    }, AB_PRINCIPLES.map((m, i) => /*#__PURE__*/React.createElement(Reveal, {
      key: m.title,
      variant: "side",
      delay: i * 120,
      dur: 520,
      style: {
        display: 'grid',
        gridTemplateColumns: 'auto 1fr',
        columnGap: 16,
        alignItems: 'start'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        width: 44,
        height: 44,
        flex: 'none',
        border: '1px solid var(--line-hairline)',
        background: 'var(--surface-canvas)'
      }
    }, /*#__PURE__*/React.createElement(Icon, {
      name: m.icon,
      size: 26,
      color: "var(--vv-ice)"
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column'
      }
    }, /*#__PURE__*/React.createElement("h3", {
      style: {
        margin: 0,
        fontSize: 17,
        fontWeight: 600,
        letterSpacing: '-0.01em',
        lineHeight: 1.3,
        color: 'var(--vv-ice)'
      }
    }, m.title), /*#__PURE__*/React.createElement("p", {
      style: {
        margin: 0,
        fontSize: 14.5,
        lineHeight: 1.6,
        color: 'var(--text-secondary)',
        textWrap: 'pretty'
      }
    }, m.body)))));
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      paddingLeft: rail
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "fade",
    dur: 900,
    style: {
      position: 'absolute',
      left: box / 2,
      top: 22,
      bottom: 26,
      width: 1,
      background: 'var(--line-hairline)'
    }
  }), AB_PRINCIPLES.map((m, i) => /*#__PURE__*/React.createElement(Reveal, {
    key: m.title,
    variant: "side",
    delay: i * 130,
    dur: 560,
    style: {
      position: 'relative',
      marginBottom: i === AB_PRINCIPLES.length - 1 ? 0 : 40
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: -rail,
      top: wide ? '50%' : 0,
      transform: wide ? 'translateY(-50%)' : undefined,
      width: box,
      height: box,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      border: '1px solid var(--line-hairline)',
      background: 'var(--surface-canvas)'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: m.icon,
    size: wide ? 46 : 30,
    color: "var(--vv-ice)"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10,
      paddingTop: wide ? 8 : 0,
      minHeight: box
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: 20,
      fontWeight: 600,
      letterSpacing: '-0.01em',
      lineHeight: 1.3,
      color: 'var(--vv-ice)'
    }
  }, m.title), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 14.5,
      lineHeight: 1.6,
      color: 'var(--text-secondary)',
      textWrap: 'pretty',
      maxWidth: 360
    }
  }, m.body)))));
}

/* Section shell copied from Autonomy's Advantages: copy in a chamfered frame on the left, the
   three principles on a hairline spine to its right. `cells` and `ledger` keep the older
   full-width treatments inside a single frame. */
function AbPrinciples({
  variant
}) {
  const {
    Eyebrow,
    SectionFrame
  } = DS_AB();
  const stacked = useAbsMax(900);
  const mobile = useAbsMax(640);
  const tablet = useAbsMax(1024) && !mobile;
  const cols = mobile ? 1 : stacked ? 2 : 3;
  const spine = variant !== 'cells' && variant !== 'ledger';
  const items = variant === 'cells' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `repeat(${cols}, minmax(0, 1fr))`,
      gap: 1,
      background: 'var(--line-hairline)',
      border: '1px solid var(--line-hairline)'
    }
  }, AB_PRINCIPLES.map((p, i) => /*#__PURE__*/React.createElement(AbPrincipleCell, {
    key: p.n,
    item: p,
    i: i,
    mobile: mobile
  }))) : /*#__PURE__*/React.createElement("div", null, AB_PRINCIPLES.map((p, i) => /*#__PURE__*/React.createElement(AbPrincipleRow, {
    key: p.n,
    item: p,
    i: i,
    mobile: mobile
  })));
  const copy = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Principles",
    speed: 30,
    delay: 200
  })), /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 760,
    delay: 260,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)',
      textTransform: 'capitalize'
    }
  }, "The ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Fundamental"), " and Core Values We Live By Every Day"), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 440,
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)'
    }
  }, "Three commitments decide what we build and what we refuse to build. They are the reason our models run on your hardware, hold up when the link drops, and can be read by the people who have to answer for them."));
  const frameProps = mobile ? {
    chamfer: 0,
    color: 'transparent',
    blur: 0,
    accent: false,
    padX: '0px',
    padY: '0px'
  } : tablet ? {
    padX: 'clamp(48px, 6vw, 96px)',
    padY: 'clamp(48px, 6vw, 96px)'
  } : null;
  return /*#__PURE__*/React.createElement(GradientFieldAb, {
    pad: mobile ? 'clamp(64px, 15vw, 96px) var(--gutter-site)' : 'clamp(40px, 4.2vw, 72px) var(--gutter-site)',
    id: "principles"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, spine ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : 'minmax(0, 0.72fr) minmax(0, 0.68fr)',
      columnGap: 0,
      rowGap: 'clamp(36px, 5vw, 56px)',
      alignItems: 'stretch'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      alignSelf: 'center'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: mobile ? 'up' : 'frame',
    dur: 640
  }, /*#__PURE__*/React.createElement(SectionFrame, _extends({
    copyMax: "100%",
    stacked: stacked,
    contentStyle: {
      maxWidth: '100%'
    },
    fill: "transparent"
  }, frameProps), copy))), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: stacked ? '100%' : 580,
      width: '100%',
      boxSizing: 'border-box',
      justifySelf: 'start',
      alignSelf: 'center',
      paddingLeft: stacked ? 0 : 'clamp(2.5rem, 5.2vw, 7.5rem)',
      paddingRight: 0
    }
  }, /*#__PURE__*/React.createElement(AbPrinciplesSpine, {
    mobile: mobile
  }))) : /*#__PURE__*/React.createElement(Reveal, {
    variant: mobile ? 'up' : 'frame',
    dur: 640
  }, /*#__PURE__*/React.createElement(SectionFrame, _extends({
    stacked: stacked,
    fill: "transparent",
    after: /*#__PURE__*/React.createElement("div", {
      style: {
        marginTop: 'clamp(24px, 2.8vw, 40px)',
        paddingTop: 'clamp(20px, 2.4vw, 32px)',
        borderTop: '1px solid var(--line-hairline)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'baseline',
        gap: 10,
        paddingBottom: 'clamp(8px, 1.4vw, 18px)'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: AB_LBL
    }, "Three principles"), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        flex: 1,
        height: 1,
        background: 'var(--line-hairline)'
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        ...AB_LBL,
        letterSpacing: '0.1em'
      }
    }, "01\u201303")), items)
  }, frameProps), copy))));
}

/* ── Leadership ───────────────────────────────────────────────────────────────────────────
   Prose on the left for the reader, a hairline ledger on the right for the scanner — the same
   two-audience split the Contact page uses for the form and its direct lines. No portraits:
   none were supplied, and the brand ships no placeholder photography. */
function AbLedgerRows({
  rows,
  mobile
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: '1px solid var(--line-hairline)'
    }
  }, rows.map(([k, v], i) => /*#__PURE__*/React.createElement(Reveal, {
    key: k,
    variant: "up",
    delay: i * 110,
    dur: 520,
    style: {
      display: 'grid',
      gridTemplateColumns: mobile ? 'minmax(0, 1fr)' : 'minmax(0, 0.72fr) minmax(0, 1fr)',
      columnGap: 'clamp(16px, 2vw, 32px)',
      rowGap: 6,
      alignItems: 'baseline',
      padding: '16px 0',
      borderBottom: '1px solid var(--line-hairline)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: AB_LBL
  }, k), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'clamp(15px, 1.15vw, 16.5px)',
      lineHeight: 1.45,
      color: 'var(--vv-ice-82)',
      textWrap: 'pretty'
    }
  }, v))));
}
function AbLeadership({
  variant
}) {
  const {
    Eyebrow,
    SectionFrame
  } = DS_AB();
  const stacked = useAbsMax(940) || variant === 'stacked';
  const mobile = useAbsMax(640);
  const tablet = useAbsMax(1024) && !mobile;
  const heading = /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 800,
    delay: 240,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)',
      textTransform: 'capitalize',
      textWrap: 'balance'
    }
  }, "Led by founder and CEO ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Aldo Carrascoso"));
  const team = /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 420,
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      maxWidth: '55ch',
      textWrap: 'pretty'
    }
  }, "Alongside him is a team of operators, engineers, and researchers building at the frontier of edge autonomy. Our work is guided by senior advisors from the national security and intelligence communities, and backed by some of the world\u2019s leading institutional investors.");
  const pull = compact => /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 560,
    style: {
      margin: 0,
      paddingTop: compact ? 0 : 'clamp(14px, 1.6vw, 20px)',
      borderTop: compact ? 'none' : '1px solid var(--line-hairline)',
      fontSize: compact ? 'clamp(18px, 1.6vw, 23px)' : 'clamp(17px, 1.45vw, 21px)',
      fontWeight: 500,
      lineHeight: 1.45,
      letterSpacing: '-0.01em',
      color: 'var(--vv-ice-92)',
      maxWidth: '46ch',
      textWrap: 'balance'
    }
  }, "The people behind Vivum have built, scaled, and defended at the highest levels \u2014 and we are just getting started.");
  const prose = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(16px, 1.6vw, 22px)'
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Leadership",
    speed: 30,
    delay: 180
  })), heading, team, pull());
  const ledger = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(14px, 1.6vw, 20px)',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: AB_LBL
  }, "In brief"), /*#__PURE__*/React.createElement(AbLedgerRows, {
    rows: AB_LEDGER,
    mobile: mobile
  }));
  const grid = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : 'minmax(0, 1fr) minmax(0, clamp(280px, 30vw, 400px))',
      columnGap: 'clamp(36px, 5vw, 88px)',
      rowGap: 'clamp(34px, 8vw, 52px)',
      alignItems: 'start'
    }
  }, prose, /*#__PURE__*/React.createElement("div", {
    style: stacked ? null : {
      paddingLeft: 'clamp(24px, 3vw, 48px)',
      borderLeft: '1px solid var(--line-hairline)'
    }
  }, ledger));

  /* masthead — the section reads top-down instead of left-right: label rule, the founder line
     at full measure, then prose beside the pull-quote, and the ledger as a three-across footer
     strip on the frame's baseline. Same words, a masthead instead of a split. */
  const masthead = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(24px, 2.6vw, 38px)'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    delay: 140
  }, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Leadership",
    speed: 30,
    delay: 180
  }))), heading, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : 'minmax(0, 0.95fr) minmax(0, 1.05fr)',
      columnGap: 'clamp(36px, 5vw, 80px)',
      rowGap: 'clamp(22px, 5vw, 32px)',
      alignItems: 'start',
      paddingTop: 'clamp(4px, 1vw, 12px)'
    }
  }, team, /*#__PURE__*/React.createElement("div", {
    style: stacked ? null : {
      paddingLeft: 'clamp(24px, 3vw, 48px)',
      borderLeft: '1px solid var(--line-hairline)'
    }
  }, pull(true))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: mobile ? 'minmax(0, 1fr)' : `repeat(${AB_LEDGER.length}, minmax(0, 1fr))`,
      gap: mobile ? 0 : 'clamp(24px, 3vw, 56px)',
      marginTop: 'clamp(6px, 1vw, 14px)',
      paddingTop: 'clamp(20px, 2.4vw, 30px)',
      borderTop: '1px solid var(--line-hairline)'
    }
  }, AB_LEDGER.map(([k, v], i) => /*#__PURE__*/React.createElement(Reveal, {
    key: k,
    variant: "up",
    delay: 620 + i * 110,
    dur: 520,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 9,
      paddingTop: mobile && i ? 16 : 0,
      paddingBottom: mobile && i !== AB_LEDGER.length - 1 ? 16 : 0,
      borderTop: mobile && i ? '1px solid var(--line-hairline)' : 'none',
      paddingLeft: !mobile && i ? 'clamp(24px, 3vw, 56px)' : 0,
      borderLeft: !mobile && i ? '1px solid var(--line-hairline)' : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: AB_LBL
  }, k), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'clamp(15px, 1.2vw, 17px)',
      lineHeight: 1.45,
      color: 'var(--vv-ice-82)',
      textWrap: 'pretty'
    }
  }, v)))));
  const framed = variant === 'masthead' ? masthead : grid;
  return /*#__PURE__*/React.createElement(GradientFieldAb, {
    pad: mobile ? 'clamp(64px, 15vw, 96px) var(--gutter-site)' : 'clamp(40px, 4.2vw, 72px) var(--gutter-site)',
    id: "leadership"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, variant === 'plain' ? grid : /*#__PURE__*/React.createElement(Reveal, {
    variant: mobile ? 'up' : 'frame',
    dur: 680
  }, /*#__PURE__*/React.createElement(SectionFrame, _extends({
    stacked: true,
    stretch: true,
    copyMax: "100%",
    contentStyle: {
      maxWidth: '100%'
    },
    gap: 0,
    chamfer: mobile ? 28 : 44,
    chamferWide: 72,
    weight: 1,
    color: "var(--vv-ice-14)",
    accent: false,
    fill: "transparent"
  }, mobile ? {
    chamfer: 0,
    color: 'transparent',
    blur: 0,
    padX: '0px',
    padY: '0px'
  } : tablet ? {
    padX: 'clamp(48px, 6vw, 96px)',
    padY: 'clamp(48px, 6vw, 96px)'
  } : null), framed))));
}

/* ── Horizon ──────────────────────────────────────────────────────────────────────────────
   Same two-column shell as Principles: copy in the chamfered frame on the left, the two states
   the sentence names stacked on a hairline spine to its right — ice at Now, amber at Next. */
const AB_PULSE_KEYFRAMES = '@keyframes vvHorizonPulse{0%{transform:scale(1);box-shadow:0 0 0 0 rgba(217,142,55,0.55)}70%{transform:scale(1.15);box-shadow:0 0 0 10px rgba(217,142,55,0)}100%{transform:scale(1);box-shadow:0 0 0 0 rgba(217,142,55,0)}}';
function AbHorizonSpine({
  mobile
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      paddingLeft: mobile ? 26 : 'clamp(30px, 3.2vw, 54px)'
    }
  }, /*#__PURE__*/React.createElement("style", null, AB_PULSE_KEYFRAMES), /*#__PURE__*/React.createElement(Reveal, {
    variant: "fade",
    dur: 900,
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      left: 0,
      top: 6,
      bottom: 6,
      width: 1,
      background: 'linear-gradient(180deg, var(--vv-ice-24) 0%, var(--vv-ice-14) 55%, transparent 100%)'
    }
  }), AB_HORIZON.map((h, i) => /*#__PURE__*/React.createElement(Reveal, {
    key: h.k,
    variant: "side",
    delay: i * 150,
    dur: 560,
    style: {
      position: 'relative',
      display: 'flex',
      flexDirection: 'column',
      gap: 10,
      marginBottom: i === AB_HORIZON.length - 1 ? 0 : 'clamp(28px, 3.2vw, 44px)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      left: mobile ? -30 : 'calc(-1 * clamp(30px, 3.2vw, 54px) - 4px)',
      top: 6,
      width: 9,
      height: 9,
      borderRadius: '50%',
      background: i ? 'var(--vv-amber)' : 'var(--vv-graphite)',
      animation: i === 1 ? 'vvHorizonPulse 2.2s ease-out infinite' : 'none'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      ...AB_LBL,
      color: i ? 'var(--vv-amber)' : 'var(--vv-graphite)'
    }
  }, h.k), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: mobile ? 19 : 'clamp(20px, 1.8vw, 25px)',
      fontWeight: 600,
      letterSpacing: '-0.015em',
      lineHeight: 1.25,
      color: 'var(--vv-ice)',
      textTransform: 'capitalize'
    }
  }, h.title), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 15,
      lineHeight: 1.6,
      color: 'var(--text-secondary)',
      maxWidth: '44ch',
      textWrap: 'pretty'
    }
  }, h.note))));
}
function AbHorizon({
  variant
}) {
  const {
    Eyebrow,
    SectionFrame
  } = DS_AB();
  const mobile = useAbsMax(640);
  const stacked = useAbsMax(900);
  const tablet = useAbsMax(1024) && !mobile;
  const copy = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Where we are going",
    speed: 26,
    delay: 160
  })), /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 800,
    delay: 220,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)',
      textTransform: 'capitalize'
    }
  }, "From foundational autonomy to ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "coordinated intelligence")), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 360,
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      textWrap: 'pretty'
    }
  }, "We are moving from foundational autonomy toward distributed, coordinated intelligence across fleets and domains. Every deployment sharpens the platform, and every system in the field compounds its value. What we have built so far is the foundation \u2014 what comes next is bigger."), variant === 'quote' ? /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "text",
    dur: 780,
    delay: 520,
    style: {
      margin: 0,
      paddingTop: 'clamp(16px, 1.8vw, 24px)',
      borderTop: '1px solid var(--line-hairline)',
      fontSize: 'clamp(19px, 1.8vw, 25px)',
      fontWeight: 600,
      letterSpacing: '-0.02em',
      lineHeight: 1.3,
      color: 'var(--vv-ice)',
      maxWidth: '30ch'
    }
  }, "\u201CWhat we have built so far is the foundation. What comes next is bigger.\u201D") : null);
  return /*#__PURE__*/React.createElement(GradientFieldAb, {
    pad: mobile ? 'clamp(64px, 15vw, 96px) var(--gutter-site)' : 'clamp(40px, 4.2vw, 72px) var(--gutter-site)',
    id: "horizon"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : 'minmax(0, 0.62fr) minmax(0, 0.28fr)',
      columnGap: stacked ? 0 : 'clamp(56px, 7vw, 110px)',
      rowGap: 'clamp(36px, 5vw, 56px)',
      alignItems: 'stretch'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      alignSelf: 'center'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: mobile ? 'up' : 'frame',
    dur: 680
  }, /*#__PURE__*/React.createElement(SectionFrame, _extends({
    copyMax: 900,
    stacked: stacked,
    fill: "transparent"
  }, mobile ? {
    chamfer: 0,
    color: 'transparent',
    blur: 0,
    accent: false,
    padX: '0px',
    padY: '0px'
  } : null), copy))), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: stacked ? '100%' : 420,
      width: '100%',
      boxSizing: 'border-box',
      justifySelf: 'start',
      alignSelf: 'center',
      paddingLeft: stacked ? 0 : 'clamp(0.5rem, 1vw, 1rem)'
    }
  }, /*#__PURE__*/React.createElement(AbHorizonSpine, {
    mobile: mobile
  })))));
}

/* Every About section paints on the shared canvas with no band of its own — the design system's
   no-hard-cuts rule. One wrapper so the three sections cannot drift apart. */
function GradientFieldAb({
  pad,
  id,
  children
}) {
  const {
    GradientField
  } = DS_AB();
  return /*#__PURE__*/React.createElement(GradientField, {
    field: "none",
    pad: pad,
    as: "section",
    id: id
  }, children);
}
Object.assign(window, {
  AbPrinciples,
  AbLeadership,
  AbHorizon,
  AB_LBL,
  AB_PRINCIPLES,
  AB_LEDGER,
  AB_HORIZON
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/AboutSections.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/AutonomyDomains.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The Autonomy page's centrepiece, rebuilt as an INSTRUMENT rather than a content grid.

   The four domains are the same five capabilities restated per medium. Read as four blocks of
   prose that repetition looks like padding. Read as one console whose scene, telemetry and
   capability rail retune when you select a domain, the repetition IS the argument: one
   architecture, any medium. So the section is a single chamfered console — domain rail, live
   scene with the domain's identity and telemetry overlaid, and a five-cell capability
   selector under it.

   Three layouts ship, switchable in Tweaks:
     · console (default) — one large instrument, one domain at a time. The recommendation.
     · matrix  — all four domains as live tiles at once, for comparison at a glance.
     · ledger  — no scene at all: dense typographic HUD, everything visible, print-friendly.

   Deep-linkable (#air/#land/#sea/#space) and keyboard-navigable in every layout. */

const DS_AD = () => window.VivumAIDesignSystem_b2be15;

/* One keyframe pair the instrument needs and CSS-in-style cannot express. Injected once. */
if (typeof document !== 'undefined') {
  const s = document.getElementById('ad-kf') || document.createElement('style');
  s.id = 'ad-kf';
  s.textContent = ['@keyframes adCapIn{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}', '@keyframes adHudIn{0%{opacity:0;transform:translateY(8px) scale(.97)}12%{opacity:1}86%{opacity:1;transform:none}100%{opacity:0;transform:translateY(-6px)}}', '@keyframes adHudRise{from{opacity:0;transform:translateY(9px)}to{opacity:1;transform:none}}', '@keyframes adHudFall{from{opacity:1;transform:none}to{opacity:0;transform:translateY(-7px)}}', '@keyframes adHudWipeL{from{clip-path:inset(0 100% 0 0)}to{clip-path:inset(0 0 0 0)}}', '@keyframes adHudWipeR{from{clip-path:inset(0 0 0 100%)}to{clip-path:inset(0 0 0 0)}}', '@keyframes adHudWipeD{from{clip-path:inset(0 0 100% 0)}to{clip-path:inset(0 0 0 0)}}', '@keyframes adHudWipeU{from{clip-path:inset(100% 0 0 0)}to{clip-path:inset(0 0 0 0)}}', '@keyframes adHudDot{from{opacity:0;transform:scale(.4)}to{opacity:1;transform:none}}', '@keyframes adScrimIn{from{opacity:0}to{opacity:1}}', '@keyframes adTickIn{from{transform:scaleX(0)}to{transform:scaleX(1)}}', '@keyframes adSlipIn{from{opacity:0;transform:translateY(7px)}to{opacity:1;transform:none}}', '@keyframes adSlipR{from{opacity:0;transform:translateX(9px)}to{opacity:1;transform:none}}', '@keyframes adHudRule{0%{transform:scaleX(0)}18%{transform:scaleX(1)}88%{transform:scaleX(1)}100%{transform:scaleX(0)}}', '@keyframes adHudPing{0%{transform:scale(.4);opacity:.9}70%{transform:scale(2.4);opacity:0}100%{opacity:0}}', '@media (prefers-reduced-motion:reduce){[style*="adCapIn"],[style*="adHud"]{animation:none!important}}'].join('');
  if (!s.parentNode) document.head.appendChild(s);
}
const AUT_DOMAINS = [{
  id: 'air',
  label: 'Air',
  vehicle: 'Unmanned Aerial Vehicles',
  code: 'UAVs',
  image: '../../assets/plates/core-tech-adaptive-learning.png',
  intro: 'Autonomous takeoff, landing, loitering, and emergency response — no GPS required, and it outperforms standard flight controllers.',
  tel: [['Mode', 'Autonomous'], ['Power consumption', 'Ultra-low'], ['Compute', 'On-device']],
  caps: [['Start & Stop', 'Autonomous takeoff, landing, and mission initiation from a single command input, with navigation that does not depend on external communications.', 'bolt'], ['Adaptive Loitering', 'Precision hovering for fixed-position monitoring and mobile surveying in dynamic environments, for stable data acquisition and environmental analysis.', 'radar'], ['Graceful Termination', 'Emergency landing, automated docking, and fail-safe shutdown subroutines, activatable under critical conditions or system anomalies.', 'triangle-exclamation'], ['GPS-Denied Navigation', 'Adaptive navigation that processes whatever inputs remain, holding operational capability when GPS or primary sensors are compromised.', 'circle-nodes'], ['Extensible Solutions', 'Customizable autonomy frameworks supporting mission-specific requirements — sensor fusion, payload delivery, and more.', 'diagram-project']]
}, {
  id: 'land',
  label: 'Land',
  vehicle: 'Unmanned Terrestrial Vehicles',
  code: 'UTVs',
  image: '../../assets/plates/core-tech-temporal-awareness.png',
  intro: 'Fully autonomous navigation, monitoring, and emergency response at ultra-low power — outperforming standard controllers even in SWaP-constrained builds.',
  tel: [['Mode', 'Autonomous'], ['Power consumption', 'Ultra-low'], ['Compute', 'On-device']],
  caps: [['One-Touch Autonomy', 'Initiate or terminate navigation with a single button press or mode switch, independent of external communications.', 'bolt'], ['Environment Monitoring', 'Stationary monitoring and mobile surveying in changing environments — observation, image and video capture, and more.', 'radar'], ['Graceful Termination', 'Emergency stop, automated docking, and fail-safe shutdown subroutines, activatable under critical conditions or system anomalies.', 'triangle-exclamation'], ['GPS-Denied Navigation', 'Optical and landmark-based navigation systems for operation in GPS- or sensor-denied environments.', 'circle-nodes'], ['Extensible Solutions', 'Customizable autonomy frameworks supporting mission-specific requirements — sensor fusion, payload delivery, and more.', 'diagram-project']]
}, {
  id: 'sea',
  label: 'Sea',
  vehicle: 'Unmanned Maritime Vehicles',
  code: 'UMVs',
  image: '../../assets/plates/core-tech-contextual-understanding.png',
  intro: 'Fully autonomous navigation, undersea monitoring, and adaptive surveying — no human intervention, and it outperforms traditional controllers in GPS-denied water.',
  tel: [['Mode', 'Autonomous'], ['Power consumption', 'Ultra-low'], ['Compute', 'On-device']],
  caps: [['One-Touch Autonomy', 'Initiate or terminate navigation with a single button press or mode switch, independent of external communications.', 'bolt'], ['Undersea Monitoring', 'Stationary and mobile surveying in changing marine environments — observation, image and video capture, and data collection.', 'radar'], ['Emergency Protocols', 'Robust subroutines for emergency surfacing and recovery, activatable under critical conditions or system anomalies.', 'triangle-exclamation'], ['GPS-Denied Navigation', 'Optical, inertial, and landmark-based navigation systems for operation in GPS- or sensor-denied environments.', 'circle-nodes'], ['Extensible Solutions', 'Customizable autonomy frameworks supporting mission-specific requirements — sensor fusion, payload delivery, and more.', 'diagram-project']]
}, {
  id: 'space',
  label: 'Space',
  vehicle: 'Unmanned Extra-Terrestrial Vehicles',
  code: 'UEVs',
  image: '../../assets/plates/core-tech-efficient-computation.png',
  intro: 'Autonomous navigation and monitoring that adapts to novel space environments, runs independent of ground control, and keeps working when positioning systems fail.',
  tel: [['Mode', 'Autonomous'], ['Power consumption', 'Ultra-low'], ['Compute', 'On-device']],
  caps: [['Start & Stop', 'Navigation at the push of a button or the switch of a mode, independent of external communications.', 'bolt'], ['Monitor & Survey', 'Fixed-position monitoring and mobile surveying in dynamic environments — observation, image and video capture, and more.', 'radar'], ['Emergency Start & Stop', 'A range of emergency starting and stopping subroutines, engaged the moment conditions turn critical.', 'triangle-exclamation'], ['GPS-Denied Navigation', 'Optical and landmark-based navigation for the environments where satellite positioning simply is not available.', 'circle-nodes'], ['Extensible Solutions', 'Customizable autonomy frameworks built around what a programme needs — including what it has not asked for yet.', 'diagram-project']]
}];
const AD_IDS = AUT_DOMAINS.map(d => d.id);
function useAdMax(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}

/* Domain rail cell. The active mark is a 3px amber bar on the top edge — the brand's label
   rule, not a pill. Amber appears once in the rail, so the accent still points at one thing. */
function AdTab({
  d,
  active,
  mobile,
  onSelect,
  onKeyDown,
  tabRef
}) {
  const [hot, setHot] = React.useState(false);
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    role: "tab",
    id: `aut-tab-${d.id}`,
    "aria-selected": active ? 'true' : 'false',
    "aria-controls": `aut-panel-${d.id}`,
    tabIndex: active ? 0 : -1,
    ref: tabRef,
    onClick: () => onSelect(d.id),
    onKeyDown: onKeyDown,
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      position: 'relative',
      appearance: 'none',
      border: 0,
      margin: 0,
      cursor: 'pointer',
      font: 'inherit',
      textAlign: 'left',
      minHeight: 62,
      padding: mobile ? '18px 16px' : '20px 22px',
      display: 'flex',
      flexDirection: 'column',
      gap: 7,
      background: active ? 'rgba(218,232,242,0.05)' : hot ? 'rgba(218,232,242,0.03)' : 'var(--surface-canvas)',
      transition: 'background 160ms ease'
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      top: 0,
      height: 3,
      background: active ? 'var(--vv-amber)' : 'transparent',
      transition: 'background 220ms cubic-bezier(.22,.61,.36,1)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: mobile ? 15 : 17,
      fontWeight: 600,
      textTransform: 'uppercase',
      lineHeight: 1,
      color: active ? 'var(--vv-ice)' : hot ? 'var(--vv-ice-82)' : 'var(--vv-ice-56)',
      transition: 'color 160ms ease'
    }
  }, d.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      letterSpacing: '0.09em',
      textTransform: 'uppercase',
      lineHeight: 1,
      color: active ? 'var(--vv-amber)' : 'var(--vv-graphite)',
      transition: 'color 160ms ease'
    }
  }, d.code));
}
const AdHeading = ({
  d,
  mobile
}) => /*#__PURE__*/React.createElement(React.Fragment, null, "Unmanned ", /*#__PURE__*/React.createElement("span", {
  style: {
    color: 'var(--vv-amber)'
  }
}, d.vehicle.replace('Unmanned ', '')), " ", /*#__PURE__*/React.createElement("span", {
  style: {
    fontFamily: 'var(--font-mono)',
    fontWeight: 400,
    letterSpacing: '0.06em',
    color: 'var(--vv-graphite)',
    whiteSpace: 'nowrap'
  }
}, "(", d.code, ")"));

/* Telemetry strip. Every readout is a claim the copy already makes — no invented numbers, so
   the HUD stays honest instead of decorating itself with fake data. Readouts arrive one after
   another from the right, like a strip settling after a channel change. */
function AdTelemetry({
  d,
  compact
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: compact ? '6px 14px' : '8px 22px',
      alignItems: 'baseline'
    }
  }, d.tel.map(([k, v], i) => /*#__PURE__*/React.createElement("span", {
    key: k,
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 7,
      fontFamily: 'var(--font-mono)',
      fontSize: compact ? 10 : 11,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      lineHeight: 1
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-graphite)'
    }
  }, k), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, v))));
}

/* The five capabilities, all visible, re-keyed on every domain change so they re-reveal as
   the swarm re-forms. Three treatments, switchable in Tweaks — the trade is density against
   scannability, and which one wins depends on how much of the page this section should own.

   · cards    — five columns in a hairline grid. Most scannable, widest.
   · rows     — five full-width rows, icon | title | body. Densest prose, easiest to read.
   · numbered — five columns led by a large mono index, no icons. Quietest, most typographic. */
function AdCaps({
  d,
  style = 'cards',
  mobile
}) {
  const {
    Icon
  } = DS_AD();
  const wrap = {
    display: 'flex',
    flexDirection: 'column'
  };
  if (style === 'rows') {
    return /*#__PURE__*/React.createElement("div", {
      key: d.id,
      style: wrap
    }, d.caps.map((c, i) => /*#__PURE__*/React.createElement(Reveal, {
      key: c[0],
      variant: "up",
      delay: i * 80,
      dur: 460,
      style: {
        display: 'grid',
        gridTemplateColumns: mobile ? 'auto minmax(0, 1fr)' : 'auto minmax(0, 0.9fr) minmax(0, 1.5fr)',
        columnGap: mobile ? 14 : 'clamp(20px, 2.2vw, 40px)',
        rowGap: 8,
        alignItems: mobile ? 'start' : 'baseline',
        padding: mobile ? '18px 0' : 'clamp(18px, 1.9vw, 24px) 0',
        borderTop: i ? '1px solid var(--line-hairline)' : 'none'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 10,
        gridRow: mobile ? 'span 2' : undefined
      }
    }, /*#__PURE__*/React.createElement(Icon, {
      name: c[2],
      size: mobile ? 22 : 26,
      color: "var(--vv-ice)"
    })), /*#__PURE__*/React.createElement("h4", {
      style: {
        margin: 0,
        fontSize: mobile ? 15.5 : 17,
        fontWeight: 600,
        letterSpacing: '-0.008em',
        lineHeight: 1.3,
        color: 'var(--vv-ice)',
        textWrap: 'balance'
      }
    }, c[0]), /*#__PURE__*/React.createElement("p", {
      style: {
        margin: 0,
        fontSize: 14.5,
        lineHeight: 1.62,
        color: 'var(--text-secondary)',
        textWrap: 'pretty'
      }
    }, c[1]))));
  }
  if (style === 'numbered') {
    return /*#__PURE__*/React.createElement("div", {
      key: d.id,
      style: {
        display: 'grid',
        gridTemplateColumns: mobile ? 'minmax(0, 1fr)' : 'repeat(auto-fit, minmax(190px, 1fr))',
        gap: 'clamp(24px, 2.6vw, 40px)'
      }
    }, d.caps.map((c, i) => /*#__PURE__*/React.createElement(Reveal, {
      key: c[0],
      variant: "up",
      delay: i * 80,
      dur: 460,
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 10,
        paddingTop: 16,
        borderTop: '1px solid var(--line-hairline)'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: 'var(--font-mono)',
        fontSize: 26,
        fontWeight: 400,
        letterSpacing: '0.02em',
        lineHeight: 1,
        color: 'var(--vv-amber)'
      }
    }, `0${i + 1}`), /*#__PURE__*/React.createElement("h4", {
      style: {
        margin: 0,
        fontSize: 15.5,
        fontWeight: 600,
        letterSpacing: '-0.005em',
        lineHeight: 1.3,
        color: 'var(--vv-ice)',
        textWrap: 'balance'
      }
    }, c[0]), /*#__PURE__*/React.createElement("p", {
      style: {
        margin: 0,
        fontSize: 14,
        lineHeight: 1.6,
        color: 'var(--text-secondary)',
        textWrap: 'pretty'
      }
    }, c[1]))));
  }
  return /*#__PURE__*/React.createElement("div", {
    key: d.id,
    style: {
      display: 'grid',
      gridTemplateColumns: mobile ? 'minmax(0, 1fr)' : 'repeat(auto-fit, minmax(180px, 1fr))',
      gap: 1,
      background: 'var(--line-hairline)',
      border: '1px solid var(--line-hairline)'
    }
  }, d.caps.map((c, i) => /*#__PURE__*/React.createElement(Reveal, {
    key: c[0],
    variant: "up",
    delay: i * 80,
    dur: 460,
    style: {
      background: 'var(--surface-canvas)',
      padding: mobile ? '20px 18px' : 'clamp(20px, 2vw, 26px)',
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: c[2],
    size: mobile ? 22 : 26,
    color: "var(--vv-ice)"
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 10.5,
      letterSpacing: '0.1em',
      color: 'var(--vv-graphite)'
    }
  }, `0${i + 1}`)), /*#__PURE__*/React.createElement("h4", {
    style: {
      margin: 0,
      fontSize: mobile ? 15 : 15.5,
      fontWeight: 600,
      letterSpacing: '-0.005em',
      lineHeight: 1.3,
      color: 'var(--vv-ice)',
      textWrap: 'balance'
    }
  }, c[0]), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 14,
      lineHeight: 1.6,
      color: 'var(--text-secondary)',
      textWrap: 'pretty'
    }
  }, c[1]))));
}

/* Capabilities, read out INSIDE the instrument: one at a time, each landing on a free anchor in
   the canvas's middle band — never the top strip (label + telemetry) or the bottom block (heading
   + intro), so nothing ever collides. A reticle pings at the anchor and a leader line runs from it
   to one of the amber contacts in the scene, so the readout is annotating a real particle rather
   than floating over the picture. Title scrambles in, description follows, then it clears and the
   next capability acquires somewhere else.

   Purely presentational: the same five capabilities are always in the DOM as a real list for
   assistive tech and for anyone who would rather just read them. */
/* Two anchor bands. Side-linked anchors sit INBOARD, not at the frame edges: the plate is ~40%
   of the canvas wide and extends away from the link edge, so an anchor at x=8 put the plate
   squarely over the field and left an 8%-wide sliver for the leader — every candidate contact's
   run then had to cross the plate and strike through its own body text. Anchored near the
   centre, the plate covers one half and the leader has the other half of the field to itself. */
const AD_ANCHORS = [{
  x: 44,
  y: 24
}, {
  x: 57,
  y: 29
}, {
  x: 42,
  y: 34
}, {
  x: 59,
  y: 37
}, {
  x: 46,
  y: 30
}, {
  x: 55,
  y: 40
}];
const AD_ANCHORS_M = [{
  x: 40,
  y: 22
}, {
  x: 61,
  y: 28
}, {
  x: 38,
  y: 33
}, {
  x: 63,
  y: 38
}, {
  x: 42,
  y: 26
}, {
  x: 59,
  y: 36
}];
const AD_ANCHORS_C = [{
  x: 32,
  y: 22
}, {
  x: 68,
  y: 25
}, {
  x: 50,
  y: 20
}, {
  x: 38,
  y: 34
}, {
  x: 64,
  y: 32
}, {
  x: 52,
  y: 37
}];
const AD_ANCHORS_CM = [{
  x: 48,
  y: 21
}, {
  x: 54,
  y: 27
}, {
  x: 46,
  y: 33
}, {
  x: 52,
  y: 37
}, {
  x: 50,
  y: 24
}, {
  x: 48,
  y: 35
}];

/* The leader line, as its own component with its own clock. Two reasons: it must track a moving
   particle continuously (the parent used to re-render the whole HUD at 10fps to do this, which is
   both jerkier and far more expensive), and it owns the draw/retract animation that gates the
   callout. Progress runs particle → anchor, so the line grows OUT of the contact. */
function AdLeader({
  ax,
  ay,
  hotRef,
  ix,
  fallback,
  phase,
  draw = 620,
  pull = 420
}) {
  const [, force] = React.useState(0);
  const p = React.useRef(0);
  const idle = phase === 'wait';
  React.useEffect(() => {
    if (idle) {
      p.current = 0;
      return;
    }
    if (window.vvReduced && window.vvReduced()) {
      p.current = phase === 'pull' ? 0 : 1;
      const id = setInterval(() => force(v => v + 1), 250);
      return () => clearInterval(id);
    }
    const from = p.current;
    const to = phase === 'pull' ? 0 : 1;
    const dur = phase === 'draw' ? draw : phase === 'pull' ? pull : 0;
    const t0 = performance.now();
    /* An interval, deliberately, not requestAnimationFrame: rAF is starved in this environment
       (it delivers one frame and then stops), which pinned progress at zero and left the leader
       a dot sitting on the contact. The clock is also read inside the tick rather than taken
       from a frame timestamp, whose time origin does not always match performance.now() here. */
    const anim = phase === 'draw' || phase === 'pull';
    const id = setInterval(() => {
      const k = dur ? Math.min(1, Math.max(0, (performance.now() - t0) / dur)) : 1;
      p.current = from + (to - from) * (1 - Math.pow(1 - k, 3));
      force(v => v + 1);
      /* Keep ticking through the static phases: the contact itself is still moving. */
      if (k >= 1 && phase !== 'show' && phase !== 'hide') clearInterval(id);
    }, anim ? 16 : 60);
    return () => clearInterval(id);
  }, [phase, draw, pull, idle]);
  if (idle) return null;
  const pts = hotRef && hotRef.current || [];
  const t = ix >= 0 && pts[ix] || fallback;
  const k = p.current;
  const x = t.x + (ax - t.x) * k,
    y = t.y + (ay - t.y) * k;
  return (
    /*#__PURE__*/
    /* z-index 3 keeps the leader above the plate (2) and the readability scrim: the line is the
       thing that proves the callout is attached to something, so nothing may cover it. */
    React.createElement("div", {
      style: {
        position: 'absolute',
        inset: 0,
        zIndex: 3,
        pointerEvents: 'none'
      }
    }, /*#__PURE__*/React.createElement("svg", {
      viewBox: "0 0 100 100",
      preserveAspectRatio: "none",
      style: {
        position: 'absolute',
        inset: 0,
        width: '100%',
        height: '100%',
        overflow: 'visible'
      }
    }, /*#__PURE__*/React.createElement("line", {
      x1: t.x,
      y1: t.y,
      x2: x,
      y2: y,
      stroke: "var(--vv-amber)",
      strokeWidth: "1",
      strokeDasharray: "3 3",
      opacity: 0.45 + 0.35 * k,
      vectorEffect: "non-scaling-stroke"
    })), /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'absolute',
        left: `${t.x}%`,
        top: `${t.y}%`,
        width: 8,
        height: 8,
        marginLeft: -4,
        marginTop: -4,
        border: '1px solid var(--vv-amber)',
        borderRadius: '50%',
        opacity: 0.4 + 0.5 * k
      }
    }))
  );
}

/* Phase machine per capability: the line draws out of the particle, THEN the plate opens; on the
   way out the plate closes first and only then does the line withdraw. So a visible callout is
   never without its leader, and the two never animate against each other. */
/* Phase machine per capability: the line draws out of the particle, THEN the plate opens; on the
   way out the plate closes first and only then does the line withdraw. So a visible callout is
   never without its leader, and the two never animate against each other. A manual step routes
   through the SAME retirement — hide, pull, then a full redraw — so nothing is ever skipped.
   'wait' is the armed-but-empty phase: it holds the canvas clear after a tab change. */
const AD_DRAW = 620,
  AD_HIDE = 380,
  AD_PULL = 420,
  AD_ARM = 2000;
const adMod = (v, n) => (v % n + n) % n;

/* Manual transport. Sits on the canvas floor, right-hand side — hairline squares in the page's
   own button idiom, 44px on touch. Stepping restarts the phase machine, so a click always plays
   the full draw → open sequence rather than snapping. */
function AdStepBtn({
  dir,
  onClick,
  mobile
}) {
  const {
    Icon
  } = DS_AD();
  const [hot, setHot] = React.useState(false);
  const s = mobile ? 44 : 32;
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    "aria-label": dir < 0 ? 'Previous capability' : 'Next capability',
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    onFocus: () => setHot(true),
    onBlur: () => setHot(false),
    style: {
      appearance: 'none',
      width: s,
      height: s,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: 0,
      cursor: 'pointer',
      background: hot ? 'rgba(0,17,33,0.86)' : 'rgba(0,17,33,0.62)',
      border: '1px solid ' + (hot ? 'var(--vv-amber)' : 'var(--vv-ice-24)'),
      backdropFilter: 'blur(6px)',
      WebkitBackdropFilter: 'blur(6px)',
      transition: 'background 200ms ease, border-color 200ms ease'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: dir < 0 ? 'angle-left' : 'angle-right',
    size: mobile ? 20 : 16,
    color: hot ? 'var(--vv-amber)' : 'var(--vv-ice-82)'
  }));
}
function AdCapHud({
  d,
  mobile,
  hotRef,
  link = 'auto',
  dwell = 9200
}) {
  /* Mobile: force the pinned 'top' side rather than letting the aim math pick left/right — a
     side-hung plate has nowhere to go on a narrow canvas and ends up clipped. 'top' keeps the
     plate directly below its anchor (so the amber dot always reads as sitting on top of the
     plate) and switches to the AD_ANCHORS_CM set, whose x values are already centred and clear
     of the canvas edges. */
  link = mobile ? 'top' : link;
  const [step, setStep] = React.useState(0);
  const [gen, setGen] = React.useState(0);
  const [phase, setPhase] = React.useState('wait');
  const [shift, setShift] = React.useState(0);
  const pending = React.useRef(0);
  const armed = React.useRef(false);
  const root = React.useRef(null);
  const box = React.useRef(null);
  const aimIx = React.useRef(-1);
  const aimStep = React.useRef(-1);
  const aimFix = React.useRef(0);
  const plateH = React.useRef(0);
  React.useEffect(() => {
    if (window.vvReduced && window.vvReduced()) {
      setPhase('show');
      return;
    }
    const t = [];
    const dir = pending.current;
    if (dir) {
      /* Manual step: retire the visible callout properly before drawing the next one. */
      pending.current = 0;
      setPhase('hide');
      t.push(setTimeout(() => setPhase('pull'), AD_HIDE));
      t.push(setTimeout(() => setStep(v => v + dir), AD_HIDE + AD_PULL));
    } else {
      /* First readout after a tab change waits, so the new scene is read before it is annotated. */
      const hold = armed.current ? 0 : AD_ARM;
      armed.current = true;
      setPhase('wait');
      t.push(setTimeout(() => setPhase('draw'), hold));
      t.push(setTimeout(() => setPhase('show'), hold + AD_DRAW));
      t.push(setTimeout(() => setPhase('hide'), hold + AD_DRAW + dwell));
      t.push(setTimeout(() => setPhase('pull'), hold + AD_DRAW + dwell + AD_HIDE));
      t.push(setTimeout(() => setStep(v => v + 1), hold + AD_DRAW + dwell + AD_HIDE + AD_PULL));
    }
    return () => t.forEach(clearTimeout);
  }, [step, gen, dwell]);
  const jump = dir => {
    /* Nothing on screen yet — no retirement to play, so re-arm straight onto the new index. */
    if (phase === 'wait' || phase === 'pull') {
      setStep(v => v + dir);
      return;
    }
    pending.current = dir;
    setGen(g => g + 1);
  };
  const n = d.caps.length;
  const i = adMod(step, n);
  const c = d.caps[i];
  const centred = link === 'top' || link === 'bottom';
  const set = centred ? mobile ? AD_ANCHORS_CM : AD_ANCHORS_C : mobile ? AD_ANCHORS_M : AD_ANCHORS;
  /* Anchor is a pure function of step — no ref written during render. Stride 5 is coprime with
     the 6 anchors, so consecutive steps can never land on the same slot, and a re-render inside
     a step always recomputes the SAME anchor instead of jumping. */
  const a = set[adMod(step * 5 + 2, set.length)];
  const open = phase === 'show' || phase === 'hide';
  const out = phase === 'hide';
  const gap = 14;

  /* THE CONTACT IS CHOSEN FIRST, AND THE PLATE IS THEN PUT ON THE OTHER SIDE OF THE ANCHOR.
     Previously the side came from the anchor's x before the contact was known, so the plate could
     land on the same side as the only reachable contact and the leader had to cross it — a
     crossing penalty cannot help when every candidate is penalised. Deriving the side from the
     contact makes crossing geometrically impossible instead of merely discouraged: the plate
     starts a gap BEYOND the anchor on the named side, so a contact on the opposite side lies
     entirely outside the plate's x-range and no part of the run can enter it.
      The pinned Tweaks values (left/right/top/bottom) keep the scored guard, since the user has
     fixed the side and a clear-side contact may genuinely not exist. */
  const contacts = hotRef && hotRef.current || [];
  const pinned = link !== 'auto';

  /* The plate's REAL footprint, in canvas percent — width from the plate's own fixed width
     against the measured canvas, height from the last measurement (the aim latches during 'draw',
     when the plate is not mounted). Hardcoded percentages had assumed a ~1500px canvas. */
  const rootW = root.current && root.current.offsetWidth || 660;
  const rootH = root.current && root.current.offsetHeight || 360;
  const pxW = Math.min(mobile ? 200 : 268, rootW * 0.58);
  const pxH = plateH.current || (mobile ? 152 : 136);
  const pw = pxW / rootW * 100,
    ph = pxH / rootH * 100;
  const gx = gap / rootW * 100,
    gy = gap / rootH * 100,
    sy = shift / rootH * 100;
  const rectFor = s => s === 'left' ? {
    x0: a.x + gx,
    x1: a.x + gx + pw,
    y0: a.y + sy - ph / 2,
    y1: a.y + sy + ph / 2
  } : s === 'right' ? {
    x0: a.x - gx - pw,
    x1: a.x - gx,
    y0: a.y + sy - ph / 2,
    y1: a.y + sy + ph / 2
  } : s === 'top' ? {
    x0: a.x - pw / 2,
    x1: a.x + pw / 2,
    y0: a.y + gy + sy,
    y1: a.y + gy + sy + ph
  } : {
    x0: a.x - pw / 2,
    x1: a.x + pw / 2,
    y0: a.y + sy - gy - ph,
    y1: a.y + sy - gy
  };

  /* Liang–Barsky clip of the anchor→contact segment against the plate rect: catches a run that
     passes THROUGH the plate, not only one that ends inside it. The margin stays under the
     anchor's own gap so the anchor never counts as inside, which would fail every candidate. */
  const crossesRect = (pt, rect) => {
    const dx = pt.x - a.x,
      dy = pt.y - a.y,
      m = 0.8;
    const p = [-dx, dx, -dy, dy];
    const q = [a.x - (rect.x0 - m), rect.x1 + m - a.x, a.y - (rect.y0 - m), rect.y1 + m - a.y];
    let t0 = 0,
      t1 = 1;
    for (let j = 0; j < 4; j++) {
      if (p[j] === 0) {
        if (q[j] < 0) return false;
        continue;
      }
      const r = q[j] / p[j];
      if (p[j] < 0) {
        if (r > t1) return false;
        if (r > t0) t0 = r;
      } else {
        if (r < t0) return false;
        if (r < t1) t1 = r;
      }
    }
    return true;
  };
  const pinnedRect = pinned ? rectFor(link) : null;
  const away = pinned ? link === 'right' ? 1 : link === 'left' ? -1 : 0 : 0;

  /* Re-aim on a new step, on a stale index, and whenever the latch holds NO contact but the scene
     now has some — the first render of a step precedes three.js's first frame, so the pool is
     empty then. In pinned mode also re-aim once the measured plate reveals the latched contact
     would be crossed; capped per step so it cannot re-aim every tick and read as a flicker. */
  const stale = pinned && aimIx.current >= 0 && contacts[aimIx.current] && crossesRect(contacts[aimIx.current], pinnedRect) && aimFix.current < 3;
  if (aimStep.current !== step || aimIx.current >= contacts.length || aimIx.current < 0 && contacts.length || stale) {
    if (aimStep.current !== step) aimFix.current = 0;else aimFix.current += 1;
    aimStep.current = step;
    let pick = -1,
      best = -Infinity,
      near = -1,
      nearD = 1e9;
    contacts.forEach((pt, k) => {
      const dx = pt.x - a.x,
        dy = pt.y - a.y;
      const dist = Math.sqrt(dx * dx + dy * dy);
      /* Deliberately loose: the amber pool is only a handful of particles, so a tight gate
         empties it and forces the fallback — the very failure this is here to prevent. */
      if (dist < 6 || pt.y < 6 || pt.y > 86) return;
      if (dist < nearD) {
        nearD = dist;
        near = k;
      }
      /* Distance band: long enough to read as a leader, short enough to stay in frame. */
      let score = 40 - Math.abs(dist - 26);
      score += Math.min(0, 24 - Math.abs(dy)) * 0.3;
      /* A clearly sideways run reads better than a near-vertical one, and it also gives the
         derived side an unambiguous direction to place the plate. */
      score += Math.min(12, Math.abs(dx)) * 0.4;
      if (pinned) {
        if (crossesRect(pt, pinnedRect)) score -= 1e6;
        if (away) score += dx * away * 0.5;
        if (link === 'top') score += dy * 0.5;else if (link === 'bottom') score -= dy * 0.5;
      }
      if (score > best) {
        best = score;
        pick = k;
      }
    });
    aimIx.current = pick >= 0 ? pick : near;
  }
  const aimPt = aimIx.current >= 0 ? contacts[aimIx.current] : null;
  /* Plate goes opposite the contact. 'left' means the leader meets the plate's left edge, i.e.
     the plate extends rightward — so a contact to the LEFT of the anchor gets side 'left'. */
  const side = pinned ? link : aimPt ? aimPt.x < a.x ? 'left' : 'right' : a.x < 50 ? 'left' : 'right';
  /* Last resort ONLY when the scene publishes no contacts at all (radar, plate, or a starved
     instrument): a virtual point on the clear side. With a live field this never runs. */
  const fallback = {
    x: Math.max(4, Math.min(96, a.x + (side === 'left' ? -11 : 11))),
    y: Math.min(70, a.y + 24)
  };

  /* Clearance is computed from transform-free numbers (offsetHeight + the un-animated heading
     block's offset) inside a layout effect, so it resolves before paint: lift the plate off the
     heading block, then push it back down if that drove it into the telemetry strip. */
  React.useLayoutEffect(() => {
    const b = box.current,
      r = root.current;
    if (!b || !r) {
      setShift(0);
      return;
    }
    const h3 = r.parentElement && r.parentElement.querySelector('h3');
    const block = h3 && h3.parentElement;
    if (!block) {
      setShift(0);
      return;
    }
    const rootH = r.offsetHeight,
      bh = b.offsetHeight;
    plateH.current = bh;
    const blockTop = block.getBoundingClientRect().top - r.getBoundingClientRect().top;
    const base = a.y / 100 * rootH + (side === 'top' ? gap : side === 'bottom' ? -gap - bh : -bh / 2);
    let s = 0;
    const over = base + bh - (blockTop - 10);
    if (over > 0) s -= over;
    const guard = 0.17 * rootH;
    if (base + s < guard) s += guard - (base + s);
    setShift(Math.round(s));
  }, [d.id, step, mobile, a.y, side, open]);

  /* Description waits for the title to finish scrambling, then rises — the plate opens, the
     title types itself, the body follows. It reserves its space from the start, so the plate
     never resizes mid-sequence. */
  const titleMs = Math.max(420, Math.min(820, c[0].length * 16 + 140));
  const pos = side === 'left' ? {
    left: `calc(${a.x}% + ${gap}px)`,
    top: `calc(${a.y}% + ${shift}px)`,
    transform: 'translateY(-50%)'
  } : side === 'right' ? {
    right: `calc(${100 - a.x}% + ${gap}px)`,
    top: `calc(${a.y}% + ${shift}px)`,
    transform: 'translateY(-50%)'
  } : side === 'top' ? {
    left: `${a.x}%`,
    top: `calc(${a.y}% + ${gap + shift}px)`,
    transform: 'translateX(-50%)'
  } : {
    left: `${a.x}%`,
    top: `calc(${a.y}% + ${shift - gap}px)`,
    transform: 'translate(-50%, -100%)'
  };
  const wipe = side === 'left' ? 'adHudWipeL' : side === 'right' ? 'adHudWipeR' : side === 'top' ? 'adHudWipeD' : 'adHudWipeU';
  /* Mobile: the canvas is short enough that an anchor-relative plate can land partly or wholly
     off the visible instrument. Forcing `link` to 'top' above keeps every plate pinned directly
     under its anchor and inside the canvas, so no separate positioning override is needed here. */

  return /*#__PURE__*/React.createElement("div", {
    ref: root,
    style: {
      position: 'absolute',
      inset: 0,
      pointerEvents: 'none',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement(AdLeader, {
    key: 'L' + step,
    ax: a.x,
    ay: a.y,
    hotRef: hotRef,
    ix: aimIx.current,
    fallback: fallback,
    phase: phase,
    draw: AD_DRAW,
    pull: AD_PULL
  }), open ? /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    key: d.id + step + 'r',
    style: {
      position: 'absolute',
      left: `${a.x}%`,
      top: `${a.y}%`,
      width: 9,
      height: 9,
      marginLeft: -4.5,
      marginTop: -4.5,
      animation: out ? 'adHudFall ' + AD_HIDE + 'ms ease both' : 'adHudDot 260ms cubic-bezier(.16,1,.3,1) both'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'var(--vv-amber)',
      border: '1px solid var(--vv-amber)',
      borderRadius: '50%'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      border: '1px solid var(--vv-amber)',
      borderRadius: '50%',
      animation: 'adHudPing 1800ms cubic-bezier(.16,1,.3,1) infinite'
    }
  })) : null, open ?
  /*#__PURE__*/
  /* Positioner carries the link-side offset; the child carries the entrance transform, so
     the two never fight over the same property. */
  React.createElement("div", {
    ref: box,
    "aria-hidden": "true",
    key: d.id + step,
    style: {
      position: 'absolute',
      zIndex: 6,
      width: mobile ? 200 : 268,
      maxWidth: '58%',
      ...pos
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      animation: out ? 'adHudFall ' + AD_HIDE + 'ms ease both' : 'adHudRise 420ms cubic-bezier(.16,1,.3,1) both'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 7,
      padding: mobile ? '12px 14px' : '14px 17px',
      background: 'rgba(0,17,33,0.74)',
      backdropFilter: 'blur(8px) saturate(1.15)',
      WebkitBackdropFilter: 'blur(8px) saturate(1.15)',
      border: '1px solid var(--vv-ice-14)',
      textAlign: 'left',
      animation: out ? 'none' : wipe + ' 520ms cubic-bezier(.16,1,.3,1) both'
    }
  }, /*#__PURE__*/React.createElement(HudText, {
    text: c[0],
    speed: 16,
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: mobile ? 11 : 12,
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      lineHeight: 1.25,
      color: 'var(--vv-amber)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: mobile ? 10 : 10.5,
      letterSpacing: '0.015em',
      lineHeight: 1.55,
      color: 'var(--vv-ice-82)',
      textWrap: 'pretty',
      animation: out ? 'none' : 'adHudRise 460ms cubic-bezier(.16,1,.3,1) both',
      animationDelay: out ? undefined : titleMs + 'ms'
    }
  }, c[1])))) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      right: mobile ? 14 : 26,
      bottom: mobile ? 16 : 26,
      display: 'flex',
      flexDirection: 'row',
      gap: mobile ? 8 : 6,
      pointerEvents: 'auto'
    }
  }, /*#__PURE__*/React.createElement(AdStepBtn, {
    dir: -1,
    mobile: mobile,
    onClick: () => jump(-1)
  }), /*#__PURE__*/React.createElement(AdStepBtn, {
    dir: 1,
    mobile: mobile,
    onClick: () => jump(1)
  })));
}
const AdCapsSR = ({
  d
}) => /*#__PURE__*/React.createElement("ul", {
  style: {
    position: 'absolute',
    width: 1,
    height: 1,
    overflow: 'hidden',
    clip: 'rect(0 0 0 0)',
    whiteSpace: 'nowrap',
    margin: 0,
    padding: 0
  }
}, d.caps.map(c => /*#__PURE__*/React.createElement("li", {
  key: c[0]
}, c[0], ". ", c[1])));

/* ── Domain navigation ───────────────────────────────────────────────────────────────────────
   Four ways to choose a domain. They differ in what they make cheap: the rail makes comparison
   cheap (all four legible at once), the carousel makes the active domain unmissable, the dial
   makes the set feel like one instrument, and the stack trades width for a quiet vertical index.

   · rail     — four cells in a hairline grid, amber bar on the active one.
   · carousel — three visible, active centred and full-strength, neighbours fading out, arrows.
   · dial     — an arc of ticks with a pointer that swings to the selection.
   · stack    — a vertical index, one row per domain, amber rule on the active row. */
function AdNavRail({
  active,
  mobile,
  onSelect,
  onKeyDown,
  refs,
  kind = 'notch'
}) {
  const [hot, setHot] = React.useState(null);
  const bracket = kind === 'bracket',
    seam = kind === 'seam';
  return /*#__PURE__*/React.createElement("div", {
    role: "tablist",
    "aria-label": "Operating domain",
    onKeyDown: onKeyDown,
    style: {
      display: 'grid',
      gridTemplateColumns: mobile ? 'repeat(2, minmax(0, 1fr))' : 'repeat(4, minmax(0, 1fr))',
      borderBottom: '1px solid var(--line-hairline)'
    }
  }, AUT_DOMAINS.map((x, k) => {
    const on = x.id === active,
      lit = hot === x.id;
    return /*#__PURE__*/React.createElement("button", {
      key: x.id,
      type: "button",
      role: "tab",
      id: `aut-tab-${x.id}`,
      "aria-selected": on ? 'true' : 'false',
      "aria-controls": `aut-panel-${x.id}`,
      tabIndex: on ? 0 : -1,
      ref: el => {
        refs.current[x.id] = el;
      },
      onClick: () => onSelect(x.id),
      onMouseEnter: () => setHot(x.id),
      onMouseLeave: () => setHot(null),
      style: {
        position: 'relative',
        appearance: 'none',
        border: 0,
        borderLeft: k && !(mobile && k === 2) ? '1px solid var(--line-hairline)' : 'none',
        borderTop: mobile && k >= 2 ? '1px solid var(--line-hairline)' : 'none',
        margin: 0,
        cursor: 'pointer',
        font: 'inherit',
        textAlign: 'left',
        minHeight: mobile ? 58 : 66,
        padding: mobile ? '16px 14px' : '18px 20px',
        display: 'flex',
        flexDirection: 'column',
        gap: 5,
        background: on && !bracket ? 'rgba(218,232,242,0.05)' : lit && !on ? 'rgba(218,232,242,0.035)' : 'transparent',
        transition: 'background 200ms ease'
      }
    }, seam ? null : /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: 'absolute',
        left: 0,
        right: 0,
        top: 0,
        height: 3,
        background: on ? 'var(--vv-amber)' : lit ? 'var(--vv-ice-24)' : 'transparent',
        transition: 'background 220ms cubic-bezier(.22,.61,.36,1)'
      }
    }), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: 'absolute',
        left: bracket ? '50%' : 0,
        right: bracket ? 'auto' : 0,
        width: bracket ? 2 : 'auto',
        marginLeft: bracket ? -1 : 0,
        bottom: -1,
        height: bracket ? 12 : 1,
        background: on ? bracket ? 'var(--vv-amber)' : 'var(--surface-canvas)' : 'transparent',
        transition: 'background 200ms ease',
        zIndex: 1
      }
    }), seam && on ? /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: 'absolute',
        left: 0,
        right: 0,
        bottom: -1,
        height: 3,
        background: 'var(--vv-amber)',
        zIndex: 2
      }
    }) : null, /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: mobile ? 15 : 17,
        fontWeight: 600,
        textTransform: 'uppercase',
        lineHeight: 1,
        color: on || lit ? 'var(--vv-ice)' : 'var(--vv-ice-82)',
        transition: 'color 180ms ease'
      }
    }, x.label), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: 'var(--font-mono)',
        fontSize: 11,
        letterSpacing: '0.09em',
        textTransform: 'uppercase',
        lineHeight: 1,
        color: on ? 'var(--vv-amber)' : lit ? 'var(--vv-ice-82)' : 'var(--vv-graphite)',
        transition: 'color 180ms ease'
      }
    }, x.code));
  }));
}
const AdNavNotch = p => /*#__PURE__*/React.createElement(AdNavRail, _extends({}, p, {
  kind: "notch"
}));
const AdNavBracket = p => /*#__PURE__*/React.createElement(AdNavRail, _extends({}, p, {
  kind: "bracket"
}));
const AdNavSeam = p => /*#__PURE__*/React.createElement(AdNavRail, _extends({}, p, {
  kind: "seam"
}));
function AdNavCarousel({
  active,
  mobile,
  onSelect,
  onKeyDown,
  refs
}) {
  const {
    Icon
  } = DS_AD();
  const i = AD_IDS.indexOf(active);
  const n = AUT_DOMAINS.length;
  const at = k => AUT_DOMAINS[(k + n * 2) % n];
  const step = dir => onSelect(AD_IDS[(i + dir + n) % n]);
  const cells = [at(i - 1), at(i), at(i + 1)];
  const Arrow = ({
    dir,
    name
  }) => /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => step(dir),
    "aria-label": dir < 0 ? 'Previous domain' : 'Next domain',
    style: {
      appearance: 'none',
      border: '1px solid var(--line-hairline)',
      background: 'var(--surface-canvas)',
      cursor: 'pointer',
      width: mobile ? 40 : 40,
      height: mobile ? 40 : 40,
      display: 'grid',
      placeItems: 'center',
      flex: 'none'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: name,
    size: 18,
    color: "var(--vv-ice-82)"
  }));
  return /*#__PURE__*/React.createElement("div", {
    role: "tablist",
    "aria-label": "Operating domain",
    onKeyDown: onKeyDown,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: mobile ? 10 : 16
    }
  }, /*#__PURE__*/React.createElement(Arrow, {
    dir: -1,
    name: "angle-left"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      flex: 1,
      minWidth: 0,
      display: 'grid',
      gridTemplateColumns: 'repeat(3, minmax(0, 1fr))',
      alignItems: 'center',
      maskImage: 'linear-gradient(to right, rgba(0,0,0,0) 0%, #000 24%, #000 76%, rgba(0,0,0,0) 100%)',
      WebkitMaskImage: 'linear-gradient(to right, rgba(0,0,0,0) 0%, #000 24%, #000 76%, rgba(0,0,0,0) 100%)'
    }
  }, cells.map((x, k) => {
    const on = k === 1;
    return /*#__PURE__*/React.createElement("button", {
      key: x.id + k,
      type: "button",
      role: "tab",
      "aria-selected": on ? 'true' : 'false',
      tabIndex: on ? 0 : -1,
      ref: on ? el => {
        refs.current[x.id] = el;
      } : undefined,
      onClick: () => on ? null : step(k === 0 ? -1 : 1),
      style: {
        appearance: 'none',
        border: 0,
        background: 'none',
        cursor: on ? 'default' : 'pointer',
        font: 'inherit',
        padding: mobile ? '14px 6px' : '14px 10px',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: 8,
        opacity: on ? 1 : 0.34,
        transition: 'opacity 320ms cubic-bezier(.16,1,.3,1)'
      }
    }, /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        width: on ? 34 : 18,
        height: 3,
        background: on ? 'var(--vv-amber)' : 'var(--vv-graphite)',
        transition: 'width 320ms cubic-bezier(.16,1,.3,1), background 240ms ease'
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: on ? mobile ? 19 : 24 : mobile ? 14 : 16,
        fontWeight: 600,
        textTransform: 'uppercase',
        lineHeight: 1,
        color: on ? 'var(--vv-ice)' : 'var(--vv-ice-56)',
        transition: 'font-size 320ms cubic-bezier(.16,1,.3,1), color 240ms ease',
        whiteSpace: 'nowrap'
      }
    }, x.label), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: 'var(--font-mono)',
        fontSize: on ? 11.5 : 10.5,
        letterSpacing: '0.1em',
        lineHeight: 1,
        color: on ? 'var(--vv-amber)' : 'var(--vv-graphite)',
        transition: 'color 240ms ease'
      }
    }, x.code));
  })), /*#__PURE__*/React.createElement(Arrow, {
    dir: 1,
    name: "angle-right"
  }));
}
function AdNavDial({
  active,
  mobile,
  onSelect,
  onKeyDown,
  refs
}) {
  const i = AD_IDS.indexOf(active);
  const n = AUT_DOMAINS.length;
  const W = 520,
    H = 132,
    cx = W / 2,
    cy = H + 22,
    R = 132;
  const a0 = -46,
    a1 = 46;
  const ang = k => a0 + (a1 - a0) * k / (n - 1);
  const pt = (deg, r) => [cx + Math.sin(deg * Math.PI / 180) * r, cy - Math.cos(deg * Math.PI / 180) * r];
  return /*#__PURE__*/React.createElement("div", {
    role: "tablist",
    "aria-label": "Operating domain",
    onKeyDown: onKeyDown,
    style: {
      display: 'flex',
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: `0 0 ${W} ${H}`,
    style: {
      width: '100%',
      maxWidth: mobile ? 340 : 520,
      height: 'auto',
      overflow: 'visible'
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: `M ${pt(a0, R)[0]} ${pt(a0, R)[1]} A ${R} ${R} 0 0 1 ${pt(a1, R)[0]} ${pt(a1, R)[1]}`,
    fill: "none",
    stroke: "var(--vv-ice-14)",
    strokeWidth: "1"
  }), AUT_DOMAINS.map((x, k) => {
    const on = k === i;
    const [ix, iy] = pt(ang(k), R - (on ? 15 : 9));
    const [ox, oy] = pt(ang(k), R);
    const [lx, ly] = pt(ang(k), R - 34);
    return /*#__PURE__*/React.createElement("g", {
      key: x.id,
      onClick: () => onSelect(x.id),
      style: {
        cursor: 'pointer'
      }
    }, /*#__PURE__*/React.createElement("line", {
      x1: ix,
      y1: iy,
      x2: ox,
      y2: oy,
      stroke: on ? 'var(--vv-amber)' : 'var(--vv-graphite)',
      strokeWidth: on ? 3 : 1,
      style: {
        transition: 'stroke 240ms ease'
      }
    }), /*#__PURE__*/React.createElement("text", {
      x: lx,
      y: ly,
      textAnchor: "middle",
      dominantBaseline: "middle",
      style: {
        fontSize: on ? 17 : 13,
        fontWeight: 600,
        textTransform: 'uppercase',
        fill: on ? 'var(--vv-ice)' : 'var(--vv-ice-56)',
        transition: 'fill 240ms ease, font-size 260ms cubic-bezier(.16,1,.3,1)'
      }
    }, x.label), /*#__PURE__*/React.createElement("text", {
      x: lx,
      y: ly + 17,
      textAnchor: "middle",
      dominantBaseline: "middle",
      style: {
        fontFamily: 'var(--font-mono)',
        fontSize: 10,
        letterSpacing: '0.1em',
        fill: on ? 'var(--vv-amber)' : 'var(--vv-graphite)',
        transition: 'fill 240ms ease'
      }
    }, x.code), /*#__PURE__*/React.createElement("rect", {
      x: lx - 42,
      y: ly - 20,
      width: "84",
      height: "46",
      fill: "transparent"
    }, /*#__PURE__*/React.createElement("title", null, x.label)));
  }), /*#__PURE__*/React.createElement("g", {
    style: {
      transform: `rotate(${ang(i)}deg)`,
      transformOrigin: `${cx}px ${cy}px`,
      transition: 'transform 620ms cubic-bezier(.16,1,.3,1)'
    }
  }, /*#__PURE__*/React.createElement("line", {
    x1: cx,
    y1: cy - R + 46,
    x2: cx,
    y2: cy - R + 6,
    stroke: "var(--vv-amber)",
    strokeWidth: "1.5"
  })), /*#__PURE__*/React.createElement("circle", {
    cx: cx,
    cy: cy,
    r: "3",
    fill: "var(--vv-amber)"
  }), AUT_DOMAINS.map((x, k) => /*#__PURE__*/React.createElement("rect", {
    key: 'h' + x.id,
    x: pt(ang(k), R - 34)[0] - 44,
    y: pt(ang(k), R - 34)[1] - 22,
    width: "88",
    height: "52",
    fill: "transparent",
    style: {
      cursor: 'pointer'
    },
    onClick: () => onSelect(x.id),
    ref: k === i ? el => {
      refs.current[x.id] = el;
    } : undefined
  }))));
}
function AdNavStack({
  active,
  mobile,
  onSelect,
  onKeyDown,
  refs
}) {
  return /*#__PURE__*/React.createElement("div", {
    role: "tablist",
    "aria-label": "Operating domain",
    onKeyDown: onKeyDown,
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, AUT_DOMAINS.map((x, k) => {
    const on = x.id === active;
    return /*#__PURE__*/React.createElement("button", {
      key: x.id,
      type: "button",
      role: "tab",
      "aria-selected": on ? 'true' : 'false',
      tabIndex: on ? 0 : -1,
      ref: el => {
        refs.current[x.id] = el;
      },
      onClick: () => onSelect(x.id),
      style: {
        position: 'relative',
        appearance: 'none',
        border: 0,
        borderTop: k ? '1px solid var(--line-hairline)' : 'none',
        background: 'none',
        cursor: 'pointer',
        font: 'inherit',
        textAlign: 'left',
        padding: '16px 2px 16px 18px',
        display: 'flex',
        alignItems: 'baseline',
        justifyContent: 'space-between',
        gap: 12
      }
    }, /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: 'absolute',
        left: 0,
        top: 10,
        bottom: 10,
        width: 3,
        background: on ? 'var(--vv-amber)' : 'transparent',
        transition: 'background 240ms ease'
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: mobile ? 15 : 17,
        fontWeight: 600,
        textTransform: 'uppercase',
        lineHeight: 1,
        color: on ? 'var(--vv-ice)' : 'var(--vv-ice-56)',
        transition: 'color 200ms ease'
      }
    }, x.label), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: 'var(--font-mono)',
        fontSize: 11,
        letterSpacing: '0.1em',
        color: on ? 'var(--vv-amber)' : 'var(--vv-graphite)',
        transition: 'color 200ms ease'
      }
    }, x.code));
  }));
}
const AD_NAVS = {
  rail: AdNavNotch,
  bracket: AdNavBracket,
  seam: AdNavSeam,
  carousel: AdNavCarousel,
  dial: AdNavDial,
  stack: AdNavStack
};
/* Which navs sit flush ON the instrument (one border box, no gap) and which are free-standing. */
const AD_ATTACHED = {
  rail: 1,
  bracket: 1,
  seam: 1
};

/* Mobile: a native dropdown replaces whichever nav treatment is chosen for desktop. A four-way
   tab strip (or worse, a carousel/dial) is either cramped or unreadable at phone width, while a
   select is a pattern every touch user already knows, and it collapses the choice to one row. */
function AdNavSelect({
  active,
  onSelect
}) {
  const {
    Icon
  } = DS_AD();
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("label", {
    htmlFor: "aut-domain-select",
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: 'var(--vv-graphite)'
    }
  }, "Select a domain"), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative'
    }
  }, /*#__PURE__*/React.createElement("select", {
    id: "aut-domain-select",
    value: active,
    onChange: e => onSelect(e.target.value),
    style: {
      appearance: 'none',
      WebkitAppearance: 'none',
      width: '100%',
      borderTop: '1px solid var(--line-hairline)',
      borderLeft: '1px solid var(--line-hairline)',
      borderRight: '1px solid var(--line-hairline)',
      borderBottom: 'none',
      background: 'var(--surface-canvas)',
      color: 'var(--vv-ice)',
      fontSize: 15,
      fontWeight: 600,
      textTransform: 'uppercase',
      letterSpacing: '0.02em',
      fontFamily: 'inherit',
      padding: '16px 44px 16px 16px',
      cursor: 'pointer'
    }
  }, AUT_DOMAINS.map(x => /*#__PURE__*/React.createElement("option", {
    key: x.id,
    value: x.id
  }, x.label, " \u2014 ", x.code))), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      right: 16,
      top: '50%',
      transform: 'translateY(-50%)',
      pointerEvents: 'none',
      display: 'flex'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "angle-down",
    size: 14,
    color: "var(--vv-ice-82)"
  }))));
}

/* console — the recommendation. Header, domain nav, the live swarm carrying the domain's
   identity and telemetry, then all five capabilities. One domain at a time, one chamfered
   console — heading included, so the whole argument lives inside one frame. */
function AdConsole({
  d,
  scene,
  capStyle,
  capFont,
  capLink,
  corners,
  titleSize,
  particles,
  stacked,
  mobile,
  tablet,
  header,
  nav,
  navKind
}) {
  const effCorners = corners && !mobile;
  const {
    SectionFrame
  } = DS_AD();
  const hotRef = React.useRef([]);
  const side = navKind === 'stack' && !stacked;
  const attached = !!AD_ATTACHED[navKind] && !mobile;
  const mono = capFont === 'mono';
  const inCanvas = capStyle === 'hud';
  const tSize = titleSize || (mobile ? 18 : 22);
  const pSize = Math.max(12.5, tSize * 0.62);
  return /*#__PURE__*/React.createElement(Reveal, {
    variant: mobile ? 'up' : 'frame',
    dur: 720,
    delay: 120
  }, /*#__PURE__*/React.createElement(SectionFrame, _extends({
    stacked: stacked,
    copyMax: "100%",
    contentStyle: {
      maxWidth: '100%'
    },
    gap: 0,
    fill: "transparent"
  }, mobile ? {
    chamfer: 0,
    color: 'transparent',
    blur: 0,
    accent: false,
    padX: '0px',
    padY: '0px'
  } : tablet ? {
    padX: 'clamp(40px, 5vw, 80px)',
    padY: 'clamp(40px, 5vw, 80px)'
  } : null), header, /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 'clamp(26px, 3vw, 40px)',
      display: 'grid',
      gridTemplateColumns: side ? 'minmax(180px, 0.22fr) minmax(0, 1fr)' : 'minmax(0, 1fr)',
      columnGap: 'clamp(28px, 3vw, 52px)',
      rowGap: 0,
      alignItems: 'start',
      minHeight: '30rem'
    }
  }, attached ? null : nav, /*#__PURE__*/React.createElement("div", {
    role: "tabpanel",
    id: `aut-panel-${d.id}`,
    "aria-labelledby": `aut-tab-${d.id}`,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: inCanvas ? 0 : 'clamp(24px, 2.8vw, 36px)',
      minWidth: 0,
      minHeight: '30rem',
      border: attached ? '1px solid var(--line-hairline)' : 'none'
    }
  }, attached ? nav : null, /*#__PURE__*/React.createElement(DomainInstrument, {
    domainId: d.id,
    scene: scene,
    plate: d.image,
    key: scene,
    hotRef: hotRef,
    corners: effCorners,
    count: particles.count,
    glyph: particles.glyph,
    pSize: particles.size,
    extent: particles.extent,
    style: {
      height: mobile ? 'clamp(480px, 72svh, 620px)' : 'clamp(360px, 34vw, 500px)',
      minHeight: mobile ? undefined : '30rem',
      border: attached ? 'none' : '1px solid var(--line-hairline)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    key: d.id + 'scrim',
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      bottom: 0,
      height: '68%',
      pointerEvents: 'none',
      background: 'linear-gradient(to top, rgba(0,17,33,0.94) 0%, rgba(0,17,33,0.86) 18%, rgba(0,17,33,0.52) 48%, rgba(0,17,33,0) 100%)',
      maskImage: 'linear-gradient(to right, #000 0%, #000 54%, rgba(0,0,0,0) 92%)',
      WebkitMaskImage: 'linear-gradient(to right, #000 0%, #000 54%, rgba(0,0,0,0) 92%)',
      animation: 'adScrimIn 900ms ease both'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      padding: mobile ? '22px 18px' : 'clamp(28px, 3vw, 40px)',
      paddingTop: mobile ? 22 : 'clamp(18px, 2vw, 26px)',
      paddingBottom: mobile ? 84 : 'clamp(24px, 2.8vw, 32px)',
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'space-between',
      gap: 20,
      pointerEvents: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      alignItems: 'flex-start',
      justifyContent: 'space-between',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    key: d.id + 'tick',
    "aria-hidden": "true",
    style: {
      width: 30,
      height: 3,
      background: 'var(--vv-amber)',
      flex: 'none',
      transformOrigin: 'left',
      animation: 'adTickIn 520ms cubic-bezier(.16,1,.3,1) both'
    }
  }), /*#__PURE__*/React.createElement(HudText, {
    key: d.id,
    text: d.label,
    speed: 40,
    style: {
      fontSize: mobile ? 14 : 15,
      fontWeight: 600,
      letterSpacing: '0.15em',
      textTransform: 'uppercase',
      lineHeight: 1,
      color: 'var(--vv-ice)'
    }
  })), /*#__PURE__*/React.createElement(AdTelemetry, {
    key: d.id + 'tel',
    d: d,
    compact: mobile,
    animate: true
  })), /*#__PURE__*/React.createElement("div", {
    key: d.id + 'copy',
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10,
      maxWidth: mobile ? '100%' : '60ch'
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: mono ? 'var(--font-mono)' : undefined,
      fontSize: tSize,
      fontWeight: mono ? 500 : 700,
      letterSpacing: mono ? '0.01em' : '-0.018em',
      lineHeight: 1.22,
      color: 'var(--vv-ice)',
      textWrap: 'balance',
      animation: 'adSlipIn 560ms cubic-bezier(.16,1,.3,1) both',
      animationDelay: '120ms'
    }
  }, /*#__PURE__*/React.createElement(AdHeading, {
    d: d
  })), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: mono ? 'var(--font-mono)' : undefined,
      fontSize: pSize,
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      textWrap: 'pretty',
      animation: 'adSlipIn 560ms cubic-bezier(.16,1,.3,1) both',
      animationDelay: '260ms'
    }
  }, d.intro))), inCanvas ? /*#__PURE__*/React.createElement(AdCapHud, {
    key: d.id,
    d: d,
    mobile: mobile,
    hotRef: hotRef,
    link: capLink
  }) : null), inCanvas ? /*#__PURE__*/React.createElement(AdCapsSR, {
    d: d
  }) : null, inCanvas ? null : /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: attached ? 'clamp(24px, 2.8vw, 36px)' : 0
    }
  }, /*#__PURE__*/React.createElement(AdCaps, {
    d: d,
    style: capStyle,
    mobile: mobile || stacked
  }))))));
}

/* matrix — all four at once. Each tile runs the light 2D swarm (one WebGL context is a budget,
   four is a tax), and selecting a tile retunes the capabilities underneath. */
function AdMatrix({
  d,
  active,
  select,
  capStyle,
  stacked,
  mobile,
  tablet,
  header
}) {
  const {
    SectionFrame
  } = DS_AD();
  return /*#__PURE__*/React.createElement(Reveal, {
    variant: mobile ? 'up' : 'frame',
    dur: 720,
    delay: 120
  }, /*#__PURE__*/React.createElement(SectionFrame, _extends({
    stacked: stacked,
    copyMax: "100%",
    contentStyle: {
      maxWidth: '100%'
    },
    gap: 0,
    fill: "transparent"
  }, mobile ? {
    chamfer: 0,
    color: 'transparent',
    blur: 0,
    accent: false,
    padX: '0px',
    padY: '0px'
  } : tablet ? {
    padX: 'clamp(40px, 5vw, 80px)',
    padY: 'clamp(40px, 5vw, 80px)'
  } : null), header, /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 'clamp(26px, 3vw, 40px)',
      display: 'grid',
      gridTemplateColumns: mobile ? 'minmax(0, 1fr)' : 'repeat(2, minmax(0, 1fr))',
      gap: 1,
      background: 'var(--line-hairline)',
      border: '1px solid var(--line-hairline)',
      borderTopWidth: 1
    }
  }, AUT_DOMAINS.map(x => {
    const on = x.id === active;
    return /*#__PURE__*/React.createElement("button", {
      key: x.id,
      type: "button",
      onClick: () => select(x.id),
      "aria-pressed": on ? 'true' : 'false',
      style: {
        position: 'relative',
        appearance: 'none',
        border: 0,
        padding: 0,
        cursor: 'pointer',
        font: 'inherit',
        textAlign: 'left',
        background: 'var(--surface-canvas)',
        overflow: 'hidden'
      }
    }, /*#__PURE__*/React.createElement(DomainInstrument, {
      domainId: x.id,
      scene: "radar",
      plate: x.image,
      dense: true,
      live: on,
      corners: !mobile,
      style: {
        height: mobile ? 250 : 'clamp(240px, 23vw, 320px)'
      }
    }, /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: 'absolute',
        left: 0,
        right: 0,
        top: 0,
        height: 3,
        background: on ? 'var(--vv-amber)' : 'transparent',
        transition: 'background 220ms cubic-bezier(.22,.61,.36,1)'
      }
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        position: 'absolute',
        inset: 0,
        padding: mobile ? '20px 18px' : 'clamp(20px, 2vw, 28px)',
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'space-between',
        gap: 16
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 12
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 9
      }
    }, /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        width: 22,
        height: 3,
        background: on ? 'var(--vv-amber)' : 'var(--vv-graphite)',
        flex: 'none',
        transition: 'background 200ms ease'
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 14,
        fontWeight: 600,
        letterSpacing: '0.15em',
        textTransform: 'uppercase',
        lineHeight: 1,
        color: on ? 'var(--vv-ice)' : 'var(--vv-ice-56)',
        transition: 'color 200ms ease'
      }
    }, x.label)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: 'var(--font-mono)',
        fontSize: 10.5,
        letterSpacing: '0.1em',
        color: on ? 'var(--vv-amber)' : 'var(--vv-graphite)',
        transition: 'color 200ms ease'
      }
    }, x.code)), /*#__PURE__*/React.createElement("h3", {
      style: {
        margin: 0,
        fontSize: mobile ? 18 : 'clamp(18px, 1.5vw, 22px)',
        fontWeight: 700,
        letterSpacing: '-0.018em',
        lineHeight: 1.18,
        color: on ? 'var(--vv-ice)' : 'var(--vv-ice-82)',
        textWrap: 'balance',
        transition: 'color 200ms ease'
      }
    }, /*#__PURE__*/React.createElement(AdHeading, {
      d: x
    })))));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 'clamp(26px, 3vw, 38px)',
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(16px, 1.8vw, 22px)'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: mobile ? 15 : 'clamp(15.5px, 1.25vw, 17px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      textWrap: 'pretty',
      maxWidth: '78ch'
    }
  }, d.intro), /*#__PURE__*/React.createElement(AdCaps, {
    d: d,
    style: capStyle,
    mobile: mobile || stacked
  }))));
}

/* ledger — the quiet option: no scene, no interaction, everything on the page at once. This is
   the layout that survives a print, a screen reader, and a hostile network. */
function AdLedger({
  capStyle,
  mobile
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(40px, 5vw, 72px)'
    }
  }, AUT_DOMAINS.map((d, di) => /*#__PURE__*/React.createElement(Reveal, {
    key: d.id,
    variant: "up",
    dur: 620,
    delay: di * 90,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(18px, 2vw, 26px)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    id: d.id,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      paddingBottom: 'clamp(14px, 1.6vw, 20px)',
      borderBottom: '1px solid var(--line-hairline)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 14,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 30,
      height: 3,
      background: 'var(--vv-amber)',
      flex: 'none'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: mobile ? 14 : 15,
      fontWeight: 600,
      letterSpacing: '0.15em',
      textTransform: 'uppercase',
      lineHeight: 1,
      color: 'var(--vv-ice)'
    }
  }, d.label)), /*#__PURE__*/React.createElement(AdTelemetry, {
    d: d,
    compact: true
  })), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: mobile ? 21 : 'clamp(22px, 1.9vw, 28px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.18,
      color: 'var(--vv-ice)',
      textWrap: 'balance'
    }
  }, /*#__PURE__*/React.createElement(AdHeading, {
    d: d
  })), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: mobile ? 15 : 16.5,
      lineHeight: 1.62,
      color: 'var(--vv-ice-82)',
      textWrap: 'pretty',
      maxWidth: '76ch'
    }
  }, d.intro)), /*#__PURE__*/React.createElement(AdCaps, {
    d: d,
    style: capStyle,
    mobile: mobile
  }))));
}
function DomainSection({
  layout = 'console',
  scene = 'field',
  nav = 'rail',
  capStyle = 'hud',
  capFont = 'mono',
  capLink = 'auto',
  corners = true,
  titleSize = 15,
  particles
}) {
  const parts = Object.assign({
    count: 900,
    glyph: 'dot',
    size: 1,
    extent: 1
  }, particles);
  const {
    GradientField,
    Eyebrow
  } = DS_AD();
  const stacked = useAdMax(900);
  const mobile = useAdMax(640);
  const tablet = useAdMax(1024) && !mobile;
  /* The hash is the entry point, not just a bookmark: a visitor arriving from a maritime
     programme should land on SEA. Read once on mount, then kept in sync with replaceState so
     the back button is not filled with tab presses. */
  const [active, setActive] = React.useState(() => {
    const h = (typeof window !== 'undefined' ? window.location.hash : '').replace(/^#/, '');
    return AD_IDS.indexOf(h) > -1 ? h : 'air';
  });
  const refs = React.useRef({});
  React.useEffect(() => {
    const on = () => {
      const h = window.location.hash.replace(/^#/, '');
      if (AD_IDS.indexOf(h) > -1) setActive(h);
    };
    window.addEventListener('hashchange', on);
    return () => window.removeEventListener('hashchange', on);
  }, []);
  const select = React.useCallback(id => {
    setActive(id);
    if (window.history && window.history.replaceState) window.history.replaceState(null, '', `#${id}`);
  }, []);
  const onKeyDown = React.useCallback(e => {
    const i = AD_IDS.indexOf(active);
    let next = -1;
    if (e.key === 'ArrowRight' || e.key === 'ArrowDown') next = (i + 1) % AD_IDS.length;else if (e.key === 'ArrowLeft' || e.key === 'ArrowUp') next = (i - 1 + AD_IDS.length) % AD_IDS.length;else if (e.key === 'Home') next = 0;else if (e.key === 'End') next = AD_IDS.length - 1;
    if (next < 0) return;
    e.preventDefault();
    const id = AD_IDS[next];
    select(id);
    const node = refs.current[id];
    if (node && node.focus) node.focus({
      preventScroll: true
    });
  }, [active, select]);
  const d = AUT_DOMAINS.find(x => x.id === active) || AUT_DOMAINS[0];
  const Nav = AD_NAVS[nav] || AdNavNotch;
  const navEl = mobile ? /*#__PURE__*/React.createElement(AdNavSelect, {
    active: active,
    onSelect: select
  }) : /*#__PURE__*/React.createElement(Nav, {
    active: active,
    mobile: mobile,
    onSelect: select,
    onKeyDown: onKeyDown,
    refs: refs
  });
  const header = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16,
      maxWidth: 'var(--sf-copy-max, 760px)'
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Domains",
    speed: 30,
    delay: 240
  })), /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 760,
    delay: 290,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)',
      textWrap: 'balance'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Legitimate Autonomy"), " Across All Domains"), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 470,
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      textWrap: 'pretty'
    }
  }, "One evolutionary architecture, retuned for the physics of every operating environment."));
  return /*#__PURE__*/React.createElement(GradientField, {
    field: "none",
    pad: mobile ? 'clamp(64px, 15vw, 96px) var(--gutter-site)' : 'clamp(40px, 4.2vw, 72px) var(--gutter-site)',
    as: "section",
    id: "domains"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, layout === 'ledger' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(28px, 3.2vw, 48px)'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "up",
    dur: 640
  }, header), /*#__PURE__*/React.createElement(AdLedger, {
    capStyle: capStyle === 'hud' ? 'cards' : capStyle,
    mobile: mobile
  })) : layout === 'matrix' ? /*#__PURE__*/React.createElement(AdMatrix, {
    d: d,
    active: active,
    select: select,
    capStyle: capStyle === 'hud' ? 'cards' : capStyle,
    stacked: stacked,
    mobile: mobile,
    tablet: tablet,
    header: header
  }) : /*#__PURE__*/React.createElement(AdConsole, {
    d: d,
    scene: scene,
    capStyle: capStyle,
    capFont: capFont,
    capLink: capLink,
    corners: corners,
    titleSize: titleSize || 15,
    particles: parts,
    stacked: stacked,
    mobile: mobile,
    tablet: tablet,
    header: header,
    nav: navEl,
    navKind: nav
  })));
}
Object.assign(window, {
  DomainSection,
  AUT_DOMAINS
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/AutonomyDomains.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/AutonomyScreen.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Website › Autonomy. Same vocabulary as the homepage — navy canvas, chamfered frames, one
   amber accent per view, HUD reveals — composed differently so the page reads as a chapter
   rather than a second front door:

   · the hero is 74svh, not full-viewport, and carries a mono domain rail instead of a second
     button row, so the page's shape is legible in the first screen;
   · the three advantages are a hairline-gapped triptych (the design system's documented grid
     device, unused on the homepage) under a full-width header, rather than copy-beside-cards;
   · the four domains are ONE tabbed instrument (AutonomyDomains.jsx);
   · the page closes on a split two-prompt band, because the brief has two next steps, not one.

   Nothing above the fold is invented: every headline, claim and capability comes from the
   supplied copy. No figures appear anywhere on this page — none were supplied. */

const DS_A = () => window.VivumAIDesignSystem_b2be15;
const AUT_TWEAKS = /*EDITMODE-BEGIN*/{
  "mobileMenuStyle": "fullscreen",
  "domainLayout": "console",
  "domainScene": "field",
  "domainNav": "rail",
  "domainCaps": "hud",
  "domainFont": "mono",
  "domainCapLink": "auto",
  "domainCorners": false,
  "domainTitleSize": 15,
  "pGlyph": "dot",
  "pCount": 200,
  "pSize": 2.6,
  "pExtent": 1.6,
  "heroRail": true,
  "heroOpacity": 0.2,
  "ctaLayout": "banner",
  "ctaPlate": true,
  "seamAnim": "ribbons",
  "seamSize": 480,
  "seamSpeed": 2,
  "seamX": 0,
  "seamY": 0,
  "seamOpacity": 0.2,
  "mFrame": "rise",
  "mSpeed": 1.2,
  "mDelay": 0,
  "mReplay": false
} /*EDITMODE-END*/;
const AUT_PILLARS = [{
  n: '01',
  icon: 'brain-circuit',
  title: 'Legitimate Autonomy',
  body: 'Dynamic neural models deliver genuinely autonomous capability — not scripted automation.'
}, {
  n: '02',
  icon: 'microchip',
  title: 'On-device Intelligence',
  body: 'On-device learning puts fast intelligence exactly where the decision has to be made.'
}, {
  n: '03',
  icon: 'bolt',
  title: 'Ultra-efficient',
  body: 'Dramatically less power and far fewer resources. AI that holds up in the most demanding environments.'
}];
function useAutMax(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}
function useAutMin(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(min-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}

/* The hero's domain rail. Uppercase mono labels separated by the brand's middle dot — the one
   sanctioned separator — so a jump-nav over video needs no pill, chip or frosted plate to stay
   legible. Each label sets the hash (the tab strip below listens for it) and walks the page
   down to the instrument, so one tap answers "is my domain here?" and lands on it. */
/* Walks the page to `y` on a timer rather than asking for `behavior: 'smooth'`.
   Two things this has to survive, both real here: native smooth scrolling is silently a no-op
   in embedded contexts (previews, webviews, automation), and requestAnimationFrame is not
   serviced whenever the document is not considered visible. The failure mode of either is the
   worst one available — the tab changes a thousand pixels below the fold and the page appears
   not to respond at all — so this uses the same device Preloader.jsx uses for its rail: an
   interval, eased off the wall clock. Instant when the user asked for less motion, and it
   yields the moment the user scrolls for themselves. */
function vvScrollTo(y, dur = 620) {
  const at = () => window.pageYOffset || document.documentElement.scrollTop || 0;
  const from = at();
  const to = Math.max(0, y);
  if (Math.abs(to - from) < 2) return;
  if (vvReduced()) {
    window.scrollTo(0, to);
    return;
  }
  const t0 = Date.now();
  let id = 0;
  const stop = () => {
    clearInterval(id);
    window.removeEventListener('wheel', stop);
    window.removeEventListener('touchstart', stop);
    window.removeEventListener('keydown', stop);
  };
  window.addEventListener('wheel', stop, {
    passive: true,
    once: true
  });
  window.addEventListener('touchstart', stop, {
    passive: true,
    once: true
  });
  window.addEventListener('keydown', stop, {
    once: true
  });
  id = setInterval(() => {
    const p = Math.min(1, (Date.now() - t0) / dur);
    const eased = 1 - Math.pow(1 - p, 4);
    window.scrollTo(0, Math.round(from + (to - from) * eased));
    if (p >= 1) {
      window.scrollTo(0, to);
      stop();
    }
  }, 16);
}
function DomainRail({
  mobile
}) {
  const [hot, setHot] = React.useState(null);
  const jump = (e, id) => {
    e.preventDefault();
    if (window.history && window.history.replaceState) window.history.replaceState(null, '', `#${id}`);
    /* replaceState fires no hashchange, so the strip is told directly. Constructed events are
       not universally available, so fall back to a plain Event of the same name. */
    let ev;
    try {
      ev = new HashChangeEvent('hashchange');
    } catch (err) {
      ev = new Event('hashchange');
    }
    window.dispatchEvent(ev);
    /* Land on the instrument, not on the section's headline: the visitor asked for a specific
       domain, so the strip and the panel it controls are what should be under the nav when the
       page settles. In the expanded view each domain has its own anchor instead. */
    const host = document.getElementById(id) || document.querySelector('#domains [role="tablist"]') || document.getElementById('domains');
    if (!host) return;
    vvScrollTo(host.getBoundingClientRect().top + (window.pageYOffset || 0) - 104);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: mobile ? 'column' : 'row',
      alignItems: mobile ? 'flex-start' : 'baseline',
      gap: mobile ? 12 : 'clamp(16px, 1.8vw, 26px)',
      paddingTop: 'clamp(20px, 2.2vw, 30px)',
      marginTop: 'clamp(6px, 1vw, 12px)',
      borderTop: '1px solid var(--vv-ice-14)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: 'var(--vv-graphite)',
      flex: 'none'
    }
  }, "Domains"), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 'clamp(8px, 1vw, 14px)',
      flexWrap: 'wrap'
    }
  }, window.AUT_DOMAINS.map((d, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: d.id
  }, i ? /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      color: 'var(--vv-ice-56)',
      fontSize: 13
    }
  }, "\xB7") : null, /*#__PURE__*/React.createElement("a", {
    href: `#${d.id}`,
    onClick: e => jump(e, d.id),
    onMouseEnter: () => setHot(d.id),
    onMouseLeave: () => setHot(null),
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: mobile ? 12.5 : 13.5,
      fontWeight: 500,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      textDecoration: 'none',
      color: hot === d.id ? 'var(--vv-amber)' : 'var(--vv-ice-82)',
      transition: 'color 160ms ease'
    }
  }, d.label)))));
}

/* Triptych cell. The grid's 1px gaps sit over a hairline-coloured parent and each cell paints
   the canvas colour, so three cells read as one divided object with no card, fill or shadow —
   the elevation rule holds (hairlines only) and the section still has structure. */
/* Right column of Advantages, copied from the homepage's Autonomous Solutions · spine layout:
   a single hairline rail with icons sitting on it, sitting OUTSIDE the chamfered frame that
   holds the copy — two peer objects, not one frame containing everything. */
function AdvantagesSpine({
  mobile
}) {
  const {
    Icon
  } = DS_A();
  const wide = useAutMin(1560);
  const box = wide ? 96 : 64,
    rail = wide ? 132 : 92;
  if (mobile) {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 28
      }
    }, AUT_PILLARS.map((m, i) => /*#__PURE__*/React.createElement(Reveal, {
      key: m.title,
      variant: "side",
      delay: i * 120,
      dur: 520,
      style: {
        display: 'grid',
        gridTemplateColumns: 'auto 1fr',
        columnGap: 16,
        alignItems: 'start'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        width: 44,
        height: 44,
        flex: 'none',
        border: '1px solid var(--line-hairline)',
        background: 'var(--surface-canvas)'
      }
    }, /*#__PURE__*/React.createElement(Icon, {
      name: m.icon,
      size: 26,
      color: "var(--vv-ice)"
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column'
      }
    }, /*#__PURE__*/React.createElement("h3", {
      style: {
        margin: 0,
        fontSize: 17,
        fontWeight: 600,
        letterSpacing: '-0.01em',
        lineHeight: 1.3,
        color: 'var(--vv-ice)'
      }
    }, m.title), /*#__PURE__*/React.createElement("p", {
      style: {
        margin: 0,
        fontSize: 14.5,
        lineHeight: 1.6,
        color: 'var(--text-secondary)',
        textWrap: 'pretty'
      }
    }, m.body)))));
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      paddingLeft: rail
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "fade",
    dur: 900,
    style: {
      position: 'absolute',
      left: box / 2,
      top: 22,
      bottom: 26,
      width: 1,
      background: 'var(--line-hairline)'
    }
  }), AUT_PILLARS.map((m, i) => /*#__PURE__*/React.createElement(Reveal, {
    key: m.title,
    variant: "side",
    delay: i * 130,
    dur: 560,
    style: {
      position: 'relative',
      marginBottom: i === AUT_PILLARS.length - 1 ? 0 : 40
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: -rail,
      top: wide ? '50%' : 0,
      transform: wide ? 'translateY(-50%)' : undefined,
      width: box,
      height: box,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      border: '1px solid var(--line-hairline)',
      background: 'var(--surface-canvas)'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: m.icon,
    size: wide ? 46 : 30,
    color: "var(--vv-ice)"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10,
      paddingTop: wide ? 8 : 0,
      minHeight: box
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: 20,
      fontWeight: 600,
      letterSpacing: '-0.01em',
      lineHeight: 1.3,
      color: 'var(--vv-ice)'
    }
  }, m.title), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 14.5,
      lineHeight: 1.6,
      color: 'var(--text-secondary)',
      textWrap: 'pretty',
      maxWidth: 360
    }
  }, m.body)))));
}
const AUT_CTAS = [{
  label: 'About',
  title: 'Meet the Vivum team and discover our mission',
  action: 'About us',
  href: '/about'
}, {
  label: 'Contact',
  title: 'Get a tailored technical consultation',
  action: 'Contact us',
  href: '/contact'
}];

/* Closing band, banner variant — the homepage's own closing device (one chamfered frame,
   amber phrase, paragraph, primary button, quiet second link) rather than two competing
   cards. Recommended default: it reads as one coherent closing statement, and it is the
   vocabulary every other page on the site already ends on. */
function ClosingBanner({
  onNavigate,
  mobile,
  tablet,
  stacked,
  plate
}) {
  const {
    SectionFrame,
    Button
  } = DS_A();
  const QuietLink = window.QuietLink;
  return /*#__PURE__*/React.createElement(Reveal, {
    variant: "frame",
    dur: 760
  }, /*#__PURE__*/React.createElement(SectionFrame, {
    accent: true,
    chamfer: 44,
    weight: 1,
    color: "var(--vv-ice-14)",
    stacked: stacked,
    contentStyle: {
      maxWidth: '100%'
    },
    padX: "clamp(64px, 7vw, 120px)",
    plateSrc: plate ? '../../assets/plates/home-banner-video.mp4' : undefined,
    plateOpacity: 0.2,
    plateShift: "clamp(30px, 20vw, 300px)",
    plateScale: 1.7,
    fill: "rgba(218,232,242,0.02)"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: stacked ? 'column' : 'row',
      justifyContent: 'center',
      columnGap: 'clamp(40px, 6vw, 96px)',
      rowGap: 'clamp(28px, 3.4vw, 40px)',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'flex-start',
      gap: 16,
      maxWidth: tablet ? '100%' : 680,
      flex: '0 1 auto'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 800,
    delay: 280,
    style: {
      margin: 0,
      fontSize: mobile ? 'clamp(26px, 2.5vw, 36px)' : 'clamp(30px, 3.2vw, 46px)',
      fontWeight: 700,
      letterSpacing: mobile ? '-0.02em' : '-0.03em',
      lineHeight: mobile ? 1.15 : 1.08,
      color: 'var(--vv-ice)',
      maxWidth: tablet ? '100%' : 680,
      textWrap: tablet ? 'wrap' : 'balance'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Legitimate Autonomy"), " for Every Fleet, Every Domain"), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 460,
    style: {
      margin: 0,
      fontSize: 'clamp(17px, 1.35vw, 20px)',
      lineHeight: 1.5,
      color: 'var(--vv-ice-82)',
      maxWidth: tablet ? '100%' : 560,
      textWrap: tablet ? 'wrap' : 'balance'
    }
  }, "Tell us your platform and the domain you operate in \u2014 we\u2019ll show you what an evolved model does with it."), QuietLink ? /*#__PURE__*/React.createElement(Reveal, {
    variant: "side",
    delay: 900,
    dur: 480
  }, /*#__PURE__*/React.createElement(QuietLink, {
    href: "#/about",
    label: "Learn more about us",
    onClick: e => {
      e.preventDefault();
      onNavigate('/about');
    }
  })) : null), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'center',
      flex: mobile || tablet ? '1 1 100%' : '0 0 auto',
      width: mobile || tablet ? '100%' : undefined
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "rise",
    delay: 740,
    dur: 520,
    style: {
      width: mobile || tablet ? '100%' : undefined
    }
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    size: "lg",
    onClick: () => onNavigate('/contact'),
    style: {
      height: 58,
      padding: '0 38px',
      fontSize: 17,
      ...(mobile || tablet ? {
        width: '100%'
      } : null)
    }
  }, "Contact us"))))));
}
function AutonomyScreen({
  onNavigate
}) {
  const {
    VideoBackdrop,
    GradientField,
    Eyebrow,
    Button,
    SectionFrame
  } = DS_A();
  const [t, setTweak] = useTweaks(AUT_TWEAKS);
  React.useEffect(() => {
    window.vvSetMobileMenuStyle && window.vvSetMobileMenuStyle(t.mobileMenuStyle);
  }, [t.mobileMenuStyle]);
  React.useLayoutEffect(() => {
    window.vvSetMotion({
      frame: t.mFrame,
      speed: t.mSpeed,
      delay: t.mDelay,
      replay: t.mReplay
    });
  }, [t.mFrame, t.mSpeed, t.mDelay, t.mReplay]);
  const stacked = useAutMax(900);
  const mobile = useAutMax(640);
  const tablet = useAutMax(1024) && !mobile;
  const navMobile = useAutMax(760);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      isolation: 'isolate'
    }
  }, /*#__PURE__*/React.createElement(VideoBackdrop, {
    src: "../../assets/plates/dynamic-neural-models-v4.mp4",
    scrim: "bottom",
    pingPong: true,
    minHeight: mobile ? 'max(560px, 78svh)' : 'max(560px, 74svh)',
    align: "end",
    edgeFade: false,
    foot: "14%",
    pad: mobile ? '170px var(--gutter-site) clamp(76px, 18vw, 108px)' : navMobile ? '176px var(--gutter-site) 48px' : '180px var(--gutter-site) clamp(72px, 7vw, 112px)',
    plateOpacity: t.heroOpacity
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site)',
      margin: '0 auto',
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(14px, 1.1vw, 18px)'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    delay: 140
  }, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Autonomy",
    speed: 34
  }))), /*#__PURE__*/React.createElement(Reveal, {
    as: "h1",
    variant: "text",
    dur: 880,
    delay: 260,
    style: {
      margin: 0,
      fontSize: 'clamp(30px, 3.2vw, 46px)',
      fontWeight: 700,
      letterSpacing: '-0.03em',
      lineHeight: 1.08,
      color: 'var(--vv-ice)',
      maxWidth: '17.5em',
      textWrap: 'balance'
    }
  }, "Autonomous Solutions for ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Unmanned Vehicles")), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 620,
    style: {
      margin: 0,
      fontSize: 'clamp(17px, 1.35vw, 20px)',
      lineHeight: 1.5,
      color: 'var(--vv-ice-82)',
      maxWidth: 640
    }
  }, "Legitimate autonomy across air, land, sea, and space \u2014 evolved onto the hardware you already field."), t.heroRail !== false ? /*#__PURE__*/React.createElement(Reveal, {
    variant: "up",
    delay: 860,
    dur: 520,
    style: {
      marginTop: 'clamp(10px, 1vw, 18px)'
    }
  }, /*#__PURE__*/React.createElement(DomainRail, {
    mobile: mobile
  })) : null))), /*#__PURE__*/React.createElement(GradientField, {
    field: "none",
    pad: mobile ? 'clamp(64px, 15vw, 96px) var(--gutter-site)' : 'clamp(40px, 4.2vw, 72px) var(--gutter-site)',
    as: "section",
    id: "advantages"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : 'minmax(0, 0.72fr) minmax(0, 0.68fr)',
      columnGap: 0,
      rowGap: 'clamp(36px, 5vw, 56px)',
      alignItems: 'stretch'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      alignSelf: 'center'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: mobile ? 'up' : 'frame',
    dur: 640
  }, /*#__PURE__*/React.createElement(SectionFrame, _extends({
    copyMax: "100%",
    stacked: stacked,
    contentStyle: {
      maxWidth: '100%'
    },
    fill: "transparent"
  }, mobile ? {
    chamfer: 0,
    color: 'transparent',
    blur: 0,
    accent: false,
    padX: '0px',
    padY: '0px'
  } : tablet ? {
    padX: 'clamp(48px, 6vw, 96px)',
    padY: 'clamp(48px, 6vw, 96px)'
  } : null), /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Advantages",
    speed: 30,
    delay: 240
  })), /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 760,
    delay: 290,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)',
      textWrap: 'balance'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Evolutionary AI"), " Unlocks the Potential of Your Systems"), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 470,
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      textWrap: 'pretty'
    }
  }, "Three properties carry the whole system: models that learn rather than follow a script, intelligence that runs where the decision is made, and an efficiency envelope that survives the field \u2014 which is what turns hardware you already field into an autonomous platform.")))), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: stacked ? '100%' : 520,
      width: '100%',
      boxSizing: 'border-box',
      justifySelf: 'start',
      alignSelf: 'center',
      paddingLeft: stacked ? 0 : 'clamp(2.5rem, 5.2vw, 7.5rem)',
      paddingRight: 0
    }
  }, /*#__PURE__*/React.createElement(AdvantagesSpine, {
    mobile: mobile
  }))))), /*#__PURE__*/React.createElement(SeamAnimation, {
    variant: t.seamAnim,
    size: t.seamSize,
    speed: t.seamSpeed,
    x: t.seamX,
    y: t.seamY,
    opacity: t.seamOpacity
  }), /*#__PURE__*/React.createElement(DomainSection, {
    layout: t.domainLayout,
    scene: t.domainScene,
    nav: t.domainNav,
    capStyle: t.domainCaps,
    capFont: t.domainFont,
    capLink: t.domainCapLink,
    corners: t.domainCorners,
    titleSize: t.domainTitleSize,
    particles: {
      glyph: t.pGlyph,
      count: t.pCount,
      size: t.pSize,
      extent: t.pExtent
    }
  }), /*#__PURE__*/React.createElement(GradientField, {
    field: "none",
    pad: mobile ? 'clamp(64px, 15vw, 96px) var(--gutter-site) clamp(96px, 22vw, 140px)' : 'clamp(40px, 4.2vw, 72px) var(--gutter-site) clamp(48px, 5vw, 88px)',
    as: "section",
    id: "next"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, t.ctaLayout === 'split' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : 'repeat(2, minmax(0, 1fr))',
      gap: stacked ? 'clamp(26px, 6vw, 44px)' : 'clamp(16px, 1.8vw, 28px)',
      alignItems: 'stretch'
    }
  }, AUT_CTAS.map((c, i) => /*#__PURE__*/React.createElement(Reveal, {
    key: c.label,
    variant: "frame",
    dur: 760,
    delay: i * 160,
    style: {
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement(SectionFrame, {
    accent: true,
    chamfer: 36,
    chamferWide: 64,
    weight: 1,
    color: "var(--vv-ice-14)",
    stacked: true,
    stretch: true,
    copyMax: "100%",
    contentStyle: {
      maxWidth: '100%',
      alignItems: 'flex-start',
      flex: '1 1 auto'
    },
    gap: 18,
    padX: mobile ? 'clamp(24px, 7vw, 34px)' : 'clamp(30px, 3.4vw, 54px)',
    padY: mobile ? 'clamp(28px, 8vw, 40px)' : 'clamp(34px, 3.6vw, 56px)',
    fill: "rgba(218,232,242,0.02)",
    style: {
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement(HudText, {
    text: c.label,
    speed: 30,
    delay: 240 + i * 120,
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: 'var(--vv-graphite)'
    }
  }), /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 780,
    delay: 300 + i * 140,
    style: {
      margin: 0,
      fontSize: mobile ? 'clamp(21px, 2vw, 26px)' : 'clamp(22px, 2.1vw, 30px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.2,
      color: 'var(--vv-ice)',
      maxWidth: '17em',
      textWrap: 'balance'
    }
  }, c.title), /*#__PURE__*/React.createElement(Reveal, {
    variant: "rise",
    delay: 700 + i * 140,
    dur: 520,
    style: {
      marginTop: 'auto',
      paddingTop: 'clamp(10px, 1.4vw, 18px)',
      width: mobile ? '100%' : undefined
    }
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    size: "lg",
    onClick: () => onNavigate(c.href),
    style: mobile ? {
      width: '100%'
    } : undefined
  }, c.action)))))) : /*#__PURE__*/React.createElement(ClosingBanner, {
    onNavigate: onNavigate,
    mobile: mobile,
    tablet: tablet,
    stacked: stacked,
    plate: t.ctaPlate
  }))), /*#__PURE__*/React.createElement(TweaksPanel, null, /*#__PURE__*/React.createElement(TweakSection, {
    label: "Domains"
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Layout",
    value: t.domainLayout,
    options: ['console', 'matrix', 'ledger'],
    onChange: v => setTweak('domainLayout', v)
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Instrument",
    value: t.domainScene,
    options: ['swarm', 'field', 'radar', 'off'],
    onChange: v => setTweak('domainScene', v)
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Domain nav",
    value: t.domainNav,
    options: ['rail', 'bracket', 'seam', 'carousel', 'dial', 'stack'],
    onChange: v => setTweak('domainNav', v)
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Capabilities",
    value: t.domainCaps,
    options: ['hud', 'cards', 'rows', 'numbered'],
    onChange: v => setTweak('domainCaps', v)
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Canvas type",
    value: t.domainFont,
    options: ['sans', 'mono'],
    onChange: v => setTweak('domainFont', v)
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Callout link edge",
    value: t.domainCapLink,
    options: ['auto', 'left', 'right', 'top', 'bottom'],
    onChange: v => setTweak('domainCapLink', v)
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Canvas corner marks",
    value: t.domainCorners,
    onChange: v => setTweak('domainCorners', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Canvas title size",
    value: t.domainTitleSize,
    min: 13,
    max: 34,
    step: 1,
    onChange: v => setTweak('domainTitleSize', v)
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Particle shape",
    value: t.pGlyph,
    options: ['dot', 'chevron', 'box'],
    onChange: v => setTweak('pGlyph', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Particle size",
    value: t.pSize,
    min: 0.4,
    max: 2.6,
    step: 0.1,
    onChange: v => setTweak('pSize', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Particle count",
    value: t.pCount,
    min: 200,
    max: 2400,
    step: 100,
    onChange: v => setTweak('pCount', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Particle field size",
    value: t.pExtent,
    min: 0.5,
    max: 1.6,
    step: 0.05,
    onChange: v => setTweak('pExtent', v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Closing"
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "CTA layout",
    value: t.ctaLayout,
    options: ['banner', 'split'],
    onChange: v => setTweak('ctaLayout', v)
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Background video",
    value: t.ctaPlate,
    onChange: v => setTweak('ctaPlate', v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Seam background"
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Animation",
    value: t.seamAnim,
    options: [{
      value: 'texture',
      label: 'Texture — film grain'
    }, {
      value: 'nebula',
      label: 'Nebula — turbulence cloud'
    }, {
      value: 'ash',
      label: 'Ash — falling grit'
    }, {
      value: 'sheen',
      label: 'Sheen — satin sweep'
    }, {
      value: 'weave',
      label: 'Weave — combed fibre'
    }, {
      value: 'smoke',
      label: 'Smoke — climbing plumes'
    }, {
      value: 'marble',
      label: 'Marble — breathing veins'
    }, {
      value: 'static',
      label: 'Static — signal loss'
    }, {
      value: 'ribbons',
      label: 'Ribbons — sliding bands'
    }, {
      value: 'starfield',
      label: 'Starfield — twinkling depths'
    }, {
      value: 'drift',
      label: 'Drift — crossing blooms'
    }, {
      value: 'scan',
      label: 'Scan — sweeping hairlines'
    }, {
      value: 'pulse',
      label: 'Pulse — propagating rings'
    }, {
      value: 'lattice',
      label: 'Lattice — drifting mesh'
    }, {
      value: 'orbit',
      label: 'Orbit — tracking dots'
    }, {
      value: 'contours',
      label: 'Contours — breathing terrain'
    }, {
      value: 'motes',
      label: 'Motes — rising particles'
    }, {
      value: 'waves',
      label: 'Waves — swaying crests'
    }, {
      value: 'shafts',
      label: 'Shafts — angled light'
    }, {
      value: 'off',
      label: 'Off'
    }],
    onChange: v => setTweak('seamAnim', v)
  }), t.seamAnim !== 'off' && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Size",
    value: t.seamSize,
    min: 120,
    max: 640,
    step: 10,
    unit: "px",
    onChange: v => setTweak('seamSize', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Speed",
    value: t.seamSpeed,
    min: 0.25,
    max: 3,
    step: 0.05,
    unit: "\xD7",
    onChange: v => setTweak('seamSpeed', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "X position",
    value: t.seamX,
    min: -50,
    max: 50,
    step: 1,
    unit: "%",
    onChange: v => setTweak('seamX', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Y position",
    value: t.seamY,
    min: -100,
    max: 100,
    step: 2,
    unit: "%",
    onChange: v => setTweak('seamY', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Opacity",
    value: t.seamOpacity,
    min: 0.02,
    max: 1,
    step: 0.01,
    onChange: v => setTweak('seamOpacity', v)
  })), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Page"
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Hero domain rail",
    value: t.heroRail !== false,
    onChange: v => setTweak('heroRail', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Hero video opacity",
    value: t.heroOpacity,
    min: 0.05,
    max: 1,
    step: 0.05,
    onChange: v => setTweak('heroOpacity', v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Motion"
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Chamfered frames",
    value: t.mFrame,
    options: window.VV_FRAME_OPTIONS,
    onChange: v => setTweak('mFrame', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Speed",
    value: t.mSpeed,
    min: 0.25,
    max: 2.5,
    step: 0.05,
    unit: "\xD7",
    onChange: v => setTweak('mSpeed', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Delay",
    value: t.mDelay,
    min: 0,
    max: 800,
    step: 20,
    unit: "ms",
    onChange: v => setTweak('mDelay', v)
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Replay on re-entry",
    value: t.mReplay,
    onChange: v => setTweak('mReplay', v)
  })));
}
Object.assign(window, {
  AutonomyScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/AutonomyScreen.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/BriefingScreen.jsx
try { (() => {
const DS = () => window.VivumAIDesignSystem_b2be15;
function BriefingScreen({
  onToast
}) {
  const {
    GradientField,
    Eyebrow,
    Panel,
    Input,
    Select,
    Checkbox,
    Button,
    Modal,
    Divider,
    Icon,
    StatusDot
  } = DS();
  const [sent, setSent] = React.useState(false);
  const [org, setOrg] = React.useState('');
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative'
    }
  }, /*#__PURE__*/React.createElement(GradientField, {
    field: "left",
    pad: "150px var(--gutter-site) 130px",
    as: "section",
    style: {
      minHeight: 860
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site)',
      margin: '0 auto',
      display: 'grid',
      gridTemplateColumns: '1fr 560px',
      gap: 96,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Briefing"), /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontSize: 'clamp(30px, 3.2vw, 46px)',
      fontWeight: 700,
      letterSpacing: '-0.03em',
      lineHeight: 1.08,
      color: 'var(--vv-ice)'
    }
  }, "Forty-Five Minutes, No Slideware"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      maxWidth: 520
    }
  }, "We walk the runtime, the adaptation loop, and what integration looks like on your hardware. Bring an engineer."), /*#__PURE__*/React.createElement(Divider, {
    spacing: 12
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 20
    }
  }, [['schedule', 'Scheduling', 'Within ten business days, remote or on site.'], ['groups', 'Who attends', 'Two from Vivum: one systems, one research.'], ['lock', 'Handling', 'Under NDA on request. No recordings.']].map(([icon, k, v]) => /*#__PURE__*/React.createElement("div", {
    key: k,
    style: {
      display: 'flex',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: icon,
    size: 20,
    color: "var(--vv-blue)",
    style: {
      marginTop: 2
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 14.5,
      fontWeight: 600,
      color: 'var(--vv-ice)'
    }
  }, k), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13.5,
      lineHeight: 1.5,
      color: 'var(--text-secondary)'
    }
  }, v))))), /*#__PURE__*/React.createElement(StatusDot, {
    tone: "live",
    mono: true,
    style: {
      marginTop: 8
    }
  }, "Next available \xB7 3 Sep 2026")), /*#__PURE__*/React.createElement(Panel, {
    title: "Request a briefing",
    meta: "All fields required unless marked optional",
    pad: 28
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 20
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(Input, {
    label: "Name",
    placeholder: "Full name"
  }), /*#__PURE__*/React.createElement(Input, {
    label: "Work email",
    placeholder: "name@organization.gov",
    type: "email"
  })), /*#__PURE__*/React.createElement(Input, {
    label: "Organization",
    placeholder: "Program office, prime, or operator",
    value: org,
    onChange: e => setOrg(e.target.value)
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(Select, {
    label: "Domain",
    options: ['Air', 'Land', 'Sea', 'Space', 'Edge devices'],
    defaultValue: "Land"
  }), /*#__PURE__*/React.createElement(Select, {
    label: "Stage",
    options: ['Evaluating', 'Prototyping', 'Program of record'],
    defaultValue: "Prototyping"
  })), /*#__PURE__*/React.createElement(Input, {
    label: "Platform (optional)",
    placeholder: "Airframe, hull, or compute module",
    hint: "Helps us bring the right engineer."
  }), /*#__PURE__*/React.createElement(Divider, {
    spacing: 2
  }), /*#__PURE__*/React.createElement(Checkbox, {
    label: "Under NDA",
    hint: "We will send our mutual NDA before the call."
  }), /*#__PURE__*/React.createElement(Checkbox, {
    label: "Send the capability overview first",
    defaultChecked: true
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 12,
      marginTop: 6
    }
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    fullWidth: true,
    icon: "arrow_forward",
    onClick: () => setSent(true)
  }, "Submit request")), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      lineHeight: 1.5,
      color: 'var(--text-caption)'
    }
  }, "Export-controlled discussions are handled separately \u2014 write to ", /*#__PURE__*/React.createElement("a", {
    href: "#"
  }, "brand@vivum.ai"), "."))))), /*#__PURE__*/React.createElement(Modal, {
    open: sent,
    width: 460,
    title: "Request received",
    meta: org ? `Organization · ${org}` : 'We reply within two business days',
    onClose: () => setSent(false),
    actions: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      size: "sm",
      onClick: () => setSent(false)
    }, "Close"), /*#__PURE__*/React.createElement(Button, {
      variant: "primary",
      size: "sm",
      onClick: () => {
        setSent(false);
        onToast({
          title: 'Overview sent',
          body: 'Capability overview · PDF · 2.4 MB',
          tone: 'live'
        });
      }
    }, "Send overview now"))
  }, "A systems engineer will confirm a slot within two business days. Nothing else is needed from you."));
}
Object.assign(window, {
  BriefingScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/BriefingScreen.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/ContactForm.jsx
try { (() => {
const DS_F = () => window.VivumAIDesignSystem_b2be15;

/* ── Page-scale fields ────────────────────────────────────────────────────────────────────
   The kit's Input/Select are console scale: 40px tall, 14px text, built for a dense product
   panel. A marketing contact form is the opposite problem — it is the one thing on the page a
   visitor has to operate, on a phone, possibly one-handed. So these are built at page scale
   (54px, 16px — under 16px iOS zooms the whole viewport on focus) from the same tokens the
   kit's fields use: 4% ice wash, hairline border, blue focus, amber error. Same material,
   right size. `fieldStyle` swaps the chrome without touching the geometry. */
/* Two rules that cannot be inline: the popover's entrance keyframe, and default link colours
   for the page (an editor-added link must never fall back to browser blue). Injected once. */
if (typeof document !== 'undefined' && !document.getElementById('vv-ct-css')) {
  const _s = document.createElement('style');
  _s.id = 'vv-ct-css';
  _s.textContent = '@keyframes ctPop{from{opacity:0;transform:translateY(-6px)}to{opacity:1;transform:none}}' + '#contact a{color:var(--vv-ice);text-decoration:none}#contact a:hover{color:var(--vv-amber)}' + '@media (prefers-reduced-motion:reduce){#contact *{animation-duration:0.01ms!important}}';
  document.head.appendChild(_s);
}
const CT_H = 54;
const CT_LBL = {
  fontFamily: 'var(--font-mono)',
  fontSize: 11,
  letterSpacing: '0.18em',
  textTransform: 'uppercase',
  color: 'var(--vv-graphite)',
  lineHeight: 1
};
const CT_TXT = {
  fontFamily: 'var(--font-sans)',
  fontSize: 16,
  lineHeight: 1.5,
  color: 'var(--vv-ice)',
  letterSpacing: '0.005em'
};
const CT_EMOJI = "'Apple Color Emoji','Segoe UI Emoji','Noto Color Emoji','Twemoji Mozilla',sans-serif";
const CT_EASE = 'cubic-bezier(.16,1,.3,1)';
function ctChrome(variant, state) {
  const line = state === 'error' ? 'var(--vv-amber)' : state === 'focus' ? 'var(--vv-blue)' : null;
  const t = {
    transition: `border-color 160ms ${CT_EASE}, box-shadow 160ms ${CT_EASE}, background 160ms ${CT_EASE}`
  };
  if (variant === 'underline') {
    return {
      ...t,
      background: 'transparent',
      border: 0,
      borderBottom: `1px solid ${line || 'var(--vv-ice-14)'}`,
      borderRadius: 0,
      boxShadow: state === 'focus' ? '0 1px 0 var(--vv-blue)' : 'none'
    };
  }
  if (variant === 'outline') {
    return {
      ...t,
      background: 'transparent',
      border: `1px solid ${line || 'var(--vv-ice-14)'}`,
      borderRadius: 0,
      boxShadow: state === 'focus' ? 'inset 0 0 0 1px var(--vv-blue-24)' : 'none'
    };
  }
  return {
    ...t,
    background: 'var(--vv-ice-04)',
    border: `1px solid ${line || 'var(--line-hairline)'}`,
    borderRadius: 'var(--radius-sm)',
    boxShadow: state === 'focus' ? 'inset 0 0 0 1px var(--vv-blue-24)' : 'none'
  };
}
const ctPadX = variant => variant === 'underline' ? 2 : 16;

/* label · control · one caption slot. The caption is always rendered, never conditionally
   mounted: an error appearing under a field must not push the rest of the form down the page
   while the user is reading it. */
function CtRow({
  label,
  hint,
  error,
  variant,
  htmlFor,
  children,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10,
      minWidth: 0,
      ...style
    }
  }, label ? /*#__PURE__*/React.createElement("label", {
    htmlFor: htmlFor,
    style: CT_LBL
  }, label) : null, children, /*#__PURE__*/React.createElement("span", {
    "aria-live": "polite",
    style: {
      minHeight: 15,
      fontSize: 12.5,
      lineHeight: 1.2,
      color: error ? 'var(--vv-amber)' : 'var(--text-caption)'
    }
  }, error || hint || ''));
}
function CtField({
  id,
  label,
  hint,
  error,
  value,
  onChange,
  onBlur,
  placeholder,
  type = 'text',
  variant,
  autoComplete,
  inputMode,
  inputRef
}) {
  const [focus, setFocus] = React.useState(false);
  const state = error ? 'error' : focus ? 'focus' : 'rest';
  return /*#__PURE__*/React.createElement(CtRow, {
    label: label,
    hint: hint,
    error: error,
    variant: variant,
    htmlFor: id
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      height: CT_H,
      padding: `0 ${ctPadX(variant)}px`,
      boxSizing: 'border-box',
      ...ctChrome(variant, state)
    }
  }, /*#__PURE__*/React.createElement("input", {
    id: id,
    ref: inputRef,
    type: type,
    value: value,
    placeholder: placeholder,
    autoComplete: autoComplete,
    inputMode: inputMode,
    onChange: e => onChange(e.target.value),
    onFocus: () => setFocus(true),
    onBlur: () => {
      setFocus(false);
      onBlur && onBlur();
    },
    style: {
      flex: 1,
      minWidth: 0,
      background: 'transparent',
      border: 0,
      outline: 'none',
      padding: 0,
      ...CT_TXT
    }
  })));
}
function CtArea({
  id,
  label,
  hint,
  error,
  value,
  onChange,
  onBlur,
  placeholder,
  variant,
  rows = 6
}) {
  const [focus, setFocus] = React.useState(false);
  const state = error ? 'error' : focus ? 'focus' : 'rest';
  return /*#__PURE__*/React.createElement(CtRow, {
    label: label,
    hint: hint,
    error: error,
    variant: variant,
    htmlFor: id
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      padding: variant === 'underline' ? '0 2px 12px' : '14px 16px',
      boxSizing: 'border-box',
      ...ctChrome(variant, state)
    }
  }, /*#__PURE__*/React.createElement("textarea", {
    id: id,
    rows: rows,
    value: value,
    placeholder: placeholder,
    onChange: e => onChange(e.target.value),
    onFocus: () => setFocus(true),
    onBlur: () => {
      setFocus(false);
      onBlur && onBlur();
    },
    style: {
      display: 'block',
      width: '100%',
      minHeight: 132,
      resize: 'vertical',
      background: 'transparent',
      border: 0,
      outline: 'none',
      padding: 0,
      ...CT_TXT
    }
  })));
}

/* ── One popover, two pickers ─────────────────────────────────────────────────────────────
   Native <select> can't carry a flag, a dial code and a searchable list, and on the topic
   field it can't show all five options at once — so both pickers are one listbox
   implementation instead of two half-solutions. Full keyboard contract: type-ahead search,
   arrows, Home/End, Enter, Escape, click-outside, and roving aria-activedescendant. */
function useCtDismiss(ref, open, close) {
  React.useEffect(() => {
    if (!open) return undefined;
    const onDown = e => {
      if (ref.current && !ref.current.contains(e.target)) close();
    };
    const onKey = e => {
      if (e.key === 'Escape') {
        e.stopPropagation();
        close();
      }
    };
    document.addEventListener('pointerdown', onDown, true);
    document.addEventListener('keydown', onKey, true);
    return () => {
      document.removeEventListener('pointerdown', onDown, true);
      document.removeEventListener('keydown', onKey, true);
    };
  }, [open, close, ref]);
}
function CtOption({
  item,
  active,
  selected,
  onPick,
  id,
  showDial,
  dataI
}) {
  const {
    Icon
  } = DS_F();
  return /*#__PURE__*/React.createElement("li", {
    id: id,
    role: "option",
    "aria-selected": selected,
    "data-i": dataI,
    onMouseDown: e => {
      e.preventDefault();
      onPick(item);
    },
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      padding: '11px 14px 11px 12px',
      cursor: 'pointer',
      minWidth: 0,
      borderLeft: `2px solid ${active ? 'var(--vv-amber)' : 'transparent'}`,
      background: active ? 'var(--vv-amber-08)' : 'transparent',
      transition: `background 120ms ${CT_EASE}`
    }
  }, item.flag ? /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      fontFamily: CT_EMOJI,
      fontSize: 15,
      lineHeight: 1,
      flex: '0 0 auto',
      width: 22
    }
  }, item.flag) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      fontSize: 14.5,
      lineHeight: 1.35,
      color: active || selected ? 'var(--vv-ice)' : 'var(--vv-ice-82)',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, item.label), showDial ? /*#__PURE__*/React.createElement("span", {
    style: {
      flex: '0 0 auto',
      fontFamily: 'var(--font-mono)',
      fontSize: 12.5,
      letterSpacing: '0.04em',
      color: 'var(--vv-graphite)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, item.dial) : null, selected ? /*#__PURE__*/React.createElement(Icon, {
    name: "check",
    size: 14,
    color: "var(--vv-amber)",
    style: {
      flex: '0 0 auto'
    }
  }) : null);
}
function CtListbox({
  listId,
  items,
  selectedValue,
  active,
  setActive,
  onPick,
  showDial,
  search,
  setSearch,
  searchPlaceholder,
  width,
  align = 'left'
}) {
  const {
    Icon
  } = DS_F();
  const listRef = React.useRef(null);
  React.useEffect(() => {
    const el = listRef.current && listRef.current.querySelector(`[data-i="${active}"]`);
    if (!el) return;
    const box = listRef.current;
    if (el.offsetTop < box.scrollTop) box.scrollTop = el.offsetTop;else if (el.offsetTop + el.offsetHeight > box.scrollTop + box.clientHeight) box.scrollTop = el.offsetTop + el.offsetHeight - box.clientHeight;
  }, [active]);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 'calc(100% + 6px)',
      zIndex: 40,
      width: width || '100%',
      [align]: 0,
      background: 'var(--surface-raised)',
      border: '1px solid var(--vv-ice-14)',
      borderRadius: 'var(--radius-sm)',
      boxShadow: '0 24px 60px -18px rgba(0,4,10,0.86)',
      overflow: 'hidden',
      animation: `ctPop 200ms ${CT_EASE} both`
    }
  }, setSearch ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 9,
      padding: '0 12px',
      height: 42,
      borderBottom: '1px solid var(--line-hairline)'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "magnifying-glass",
    size: 14,
    color: "var(--vv-graphite)"
  }), /*#__PURE__*/React.createElement("input", {
    autoFocus: true,
    value: search,
    onChange: e => {
      setSearch(e.target.value);
      setActive(0);
    },
    placeholder: searchPlaceholder,
    "aria-label": searchPlaceholder,
    "aria-controls": listId,
    "aria-autocomplete": "list",
    style: {
      flex: 1,
      minWidth: 0,
      background: 'transparent',
      border: 0,
      outline: 'none',
      padding: 0,
      fontFamily: 'var(--font-sans)',
      fontSize: 14,
      color: 'var(--vv-ice)'
    }
  })) : null, /*#__PURE__*/React.createElement("ul", {
    ref: listRef,
    id: listId,
    role: "listbox",
    style: {
      listStyle: 'none',
      margin: 0,
      padding: '4px 0',
      maxHeight: 268,
      overflowY: 'auto',
      overscrollBehavior: 'contain'
    }
  }, items.length ? items.map((it, i) => /*#__PURE__*/React.createElement(CtOption, {
    key: it.value,
    dataI: i,
    id: `${listId}-o${i}`,
    item: it,
    active: i === active,
    selected: it.value === selectedValue,
    onPick: onPick,
    showDial: showDial
  })) : /*#__PURE__*/React.createElement("li", {
    style: {
      padding: '14px',
      fontSize: 13.5,
      color: 'var(--vv-graphite)'
    }
  }, "No match")));
}

/* Shared open/active/keyboard state for both pickers. */
function useCtPicker(items, selectedValue, onSelect) {
  const [open, setOpen] = React.useState(false);
  const [active, setActive] = React.useState(0);
  const close = React.useCallback(() => {
    setOpen(false);
  }, []);
  const pick = it => {
    onSelect(it);
    setOpen(false);
  };
  const openAt = () => {
    const i = items.findIndex(x => x.value === selectedValue);
    setActive(i < 0 ? 0 : i);
    setOpen(true);
  };
  const onKeyDown = e => {
    if (!open) {
      if (e.key === 'ArrowDown' || e.key === 'Enter' || e.key === ' ') {
        e.preventDefault();
        openAt();
      }
      return;
    }
    if (e.key === 'ArrowDown') {
      e.preventDefault();
      setActive(a => Math.min(items.length - 1, a + 1));
    } else if (e.key === 'ArrowUp') {
      e.preventDefault();
      setActive(a => Math.max(0, a - 1));
    } else if (e.key === 'Home') {
      e.preventDefault();
      setActive(0);
    } else if (e.key === 'End') {
      e.preventDefault();
      setActive(items.length - 1);
    } else if (e.key === 'Enter') {
      e.preventDefault();
      if (items[active]) pick(items[active]);
    } else if (e.key === 'Tab') {
      setOpen(false);
    }
  };
  return {
    open,
    setOpen,
    openAt,
    close,
    active,
    setActive,
    pick,
    onKeyDown
  };
}

/* Phone: one field, two controls. The dial code is a trigger inside the field's own chrome
   rather than a separate select beside it, so the number and its country read as the single
   value they are — and the number input keeps the full remaining width on a phone. */
function CtPhone({
  label,
  hint,
  variant,
  country,
  setCountry,
  phone,
  setPhone,
  error,
  onBlur
}) {
  const {
    Icon
  } = DS_F();
  const raw = window.VV_COUNTRIES || [];
  const all = React.useMemo(() => raw.map(([iso, name, dial, flag]) => ({
    value: iso,
    label: name,
    dial,
    flag
  })), [raw]);
  const [search, setSearch] = React.useState('');
  const items = React.useMemo(() => {
    const q = search.trim().toLowerCase().replace(/^\+/, '');
    if (!q) return all;
    return all.filter(c => c.label.toLowerCase().includes(q) || c.value.toLowerCase() === q || c.dial.replace('+', '').startsWith(q));
  }, [all, search]);
  const sel = all.find(c => c.value === country) || all[0];
  const p = useCtPicker(items, country, it => {
    setCountry(it.value);
    setSearch('');
  });
  const ref = React.useRef(null);
  useCtDismiss(ref, p.open, () => {
    p.close();
    setSearch('');
  });
  const [focus, setFocus] = React.useState(false);
  const state = error ? 'error' : focus || p.open ? 'focus' : 'rest';
  if (!sel) return null;
  return /*#__PURE__*/React.createElement(CtRow, {
    label: label,
    hint: hint,
    error: error,
    variant: variant,
    htmlFor: "ct-phone"
  }, /*#__PURE__*/React.createElement("span", {
    ref: ref,
    style: {
      position: 'relative',
      display: 'flex',
      alignItems: 'stretch',
      height: CT_H,
      boxSizing: 'border-box',
      ...ctChrome(variant, state)
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    role: "combobox",
    "aria-expanded": p.open,
    "aria-haspopup": "listbox",
    "aria-controls": "ct-cc-list",
    "aria-activedescendant": p.open && items[p.active] ? `ct-cc-list-o${p.active}` : undefined,
    "aria-label": `Country code — ${sel.label} ${sel.dial}`,
    onClick: () => p.open ? p.close() : p.openAt(),
    onKeyDown: p.onKeyDown,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      height: '100%',
      padding: `0 12px 0 ${ctPadX(variant)}px`,
      background: 'transparent',
      border: 0,
      borderRight: `1px solid ${variant === 'underline' ? 'transparent' : 'var(--line-hairline)'}`,
      cursor: 'pointer',
      flex: '0 0 auto',
      color: 'var(--vv-ice)',
      marginRight: variant === 'underline' ? 4 : 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      fontFamily: CT_EMOJI,
      fontSize: 15,
      lineHeight: 1
    }
  }, sel.flag), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 14,
      letterSpacing: '0.04em',
      fontVariantNumeric: 'tabular-nums'
    }
  }, sel.dial), /*#__PURE__*/React.createElement(Icon, {
    name: "angle-down",
    size: 12,
    color: "var(--vv-graphite)",
    style: {
      transform: p.open ? 'rotate(180deg)' : 'none',
      transition: `transform 200ms ${CT_EASE}`
    }
  })), /*#__PURE__*/React.createElement("input", {
    id: "ct-phone",
    type: "tel",
    inputMode: "tel",
    autoComplete: "tel-national",
    value: phone,
    placeholder: "555 000 1234",
    onChange: e => setPhone(e.target.value),
    onFocus: () => setFocus(true),
    onBlur: () => {
      setFocus(false);
      onBlur && onBlur();
    },
    style: {
      flex: 1,
      minWidth: 0,
      background: 'transparent',
      border: 0,
      outline: 'none',
      padding: `0 ${ctPadX(variant)}px 0 12px`,
      ...CT_TXT
    }
  }), p.open ? /*#__PURE__*/React.createElement(CtListbox, {
    listId: "ct-cc-list",
    items: items,
    selectedValue: country,
    active: p.active,
    setActive: p.setActive,
    onPick: p.pick,
    showDial: true,
    search: search,
    setSearch: setSearch,
    searchPlaceholder: "Search country or code",
    width: "min(340px, calc(100vw - 3rem))"
  }) : null));
}
function CtSelect({
  id,
  label,
  hint,
  error,
  variant,
  options,
  value,
  onChange,
  placeholder,
  onBlur
}) {
  const {
    Icon
  } = DS_F();
  const items = options.map(o => ({
    value: o,
    label: o
  }));
  const p = useCtPicker(items, value, it => {
    onChange(it.value);
    onBlur && onBlur(it.value);
  });
  const ref = React.useRef(null);
  useCtDismiss(ref, p.open, p.close);
  const state = error ? 'error' : p.open ? 'focus' : 'rest';
  return /*#__PURE__*/React.createElement(CtRow, {
    label: label,
    hint: hint,
    error: error,
    variant: variant
  }, /*#__PURE__*/React.createElement("span", {
    ref: ref,
    style: {
      position: 'relative',
      display: 'block'
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    id: id,
    role: "combobox",
    "aria-expanded": p.open,
    "aria-haspopup": "listbox",
    "aria-controls": `${id}-list`,
    "aria-label": typeof label === 'string' ? label : undefined,
    "aria-activedescendant": p.open && items[p.active] ? `${id}-list-o${p.active}` : undefined,
    onClick: () => p.open ? p.close() : p.openAt(),
    onKeyDown: p.onKeyDown,
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12,
      width: '100%',
      height: CT_H,
      padding: `0 ${ctPadX(variant)}px`,
      boxSizing: 'border-box',
      cursor: 'pointer',
      textAlign: 'left',
      ...ctChrome(variant, state)
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      ...CT_TXT,
      color: value ? 'var(--vv-ice)' : 'var(--vv-graphite)',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, value || placeholder), /*#__PURE__*/React.createElement(Icon, {
    name: "angle-down",
    size: 13,
    color: "var(--vv-graphite)",
    style: {
      flex: '0 0 auto',
      transform: p.open ? 'rotate(180deg)' : 'none',
      transition: `transform 200ms ${CT_EASE}`
    }
  })), p.open ? /*#__PURE__*/React.createElement(CtListbox, {
    listId: `${id}-list`,
    items: items,
    selectedValue: value,
    active: p.active,
    setActive: p.setActive,
    onPick: p.pick
  }) : null));
}

/* The same five choices as visible targets. Five options is under the threshold where a
   dropdown earns its collapse — chips cost one tap instead of two and show the whole set
   without opening anything. Live on the `topicControl` tweak. */
function CtChips({
  label,
  hint,
  error,
  options,
  value,
  onChange
}) {
  return /*#__PURE__*/React.createElement(CtRow, {
    label: label,
    hint: hint,
    error: error
  }, /*#__PURE__*/React.createElement("div", {
    role: "radiogroup",
    "aria-label": typeof label === 'string' ? label : 'Topic',
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, options.map(o => {
    const on = value === o;
    return /*#__PURE__*/React.createElement("button", {
      key: o,
      type: "button",
      role: "radio",
      "aria-checked": on,
      onClick: () => onChange(on ? '' : o),
      style: {
        minHeight: 44,
        padding: '10px 16px',
        cursor: 'pointer',
        textAlign: 'left',
        fontFamily: 'var(--font-sans)',
        fontSize: 14.5,
        lineHeight: 1.3,
        color: on ? 'var(--vv-ice)' : 'var(--vv-ice-82)',
        background: on ? 'var(--vv-amber-08)' : 'var(--vv-ice-04)',
        border: `1px solid ${on ? 'var(--vv-amber-24)' : 'var(--line-hairline)'}`,
        borderRadius: 'var(--radius-sm)',
        transition: `background 160ms ${CT_EASE}, border-color 160ms ${CT_EASE}, color 160ms ${CT_EASE}`
      }
    }, o);
  })));
}
const CT_TOPICS = ['Robotics', 'Autonomy', 'Network & Traffic Management', 'High-Performance Real-Time Applications', 'Something else'];
const CT_OTHER = 'Something else';
const CT_EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/;
function CtReceipt({
  ref_,
  reply,
  onReset,
  mobile
}) {
  const {
    Icon,
    Button
  } = DS_F();
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(18px, 2vw, 26px)',
      minHeight: 340,
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "circle-check",
    size: 30,
    color: "var(--vv-amber)"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: 'clamp(22px, 2vw, 28px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)'
    }
  }, "Message sent"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 'clamp(15.5px, 1.2vw, 17px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      maxWidth: '46ch',
      textWrap: 'pretty'
    }
  }, "It is with a systems engineer, not a queue. You will hear back from a person who can answer the technical question.")), /*#__PURE__*/React.createElement("dl", {
    style: {
      margin: 0,
      display: 'grid',
      gridTemplateColumns: 'auto 1fr',
      columnGap: 20,
      rowGap: 0,
      borderTop: '1px solid var(--line-hairline)'
    }
  }, [['Reference', ref_], ['Reply by', reply]].map(([k, v]) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: k
  }, /*#__PURE__*/React.createElement("dt", {
    style: {
      ...CT_LBL,
      padding: '14px 0',
      borderBottom: '1px solid var(--line-hairline)'
    }
  }, k), /*#__PURE__*/React.createElement("dd", {
    style: {
      margin: 0,
      padding: '14px 0',
      borderBottom: '1px solid var(--line-hairline)',
      fontFamily: 'var(--font-mono)',
      fontSize: 13.5,
      letterSpacing: '0.06em',
      color: 'var(--vv-ice)',
      textAlign: 'right',
      fontVariantNumeric: 'tabular-nums'
    }
  }, v)))), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    size: "lg",
    onClick: onReset,
    style: mobile ? {
      width: '100%'
    } : {
      alignSelf: 'flex-start'
    }
  }, "Send another"));
}
function CtForm({
  variant = 'wash',
  topicControl = 'dropdown',
  pair = false,
  sendWidth = 'full',
  mobile,
  onToast
}) {
  const {
    Button,
    Icon
  } = DS_F();
  const emailRef = React.useRef(null);
  /* True once the user has done anything at all — gates blur-validation so a programmatic
     focus/blur cycle (see setEmailRef below) can never surface an error the user didn't earn,
     no matter how many times layout hooks remount the field before settling. */
  const interactedRef = React.useRef(false);
  React.useEffect(() => {
    const mark = () => {
      interactedRef.current = true;
    };
    window.addEventListener('pointerdown', mark, {
      capture: true,
      once: true
    });
    window.addEventListener('keydown', mark, {
      capture: true,
      once: true
    });
    return () => {
      window.removeEventListener('pointerdown', mark, {
        capture: true
      });
      window.removeEventListener('keydown', mark, {
        capture: true
      });
    };
  }, []);
  /* A ref callback, not a one-shot mount effect: it fires again on every remount of the input
     (a layout hook flipping after first paint replaces the node), so the pending focus keeps
     finding whichever element is actually in the DOM instead of landing on one that's already
     gone. */
  const setEmailRef = React.useCallback(node => {
    emailRef.current = node;
    /* Synchronous, not requestAnimationFrame: rAF is only serviced once the page has an
       actual painted frame, and an inactive/headless capture of this page can starve that
       indefinitely. Focusing directly off the ref attach has no such dependency. */
    if (node && !interactedRef.current && document.activeElement !== node) node.focus({
      preventScroll: true
    });
  }, []);
  const [email, setEmail] = React.useState('');
  const [country, setCountry] = React.useState('US');
  const [phone, setPhone] = React.useState('');
  const [topic, setTopic] = React.useState('');
  const [other, setOther] = React.useState('');
  const [msg, setMsg] = React.useState('');
  const [err, setErr] = React.useState({});
  const [sent, setSent] = React.useState(null);
  const check = (k, v) => {
    if (k === 'email') return !v.trim() ? 'An email address is how we reply.' : CT_EMAIL_RE.test(v.trim()) ? '' : 'That address is missing something.';
    if (k === 'phone') return !v.trim() ? '' : v.replace(/\D/g, '').length >= 6 ? '' : 'That number looks short.';
    if (k === 'other') return topic === CT_OTHER && !v.trim() ? 'Tell us in a word or two.' : '';
    if (k === 'msg') return v.trim().length ? '' : 'A sentence is enough to start.';
    return '';
  };
  /* Validate on blur, never on keystroke — an error that appears while you are still typing
     the address is noise, and it is the single most common defect in web forms. */
  const blur = (k, v) => {
    if (!interactedRef.current) return;
    setErr(e => ({
      ...e,
      [k]: check(k, v)
    }));
  };
  const edit = (k, set) => v => {
    set(v);
    if (err[k]) setErr(e => ({
      ...e,
      [k]: ''
    }));
  };
  const submit = e => {
    e.preventDefault();
    const next = {
      email: check('email', email),
      phone: check('phone', phone),
      other: check('other', other),
      msg: check('msg', msg)
    };
    setErr(next);
    const bad = Object.keys(next).find(k => next[k]);
    if (bad) {
      const el = document.getElementById(bad === 'msg' ? 'ct-msg' : bad === 'other' ? 'ct-other' : bad === 'phone' ? 'ct-phone' : 'ct-email');
      if (el) el.focus({
        preventScroll: false
      });
      return;
    }
    const d = new Date();
    const rid = `VV-${String(d.getMonth() + 1).padStart(2, '0')}${String(d.getDate()).padStart(2, '0')}-${Math.random().toString(16).slice(2, 6).toUpperCase()}`;
    /* Two business days, counted honestly rather than +2 calendar days. */
    const by = new Date(d);
    let left = 2;
    while (left > 0) {
      by.setDate(by.getDate() + 1);
      if (by.getDay() !== 0 && by.getDay() !== 6) left -= 1;
    }
    setSent({
      ref: rid,
      reply: by.toLocaleDateString('en-US', {
        day: 'numeric',
        month: 'short',
        year: 'numeric'
      })
    });
    if (onToast) onToast({
      title: 'Message sent',
      body: `Reference ${rid}`,
      tone: 'live'
    });
  };
  if (sent) return /*#__PURE__*/React.createElement(CtReceipt, {
    ref_: sent.ref,
    reply: sent.reply,
    mobile: mobile,
    onReset: () => {
      setSent(null);
      setEmail('');
      setPhone('');
      setTopic('');
      setOther('');
      setMsg('');
      setErr({});
    }
  });
  const emailField = /*#__PURE__*/React.createElement(CtField, {
    id: "ct-email",
    type: "email",
    autoComplete: "email",
    inputRef: setEmailRef,
    variant: variant,
    label: "Your email address",
    placeholder: "name@organization.com",
    value: email,
    onChange: edit('email', setEmail),
    onBlur: () => blur('email', email),
    error: err.email
  });
  const phoneField = /*#__PURE__*/React.createElement(CtPhone, {
    variant: variant,
    label: /*#__PURE__*/React.createElement(React.Fragment, null, "Your phone ", /*#__PURE__*/React.createElement("span", {
      style: {
        color: 'var(--vv-ice-40, rgba(218,232,242,0.45))',
        letterSpacing: '0.18em'
      }
    }, "(optional)")),
    country: country,
    setCountry: setCountry,
    phone: phone,
    setPhone: edit('phone', setPhone),
    error: err.phone,
    onBlur: () => blur('phone', phone)
  });
  return /*#__PURE__*/React.createElement("form", {
    noValidate: true,
    onSubmit: submit,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(8px, 0.9vw, 14px)'
    }
  }, pair && !mobile ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'minmax(0, 1fr) minmax(0, 1fr)',
      gap: 'clamp(14px, 1.6vw, 24px)'
    }
  }, emailField, phoneField) : /*#__PURE__*/React.createElement(React.Fragment, null, emailField, phoneField), topicControl === 'chips' ? /*#__PURE__*/React.createElement(CtChips, {
    label: "What can we help with?",
    options: CT_TOPICS,
    value: topic,
    onChange: v => {
      setTopic(v);
      setErr(e => ({
        ...e,
        other: ''
      }));
    }
  }) : /*#__PURE__*/React.createElement(CtSelect, {
    id: "ct-topic",
    variant: variant,
    label: "What can we help with?",
    placeholder: "Select a topic",
    options: CT_TOPICS,
    value: topic,
    onChange: v => {
      setTopic(v);
      setErr(e => ({
        ...e,
        other: ''
      }));
    }
  }), /*#__PURE__*/React.createElement(CtArea, {
    id: "ct-msg",
    variant: variant,
    label: "Your message",
    placeholder: "Enter your message here",
    hint: "The platform, the constraint, the question.",
    value: msg,
    onChange: edit('msg', setMsg),
    onBlur: () => blur('msg', msg),
    error: err.msg
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      alignItems: 'center',
      gap: 'clamp(14px, 1.6vw, 22px)',
      marginTop: 'clamp(4px, 0.6vw, 10px)'
    }
  }, /*#__PURE__*/React.createElement(Button, {
    type: "submit",
    variant: "primary",
    size: "lg",
    iconAfter: "arrow-right",
    style: sendWidth === 'full' || mobile ? {
      width: '100%',
      height: 58,
      fontSize: 17
    } : {
      height: 58,
      padding: '0 34px',
      fontSize: 17
    }
  }, "Send message")));
}
Object.assign(window, {
  CtForm,
  CtField,
  CtArea,
  CtPhone,
  CtSelect,
  CtChips,
  CT_LBL,
  CT_EMOJI,
  CT_EASE,
  CT_TOPICS
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/ContactForm.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/ContactScreen.jsx
try { (() => {
const DS_C = () => window.VivumAIDesignSystem_b2be15;
function useCtMax(q) {
  const [hit, setHit] = React.useState(() => typeof window !== 'undefined' && window.matchMedia(`(max-width: ${q}px)`).matches);
  React.useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}
const CT_TWEAKS = /*EDITMODE-BEGIN*/{
  "layout": "unified",
  "formFrame": "bezel",
  "fieldStyle": "wash",
  "topicControl": "dropdown",
  "pairFields": false,
  "sendWidth": "full",
  "heading": "warm",
  "lines": "ledger",
  "copyBtn": true,
  "mapLink": false,
  "headingInFrame": false,
  "backdrop": "field",
  "mFrame": "rise",
  "mSpeed": 1.2,
  "mDelay": 0,
  "mReplay": false,
  "seamAnim": "ribbons",
  "seamSize": 480,
  "seamSpeed": 2,
  "seamX": 0,
  "seamY": 0,
  "seamOpacity": 0.2
} /*EDITMODE-END*/;
const CT_ADDRESS = ['611 Gateway Blvd (Suite 200)', 'San Francisco, CA 94080'];
const CT_MAP = 'https://www.google.com/maps/search/?api=1&query=611+Gateway+Blvd+Suite+200+South+San+Francisco+CA+94080';
const CT_LINES = [{
  label: 'Commercial partnerships',
  email: 'partnerships@vivum.ai'
}, {
  label: 'Defense applications',
  email: 'teaming@vivum.ai'
}];
const CT_HEADS = {
  verbatim: [/*#__PURE__*/React.createElement("span", {
    key: "a",
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Reach out"), ' below'],
  short: [/*#__PURE__*/React.createElement("span", {
    key: "a",
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Reach out")],
  warm: [/*#__PURE__*/React.createElement("span", {
    key: "a",
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Start"), ' A Conversation']
};

/* One direct line. The address is a real mailto link and the copy control is separate — a row
   that copies on click steals the click from the link people actually expect, and a row that
   only links leaves anyone composing in a desktop client to transcribe by hand. Both, then. */
function CtLine({
  item,
  i,
  showCopy,
  last,
  style,
  mono
}) {
  const [copied, setCopied] = React.useState(false);
  const [hot, setHot] = React.useState(false);
  const [copyHot, setCopyHot] = React.useState(false);
  const copy = () => {
    const done = () => {
      setCopied(true);
      window.setTimeout(() => setCopied(false), 1800);
    };
    if (navigator.clipboard && navigator.clipboard.writeText) navigator.clipboard.writeText(item.email).then(done, done);else done();
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 7,
      padding: '18px 0',
      borderBottom: last ? 'none' : '1px solid var(--line-hairline)',
      minWidth: 0,
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 10
    }
  }, mono ? /*#__PURE__*/React.createElement("span", {
    style: {
      ...window.CT_LBL,
      color: 'var(--vv-graphite)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, String(i + 1).padStart(2, '0')) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      ...window.CT_LBL
    }
  }, item.label)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("a", {
    href: `mailto:${item.email}`,
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      fontSize: 'clamp(15.5px, 1.2vw, 17px)',
      lineHeight: 1.35,
      color: hot ? 'var(--vv-amber)' : 'var(--vv-ice)',
      textDecoration: 'none',
      borderBottom: '1px solid transparent',
      transition: `color 160ms ${window.CT_EASE}`,
      overflowWrap: 'anywhere'
    }
  }, item.email), showCopy ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: copy,
    onMouseEnter: () => setCopyHot(true),
    onMouseLeave: () => setCopyHot(false),
    "aria-label": `Copy ${item.email}`,
    style: {
      flex: '0 0 auto',
      minHeight: 30,
      padding: '6px 9px',
      cursor: 'pointer',
      background: copied ? 'var(--vv-amber-08)' : copyHot ? 'var(--vv-ice-04)' : 'transparent',
      border: `1px solid ${copied ? 'var(--vv-amber-24)' : copyHot ? 'var(--vv-ice-24)' : 'var(--line-hairline)'}`,
      borderRadius: 'var(--radius-sm)',
      fontFamily: 'var(--font-mono)',
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: copied ? 'var(--vv-amber)' : copyHot ? 'var(--vv-ice)' : 'var(--vv-graphite)',
      transition: `all 160ms ${window.CT_EASE}`
    }
  }, copied ? 'Copied' : 'Copy') : null));
}
function CtOffice({
  mapLink
}) {
  const {
    Icon
  } = DS_C();
  const [hot, setHot] = React.useState(false);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: window.CT_LBL
  }, "Office"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'clamp(15.5px, 1.2vw, 17px)',
      lineHeight: 1.5,
      color: 'var(--vv-ice-82)'
    }
  }, "South San Francisco, CA"), CT_ADDRESS.map(l => /*#__PURE__*/React.createElement("span", {
    key: l,
    style: {
      fontSize: 'clamp(15.5px, 1.2vw, 17px)',
      lineHeight: 1.5,
      color: 'var(--vv-ice-82)'
    }
  }, l))), mapLink ? /*#__PURE__*/React.createElement("a", {
    href: CT_MAP,
    target: "_blank",
    rel: "noreferrer noopener",
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      marginTop: 2,
      fontSize: 14.5,
      textDecoration: 'none',
      color: hot ? 'var(--vv-amber)' : 'var(--vv-graphite)',
      transition: `color 160ms ${window.CT_EASE}`
    }
  }, "Open in Maps", /*#__PURE__*/React.createElement(Icon, {
    name: "arrow-up-right",
    size: 13,
    style: {
      transform: hot ? 'translate(2px, -2px)' : 'none',
      transition: `transform 160ms ${window.CT_EASE}`
    }
  })) : null);
}

/* Right column: the escape hatch for anyone who would rather not fill in a form at all — so
   it is given equal typographic weight to the form's own heading, not tucked in as a footnote. */
function CtRail({
  lines,
  showCopy,
  mapLink,
  mobile,
  spine
}) {
  const {
    Bezel,
    Eyebrow
  } = DS_C();
  const rows = lines === 'cards' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gap: 'clamp(10px, 1.1vw, 14px)'
    }
  }, CT_LINES.map((c, i) => /*#__PURE__*/React.createElement(Bezel, {
    key: c.email,
    chamfer: 20,
    weight: 1,
    pad: "18px 20px",
    accent: false,
    color: "var(--vv-ice-14)",
    style: {
      background: 'rgba(218,232,242,0.02)'
    }
  }, /*#__PURE__*/React.createElement(CtLine, {
    item: c,
    i: i,
    showCopy: showCopy,
    last: true,
    style: {
      padding: 0
    }
  })))) : lines === 'plain' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gap: 'clamp(18px, 2vw, 26px)'
    }
  }, CT_LINES.map((c, i) => /*#__PURE__*/React.createElement(CtLine, {
    key: c.email,
    item: c,
    i: i,
    showCopy: showCopy,
    last: true,
    style: {
      padding: 0
    }
  }))) : /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: '1px solid var(--line-hairline)'
    }
  }, CT_LINES.map((c, i) => /*#__PURE__*/React.createElement(CtLine, {
    key: c.email,
    item: c,
    i: i,
    showCopy: showCopy,
    mono: true,
    last: false
  })));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(22px, 2.4vw, 32px)',
      minWidth: 0,
      ...(spine ? {
        paddingLeft: 'clamp(24px, 3vw, 48px)',
        borderLeft: '1px solid var(--line-hairline)'
      } : null)
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    delay: 140
  }, /*#__PURE__*/React.createElement(Eyebrow, {
    size: "sm"
  }, "Email us")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(8px, 0.8vw, 12px)'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 720,
    delay: 240,
    style: {
      margin: 0,
      fontSize: 'clamp(21px, 1.8vw, 26px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.2,
      color: 'var(--vv-ice)'
    }
  }, "Prefer to ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "write directly"), "?"), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 320,
    dur: 560,
    style: {
      margin: 0,
      fontSize: 14.5,
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      maxWidth: '38ch'
    }
  }, "Send us a note and we will get back to you as soon as we can.")), /*#__PURE__*/React.createElement(Reveal, {
    variant: "up",
    delay: 380,
    dur: 560
  }, rows), /*#__PURE__*/React.createElement(Reveal, {
    variant: "up",
    delay: 520,
    dur: 560,
    style: {
      borderTop: lines === 'ledger' ? 'none' : '1px solid var(--line-hairline)'
    }
  }, /*#__PURE__*/React.createElement(CtOffice, {
    mapLink: mapLink
  })));
}
function ContactScreen({
  onNavigate,
  onToast
}) {
  const {
    GradientField,
    Eyebrow,
    SectionFrame
  } = DS_C();
  const [t, setTweak] = useTweaks(CT_TWEAKS);
  React.useLayoutEffect(() => {
    window.vvSetMotion({
      frame: t.mFrame,
      speed: t.mSpeed,
      delay: t.mDelay,
      replay: t.mReplay
    });
  }, [t.mFrame, t.mSpeed, t.mDelay, t.mReplay]);
  const stackTweak = t.layout === 'stacked';
  const stacked = useCtMax(940) || stackTweak;
  const mobile = useCtMax(640);
  const unified = t.layout === 'unified' && !stacked;
  /* Fluid, not fixed: at the 940 threshold a hard 400px rail leaves the form too narrow to
     hold a readable field, and the rail's content is two addresses — it can afford to shrink. */
  const railW = t.layout === 'offset' ? 'minmax(0, clamp(232px, 22vw, 320px))' : 'minmax(0, clamp(276px, 29vw, 400px))';
  const form = /*#__PURE__*/React.createElement(CtForm, {
    variant: t.fieldStyle,
    topicControl: t.topicControl,
    pair: t.pairFields === true,
    sendWidth: t.sendWidth,
    mobile: mobile,
    onToast: onToast
  });
  const rail = /*#__PURE__*/React.createElement(CtRail, {
    lines: t.lines,
    showCopy: t.copyBtn !== false,
    mapLink: t.mapLink !== false,
    mobile: mobile,
    spine: t.layout === 'offset' && !stacked
  });
  const headingInFrame = t.headingInFrame === true;
  const heading = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(14px, 1.1vw, 18px)',
      maxWidth: 640
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    delay: 80
  }, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Contact",
    speed: 34
  }))), /*#__PURE__*/React.createElement(Reveal, {
    as: "h1",
    variant: "text",
    dur: 860,
    delay: 180,
    style: {
      margin: 0,
      fontSize: 'clamp(30px, 3.2vw, 46px)',
      fontWeight: 700,
      letterSpacing: '-0.03em',
      lineHeight: 1.08,
      color: 'var(--vv-ice)',
      textWrap: 'balance'
    }
  }, CT_HEADS[t.heading] || CT_HEADS.verbatim), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 420,
    style: {
      margin: 0,
      fontSize: 'clamp(17px, 1.35vw, 20px)',
      lineHeight: 1.5,
      color: 'var(--vv-ice-82)',
      maxWidth: 560,
      textWrap: 'pretty'
    }
  }, "We are here to answer questions, explore collaborations, and everything in between."));

  /* The form's own container. `bezel` is the page's default because the form is this page's
     one instrument and the chamfered frame is how every other page marks out its subject;
     `bare` sets the fields straight onto the field for a lighter page; `panel` fills the
     frame so the form reads as a surface you write on. Padding is left to `padX` alone —
     `SectionFrame` grows `padY` to match it on all four sides by default; passing an
     independent `padY` here is what broke that balance. */
  const framed = body => {
    if (mobile || t.formFrame === 'bare') return body;
    return /*#__PURE__*/React.createElement(Reveal, {
      variant: "frame",
      dur: 680,
      delay: 120
    }, /*#__PURE__*/React.createElement(SectionFrame, {
      stacked: true,
      stretch: true,
      copyMax: "100%",
      contentStyle: {
        maxWidth: '100%'
      },
      gap: 0,
      chamfer: 44,
      chamferWide: 72,
      weight: 1,
      color: "var(--vv-ice-14)",
      accent: false,
      padX: "clamp(30px, 3.6vw, 56px)",
      fill: t.formFrame === 'panel' ? 'var(--vv-ice-04)' : 'rgba(218,232,242,0.02)'
    }, body));
  };
  const formPane = headingInFrame ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(28px, 3.4vw, 44px)'
    }
  }, heading, form) : form;

  /* Single row now: the heading lives in its own section above, so this grid only ever lays
     out the frame and the rail side by side. */
  const cell = col => stacked ? null : {
    gridColumn: col
  };
  const body = /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto',
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : unified ? 'minmax(0, 1fr)' : `minmax(0, 1fr) ${railW}`,
      columnGap: 'clamp(36px, 5vw, 88px)',
      rowGap: mobile ? 'clamp(38px, 9vw, 54px)' : 'clamp(34px, 3.6vw, 52px)',
      alignItems: 'start'
    }
  }, unified ? /*#__PURE__*/React.createElement("div", {
    style: cell('1')
  }, framed(/*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `minmax(0, 1fr) ${railW}`,
      columnGap: 'clamp(32px, 3.6vw, 64px)',
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      paddingRight: 'clamp(24px, 3vw, 48px)',
      borderRight: '1px solid var(--line-hairline)'
    }
  }, formPane), /*#__PURE__*/React.createElement("div", {
    style: {
      paddingLeft: 'clamp(24px, 3vw, 48px)'
    }
  }, rail)))) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: cell('1')
  }, framed(formPane)), /*#__PURE__*/React.createElement("div", {
    style: cell('2')
  }, rail))));
  const headPad = mobile ? 'clamp(44px, 11vw, 72px) var(--gutter-site) clamp(32px, 8vw, 44px)' : 'clamp(52px, 5.4vw, 92px) var(--gutter-site) clamp(34px, 3.6vw, 52px)';
  const formPad = mobile ? '0 var(--gutter-site) clamp(72px, 16vw, 112px)' : '0 var(--gutter-site) clamp(52px, 5.6vw, 100px)';

  /* `grid` and `field` are the two remaining canvas treatments — the moving-plate video is
     gone. `field="none"` on GradientField keeps the flat navy canvas: the `left` bloom's
     radial glow used to show through the frame's near-transparent fill, reading as a stray
     circular gradient behind the chamfer. */
  const gridWash = /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      inset: 0,
      pointerEvents: 'none',
      backgroundImage: 'repeating-linear-gradient(to right, rgba(218,232,242,0.06) 0 1px, transparent 1px 88px), repeating-linear-gradient(to bottom, rgba(218,232,242,0.06) 0 1px, transparent 1px 88px)',
      maskImage: 'radial-gradient(115% 88% at 14% 0%, rgba(0,0,0,0.95) 0%, rgba(0,0,0,0.35) 52%, rgba(0,0,0,0) 78%)',
      WebkitMaskImage: 'radial-gradient(115% 88% at 14% 0%, rgba(0,0,0,0.95) 0%, rgba(0,0,0,0.35) 52%, rgba(0,0,0,0) 78%)'
    }
  });
  return /*#__PURE__*/React.createElement("div", {
    id: "contact",
    style: {
      isolation: 'isolate'
    }
  }, /*#__PURE__*/React.createElement("section", {
    style: {
      position: 'relative',
      padding: headPad
    }
  }, !headingInFrame && /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto',
      width: '100%'
    }
  }, heading)), /*#__PURE__*/React.createElement(SeamAnimation, {
    variant: t.seamAnim,
    size: t.seamSize,
    speed: t.seamSpeed,
    x: t.seamX,
    y: t.seamY,
    opacity: t.seamOpacity
  }), t.backdrop === 'grid' ? /*#__PURE__*/React.createElement("section", {
    style: {
      position: 'relative',
      padding: formPad,
      overflow: 'hidden'
    }
  }, gridWash, body) : /*#__PURE__*/React.createElement(GradientField, {
    field: "none",
    pad: formPad,
    as: "section",
    style: {
      position: 'relative'
    }
  }, body), /*#__PURE__*/React.createElement(TweaksPanel, null, /*#__PURE__*/React.createElement(TweakSection, {
    label: "Layout"
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Columns",
    value: t.layout,
    options: ['split', 'offset', 'unified', 'stacked'],
    onChange: v => setTweak('layout', v)
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Form frame",
    value: t.formFrame,
    options: ['bezel', 'panel', 'bare'],
    onChange: v => setTweak('formFrame', v)
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Backdrop",
    value: t.backdrop,
    options: ['grid', 'field'],
    onChange: v => setTweak('backdrop', v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Form"
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Field style",
    value: t.fieldStyle,
    options: ['wash', 'outline', 'underline'],
    onChange: v => setTweak('fieldStyle', v)
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Topic control",
    value: t.topicControl,
    options: ['dropdown', 'chips'],
    onChange: v => setTweak('topicControl', v)
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Email + phone on one row",
    value: t.pairFields === true,
    onChange: v => setTweak('pairFields', v)
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Send button",
    value: t.sendWidth,
    options: ['full', 'inline'],
    onChange: v => setTweak('sendWidth', v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Copy"
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Headline",
    value: t.heading,
    options: ['verbatim', 'short', 'warm'],
    onChange: v => setTweak('heading', v)
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Headline inside frame",
    value: headingInFrame,
    onChange: v => setTweak('headingInFrame', v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Direct lines"
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Style",
    value: t.lines,
    options: ['ledger', 'cards', 'plain'],
    onChange: v => setTweak('lines', v)
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Copy button",
    value: t.copyBtn !== false,
    onChange: v => setTweak('copyBtn', v)
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Open in Maps link",
    value: t.mapLink !== false,
    onChange: v => setTweak('mapLink', v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Motion"
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Chamfered frames",
    value: t.mFrame,
    options: window.VV_FRAME_OPTIONS,
    onChange: v => setTweak('mFrame', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Speed",
    value: t.mSpeed,
    min: 0.25,
    max: 2.5,
    step: 0.05,
    unit: "\xD7",
    onChange: v => setTweak('mSpeed', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Delay",
    value: t.mDelay,
    min: 0,
    max: 800,
    step: 20,
    unit: "ms",
    onChange: v => setTweak('mDelay', v)
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Replay on re-entry",
    value: t.mReplay,
    onChange: v => setTweak('mReplay', v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Seam background"
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Animation",
    value: t.seamAnim,
    options: [{
      value: 'texture',
      label: 'Texture — film grain'
    }, {
      value: 'nebula',
      label: 'Nebula — turbulence cloud'
    }, {
      value: 'ash',
      label: 'Ash — falling grit'
    }, {
      value: 'sheen',
      label: 'Sheen — satin sweep'
    }, {
      value: 'weave',
      label: 'Weave — combed fibre'
    }, {
      value: 'smoke',
      label: 'Smoke — climbing plumes'
    }, {
      value: 'marble',
      label: 'Marble — breathing veins'
    }, {
      value: 'static',
      label: 'Static — signal loss'
    }, {
      value: 'ribbons',
      label: 'Ribbons — sliding bands'
    }, {
      value: 'starfield',
      label: 'Starfield — twinkling depths'
    }, {
      value: 'drift',
      label: 'Drift — crossing blooms'
    }, {
      value: 'scan',
      label: 'Scan — sweeping hairlines'
    }, {
      value: 'pulse',
      label: 'Pulse — propagating rings'
    }, {
      value: 'lattice',
      label: 'Lattice — drifting mesh'
    }, {
      value: 'orbit',
      label: 'Orbit — tracking dots'
    }, {
      value: 'contours',
      label: 'Contours — breathing terrain'
    }, {
      value: 'motes',
      label: 'Motes — rising particles'
    }, {
      value: 'waves',
      label: 'Waves — swaying crests'
    }, {
      value: 'shafts',
      label: 'Shafts — angled light'
    }, {
      value: 'off',
      label: 'Off'
    }],
    onChange: v => setTweak('seamAnim', v)
  }), t.seamAnim !== 'off' && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Size",
    value: t.seamSize,
    min: 120,
    max: 640,
    step: 10,
    unit: "px",
    onChange: v => setTweak('seamSize', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Speed",
    value: t.seamSpeed,
    min: 0.25,
    max: 3,
    step: 0.05,
    unit: "\xD7",
    onChange: v => setTweak('seamSpeed', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "X position",
    value: t.seamX,
    min: -50,
    max: 50,
    step: 1,
    unit: "%",
    onChange: v => setTweak('seamX', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Y position",
    value: t.seamY,
    min: -100,
    max: 100,
    step: 2,
    unit: "%",
    onChange: v => setTweak('seamY', v)
  }), /*#__PURE__*/React.createElement(TweakSlider, {
    label: "Opacity",
    value: t.seamOpacity,
    min: 0.02,
    max: 1,
    step: 0.01,
    onChange: v => setTweak('seamOpacity', v)
  }))));
}
Object.assign(window, {
  ContactScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/ContactScreen.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/DomainInstrument.jsx
try { (() => {
/* The domain instrument: a SWARM that takes the shape of the platform the domain flies, drives,
   dives or orbits. One particle set, sampled from four silhouettes (fighter jet from above, tank
   in profile, submarine in profile, satellite head-on), morphed between them — nothing is
   created or destroyed on a domain change, the same swarm re-forms. That is the section's whole
   argument made literal: one architecture, retuned for the medium.

   three.js draws it; the morph and the per-domain parameter easing run on the render loop's own
   clock rather than an animation library — one dependency instead of two, and the tween cannot
   desync from the frame that consumes it. three.js is loaded lazily and is optional: no WebGL, a
   blocked CDN, or a reduced-motion preference falls back to the same silhouettes drawn on a 2D
   canvas (also offered on its own as the lighter `radar` scene), and that in turn falls back to
   the domain plate. The instrument is never a requirement for reading the page. */

const DI_LIBS = [['THREE', 'https://unpkg.com/three@0.149.0/build/three.min.js']];
let _diLoad = null;
function diLoadLibs() {
  if (_diLoad) return _diLoad;
  _diLoad = Promise.all(DI_LIBS.map(([g, src]) => new Promise(res => {
    if (window[g]) return res(true);
    const prev = document.querySelector(`script[data-di="${g}"]`);
    if (prev) {
      prev.addEventListener('load', () => res(!!window[g]));
      prev.addEventListener('error', () => res(false));
      return;
    }
    const s = document.createElement('script');
    s.src = src;
    s.async = true;
    s.dataset.di = g;
    s.onload = () => res(!!window[g]);
    s.onerror = () => res(false);
    document.head.appendChild(s);
  }))).then(() => ({
    three: !!window.THREE
  }));
  return _diLoad;
}
const DI_ICE = 0xdae8f2,
  DI_AMBER = 0xec9a00;

/* ── Silhouettes ─────────────────────────────────────────────────────────────────────────────
   Normalised roughly to x ∈ [-1, 1]. Polygons and ellipses only: enough to read as the platform
   in a few hundred points, cheap enough to sample on the main thread, and symmetric halves are
   declared once and mirrored. `t` is the swarm's depth (thickness) for that part. */
const mir = pts => pts.map(([x, y]) => [-x, y]);
const poly = (pts, t) => ({
  pts,
  t: t == null ? 0.16 : t
});
const ell = (cx, cy, rx, ry, t) => ({
  cx,
  cy,
  rx,
  ry,
  t: t == null ? 0.16 : t,
  kind: 'ellipse'
});
const DI_SHAPES = {
  /* Fighter jet, plan view, nose up: slim fuselage, swept delta wings, tailplanes. */
  air: (() => {
    const wing = [[0.07, 0.28], [0.92, -0.30], [0.92, -0.46], [0.10, -0.12]];
    const tail = [[0.05, -0.54], [0.40, -0.80], [0.40, -0.92], [0.05, -0.70]];
    return [poly([[0, 1.02], [0.075, 0.52], [0.10, -0.34], [0.07, -0.80], [-0.07, -0.80], [-0.10, -0.34], [-0.075, 0.52]], 0.13), poly(wing, 0.05), poly(mir(wing), 0.05), poly(tail, 0.05), poly(mir(tail), 0.05), poly([[-0.035, -0.62], [0.035, -0.62], [0.05, -0.95], [-0.05, -0.95]], 0.16)];
  })(),
  /* Main battle tank, profile: track run, hull, turret, gun. */
  land: [poly([[-0.97, -0.46], [0.97, -0.46], [0.92, -0.14], [-0.92, -0.14]], 0.34), ell(-0.62, -0.32, 0.11, 0.11, 0.30), ell(-0.21, -0.32, 0.11, 0.11, 0.30), ell(0.21, -0.32, 0.11, 0.11, 0.30), ell(0.62, -0.32, 0.11, 0.11, 0.30), poly([[-0.90, -0.14], [0.90, -0.14], [0.80, 0.18], [-0.82, 0.18]], 0.34), poly([[-0.34, 0.18], [0.34, 0.18], [0.24, 0.48], [-0.26, 0.48]], 0.28), poly([[0.22, 0.28], [1.00, 0.24], [1.00, 0.37], [0.22, 0.41]], 0.09)],
  /* Submarine, profile: pressure hull, sail, stern planes, screw. */
  sea: [ell(0.02, 0, 0.94, 0.21, 0.34), poly([[-0.16, 0.16], [0.16, 0.16], [0.11, 0.46], [-0.12, 0.46]], 0.14), poly([[-0.86, 0.08], [-0.70, 0.08], [-0.78, 0.62], [-0.90, 0.58]], 0.07), poly([[-0.84, -0.06], [-0.62, -0.34], [-0.80, -0.42], [-0.94, -0.12]], 0.07), poly([[0.62, -0.14], [0.80, -0.34], [0.66, -0.40], [0.54, -0.18]], 0.07), poly([[-0.99, -0.05], [-0.90, -0.05], [-0.90, 0.05], [-0.99, 0.05]], 0.20)],
  /* Satellite, head-on: bus, two solar wings, dish on a short mast. */
  space: (() => {
    const panel = [[0.24, -0.24], [0.98, -0.24], [0.98, 0.24], [0.24, 0.24]];
    return [poly([[-0.20, -0.32], [0.20, -0.32], [0.20, 0.30], [-0.20, 0.30]], 0.30), poly(panel, 0.04), poly(mir(panel), 0.04), poly([[-0.025, 0.30], [0.025, 0.30], [0.025, 0.46], [-0.025, 0.46]], 0.05), ell(0, 0.60, 0.26, 0.19, 0.12), poly([[-0.06, -0.32], [0.06, -0.32], [0.10, -0.52], [-0.10, -0.52]], 0.10)];
  })()
};
const diRnd = s => () => {
  s = s + 0x6d2b79f5 | 0;
  let t = Math.imul(s ^ s >>> 15, 1 | s);
  t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
  return ((t ^ t >>> 14) >>> 0) / 4294967296;
};
const diArea = p => {
  let a = 0;
  for (let i = 0, j = p.length - 1; i < p.length; j = i++) a += p[j][0] * p[i][1] - p[i][0] * p[j][1];
  return Math.abs(a) / 2;
};
const diIn = (x, y, p) => {
  let c = false;
  for (let i = 0, j = p.length - 1; i < p.length; j = i++) {
    const [xi, yi] = p[i],
      [xj, yj] = p[j];
    if (yi > y !== yj > y && x < (xj - xi) * (y - yi) / (yj - yi) + xi) c = !c;
  }
  return c;
};

/* Sample a silhouette into a flat [x,y,z,…] buffer, points distributed by part area so the
   swarm's density reads as the platform's mass rather than its outline. Deterministic: the same
   domain always samples the same cloud, so a morph back looks like the same swarm returning. */
const _diCache = {};
function diShape(id, n) {
  const key = id + ':' + n;
  if (_diCache[key]) return _diCache[key];
  const parts = DI_SHAPES[id] || DI_SHAPES.air;
  const rnd = diRnd(1337 + id.length * 977);
  const w = parts.map(p => p.kind === 'ellipse' ? Math.PI * p.rx * p.ry : diArea(p.pts));
  const total = w.reduce((a, b) => a + b, 0);
  const out = new Float32Array(n * 3);
  let k = 0;
  parts.forEach((p, i) => {
    const cnt = i === parts.length - 1 ? n - k : Math.max(1, Math.round(n * w[i] / total));
    let bb = null;
    if (!p.kind) {
      bb = [Infinity, Infinity, -Infinity, -Infinity];
      p.pts.forEach(([x, y]) => {
        bb[0] = Math.min(bb[0], x);
        bb[1] = Math.min(bb[1], y);
        bb[2] = Math.max(bb[2], x);
        bb[3] = Math.max(bb[3], y);
      });
    }
    for (let j = 0; j < cnt && k < n; j++, k++) {
      let x = 0,
        y = 0;
      if (p.kind === 'ellipse') {
        const a = rnd() * Math.PI * 2,
          r = Math.sqrt(rnd());
        x = p.cx + Math.cos(a) * p.rx * r;
        y = p.cy + Math.sin(a) * p.ry * r;
      } else {
        let tries = 0;
        do {
          x = bb[0] + rnd() * (bb[2] - bb[0]);
          y = bb[1] + rnd() * (bb[3] - bb[1]);
          tries++;
        } while (!diIn(x, y, p.pts) && tries < 48);
      }
      out[k * 3] = x;
      out[k * 3 + 1] = y;
      out[k * 3 + 2] = (rnd() - 0.5) * p.t;
    }
  });
  _diCache[key] = out;
  return out;
}

/* Per-domain HUD context around the swarm. The swarm is the subject now, so the grids, rings and
   sweep are quieter than the platform — they place it in a medium, they do not compete with it. */
const DI_PRESETS = {
  air: {
    gridO: 0.20,
    gridY: -1.55,
    ridge: 0.00,
    ringR: 3.10,
    ringTilt: 0.06,
    ringO: 0.16,
    sweepO: 0.40,
    starO: 0.00,
    ping: 0,
    camY: 0.55,
    camZ: 6.5,
    roll: 0.00,
    orbit: 0.18,
    yBase: 0.35,
    ySpread: 1.45,
    ground: 0
  },
  land: {
    gridO: 0.34,
    gridY: -1.05,
    ridge: 1.00,
    ringR: 2.70,
    ringTilt: 0.04,
    ringO: 0.12,
    sweepO: 0.34,
    starO: 0.00,
    ping: 0,
    camY: 0.60,
    camZ: 6.2,
    roll: 0.00,
    orbit: 0.06,
    yBase: -1.05,
    ySpread: 0.00,
    ground: 1
  },
  sea: {
    gridO: 0.26,
    gridY: 1.35,
    ridge: 0.30,
    ringR: 2.30,
    ringTilt: 0.04,
    ringO: 0.30,
    sweepO: 0.18,
    starO: 0.00,
    ping: 1,
    camY: 0.30,
    camZ: 6.3,
    roll: 0.00,
    orbit: 0.10,
    yBase: -0.55,
    ySpread: 1.05,
    ground: 0
  },
  space: {
    gridO: 0.00,
    gridY: -1.05,
    ridge: 0.00,
    ringR: 3.00,
    ringTilt: 0.80,
    ringO: 0.22,
    sweepO: 0.12,
    starO: 1.00,
    ping: 0,
    camY: 0.35,
    camZ: 6.6,
    roll: 0.10,
    orbit: 1.00,
    yBase: 0.00,
    ySpread: 1.95,
    ground: 0
  }
};
const DI_HOT = 7,
  DI_GRID = 12,
  DI_HALF = 4.2,
  DI_SCALE = 2.45;

/* Sprite glyphs for the particle. `box` is the GPU's own default point (no texture at all — the
   cheapest of the three); dot and chevron are tiny canvas textures, drawn once and shared. */
const _diTex = {};
function diTexture(THREE, kind) {
  if (kind === 'box') return null;
  if (_diTex[kind]) return _diTex[kind];
  const S = 64,
    cv = document.createElement('canvas');
  cv.width = S;
  cv.height = S;
  const c = cv.getContext('2d');
  c.fillStyle = '#fff';
  if (kind === 'chevron') {
    c.strokeStyle = '#fff';
    c.lineWidth = S * 0.16;
    c.lineJoin = 'miter';
    c.beginPath();
    c.moveTo(S * 0.14, S * 0.70);
    c.lineTo(S * 0.5, S * 0.24);
    c.lineTo(S * 0.86, S * 0.70);
    c.stroke();
  } else {
    c.beginPath();
    c.arc(S / 2, S / 2, S * 0.42, 0, Math.PI * 2);
    c.fill();
  }
  const tex = new THREE.CanvasTexture(cv);
  tex.needsUpdate = true;
  _diTex[kind] = tex;
  return tex;
}
function diGridGeo(THREE) {
  const step = DI_HALF * 2 / DI_GRID,
    pos = [];
  for (let i = 0; i <= DI_GRID; i++) {
    const p = -DI_HALF + i * step;
    pos.push(-DI_HALF, 0, p, DI_HALF, 0, p, p, 0, -DI_HALF, p, 0, DI_HALF);
  }
  const g = new THREE.BufferGeometry();
  g.setAttribute('position', new THREE.Float32BufferAttribute(pos, 3));
  return g;
}
const diRidge = (x, z) => Math.sin(x * 0.8) * 0.20 + Math.cos(z * 0.62) * 0.24 + Math.sin((x + z) * 0.34) * 0.14;
function MorphScene({
  domainId,
  mode = 'swarm',
  live,
  onFail,
  count = 900,
  glyph = 'dot',
  pSize = 1,
  extent = 1,
  hotRef
}) {
  const host = React.useRef(null);
  const api = React.useRef(null);
  const disposer = React.useRef(null);
  const listeners = React.useRef(null);
  const opts = React.useRef({
    pSize,
    extent
  });
  opts.current.pSize = pSize;
  opts.current.extent = extent;
  const [ready, setReady] = React.useState(false);
  const DI_N = Math.max(120, Math.min(2600, Math.round(count)));
  React.useEffect(() => {
    let dead = false,
      raf = 0,
      io = null,
      ro = null;
    diLoadLibs().then(({
      three
    }) => {
      if (dead || !host.current) return;
      if (!three || !window.THREE) return onFail && onFail();
      const THREE = window.THREE;
      let renderer;
      try {
        renderer = new THREE.WebGLRenderer({
          antialias: true,
          alpha: true,
          powerPreference: 'low-power'
        });
      } catch (e) {
        return onFail && onFail();
      }
      if (!renderer || !renderer.getContext()) return onFail && onFail();
      const el = host.current;
      renderer.setClearAlpha(0);
      renderer.setPixelRatio(Math.min(window.devicePixelRatio || 1, 2));
      el.appendChild(renderer.domElement);
      Object.assign(renderer.domElement.style, {
        display: 'block',
        width: '100%',
        height: '100%'
      });
      const scene = new THREE.Scene();
      const camera = new THREE.PerspectiveCamera(42, 1, 0.1, 100);
      const p = Object.assign({}, DI_PRESETS[domainId] || DI_PRESETS.air);
      const target = Object.assign({}, p);
      const morph = {
        m: 1,
        dur: 1.5
      };
      const gridGeo = diGridGeo(THREE);
      const grid = new THREE.LineSegments(gridGeo, new THREE.LineBasicMaterial({
        color: DI_ICE,
        transparent: true,
        opacity: 0
      }));
      scene.add(grid);
      const gridBase = Float32Array.from(gridGeo.attributes.position.array);
      const rnd = diRnd(7331);
      const jitter = new Float32Array(DI_N * 4);
      for (let i = 0; i < DI_N; i++) {
        jitter[i * 4] = rnd() * Math.PI * 2;
        jitter[i * 4 + 1] = 0.4 + rnd() * 1.2;
        jitter[i * 4 + 2] = rnd();
        jitter[i * 4 + 3] = rnd() * Math.PI * 2;
      }
      let from = diShape(domainId, DI_N),
        to = from;
      let shaped = mode !== 'field';
      const tex = diTexture(THREE, glyph);
      const mk = (n, color, size, op) => {
        const g = new THREE.BufferGeometry();
        g.setAttribute('position', new THREE.Float32BufferAttribute(new Float32Array(n * 3), 3));
        const o = new THREE.Points(g, new THREE.PointsMaterial({
          color,
          size,
          transparent: true,
          opacity: op,
          sizeAttenuation: true,
          map: tex,
          alphaTest: tex ? 0.18 : 0,
          depthWrite: false
        }));
        scene.add(o);
        return o;
      };
      const coldSize = glyph === 'box' ? 0.036 : 0.052,
        hotSize = glyph === 'box' ? 0.062 : 0.086;
      const cold = mk(DI_N - DI_HOT, DI_ICE, coldSize, 0.72);
      const hot = mk(DI_HOT, DI_AMBER, hotSize, 0.95);
      const ringGeo = (() => {
        const pts = [];
        for (let i = 0; i <= 96; i++) {
          const a = i / 96 * Math.PI * 2;
          pts.push(Math.cos(a), 0, Math.sin(a));
        }
        const g = new THREE.BufferGeometry();
        g.setAttribute('position', new THREE.Float32BufferAttribute(pts, 3));
        return g;
      })();
      const rings = [0, 1, 2].map(() => {
        const o = new THREE.Line(ringGeo, new THREE.LineBasicMaterial({
          color: DI_ICE,
          transparent: true,
          opacity: 0
        }));
        scene.add(o);
        return o;
      });
      const sweep = new THREE.Group();
      const sweepLines = [0, 1, 2, 3, 4].map(i => {
        const g = new THREE.BufferGeometry();
        g.setAttribute('position', new THREE.Float32BufferAttribute([0, 0, 0, 1, 0, 0], 3));
        const o = new THREE.Line(g, new THREE.LineBasicMaterial({
          color: DI_AMBER,
          transparent: true,
          opacity: 0
        }));
        o.rotation.y = i * 0.075;
        sweep.add(o);
        return o;
      });
      scene.add(sweep);
      const stars = (() => {
        const n = 260,
          a = new Float32Array(n * 3);
        for (let i = 0; i < n; i++) {
          const th = rnd() * Math.PI * 2,
            ph = Math.acos(2 * rnd() - 1),
            r = 11 + rnd() * 7;
          a[i * 3] = Math.sin(ph) * Math.cos(th) * r;
          a[i * 3 + 1] = Math.cos(ph) * r * 0.6;
          a[i * 3 + 2] = Math.sin(ph) * Math.sin(th) * r;
        }
        const g = new THREE.BufferGeometry();
        g.setAttribute('position', new THREE.Float32BufferAttribute(a, 3));
        const o = new THREE.Points(g, new THREE.PointsMaterial({
          color: DI_ICE,
          size: 0.07,
          transparent: true,
          opacity: 0,
          sizeAttenuation: true
        }));
        scene.add(o);
        return o;
      })();
      const size = () => {
        const w = el.clientWidth || 640,
          h = el.clientHeight || 380;
        renderer.setSize(w, h, false);
        camera.aspect = w / Math.max(1, h);
        camera.updateProjectionMatrix();
      };
      size();
      if (window.ResizeObserver) {
        ro = new ResizeObserver(size);
        ro.observe(el);
      }
      const lean = {
        x: 0,
        y: 0,
        tx: 0,
        ty: 0
      };
      const onMove = e => {
        const r = el.getBoundingClientRect();
        if (!r.width || !r.height) return;
        lean.tx = ((e.clientX - r.left) / r.width - 0.5) * 2;
        lean.ty = ((e.clientY - r.top) / r.height - 0.5) * 2;
        play();
      };
      const onLeave = () => {
        lean.tx = 0;
        lean.ty = 0;
      };
      el.addEventListener('pointermove', onMove);
      el.addEventListener('pointerleave', onLeave);
      const ease = x => 1 - Math.pow(1 - x, 3);
      let last = 0,
        frames = 0;
      const draw = t => {
        frames++;
        /* The clock the frame already has, driving both tweens. dt is clamped so a backgrounded
           tab resuming does not teleport the swarm through its whole morph in one frame. */
        const dt = Math.min(0.05, Math.max(0, t - last));
        last = t;
        if (morph.m < 1) morph.m = Math.min(1, morph.m + dt / morph.dur);
        const k = 1 - Math.pow(0.0015, dt);
        for (const key in target) p[key] += (target[key] - p[key]) * k;
        cold.material.size = coldSize * opts.current.pSize;
        hot.material.size = hotSize * opts.current.pSize;
        const ext = opts.current.extent;
        grid.material.opacity = p.gridO;
        grid.position.y = p.gridY;
        if (p.ridge > 0.001) {
          const arr = gridGeo.attributes.position.array;
          for (let i = 0; i < arr.length; i += 3) arr[i + 1] = p.ridge * diRidge(gridBase[i], gridBase[i + 2]);
          gridGeo.attributes.position.needsUpdate = true;
        } else if (gridGeo.attributes.position.array[1] !== 0) {
          gridGeo.attributes.position.array.set(gridBase);
          gridGeo.attributes.position.needsUpdate = true;
        }

        /* The swarm. Each particle re-forms on its own slightly staggered schedule, so a domain
           change reads as a formation regrouping rather than a crossfade. */
        const ca = cold.geometry.attributes.position.array,
          ha = hot.geometry.attributes.position.array;
        const yaw = shaped ? Math.sin(t * 0.16) * 0.42 + p.roll * t * 0.1 : 0;
        const cy = Math.cos(yaw),
          sy = Math.sin(yaw);
        if (!shaped) {
          /* Free field: every contact keeps its own radius, bearing and rate, and `orbit` decides
             whether the medium reads as drift (air, land, sea) or as a closed orbit (space). */
          for (let i = 0; i < DI_N; i++) {
            const ph = jitter[i * 4],
              sp = jitter[i * 4 + 1],
              rr = jitter[i * 4 + 2],
              yy = jitter[i * 4 + 3] / (Math.PI * 2) * 2 - 1;
            const ang = ph + t * 0.1 * sp * (0.3 + 0.7 * p.orbit);
            const rad = (0.35 + rr * 0.95) * (0.62 + 0.38 * p.orbit) * (p.ringR / 3) * 3 * ext;
            const wob = (1 - p.orbit) * Math.sin(t * 0.5 + ph) * 0.5;
            const x = Math.cos(ang) * rad + wob;
            const z = Math.sin(ang) * rad;
            /* Ground domains do not fly. LAND locks every contact onto the terrain surface — its
               height comes from the same ridge function the grid is displaced by, so a vehicle
               rides the hill rather than hovering over it — and trades vertical wander for a
               slow track along the plane. */
            const y = p.ground > 0.5 ? p.gridY + p.ridge * diRidge(x, z) + 0.045 : p.yBase + yy * p.ySpread * ext + Math.sin(t * 0.7 + ph) * 0.05 + p.orbit * Math.sin(ang) * p.ringTilt * 1.6;
            const tgt = i < DI_HOT ? ha : ca,
              o = (i < DI_HOT ? i : i - DI_HOT) * 3;
            tgt[o] = x;
            tgt[o + 1] = y;
            tgt[o + 2] = z;
          }
          cold.geometry.attributes.position.needsUpdate = true;
          hot.geometry.attributes.position.needsUpdate = true;
        } else for (let i = 0; i < DI_N; i++) {
          const d = jitter[i * 4 + 2] * 0.34;
          const mi = Math.max(0, Math.min(1, (morph.m - d) / (1 - 0.34)));
          const e = ease(mi);
          const k3 = i * 3;
          const bx = from[k3] + (to[k3] - from[k3]) * e;
          const by = from[k3 + 1] + (to[k3 + 1] - from[k3 + 1]) * e;
          const bz = from[k3 + 2] + (to[k3 + 2] - from[k3 + 2]) * e;
          /* Mid-morph the particle bows outward: the formation opens up, then closes on target. */
          const open = Math.sin(mi * Math.PI) * 0.55;
          const ph = jitter[i * 4],
            sp = jitter[i * 4 + 1];
          const jx = Math.sin(t * 0.55 * sp + ph) * 0.020 + Math.cos(ph) * open;
          const jy = Math.cos(t * 0.62 * sp + ph) * 0.020 + Math.sin(ph * 1.7) * open * 0.6;
          const jz = Math.sin(t * 0.48 * sp + jitter[i * 4 + 3]) * 0.020;
          const x = (bx + jx) * DI_SCALE * ext,
            y = (by + jy) * DI_SCALE * ext + (p.ground > 0.5 ? p.gridY + 0.9 : 0),
            z = (bz + jz) * DI_SCALE * ext;
          const tgt = i < DI_HOT ? ha : ca,
            o = (i < DI_HOT ? i : i - DI_HOT) * 3;
          tgt[o] = x * cy + z * sy;
          tgt[o + 1] = y;
          tgt[o + 2] = -x * sy + z * cy;
          if (i === DI_N - 1) {
            cold.geometry.attributes.position.needsUpdate = true;
            hot.geometry.attributes.position.needsUpdate = true;
          }
        }
        rings.forEach((o, i) => {
          const grow = p.ping > 0.01 ? (t * 0.26 + i * 0.34) % 1 : 1;
          const r = p.ringR * (p.ping > 0.01 ? grow : 1 + i * 0.3);
          o.scale.set(r, 1, r);
          o.rotation.x = p.ringTilt * (0.6 + i * 0.4);
          o.rotation.z = p.ringTilt * (i - 1) * 0.5;
          o.position.y = p.gridY + 0.05;
          o.material.opacity = p.ringO * (p.ping > 0.01 ? Math.max(0, 1 - grow) : 1 - i * 0.24);
        });
        sweep.rotation.y = -t * 0.38;
        sweep.position.y = p.gridY + 0.05;
        sweep.rotation.x = p.ringTilt * 0.4;
        sweepLines.forEach((o, i) => {
          o.scale.x = p.ringR * 1.6;
          o.material.opacity = p.sweepO * (1 - i * 0.19);
        });
        stars.material.opacity = p.starO * 0.65;
        stars.rotation.y = t * 0.01;
        lean.x += (lean.tx - lean.x) * (1 - Math.pow(0.002, dt));
        lean.y += (lean.ty - lean.y) * (1 - Math.pow(0.002, dt));
        camera.position.set(Math.sin(t * 0.05) * 0.8 + lean.x * 0.85, p.camY - lean.y * 0.5, p.camZ - Math.abs(lean.x) * 0.18);
        camera.lookAt(lean.x * -0.22, lean.y * 0.16, 0);
        renderer.render(scene, camera);

        /* Publish the amber contacts in normalised screen space: the HUD callout above the canvas
           draws its connector to a real particle, so the annotation is attached to the scene
           rather than floating over it. */
        if (hotRef) {
          const out = [];
          const v = new THREE.Vector3();
          for (let i = 0; i < DI_HOT; i++) {
            v.set(ha[i * 3], ha[i * 3 + 1], ha[i * 3 + 2]).project(camera);
            if (v.z > 1) continue;
            out.push({
              x: (v.x * 0.5 + 0.5) * 100,
              y: (-v.y * 0.5 + 0.5) * 100
            });
          }
          hotRef.current = out;
        }
      };
      let t0 = performance.now() / 1000,
        playing = false;
      const loop = () => {
        if (dead) return;
        draw(performance.now() / 1000 - t0);
        raf = requestAnimationFrame(loop);
      };
      const play = () => {
        if (!playing && live && !dead) {
          playing = true;
          loop();
        }
      };
      const stop = () => {
        playing = false;
        cancelAnimationFrame(raf);
      };
      draw(0);
      setReady(true);
      play();
      if (window.IntersectionObserver) {
        io = new IntersectionObserver(es => {
          if (!es[0]) return;
          es[0].isIntersecting ? play() : stop();
        }, {
          threshold: 0.01
        });
        io.observe(el);
      }
      listeners.current = () => {
        el.removeEventListener('pointermove', onMove);
        el.removeEventListener('pointerleave', onLeave);
      };
      disposer.current = () => {
        [gridGeo, ringGeo, cold.geometry, hot.geometry, stars.geometry].forEach(g => g && g.dispose && g.dispose());
        [grid.material, cold.material, hot.material, stars.material].concat(rings.map(r => r.material), sweepLines.map(l => l.material)).forEach(m => m && m.dispose && m.dispose());
        sweepLines.forEach(l => l.geometry && l.geometry.dispose());
        renderer.dispose();
        if (renderer.forceContextLoss) renderer.forceContextLoss();
      };
      api.current = {
        live,
        draw,
        shapeId: domainId,
        goto(id) {
          if (api.current) api.current.shapeId = id;
          const next = diShape(id, DI_N);
          /* Freeze the current interpolated cloud as the new origin, so a mid-flight change
             starts from where the swarm actually is rather than snapping back. */
          const snap = new Float32Array(DI_N * 3);
          for (let i = 0; i < DI_N * 3; i++) {
            const d = jitter[Math.floor(i / 3) * 4 + 2] * 0.34;
            const e = ease(Math.max(0, Math.min(1, (morph.m - d) / (1 - 0.34))));
            snap[i] = from[i] + (to[i] - from[i]) * e;
          }
          from = snap;
          to = next;
          Object.assign(target, DI_PRESETS[id] || DI_PRESETS.air);
          if (!live) {
            morph.m = 1;
            Object.assign(p, target);
            draw(last);
            return;
          }
          morph.m = 0;
          play();
          const seen = frames;
          setTimeout(() => {
            if (dead || frames !== seen) return;
            morph.m = 1;
            Object.assign(p, target);
            draw(last);
          }, 420);
        }
      };
    });
    return () => {
      dead = true;
      cancelAnimationFrame(raf);
      if (io) io.disconnect();
      if (ro) ro.disconnect();
      if (listeners.current) {
        listeners.current();
        listeners.current = null;
      }
      if (disposer.current) {
        try {
          disposer.current();
        } catch (e) {}
        disposer.current = null;
      }
      const el = host.current;
      if (el) while (el.firstChild) el.removeChild(el.firstChild);
      api.current = null;
    };
  }, []);
  React.useEffect(() => {
    if (api.current && api.current.shapeId !== domainId) api.current.goto(domainId);
  }, [domainId, ready]);
  return /*#__PURE__*/React.createElement("div", {
    ref: host,
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      inset: 0
    }
  });
}

/* The light instrument, and the fallback for every reason the 3D one cannot run: the SAME
   silhouettes, drawn as a drifting 2D swarm under a scanning beam. Canvas, no dependencies. */
function SwarmCanvas({
  domainId,
  live,
  dense = false
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    const cv = ref.current;
    if (!cv) return;
    const ctx = cv.getContext('2d');
    if (!ctx) return;
    let raf = 0,
      dead = false,
      io = null,
      ro = null,
      playing = false;
    const n = dense ? 320 : 620;
    const pts = diShape(domainId, n);
    const rnd = diRnd(99);
    const ph = new Float32Array(n * 2);
    for (let i = 0; i < n; i++) {
      ph[i * 2] = rnd() * Math.PI * 2;
      ph[i * 2 + 1] = 0.4 + rnd() * 1.1;
    }
    const size = () => {
      const dpr = Math.min(window.devicePixelRatio || 1, 2);
      cv.width = Math.max(1, cv.clientWidth * dpr);
      cv.height = Math.max(1, cv.clientHeight * dpr);
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    };
    size();
    if (window.ResizeObserver) {
      ro = new ResizeObserver(size);
      ro.observe(cv);
    }
    const t0 = performance.now();
    const draw = () => {
      const w = cv.clientWidth,
        h = cv.clientHeight,
        cx = w / 2,
        cy = h / 2;
      const R = Math.min(w * 0.44, h * 0.62),
        t = (performance.now() - t0) / 1000;
      ctx.clearRect(0, 0, w, h);
      ctx.strokeStyle = 'rgba(218,232,242,0.10)';
      ctx.lineWidth = 1;
      for (let i = 1; i <= 2; i++) {
        ctx.beginPath();
        ctx.ellipse(cx, cy, R * (0.72 + i * 0.24), R * (0.72 + i * 0.24) * 0.34, 0, 0, Math.PI * 2);
        ctx.stroke();
      }
      const beamX = cx + Math.sin(t * 0.5) * R * 1.05;
      const g = ctx.createLinearGradient(beamX - 26, 0, beamX + 26, 0);
      g.addColorStop(0, 'rgba(236,154,0,0)');
      g.addColorStop(0.5, 'rgba(236,154,0,0.20)');
      g.addColorStop(1, 'rgba(236,154,0,0)');
      ctx.fillStyle = g;
      ctx.fillRect(beamX - 26, 0, 52, h);
      const yaw = Math.sin(t * 0.16) * 0.34;
      for (let i = 0; i < n; i++) {
        const k = i * 3;
        const jx = Math.sin(t * 0.5 * ph[i * 2 + 1] + ph[i * 2]) * 0.016;
        const jy = Math.cos(t * 0.58 * ph[i * 2 + 1] + ph[i * 2]) * 0.016;
        const x = (pts[k] + jx) * Math.cos(yaw) + pts[k + 2] * Math.sin(yaw);
        const y = pts[k + 1] + jy;
        const px = cx + x * R,
          py = cy - y * R;
        const lit = Math.max(0, 1 - Math.abs(px - beamX) / 30);
        const hotP = i < (dense ? 3 : 6);
        ctx.beginPath();
        ctx.arc(px, py, hotP ? 1.9 : 1.15, 0, Math.PI * 2);
        ctx.fillStyle = hotP ? `rgba(236,154,0,${0.6 + lit * 0.4})` : `rgba(218,232,242,${0.34 + lit * 0.55})`;
        ctx.fill();
      }
    };
    const loop = () => {
      if (dead) return;
      draw();
      raf = requestAnimationFrame(loop);
    };
    draw();
    const play = () => {
      if (!playing && live) {
        playing = true;
        loop();
      }
    };
    const stop = () => {
      playing = false;
      cancelAnimationFrame(raf);
    };
    if (window.IntersectionObserver) {
      io = new IntersectionObserver(es => {
        es[0] && es[0].isIntersecting ? play() : stop();
      }, {
        threshold: 0.05
      });
      io.observe(cv);
    } else play();
    return () => {
      dead = true;
      cancelAnimationFrame(raf);
      if (io) io.disconnect();
      if (ro) ro.disconnect();
    };
  }, [domainId, live, dense]);
  return /*#__PURE__*/React.createElement("canvas", {
    ref: ref,
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      inset: 0,
      width: '100%',
      height: '100%',
      display: 'block'
    }
  });
}

/* Corner brackets + a slow scanline: the page's own HUD vocabulary, reused so the instrument
   reads as one more chamfered frame rather than an embedded widget. */
function DomainInstrument({
  domainId,
  scene = 'field',
  plate,
  live = true,
  dense = false,
  count = 900,
  glyph = 'dot',
  pSize = 1,
  extent = 1,
  corners = true,
  hotRef,
  children,
  style
}) {
  const reduced = window.vvReduced ? window.vvReduced() : false;
  const on = live && !reduced;
  const [failed, setFailed] = React.useState(false);
  const three = (scene === 'swarm' || scene === 'field') && !failed;
  const mode = scene === 'off' ? 'off' : three ? 'three' : 'radar';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      overflow: 'hidden',
      background: 'radial-gradient(120% 90% at 50% 34%, rgba(40,129,181,0.10) 0%, rgba(0,17,33,0) 62%)',
      ...style
    }
  }, plate && mode === 'off' ? /*#__PURE__*/React.createElement("img", {
    src: plate,
    alt: "",
    style: {
      position: 'absolute',
      inset: 0,
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      opacity: 0.34,
      filter: 'grayscale(1)',
      maskImage: 'radial-gradient(circle closest-side, #000 0%, rgba(0,0,0,0.5) 42%, rgba(0,0,0,0) 72%)',
      WebkitMaskImage: 'radial-gradient(circle closest-side, #000 0%, rgba(0,0,0,0.5) 42%, rgba(0,0,0,0) 72%)'
    }
  }) : null, mode === 'three' ? /*#__PURE__*/React.createElement(MorphScene, {
    key: scene + ':' + count + ':' + glyph,
    domainId: domainId,
    mode: scene,
    live: on,
    count: count,
    glyph: glyph,
    pSize: pSize,
    extent: extent,
    hotRef: hotRef,
    onFail: () => setFailed(true)
  }) : null, mode === 'radar' ? /*#__PURE__*/React.createElement(SwarmCanvas, {
    domainId: domainId,
    live: on,
    dense: dense
  }) : null, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      inset: 0,
      background: 'repeating-linear-gradient(to bottom, rgba(218,232,242,0.035) 0 1px, rgba(0,0,0,0) 1px 3px)',
      opacity: 0.6,
      pointerEvents: 'none'
    }
  }), children, corners ? [['top', 'left'], ['top', 'right'], ['bottom', 'left'], ['bottom', 'right']].map(([v, h]) => /*#__PURE__*/React.createElement("span", {
    key: v + h,
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      [v]: 12,
      [h]: 12,
      width: 12,
      height: 12,
      [`border${v === 'top' ? 'Top' : 'Bottom'}`]: '1px solid var(--vv-amber)',
      [`border${h === 'left' ? 'Left' : 'Right'}`]: '1px solid var(--vv-amber)',
      opacity: 0.62,
      pointerEvents: 'none',
      zIndex: 4
    }
  })) : null);
}
Object.assign(window, {
  DomainInstrument,
  SwarmCanvas,
  DI_PRESETS,
  diShape
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/DomainInstrument.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/HomeCapabilities.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const DS_C = () => window.VivumAIDesignSystem_b2be15;
const CAP_ITEMS = [{
  n: '01',
  icon: 'microchip',
  title: 'Off-the-Shelf Hardware',
  body: 'CPUs and microcontrollers already in the platform — no accelerator required.'
}, {
  n: '02',
  icon: 'memory',
  title: 'Reconfigurable Silicon',
  body: 'FPGAs and custom ASICs, with models fine-tuned to each device.'
}, {
  n: '03',
  icon: 'drone',
  title: 'Uncrewed Systems',
  body: 'Evolving uncrewed systems into fully autonomous entities.'
}];
const CAP_TWEAKS = /*EDITMODE-BEGIN*/{
  "capLayout": "spine",
  "capIconTone": "ice",
  "capIndex": false,
  "capCta": true,
  "capSticky": false
} /*EDITMODE-END*/;
function useNarrow(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}

/* The section container hits its max width at 1560px of viewport (1400 cap + 2 * 80 gutter),
   the same threshold SectionFrame opens its corner cut on. Past it the icon column steps up to
   match the copy it labels instead of staying a small glyph beside a big block. */
const WIDE_MQ = 1560;
function useMinWidth(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(min-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}

/* Ledger: hairline-ruled rows, index in mono. The rule set carries the eye down the
   column, so the rows need no fill of their own until hovered. */
function LedgerRow({
  item,
  tone,
  showIndex,
  last,
  fluid,
  wide,
  mobile,
  index = 0
}) {
  const {
    Icon
  } = DS_C();
  const [hot, setHot] = React.useState(false);
  return /*#__PURE__*/React.createElement(Reveal, {
    variant: "up",
    delay: index * 110,
    dur: 560,
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      display: 'grid',
      gridTemplateColumns: 'auto 1fr',
      columnGap: wide ? 30 : 22,
      alignItems: wide ? 'center' : 'start',
      padding: '26px 20px 28px 0',
      borderTop: '1px solid var(--line-hairline)',
      borderBottom: last ? '1px solid var(--line-hairline)' : 'none',
      background: hot ? 'var(--vv-ice-04, rgba(218,232,242,0.04))' : 'transparent',
      transition: 'background var(--dur-fast) var(--ease-out)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      justifyContent: 'center',
      gap: 12,
      paddingTop: wide ? 0 : 2,
      width: wide ? 64 : undefined,
      alignSelf: wide ? 'stretch' : undefined
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: item.icon,
    size: wide ? 42 : 26,
    color: tone
  }), showIndex ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      letterSpacing: '0.08em',
      color: hot ? 'var(--vv-amber)' : 'var(--text-caption, var(--text-secondary))'
    }
  }, item.n) : null), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: mobile ? 17 : 20,
      fontWeight: 600,
      letterSpacing: '-0.01em',
      color: 'var(--vv-ice)'
    }
  }, item.title), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 14.5,
      lineHeight: 1.6,
      color: 'var(--text-secondary)',
      maxWidth: fluid ? '68ch' : wide ? 600 : 460
    }
  }, item.body)));
}

/* Plates: each capability on its own chamfered card, matching the Bezel corner language.
   The frame is a Bezel rather than a clip-pathed border: clip-path slices a real 1px border
   off at the diagonals, which is what left the corners looking cut open. */
function CapPlate({
  item,
  tone,
  showIndex,
  fluid,
  wide,
  chamferFill = 'rgba(218,232,242,0.02)',
  mobile,
  index = 0
}) {
  const {
    Icon,
    Bezel
  } = DS_C();
  const [hot, setHot] = React.useState(false);
  return /*#__PURE__*/React.createElement(Reveal, {
    variant: "frame",
    delay: (fluid ? 0 : 640) + index * 170,
    dur: 620,
    style: {
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement(Bezel, {
    accent: false,
    chamfer: 16,
    weight: 1,
    pad: "26px",
    color: hot ? 'var(--vv-ice-14)' : 'var(--line-hairline)',
    fill: hot ? 'rgba(218,232,242,0.05)' : chamferFill,
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      transition: 'none',
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'auto minmax(0, 1fr)',
      columnGap: wide ? 28 : 20,
      alignItems: wide ? 'center' : 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      justifyContent: 'center',
      gap: 10,
      paddingTop: wide ? 0 : 2,
      width: wide ? 60 : 30,
      alignSelf: wide ? 'stretch' : undefined
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: item.icon,
    size: wide ? 40 : 26,
    color: tone
  }), showIndex ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      letterSpacing: '0.08em',
      color: 'var(--text-secondary)'
    }
  }, item.n) : null), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: mobile ? 17 : 20,
      fontWeight: 600,
      letterSpacing: '-0.01em',
      color: 'var(--vv-ice)'
    }
  }, item.title), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 14.5,
      lineHeight: 1.6,
      color: 'var(--text-secondary)',
      maxWidth: fluid ? '68ch' : wide ? 600 : 460
    }
  }, item.body)))));
}

/* Spine: a single vertical rail with the icons sitting on it — reads as one system with
   three stages rather than three unrelated cards. The rail is a flat hairline, the same
   weight as every other border on the page: an amber gradient down it made the rail itself
   the loudest thing in the section, ahead of the copy it is only there to connect. */
function SpineList({
  tone,
  showIndex,
  fluid,
  wide,
  mobile
}) {
  const {
    Icon
  } = DS_C();
  const box = wide ? 96 : 64,
    rail = wide ? 132 : 92;
  if (mobile) {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 28
      }
    }, CAP_ITEMS.map((m, i) => /*#__PURE__*/React.createElement(Reveal, {
      key: m.title,
      variant: "side",
      delay: i * 120,
      dur: 520,
      style: {
        display: 'grid',
        gridTemplateColumns: 'auto 1fr',
        columnGap: 16,
        alignItems: 'start'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        width: 44,
        height: 44,
        flex: 'none',
        border: '1px solid var(--line-hairline)',
        background: 'var(--surface-canvas)'
      }
    }, /*#__PURE__*/React.createElement(Icon, {
      name: m.icon,
      size: 26,
      color: tone
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column'
      }
    }, showIndex ? /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: 'var(--font-mono)',
        fontSize: 11,
        letterSpacing: '0.08em',
        color: 'var(--text-secondary)'
      }
    }, m.n) : null, /*#__PURE__*/React.createElement("h3", {
      style: {
        margin: 0,
        fontSize: 17,
        fontWeight: 600,
        letterSpacing: '-0.01em',
        lineHeight: 1.3,
        color: 'var(--vv-ice)'
      }
    }, m.title), /*#__PURE__*/React.createElement("p", {
      style: {
        margin: 0,
        fontSize: 14.5,
        lineHeight: 1.6,
        color: 'var(--text-secondary)',
        textWrap: 'pretty'
      }
    }, m.body)))));
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      paddingLeft: rail
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "fade",
    dur: 900,
    style: {
      position: 'absolute',
      left: box / 2,
      top: 22,
      bottom: 26,
      width: 1,
      background: 'var(--line-hairline)'
    }
  }), CAP_ITEMS.map((m, i) =>
  /*#__PURE__*/
  /* Row gap is a MARGIN, not padding: the icon box is absolutely positioned against
     this row, so padding would put the gap inside its containing block and `top: 50%`
     would centre on copy-plus-gap — dropping the icon ~20px below its copy on every row
     but the last, and pushing the rail's start out above the first box. */
  React.createElement(Reveal, {
    key: m.title,
    variant: "side",
    delay: i * 130,
    dur: 560,
    style: {
      position: 'relative',
      marginBottom: i === CAP_ITEMS.length - 1 ? 0 : 40
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: -rail,
      top: wide ? '50%' : 0,
      transform: wide ? 'translateY(-50%)' : undefined,
      width: box,
      height: box,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      border: '1px solid var(--line-hairline)',
      background: 'var(--surface-canvas)'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: m.icon,
    size: wide ? 46 : 30,
    color: tone
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: mobile ? 0 : 10,
      paddingTop: wide ? 8 : 0,
      minHeight: box
    }
  }, showIndex ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      letterSpacing: '0.08em',
      color: 'var(--text-secondary)'
    }
  }, m.n) : null, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: mobile ? 17 : 20,
      fontWeight: 600,
      letterSpacing: '-0.01em',
      lineHeight: 1.3,
      color: 'var(--vv-ice)'
    }
  }, m.title), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 14.5,
      lineHeight: 1.6,
      color: 'var(--text-secondary)',
      textWrap: 'pretty',
      maxWidth: fluid ? '68ch' : wide ? 340 : 360
    }
  }, m.body)))));
}
function CapCta({
  onNavigate
}) {
  const [hot, setHot] = React.useState(false);
  return /*#__PURE__*/React.createElement("a", {
    href: "#/autonomy",
    onClick: e => {
      e.preventDefault();
      onNavigate && onNavigate('/autonomy');
    },
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 9,
      alignSelf: 'flex-start',
      paddingTop: 10,
      fontSize: 'clamp(15px, 1.2vw, 17px)',
      fontWeight: 400,
      lineHeight: 1,
      textDecoration: 'none',
      whiteSpace: 'nowrap',
      color: hot ? 'var(--vv-amber)' : 'var(--vv-graphite)',
      transition: 'color 160ms ease'
    }
  }, "Explore autonomy", /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      fontSize: '1.05em',
      lineHeight: 1,
      transform: hot ? 'translateX(3px)' : 'none',
      transition: 'transform 160ms ease'
    }
  }, "\u2192"));
}

/* Copy geometry is SectionFrame's job now — padding tokens, the copy cap, and the rule that
   the copy sits flush to the left padding. That last one is why this headline and the
   Biological Intelligence headline above it start at the same x. */

function CapabilitiesSection({
  extraTweaks,
  onNavigate,
  chamferFill = 'rgba(218,232,242,0.02)'
}) {
  const {
    GradientField,
    Eyebrow,
    SectionFrame
  } = DS_C();
  const [t, setTweak] = useTweaks(CAP_TWEAKS);
  const stacked = useNarrow(820);
  const mobile = useNarrow(640);
  const tablet = useNarrow(1024) && !mobile;
  const wide = useMinWidth(WIDE_MQ);
  const tone = t.capIconTone === 'ice' ? 'var(--vv-ice)' : 'var(--vv-amber)';
  const showIndex = t.capIndex === true;
  const right = t.capLayout === 'plates' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridAutoRows: '1fr',
      gap: 16
    }
  }, CAP_ITEMS.map((m, i) => /*#__PURE__*/React.createElement(CapPlate, {
    key: m.title,
    index: i,
    item: m,
    tone: tone,
    showIndex: showIndex,
    fluid: stacked,
    wide: wide,
    chamferFill: chamferFill,
    mobile: mobile
  }))) : t.capLayout === 'spine' ? /*#__PURE__*/React.createElement(SpineList, {
    tone: tone,
    showIndex: showIndex,
    fluid: stacked,
    wide: wide,
    mobile: mobile || tablet
  }) : /*#__PURE__*/React.createElement("div", null, CAP_ITEMS.map((m, i) => /*#__PURE__*/React.createElement(LedgerRow, {
    key: m.title,
    index: i,
    item: m,
    tone: tone,
    showIndex: showIndex,
    fluid: stacked,
    wide: wide,
    last: i === CAP_ITEMS.length - 1,
    mobile: mobile
  })));
  const copy = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Autonomous Solutions",
    speed: 26,
    delay: 260
  })), /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 760,
    delay: 300,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Built for the Mission"), " and Tuned to the Hardware You Field"), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 480,
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      textWrap: 'pretty'
    }
  }, "Autonomous capabilities evolved to integrate seamlessly with your existing systems, from off-the-shelf hardware to cutting-edge architectures."), t.capCta && !mobile ? /*#__PURE__*/React.createElement(Reveal, {
    variant: "side",
    delay: 1150,
    dur: 480
  }, /*#__PURE__*/React.createElement(CapCta, {
    onNavigate: onNavigate
  })) : null);
  return /*#__PURE__*/ /* field="none": was "base" — the bloom is off, the field kept for future use. */React.createElement(GradientField, {
    field: "none",
    pad: mobile ? 'clamp(64px, 15vw, 96px) var(--gutter-site)' : 'clamp(40px, 4.2vw, 72px) var(--gutter-site)',
    as: "section",
    id: "mission"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, /*#__PURE__*/React.createElement(RevealGroup, {
    on: stacked
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : `minmax(min(100%, 380px), ${wide ? '0.76fr' : '0.95fr'}) minmax(0, 0.55fr)`,
      columnGap: wide ? 'clamp(20px, 1.6vw, 32px)' : 'clamp(28px, 2.6vw, 48px)',
      rowGap: 'clamp(36px, 5vw, 56px)',
      alignItems: 'stretch'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      alignSelf: t.capSticky && !stacked ? 'start' : 'center',
      ...(t.capSticky && !stacked ? {
        position: 'sticky',
        top: 'clamp(88px, 12vh, 132px)'
      } : null)
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: mobile ? 'up' : 'frame',
    dur: 640
  }, /*#__PURE__*/React.createElement(SectionFrame, _extends({
    copyMax: "100%",
    stacked: stacked,
    contentStyle: {
      maxWidth: '100%'
    },
    fill: mobile ? 'transparent' : chamferFill
  }, mobile ? {
    chamfer: 0,
    color: 'transparent',
    blur: 0,
    accent: false,
    padX: '0px',
    padY: '0px'
  } : tablet ? {
    padX: 'clamp(48px, 6vw, 96px)',
    padY: 'clamp(48px, 6vw, 96px)'
  } : null), copy))), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: stacked ? '100%' : wide ? 520 : 420,
      width: '100%',
      justifySelf: stacked ? 'start' : 'end',
      alignSelf: 'center'
    }
  }, /*#__PURE__*/React.createElement(RevealSeq, {
    ms: stacked ? 780 : 0
  }, right))), t.capCta && mobile ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'clamp(24px, 5vw, 36px)'
    }
  }, /*#__PURE__*/React.createElement(RevealSeq, {
    ms: 1450
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "side",
    dur: 480
  }, /*#__PURE__*/React.createElement(CapCta, {
    onNavigate: onNavigate
  })))) : null)), /*#__PURE__*/React.createElement(TweaksPanel, null, extraTweaks));
}
Object.assign(window, {
  CapabilitiesSection
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/HomeCapabilities.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/HomeComparison.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const DS = () => window.VivumAIDesignSystem_b2be15;
const ROWS = [{
  dim: 'Learning Paradigm',
  ours: {
    title: 'Dynamic Learning',
    body: 'Dynamic Learning, sometimes referred to as Liquid Learning, enables AI models to continuously learn and adapt in real-time, without the need for offline retraining or simulations. Leverages techniques like Liquid Time-Constant Networks (LTCs), Continuous Time Recurrent Neural Networks (CTRNNs), and reservoir models to create highly adaptive, efficient systems.'
  },
  theirs: {
    title: 'Deep and Reinforcement Learning',
    body: 'Deep learning and reinforcement learning, the most widely-used approaches in modern AI, train large neural networks on vast datasets. They often require extensive offline training (and simulation) and lack real-time adaptability, making them less suitable for dynamic environments. Updating the models with new data typically involves time-consuming and resource-intensive retraining.'
  }
}, {
  dim: 'Computational Basis',
  ours: {
    title: 'Actual Neural Circuitry',
    body: 'Bio-inspired algorithms that mimic the principles of natural evolution and the brain’s plasticity.'
  },
  theirs: {
    title: 'Synthetic Neural Networks',
    body: 'Artificial networks composed of interconnected nodes, organized into hierarchical layers, that learn from data.'
  }
}, {
  dim: 'Data Processing',
  ours: {
    title: 'Continuous and Dynamic',
    body: 'Online learning from high-velocity, high-volume data streams in real-time. Evolutionary AI is designed to process data continuously, updating its models without the need for batch processing or offline training.'
  },
  theirs: {
    title: 'Discrete and Static',
    body: 'Offline training on large, static datasets that have been carefully curated and annotated. Deep learning models are extremely data-hungry, often requiring millions of labeled examples to achieve state-of-the-art performance. Once trained, these models can be deployed for efficient inference on new data but struggle to adapt to changing environments.'
  }
}, {
  dim: 'Explainability',
  ours: {
    title: 'Transparent and Interpretable',
    body: 'Evolutionary AI offers greater transparency and interpretability through techniques like rule extraction, decision trees, and attention-gated routing. These mechanisms provide human-readable explanations for the model’s predictions, making it easier to debug, audit, and trust. Crucial for high-stakes applications in autonomous systems and robotics.'
  },
  theirs: {
    title: 'Black Box Models',
    body: 'Conventional deep learning models are notoriously opaque and difficult to interpret, often described as “black boxes.” Due to their complex, nonlinear structure, it can be challenging to trace how specific inputs lead to particular outputs. This lack of explainability hinders adoption in mission-critical applications where accountability and transparency are paramount.'
  }
}, {
  dim: 'Key Advantages',
  ours: {
    title: 'Adaptability, Efficiency, Real-Time Learning',
    body: 'Evolutionary AI excels in dynamic environments, continuously learning and adapting to new data patterns without the need for retraining. Its brain-inspired computational basis enables highly efficient processing, making it suitable for edge devices and energy-constrained applications. Ideal for autonomous systems, robotics, and sensor fusion.'
  },
  theirs: {
    title: 'High Performance on Specific Tasks',
    body: 'Deep learning has achieved state-of-the-art results on a wide range of tasks, particularly in areas like computer vision, natural language processing, and pattern recognition. Well-established frameworks, tools, and large pre-trained models are readily available, making it easier to develop and deploy deep learning solutions for specific applications.'
  }
}, {
  dim: 'Use Cases',
  ours: {
    list: ['Autonomous Vehicles', 'Advanced Robotics', 'Edge Computing', 'Sensor Fusion', 'Anomaly Detection', 'Predictive Maintenance', 'Real-Time Decision Making', 'High Frequency / Volume Networks']
  },
  theirs: {
    list: ['Image and Speech Recognition', 'Natural Language Processing', 'Recommendation Systems', 'Fraud Detection', 'Medical Diagnosis', 'Predictive Analytics']
  }
}];
function useMaxWidth(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}
const LABEL = {
  fontSize: 12.5,
  fontWeight: 600,
  letterSpacing: 'var(--track-label)',
  textTransform: 'uppercase'
};
const MICRO = {
  fontSize: 11,
  fontWeight: 600,
  letterSpacing: '0.12em',
  textTransform: 'uppercase',
  lineHeight: 1
};

/* How the Evolutionary AI column is set apart from Conventional AI — every option stays inside
   the page's existing amber/hairline vocabulary, nothing invented. `none`/`tint` were already
   here; `rule`/`glow`/`corner` are the new subtle alternatives, all still just a border color,
   a low-alpha wash, or a tiny label chip — never a solid fill. */
const OURS_EMPHASIS = {
  none: {
    edge: 'var(--line-hairline)',
    fill: 'transparent'
  },
  rule: {
    edge: 'var(--vv-amber-24)',
    fill: 'transparent'
  },
  glow: {
    edge: 'var(--line-hairline)',
    fill: 'radial-gradient(130% 160% at 0% 0%, var(--vv-amber-08), transparent 62%)'
  },
  corner: {
    edge: 'var(--line-hairline)',
    fill: 'transparent',
    chip: true
  }
};
/* Tint is the one emphasis with user-adjustable color/opacity (Tweaks), so its fill is built
   from raw rgb + a live alpha instead of a fixed token like the other variants. */
const TINT_RGB = {
  blue: '40,129,181',
  amber: '236,154,0',
  ice: '218,232,242',
  green: '34,166,110'
};
function oursStyle(emphasis, tintColor = 'blue', tintOpacity = 0.08) {
  if (emphasis === 'tint') {
    const rgb = TINT_RGB[tintColor] || TINT_RGB.blue;
    return {
      edge: '1px solid var(--line-hairline)',
      fill: `rgba(${rgb}, ${tintOpacity})`,
      chip: false
    };
  }
  const e = OURS_EMPHASIS[emphasis] || OURS_EMPHASIS.none;
  return {
    edge: `1px solid ${e.edge}`,
    fill: e.fill,
    chip: !!e.chip
  };
}

/* Which side you are reading is carried by an amber tick and text color, not by a filled cell —
   the same way the rest of the page marks emphasis. `marker` swaps the tick for a small amber
   icon (checkmark family) instead — same role, same color, different glyph. */
function Marker({
  marker,
  size = 14
}) {
  if (!marker || marker === 'stick') {
    return /*#__PURE__*/React.createElement("span", {
      style: {
        width: 18,
        height: 2,
        background: 'var(--vv-amber)',
        display: 'block',
        flex: '0 0 auto'
      }
    });
  }
  const {
    Icon
  } = DS();
  return /*#__PURE__*/React.createElement(Icon, {
    name: marker,
    size: size,
    color: "var(--vv-amber)",
    style: {
      flex: '0 0 auto'
    }
  });
}
function SideLabel({
  ours,
  chip,
  marker,
  mobile
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 9,
      ...(ours && chip ? {
        padding: '5px 12px 5px 8px',
        margin: '-5px 0 -5px -8px',
        borderRadius: 999,
        background: 'var(--vv-amber-08)'
      } : null)
    }
  }, ours ? /*#__PURE__*/React.createElement(Marker, {
    marker: marker,
    size: 14
  }) : mobile ? /*#__PURE__*/React.createElement("span", {
    style: {
      width: 18,
      height: 2,
      background: 'var(--text-label)',
      display: 'block',
      flex: '0 0 auto'
    }
  }) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      ...MICRO,
      color: ours ? 'var(--vv-ice)' : 'var(--text-label)'
    }
  }, ours ? 'Evolutionary AI' : 'Conventional AI'));
}
function Chips({
  items,
  ours
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, items.map(t => /*#__PURE__*/React.createElement("span", {
    key: t,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      minHeight: 26,
      padding: '5px 10px',
      lineHeight: 1.3,
      borderRadius: 'var(--radius-sm)',
      border: '1px solid var(--line-hairline)',
      fontSize: 12.5,
      color: ours ? 'var(--vv-ice-82)' : 'var(--text-secondary)'
    }
  }, t)));
}

/* One cell's content. Every border in the table is the same hairline — which side you are
   reading is carried by the header label and the text color, nothing structural. */
function Side({
  data,
  ours,
  marker,
  hideMarker,
  mobile
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: mobile ? 10 : 12
    }
  }, data.title ? /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      fontSize: mobile ? 14.5 : 18,
      fontWeight: 700,
      letterSpacing: '-0.01em',
      lineHeight: 1.3,
      color: ours ? 'var(--vv-ice)' : 'var(--text-secondary)',
      textWrap: 'pretty'
    }
  }, ours && !hideMarker ? /*#__PURE__*/React.createElement(Marker, {
    marker: marker,
    size: 13
  }) : null, /*#__PURE__*/React.createElement("span", null, data.title)) : null, data.body ? /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: mobile ? 14.5 : 14,
      lineHeight: 1.6,
      color: 'var(--text-secondary)',
      textWrap: 'pretty'
    }
  }, data.body) : null, data.list ? /*#__PURE__*/React.createElement(Chips, {
    items: data.list,
    ours: ours
  }) : null);
}

/* ── Ledger: the table as a spec sheet. No fills anywhere; hairlines do the structure and one
   continuous amber rule marks the column that matters. ─────────────────────────────────────── */
function Ledger({
  emphasis,
  tintColor,
  tintOpacity,
  marker,
  sticky,
  stacked,
  mobile
}) {
  const {
    edge: oursEdge,
    chip
  } = oursStyle(emphasis, tintColor, tintOpacity);
  const edgeStyle = mobile ? {
    borderLeft: 'none',
    paddingLeft: 0
  } : {};
  const head = {
    ...LABEL,
    padding: '22px 0 20px',
    borderBottom: '1px solid var(--line-hairline)',
    display: 'flex',
    alignItems: 'center',
    gap: 12,
    ...(sticky ? {
      position: 'sticky',
      top: 0,
      zIndex: 2,
      background: 'var(--vv-navy-72)',
      backdropFilter: 'blur(10px)'
    } : null)
  };
  const pad = 'clamp(20px, 2.2vw, 32px)';
  const cell = {
    padding: `28px 0`,
    borderBottom: '1px solid var(--line-hairline)'
  };
  if (stacked) {
    const dimStyle = mobile ? {
      fontSize: 15.5,
      fontWeight: 700,
      letterSpacing: '-0.005em',
      textTransform: 'none',
      color: 'var(--vv-ice)'
    } : {
      ...LABEL,
      color: 'var(--text-label)'
    };
    return /*#__PURE__*/React.createElement("div", {
      style: {
        borderTop: '1px solid var(--line-hairline)'
      }
    }, ROWS.map((r, i) => /*#__PURE__*/React.createElement(Reveal, {
      key: r.dim,
      variant: "up",
      delay: i * 90,
      dur: 560,
      style: {
        padding: mobile ? '36px 0' : '28px 0',
        borderBottom: '1px solid var(--line-hairline)',
        display: 'flex',
        flexDirection: 'column',
        gap: mobile ? 22 : 20
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: dimStyle
    }, r.dim), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: mobile ? 10 : 12,
        paddingLeft: 16,
        borderLeft: oursEdge,
        ...edgeStyle
      }
    }, /*#__PURE__*/React.createElement(SideLabel, {
      ours: true,
      chip: chip,
      marker: marker
    }), /*#__PURE__*/React.createElement(Side, {
      data: r.ours,
      ours: true,
      marker: marker,
      hideMarker: mobile,
      mobile: mobile
    })), "            ", /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: mobile ? 10 : 12,
        paddingLeft: 16,
        borderLeft: '1px solid var(--line-hairline)',
        marginTop: mobile ? 14 : 0,
        ...edgeStyle
      }
    }, /*#__PURE__*/React.createElement(SideLabel, {
      mobile: mobile
    }), /*#__PURE__*/React.createElement(Side, {
      data: r.theirs,
      mobile: mobile
    })))));
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'minmax(140px, 232px) 1fr 1fr',
      borderTop: '1px solid var(--line-hairline)'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "up",
    dur: 460,
    delay: 0,
    style: {
      ...head,
      paddingRight: pad
    }
  }), /*#__PURE__*/React.createElement(Reveal, {
    variant: "up",
    dur: 460,
    delay: 65,
    style: {
      ...head,
      paddingLeft: pad,
      paddingRight: pad,
      borderLeft: oursEdge,
      ...edgeStyle
    }
  }, /*#__PURE__*/React.createElement(SideLabel, {
    ours: true,
    chip: chip,
    marker: marker
  })), /*#__PURE__*/React.createElement(Reveal, {
    variant: "up",
    dur: 460,
    delay: 130,
    style: {
      ...head,
      paddingLeft: pad,
      borderLeft: '1px solid var(--line-hairline)',
      ...edgeStyle
    }
  }, /*#__PURE__*/React.createElement(SideLabel, null)), ROWS.map((r, i) => [/*#__PURE__*/React.createElement(Reveal, {
    key: `${r.dim}-d`,
    variant: "up",
    dur: 480,
    delay: 100 + i * 95,
    style: {
      ...cell,
      paddingRight: pad,
      display: 'flex',
      flexDirection: 'column',
      gap: 16,
      alignItems: 'flex-start'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      ...LABEL,
      color: 'var(--text-label)'
    }
  }, r.dim)), /*#__PURE__*/React.createElement(Reveal, {
    key: `${r.dim}-o`,
    variant: "up",
    dur: 480,
    delay: 165 + i * 95,
    style: {
      ...cell,
      paddingLeft: pad,
      paddingRight: pad,
      borderLeft: oursEdge,
      ...edgeStyle
    }
  }, /*#__PURE__*/React.createElement(Side, {
    data: r.ours,
    ours: true,
    marker: marker,
    hideMarker: mobile,
    mobile: mobile
  })), /*#__PURE__*/React.createElement(Reveal, {
    key: `${r.dim}-t`,
    variant: "up",
    dur: 480,
    delay: 230 + i * 95,
    style: {
      ...cell,
      paddingLeft: pad,
      borderLeft: '1px solid var(--line-hairline)',
      ...edgeStyle
    }
  }, /*#__PURE__*/React.createElement(Side, {
    data: r.theirs,
    mobile: mobile
  }))]));
}

/* ── Cards: the same content in the chamfered frames used by Evolutionary AI, one per
   dimension, so the comparison reads as part of that family rather than as a table. ───────── */
function CmpCard({
  row,
  stacked,
  emphasis,
  tintColor,
  tintOpacity,
  marker,
  chamferFill = 'rgba(218,232,242,0.02)',
  mobile
}) {
  const {
    Bezel,
    Divider
  } = DS();
  const [hot, setHot] = React.useState(false);
  const {
    edge: oursEdge,
    fill: oursFill,
    chip
  } = oursStyle(emphasis, tintColor, tintOpacity);
  return /*#__PURE__*/React.createElement(Bezel, {
    accent: false,
    chamfer: 16,
    weight: 1,
    pad: "clamp(24px, 2.4vw, 30px)",
    color: hot ? 'var(--vv-ice-14)' : 'var(--line-hairline)',
    fill: hot ? 'rgba(218,232,242,0.05)' : chamferFill,
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      transition: 'none',
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18,
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      ...LABEL,
      color: 'var(--text-label)'
    }
  }, row.dim), /*#__PURE__*/React.createElement(Divider, null), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : 'repeat(2, minmax(0, 1fr))',
      gap: 20
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      paddingLeft: 16,
      borderLeft: oursEdge,
      background: oursFill
    }
  }, /*#__PURE__*/React.createElement(SideLabel, {
    ours: true,
    chip: chip,
    marker: marker
  }), /*#__PURE__*/React.createElement(Side, {
    data: row.ours,
    ours: true,
    marker: marker,
    hideMarker: mobile
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      paddingLeft: 16,
      borderLeft: '1px solid var(--line-hairline)'
    }
  }, /*#__PURE__*/React.createElement(SideLabel, null), /*#__PURE__*/React.createElement(Side, {
    data: row.theirs
  })))));
}

/* ── Spine: no frames and no label column. The dimension names become ruled bands running down
   the section, which keeps the full container width for the two descriptions. ─────────────── */
function Spine({
  stacked,
  emphasis,
  tintColor,
  tintOpacity,
  marker,
  mobile
}) {
  const {
    edge: oursEdge,
    fill: oursFill,
    chip
  } = oursStyle(emphasis, tintColor, tintOpacity);
  const pad = 'clamp(20px, 2.4vw, 36px)';
  return /*#__PURE__*/React.createElement("div", null, ROWS.map((r, i) => /*#__PURE__*/React.createElement(Reveal, {
    key: r.dim,
    variant: "up",
    delay: i * 90,
    dur: 560,
    style: {
      borderTop: '1px solid var(--line-hairline)',
      padding: '32px 0 36px',
      display: 'flex',
      flexDirection: 'column',
      gap: 22
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 24
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      ...LABEL,
      color: 'var(--text-label)'
    }
  }, r.dim)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : 'repeat(2, minmax(0, 1fr))',
      gap: 'clamp(24px, 3vw, 56px)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      paddingLeft: pad,
      borderLeft: oursEdge,
      background: oursFill
    }
  }, /*#__PURE__*/React.createElement(SideLabel, {
    ours: true,
    chip: chip,
    marker: marker
  }), /*#__PURE__*/React.createElement(Side, {
    data: r.ours,
    ours: true,
    marker: marker,
    hideMarker: mobile
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      paddingLeft: pad,
      borderLeft: '1px solid var(--line-hairline)'
    }
  }, /*#__PURE__*/React.createElement(SideLabel, null), /*#__PURE__*/React.createElement(Side, {
    data: r.theirs
  }))))), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: '1px solid var(--line-hairline)'
    }
  }));
}
function ComparisonSection({
  layout = 'ledger',
  emphasis = 'none',
  tintColor = 'blue',
  tintOpacity = 0.08,
  marker = 'stick',
  sticky = false,
  chamferFill = 'rgba(218,232,242,0.02)'
}) {
  const {
    GradientField,
    Eyebrow,
    SectionFrame
  } = DS();
  const stacked = useMaxWidth(1024);
  const mobile = useMaxWidth(1024);
  const trueMobile = useMaxWidth(640);
  const tablet = mobile && !trueMobile;
  const gutter = 'clamp(24px, 2.6vw, 40px)';
  return /*#__PURE__*/ /* field="none": was "crest" — the bloom is off, the field kept for future use. */React.createElement(GradientField, {
    field: "none",
    pad: trueMobile ? 'clamp(64px, 15vw, 96px) var(--gutter-site)' : 'clamp(40px, 4.2vw, 72px) var(--gutter-site)',
    as: "section",
    id: "comparison"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: mobile ? 'up' : 'frame',
    dur: 660,
    group: true
  }, /*#__PURE__*/React.createElement(SectionFrame, _extends({
    copyMax: "100%",
    stacked: stacked,
    gap: 0,
    contentStyle: {
      maxWidth: '100%'
    },
    fill: mobile ? 'transparent' : chamferFill
  }, mobile ? {
    chamfer: 0,
    color: 'transparent',
    blur: 0,
    accent: false,
    pad: '0px'
  } : null), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Comparative Analysis",
    speed: 26,
    delay: 220
  })), /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 760,
    delay: 260,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)',
      textWrap: mobile ? 'wrap' : 'balance'
    }
  }, "Our ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Evolutionary AI"), " vs. Conventional AI")), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'clamp(36px, 3.6vw, 56px)'
    }
  }, layout === 'cards' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : 'repeat(2, minmax(0, 1fr))',
      gap: gutter,
      alignItems: 'stretch'
    }
  }, ROWS.map((r, i) => /*#__PURE__*/React.createElement(Reveal, {
    key: r.dim,
    variant: "frame",
    delay: i * 100,
    dur: 620,
    style: {
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement(CmpCard, {
    row: r,
    stacked: stacked,
    emphasis: emphasis,
    tintColor: tintColor,
    tintOpacity: tintOpacity,
    marker: marker,
    chamferFill: chamferFill,
    mobile: mobile
  })))) : layout === 'spine' ? /*#__PURE__*/React.createElement(Spine, {
    stacked: stacked,
    emphasis: emphasis,
    tintColor: tintColor,
    tintOpacity: tintOpacity,
    marker: marker,
    mobile: mobile
  }) : /*#__PURE__*/React.createElement(Ledger, {
    emphasis: emphasis,
    tintColor: tintColor,
    tintOpacity: tintOpacity,
    marker: marker,
    sticky: sticky,
    stacked: stacked,
    mobile: mobile
  }))))));
}
Object.assign(window, {
  ComparisonSection
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/HomeComparison.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/HomeScience.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const DS = () => window.VivumAIDesignSystem_b2be15;
const NEURAL_TRAITS = [{
  image: '../../assets/plates/core-tech-adaptive-learning.png',
  title: 'Adaptive Learning',
  body: 'Continuously update knowledge based on new experiences.'
}, {
  image: '../../assets/plates/core-tech-temporal-awareness.png',
  title: 'Temporal Awareness',
  body: 'Seamlessly integrate and process information over time.'
}, {
  image: '../../assets/plates/core-tech-contextual-understanding.png',
  title: 'Contextual Understanding',
  body: 'Interpret data within the context of the environment.'
}, {
  image: '../../assets/plates/core-tech-efficient-computation.png',
  title: 'Efficient Computation',
  body: 'Perform complex tasks with minimal memory consumption.'
}];

/* Radial-fade mask on the trait thumbnails — same closest-side stops as the Biological
   Intelligence hung mark and the Dynamic Neural Models brain image, so a square photo reads
   as a soft glow instead of a hard-edged tile. */
const TRAIT_MASK = 'radial-gradient(circle closest-side, rgb(0, 0, 0) 0%, rgba(0, 0, 0, 0.88) 55%, rgba(0, 0, 0, 0.35) 78%, rgba(0, 0, 0, 0) 100%)';
function TraitImg({
  item,
  size
}) {
  return /*#__PURE__*/React.createElement("img", {
    src: item.image,
    alt: "",
    style: {
      width: size,
      height: size,
      aspectRatio: '1',
      objectFit: 'cover',
      display: 'block',
      flex: 'none',
      maskImage: TRAIT_MASK,
      WebkitMaskImage: TRAIT_MASK
    }
  });
}
const EVO_PILLARS = [{
  icon: 'dna',
  title: 'Natural Selection Optimization',
  body: 'Our IP Core utilizes Genetic Algorithms and other Evolutionary Algorithms to simulate natural selection as a singular force for generating diverse solutions across processors and models, evolving AI systems that meet specific goals or behaviors. We don\u2019t explicitly program AI agents; instead, our approach creates evolutionary pressures that favor high-performing solutions, resulting in more efficient and adaptable AI systems.'
}, {
  icon: 'gauge-high',
  title: 'Accelerated Artificial Evolution',
  body: 'We leverage reconfigurable hardware like FPGAs to dramatically speed up neural circuit evaluations to evolve Dynamic Neural Networks, achieving results orders of magnitude faster than traditional methods. Our proprietary high-throughput system significantly reduces the cost and development time typically associated with training complex neural networks, making advanced tailored AI solutions more accessible and efficient.'
}, {
  icon: 'brain-circuit',
  title: 'Dynamic Neural Networks',
  body: 'Our Dynamic Neural Models generate explainable, contextual responses to new stimuli, adapting in real-time to unfamiliar scenarios. This approach enables our solutions to efficiently tackle complex, time-dependent tasks with greater decision transparency than conventional deep and reinforcement learning methods. Crucially, this enhanced explainability ensures safety in mission-critical applications by enabling verification of AI decisions in high-stakes environments.'
}, {
  icon: 'microchip',
  title: 'Delivering Legitimate Autonomous Capabilities',
  body: 'We integrate our evolved models directly into target hardware, whether it\u2019s a CPU, microcontroller, FPGA, or custom ASICs, allowing them to fine-tune to each device\u2019s specific characteristics. This results in highly efficient autonomous systems that require significantly less energy and fewer computational resources, enabling true on-device learning even in size, weight, and power (SWaP) constrained environments.'
}];

/* Thin corner-chevron, not an icon glyph — two 1.5px strokes mitred to a point, matching the
   hairline vocabulary (Bezel outline, Divider) instead of a bolder icon-font arrow. */
function SliderArrow({
  dir,
  onClick
}) {
  const {
    Icon
  } = DS();
  const [hot, setHot] = React.useState(false);
  const mobile = useMaxWidth(640);
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    "aria-label": dir === 'left' ? 'Previous' : 'Next',
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      position: 'absolute',
      top: '50%',
      [dir]: mobile ? 'calc(var(--gutter-site) - 16px)' : 26,
      transform: 'translateY(-50%)',
      zIndex: 3,
      appearance: 'none',
      background: 'none',
      border: 0,
      padding: 16,
      margin: 0,
      cursor: 'pointer',
      color: hot ? 'var(--vv-ice)' : 'var(--vv-ice-56)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      transition: 'color 160ms ease'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: dir === 'left' ? 'angle-left' : 'angle-right',
    size: 48,
    color: "currentColor"
  }));
}

/* One trait per slide instead of a stacked list: the chamfered frame becomes the slide itself
   (image as `decor`, filling the frame edge-to-edge since decor is already inset:0/cover), with
   title+body scrimmed over the bottom third. Same Bezel device as the rest of the section, so a
   slider reads as one more instrument rather than a bolted-on carousel widget. */
const SLIDER_IMG_STYLE = {
  width: '100%',
  height: '100%',
  objectFit: 'cover',
  display: 'block',
  opacity: 0.4,
  filter: 'grayscale(1)',
  maskImage: TRAIT_MASK,
  WebkitMaskImage: TRAIT_MASK
};
const AUTO_MS = 5000;
const RESUME_MS = 6000;
/* One trait per slide, everything (title, body, dots) overlaid on the image itself instead of
   stacked below it, so the slider's footprint is just the image — not image-plus-caption-rows.
   Auto-advance runs on an interval that any interaction (arrow or dot) clears; a resume timer
   restarts it after a quiet period, so a browsing user never fights the clock. */
function useMinWidth(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(min-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}

/* Same 1560px threshold SectionFrame opens its corner cut on — the point the shared page
   container (--container-site-narrow, 1400px) actually hits its cap, so "max width reached"
   means this, not an arbitrary breakpoint. */
const WIDE_MQ = 1560;

/* Transition style is a tweak (`transition` prop): "slide" pushes the incoming frame in from
   the direction of travel while the outgoing frame exits the opposite way (dual-layer, own
   easing below); "fade" is sequential, not a crossfade — the current image dissolves fully to
   0 first, THEN the swap happens (invisible, so there's no hard cut), THEN it fades back in,
   and only once it's back at full opacity does the caption follow with its own quick fade. That
   staggering is what keeps a fade from reading as "lag": two independently-timed opacities
   resolving in sequence looks intentional, whereas a dissolve where the new image and new
   caption both half-appear at once while the old ones are still half-there reads as stutter.
   The image keeps its radial vignette mask (SLIDER_IMG_STYLE/TRAIT_MASK) applied at every
   step — same element, same mask, only its opacity animates — so the edge treatment never
   flashes bare mid-transition. "none" cuts immediately. */
const SLIDE_CFG = {
  duration: 450,
  easing: 'cubic-bezier(0.22, 1, 0.36, 1)'
}; // ease-out-quint: fast off the mark, soft settle — standard for push/slide motion
const FADE_STAGE_MS = 240; // image: out, then (swap while invisible), then in
const FADE_EASING = 'ease-in-out';
const CAPTION_SEP = ' — ';
const TYPE_TICK_MS = 14;
const TYPE_CHARS_PER_TICK = 2; // fast typewriter, not a slow character-by-character crawl
function TraitSlider({
  wide,
  mobile,
  transition = 'fade'
}) {
  const n = NEURAL_TRAITS.length;
  const indexRef = React.useRef(0);
  const timerRef = React.useRef(null);
  const resumeRef = React.useRef(null);
  const timeoutsRef = React.useRef([]);
  const rafRef = React.useRef([null, null]);
  const dirRef = React.useRef(1);
  const typeTimerRef = React.useRef(null);
  const cursorTimerRef = React.useRef(null);
  const clearTimers = () => {
    timeoutsRef.current.forEach(clearTimeout);
    timeoutsRef.current = [];
  };
  const clearAuto = () => {
    if (timerRef.current) clearInterval(timerRef.current);
    timerRef.current = null;
  };

  /* Mirrors the target index for the position dots. A ref would not re-render and the derived
     item lags a fade by a stage, so the dots would answer late to their own tap. */
  const [uiIdx, setUiIdx] = React.useState(0);
  const [slideActive, setSlideActive] = React.useState(0);
  const [slideIncoming, setSlideIncoming] = React.useState(null);
  const [slidePhase, setSlidePhase] = React.useState('idle');
  const [fadeItem, setFadeItem] = React.useState(NEURAL_TRAITS[0]);
  const [imgOpacity, setImgOpacity] = React.useState(1);
  const [typedLen, setTypedLen] = React.useState(NEURAL_TRAITS[0].title.length + CAPTION_SEP.length + NEURAL_TRAITS[0].body.length);
  const [showCursor, setShowCursor] = React.useState(false);

  // Preload every trait image up front. Without this, swapping the <img> src mid-fade races
  // the network/decode: browsers keep the OLD bitmap on screen until the new one finishes
  // loading, so the "invisible" swap actually fades the old image back in and only pops to the
  // new one once it lands — the flicker. Preloading means the src swap always resolves from
  // cache, so the opacity transition is the only thing the eye ever sees.
  React.useEffect(() => {
    NEURAL_TRAITS.forEach(t => {
      const im = new Image();
      im.src = t.image;
    });
  }, []);

  // Types the caption in fast (2 chars/tick) rather than fading it — a typewriter reveal
  // reads as the instrument actively reporting a new reading, and finishes quickly enough
  // (~14ms/char pair) that it never feels like a delay tacked onto the image's own fade.
  const startTyping = React.useCallback(item => {
    clearInterval(typeTimerRef.current);
    clearInterval(cursorTimerRef.current);
    const full = item.title.length + CAPTION_SEP.length + item.body.length;
    let len = 0;
    setTypedLen(0);
    setShowCursor(true);
    typeTimerRef.current = setInterval(() => {
      len += TYPE_CHARS_PER_TICK;
      if (len >= full) {
        len = full;
        clearInterval(typeTimerRef.current);
        cursorTimerRef.current = setTimeout(() => setShowCursor(false), 300);
      }
      setTypedLen(len);
    }, TYPE_TICK_MS);
  }, []);

  // Switching the tweak mid-session (or mounting) resyncs both modes' local state to whatever
  // index is actually current, so flipping the radio never leaves a stale half-transition.
  React.useEffect(() => {
    clearTimers();
    clearInterval(typeTimerRef.current);
    clearInterval(cursorTimerRef.current);
    cancelAnimationFrame(rafRef.current[0]);
    cancelAnimationFrame(rafRef.current[1]);
    setSlideIncoming(null);
    setSlidePhase('idle');
    setSlideActive(indexRef.current);
    setUiIdx(indexRef.current);
    const item = NEURAL_TRAITS[indexRef.current];
    setFadeItem(item);
    setImgOpacity(1);
    setTypedLen(item.title.length + CAPTION_SEP.length + item.body.length);
    setShowCursor(false);
  }, [transition]);
  const goTo = React.useCallback((idx, dir = 1) => {
    idx = (idx % n + n) % n;
    setUiIdx(idx);
    if (idx === indexRef.current) return;
    indexRef.current = idx;
    if (transition === 'none') {
      clearTimers();
      clearInterval(typeTimerRef.current);
      clearInterval(cursorTimerRef.current);
      const item = NEURAL_TRAITS[idx];
      setSlideActive(idx);
      setSlideIncoming(null);
      setFadeItem(item);
      setImgOpacity(1);
      setTypedLen(item.title.length + CAPTION_SEP.length + item.body.length);
      setShowCursor(false);
      return;
    }
    if (transition === 'fade') {
      clearTimers();
      clearInterval(typeTimerRef.current);
      clearInterval(cursorTimerRef.current);
      setImgOpacity(0);
      setTypedLen(0);
      setShowCursor(false);
      timeoutsRef.current.push(setTimeout(() => {
        const item = NEURAL_TRAITS[idx];
        setFadeItem(item);
        setImgOpacity(1);
        timeoutsRef.current.push(setTimeout(() => startTyping(item), FADE_STAGE_MS));
      }, FADE_STAGE_MS));
      return;
    }
    dirRef.current = dir;
    clearTimers();
    cancelAnimationFrame(rafRef.current[0]);
    cancelAnimationFrame(rafRef.current[1]);
    setSlidePhase('idle');
    setSlideIncoming(idx);
    rafRef.current[0] = requestAnimationFrame(() => {
      rafRef.current[1] = requestAnimationFrame(() => setSlidePhase('enter'));
    });
    timeoutsRef.current.push(setTimeout(() => {
      setSlideActive(idx);
      setSlideIncoming(null);
      setSlidePhase('idle');
    }, SLIDE_CFG.duration));
  }, [n, transition, startTyping]);
  const startAuto = React.useCallback(() => {
    clearAuto();
    timerRef.current = setInterval(() => goTo(indexRef.current + 1, 1), AUTO_MS);
  }, [goTo]);
  React.useEffect(() => {
    startAuto();
    return clearAuto;
  }, [startAuto]);
  React.useEffect(() => () => {
    if (resumeRef.current) clearTimeout(resumeRef.current);
    clearTimers();
    clearInterval(typeTimerRef.current);
    clearInterval(cursorTimerRef.current);
    cancelAnimationFrame(rafRef.current[0]);
    cancelAnimationFrame(rafRef.current[1]);
  }, []);
  const onInteract = () => {
    clearAuto();
    if (resumeRef.current) clearTimeout(resumeRef.current);
    resumeRef.current = setTimeout(startAuto, RESUME_MS);
  };
  const go = d => {
    goTo(indexRef.current + d, d);
    onInteract();
  };

  /* Touch nav replaces the arrows on a phone: the arrows sat on top of the image at thumb
     height and were the only way to move, so the image carried two chevrons over it at all
     times. `pan-y` keeps vertical page scrolling native — only a decisively horizontal drag
     counts, hence comparing dx against dy rather than a bare threshold. */
  const touchRef = React.useRef(null);
  const swipe = mobile ? {
    onTouchStart: e => {
      const p = e.touches[0];
      touchRef.current = {
        x: p.clientX,
        y: p.clientY
      };
    },
    onTouchEnd: e => {
      const s = touchRef.current;
      if (!s) return;
      touchRef.current = null;
      const p = e.changedTouches[0];
      const dx = p.clientX - s.x;
      const dy = p.clientY - s.y;
      if (Math.abs(dx) > 40 && Math.abs(dx) > Math.abs(dy) * 1.2) go(dx < 0 ? 1 : -1);
    }
  } : null;

  /* On mobile the image box bleeds to the screen edges, so the text insets itself back to the
     section container's gutter instead of to the image. */
  const capPad = mobile ? '0 var(--gutter-site)' : '0 clamp(20px, 2.4vw, 30px)';
  /* Caption sits higher on mobile to clear the dot row underneath it. */
  const captionBottom = mobile ? 'clamp(40px, 10vw, 52px)' : 'clamp(72px, 8.8vw, 100px)';
  /* Position readout, mobile only: circles in ice, no amber — amber is reserved for section
     accents, and a slider position is not an accent. Aligned to the caption's own left inset so
     it reads as the last line of the caption block. */
  const dots = mobile ? /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      bottom: 'clamp(6px, 2vw, 12px)',
      padding: capPad,
      display: 'flex',
      alignItems: 'center',
      gap: 2,
      zIndex: 2
    }
  }, NEURAL_TRAITS.map((it, i) => /*#__PURE__*/React.createElement("button", {
    key: it.title,
    type: "button",
    "aria-label": `Go to slide ${i + 1} of ${n}`,
    "aria-current": i === uiIdx ? 'true' : undefined,
    onClick: () => {
      goTo(i, i > uiIdx ? 1 : -1);
      onInteract();
    },
    style: {
      appearance: 'none',
      background: 'none',
      border: 0,
      padding: '12px 7px',
      margin: 0,
      cursor: 'pointer',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      width: 7,
      height: 7,
      borderRadius: 99,
      border: `1px solid ${i === uiIdx ? 'var(--vv-ice)' : 'var(--vv-ice-56)'}`,
      background: i === uiIdx ? 'var(--vv-ice)' : 'transparent',
      transition: 'background 220ms ease, border-color 220ms ease'
    }
  })))) : null;
  const caption = (item, opacity, ms) =>
  /*#__PURE__*/
  /* Text is pinned by a fixed `bottom` offset, not stacked in a bottom-anchored flex group —
     a fixed anchor means the heading starts at the same y every slide no matter how long its
     paragraph runs, instead of the whole block sliding down when the copy is short. */
  React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      bottom: captionBottom,
      padding: capPad,
      textAlign: 'left',
      pointerEvents: 'none',
      opacity,
      transition: ms != null ? `opacity ${ms}ms ${FADE_EASING}` : undefined
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      maxWidth: 360
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 12,
      lineHeight: 1.55
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontWeight: 600,
      textTransform: 'capitalize',
      color: 'var(--vv-ice-82)'
    }
  }, item.title), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-ice-56)'
    }
  }, " \u2014 ", item.body))));

  // Same layout as `caption` above, but the title/separator/body strings are sliced down to
  // `typedLen` characters and a blinking caret trails the cut — the typewriter version used
  // only while a fade transition is actively revealing new text.
  const typedCaption = (item, len, cursorOn) => {
    const titleShown = item.title.slice(0, Math.max(0, Math.min(len, item.title.length)));
    const sepRemaining = len - item.title.length;
    const sepShown = CAPTION_SEP.slice(0, Math.max(0, Math.min(sepRemaining, CAPTION_SEP.length)));
    const bodyRemaining = sepRemaining - CAPTION_SEP.length;
    const bodyShown = item.body.slice(0, Math.max(0, Math.min(bodyRemaining, item.body.length)));
    return /*#__PURE__*/React.createElement("div", {
      style: {
        position: 'absolute',
        left: 0,
        right: 0,
        bottom: captionBottom,
        padding: capPad,
        textAlign: 'left',
        pointerEvents: 'none'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'baseline',
        maxWidth: 360
      }
    }, /*#__PURE__*/React.createElement("p", {
      style: {
        margin: 0,
        fontSize: 12,
        lineHeight: 1.55
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontWeight: 600,
        textTransform: 'capitalize',
        color: 'var(--vv-ice-82)'
      }
    }, titleShown), /*#__PURE__*/React.createElement("span", {
      style: {
        color: 'var(--vv-ice-56)'
      }
    }, sepShown, bodyShown), cursorOn ? /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'inline-block',
        width: 1.5,
        height: 10,
        marginLeft: 1,
        background: 'var(--vv-amber)',
        verticalAlign: '-1px',
        opacity: Math.floor(Date.now() / 350) % 2 ? 1 : 0.15
      }
    }) : null)));
  };
  if (transition === 'fade' || transition === 'none') {
    const full = fadeItem.title.length + CAPTION_SEP.length + fadeItem.body.length;
    const typing = transition === 'fade' && typedLen < full;
    return /*#__PURE__*/React.createElement("div", _extends({}, swipe, {
      style: {
        position: 'relative',
        aspectRatio: '1 / 1',
        overflow: 'hidden',
        touchAction: mobile ? 'pan-y' : undefined
      }
    }), /*#__PURE__*/React.createElement("img", {
      src: fadeItem.image,
      alt: "",
      style: {
        ...SLIDER_IMG_STYLE,
        opacity: SLIDER_IMG_STYLE.opacity * imgOpacity,
        transition: transition === 'fade' ? `opacity ${FADE_STAGE_MS}ms ${FADE_EASING}` : undefined
      }
    }), typing || transition === 'fade' && showCursor ? typedCaption(fadeItem, typedLen, showCursor) : caption(fadeItem, 1, null), /*#__PURE__*/React.createElement(SliderArrow, {
      dir: "left",
      onClick: () => go(-1)
    }), /*#__PURE__*/React.createElement(SliderArrow, {
      dir: "right",
      onClick: () => go(1)
    }), dots);
  }
  const renderContent = item => /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("img", {
    src: item.image,
    alt: "",
    style: SLIDER_IMG_STYLE
  }), caption(item, 1));
  const entering = slidePhase === 'enter';
  let baseStyle = {
    position: 'absolute',
    inset: 0
  };
  let incomingStyle = {
    position: 'absolute',
    inset: 0
  };
  if (slideIncoming !== null) {
    const dir = dirRef.current >= 0 ? 1 : -1;
    baseStyle = {
      ...baseStyle,
      transform: `translateX(${entering ? -dir * 100 : 0}%)`,
      transition: `transform ${SLIDE_CFG.duration}ms ${SLIDE_CFG.easing}`
    };
    incomingStyle = {
      ...incomingStyle,
      transform: `translateX(${entering ? 0 : dir * 100}%)`,
      transition: `transform ${SLIDE_CFG.duration}ms ${SLIDE_CFG.easing}`
    };
  }
  return /*#__PURE__*/React.createElement("div", _extends({}, swipe, {
    style: {
      position: 'relative',
      aspectRatio: '1 / 1',
      overflow: 'hidden',
      touchAction: mobile ? 'pan-y' : undefined
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: baseStyle
  }, renderContent(NEURAL_TRAITS[slideActive])), slideIncoming !== null && /*#__PURE__*/React.createElement("div", {
    style: incomingStyle
  }, renderContent(NEURAL_TRAITS[slideIncoming])), /*#__PURE__*/React.createElement(SliderArrow, {
    dir: "left",
    onClick: () => go(-1)
  }), /*#__PURE__*/React.createElement(SliderArrow, {
    dir: "right",
    onClick: () => go(1)
  }), dots);
}
function useMaxWidth(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}

/* The traits were four cells divided by vertical hairlines — legible, but a different device
   from every other group of small items on the page. Chamfered frames match Autonomous
   Solutions and the pillars below, and the ice icon keeps the amber for section accents. */
function TraitCard({
  item,
  tone,
  chamferFill = 'rgba(218,232,242,0.02)'
}) {
  const {
    Bezel
  } = DS();
  const [hot, setHot] = React.useState(false);
  return /*#__PURE__*/React.createElement(Bezel, {
    accent: false,
    chamfer: 16,
    weight: 1,
    pad: "clamp(22px, 2.2vw, 28px)",
    color: hot ? 'var(--vv-ice-14)' : 'var(--line-hairline)',
    fill: hot ? 'rgba(218,232,242,0.05)' : chamferFill,
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      transition: 'none',
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement(TraitImg, {
    item: item,
    size: 48
  }), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: 19,
      fontWeight: 600,
      letterSpacing: '-0.01em',
      lineHeight: 1.3,
      color: 'var(--vv-ice)',
      textWrap: 'balance'
    }
  }, item.title), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 14,
      lineHeight: 1.55,
      color: 'var(--text-secondary)',
      textWrap: 'pretty'
    }
  }, item.body)));
}

/* Section 3 — Dynamic Neural Models */
/* Same SectionFrame the Biological Intelligence section uses for its hung mark: a fixed copy
   cap, `reserve` to keep the copy clear of the art, and `decor` so the image is absolutely
   positioned and clipped by the frame's own chamfer — it can never spill past the container.
   contentStyle keeps the FRAME's content box full width (so the traits grid below still
   spans it); the copy div reads its measure off `--sf-copy-max` instead. */
const MODEL_IMG_W = 'clamp(520px, 52vw, 760px)';
// Image disabled for now (kept for later use) — flip to `true` to bring it back as the
// section's right-side decor.
const SHOW_MODEL_IMG = false;
function NeuralSection({
  traits = 'cards',
  tone = 'var(--vv-ice)',
  transition = 'fade',
  chamferFill = 'rgba(218,232,242,0.02)'
}) {
  const {
    GradientField,
    Eyebrow,
    SectionFrame
  } = DS();
  const stacked = useMaxWidth(900);
  const tight = useMaxWidth(560);
  const mobile = useMaxWidth(640);
  const tablet = useMaxWidth(1024) && !mobile;
  const wide = useMinWidth(WIDE_MQ);
  const cols = stacked ? tight ? 1 : 2 : 4;
  const modelImgStyle = w => ({
    width: w,
    aspectRatio: '1',
    objectFit: 'cover',
    display: 'block',
    opacity: 0.4,
    filter: 'grayscale(1)',
    maskImage: 'radial-gradient(closest-side, rgb(0, 0, 0) 0%, rgba(0, 0, 0, 0.88) 40%, rgba(0, 0, 0, 0.42) 64%, rgba(0, 0, 0, 0.12) 80%, rgba(0, 0, 0, 0) 100%)',
    WebkitMaskImage: 'radial-gradient(closest-side, rgb(0, 0, 0) 0%, rgba(0, 0, 0, 0.88) 40%, rgba(0, 0, 0, 0.42) 64%, rgba(0, 0, 0, 0.12) 80%, rgba(0, 0, 0, 0) 100%)'
  });
  const modelImg = stacked ? /*#__PURE__*/React.createElement("img", {
    src: "../../assets/plates/dynamic-neural-brain.jpg",
    alt: "",
    style: {
      ...modelImgStyle('clamp(220px, 60vw, 340px)'),
      margin: '0 auto'
    }
  }) : /*#__PURE__*/React.createElement("img", {
    src: "../../assets/plates/dynamic-neural-brain.jpg",
    alt: "",
    style: {
      ...modelImgStyle(MODEL_IMG_W),
      position: 'absolute',
      right: 'var(--sf-decor-right)',
      top: 'var(--sf-pad-y)'
    }
  });
  return /*#__PURE__*/ /* field="none": was "data" — the bloom is off, the field kept for future use. */React.createElement(GradientField, {
    field: "none",
    pad: mobile ? 'clamp(64px, 15vw, 96px) var(--gutter-site)' : 'clamp(40px, 4.2vw, 72px) var(--gutter-site)',
    as: "section",
    id: "models"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: mobile ? 'up' : 'frame',
    dur: 660,
    group: stacked
  }, /*#__PURE__*/React.createElement(SectionFrame, _extends({
    copyMax: "100%",
    stacked: stacked,
    gap: 0,
    contentStyle: {
      maxWidth: '100%'
    },
    decor: stacked || !SHOW_MODEL_IMG ? null : modelImg,
    fill: mobile ? 'transparent' : chamferFill
  }, mobile ? {
    chamfer: 0,
    color: 'transparent',
    blur: 0,
    accent: false,
    padX: '0px',
    padY: '0px'
  } : tablet ? {
    padX: 'clamp(48px, 6vw, 96px)',
    padY: 'clamp(48px, 6vw, 96px)'
  } : null, {
    before: stacked && SHOW_MODEL_IMG ? /*#__PURE__*/React.createElement("div", {
      style: {
        marginBottom: 'clamp(24px, 5vw, 40px)'
      }
    }, modelImg) : null
  }), traits === 'side' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : 'minmax(0, 1fr) 1px minmax(0, 0.92fr)',
      columnGap: 'clamp(16px, 1.8vw, 28px)',
      rowGap: 'clamp(32px, 4vw, 56px)',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Core technology",
    speed: 30,
    delay: 280
  })), /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 780,
    delay: 320,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)',
      textWrap: 'balance'
    }
  }, "Dynamic Neural Models That ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Mimic Real Intelligence")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 22,
      marginTop: 4
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 500,
    style: {
      margin: 0,
      fontSize: 16.5,
      lineHeight: 1.65,
      color: 'var(--text-body)'
    }
  }, "Vivum AI pioneers Dynamic Neural Models inspired by the fluidity and adaptability of Biological Intelligence. Using Liquid Time-Constant Networks, Continuous-Time Recurrent Neural Networks, Reservoir Models, and Ordinary Differential Equations, we build AI that enables human-like perception and decision-making on machines and autonomous platforms."), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 620,
    style: {
      margin: 0,
      fontSize: 16.5,
      lineHeight: 1.65,
      color: 'var(--text-secondary)'
    }
  }, "Inspired by the brain, our models efficiently process temporal and sequential data, enabling real-time adaptation and context-aware decision-making."))), stacked ? null : /*#__PURE__*/React.createElement(Reveal, {
    variant: "fade",
    dur: 860,
    delay: 700,
    style: {
      alignSelf: 'stretch',
      width: 1,
      background: 'var(--line-hairline)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'calc(-1 * var(--sf-pad-y))',
      marginLeft: mobile ? 'calc(-1 * var(--gutter-site))' : undefined,
      marginRight: mobile ? 'calc(-1 * var(--gutter-site))' : 'calc(-1 * var(--sf-pad-x) + 44px)',
      marginBottom: 'calc(-1 * var(--sf-pad-y))'
    }
  }, /*#__PURE__*/React.createElement(RevealSeq, {
    ms: stacked ? 300 : 0
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "wipe",
    dur: 820,
    delay: 780
  }, /*#__PURE__*/React.createElement(TraitSlider, {
    wide: wide,
    mobile: mobile,
    transition: transition
  }))))) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18,
      maxWidth: 'var(--sf-copy-max)'
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Core technology",
    speed: 30,
    delay: 280
  })), /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 780,
    delay: 320,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)',
      textWrap: 'balance'
    }
  }, "Dynamic Neural Models That ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Mimic Real Intelligence")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 22,
      marginTop: 4
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 500,
    style: {
      margin: 0,
      fontSize: 16.5,
      lineHeight: 1.65,
      color: 'var(--text-body)'
    }
  }, "Vivum AI pioneers the evolution of Dynamic Neural Models that mimic the fluidity and adaptability of Biological Intelligence. By leveraging techniques such as Liquid Time-Constant Networks, Continuous Time Recurrent Neural Networks, Reservoir Models, and Ordinary Differential Equations, we create AI systems that map human-like perception and decision-making onto machines and autonomous platforms."), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 620,
    style: {
      margin: 0,
      fontSize: 16.5,
      lineHeight: 1.65,
      color: 'var(--text-secondary)'
    }
  }, "Inspired by the brain\u2019s intricate workings, our dynamic neural models offer a natural and efficient approach to AI. They excel at processing temporal and sequential data, enabling real-time adaptation and context-aware decision-making."))), traits === 'columns' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `repeat(${cols}, minmax(0, 1fr))`,
      gap: 'clamp(16px, 1.8vw, 24px)',
      marginTop: 'clamp(56px, 6vw, 80px)'
    }
  }, NEURAL_TRAITS.map(t => /*#__PURE__*/React.createElement(Reveal, {
    key: t.title,
    variant: "frame",
    delay: NEURAL_TRAITS.indexOf(t) * 150,
    dur: 600,
    style: {
      padding: 'clamp(22px, 2.2vw, 28px)',
      border: '1px solid var(--line-hairline)',
      display: 'flex',
      flexDirection: 'column',
      gap: 16,
      minHeight: 200
    }
  }, /*#__PURE__*/React.createElement(TraitImg, {
    item: t,
    size: 48
  }), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: 20,
      fontWeight: 600,
      letterSpacing: '-0.01em',
      color: 'var(--vv-ice)'
    }
  }, t.title), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 14,
      lineHeight: 1.55,
      color: 'var(--text-secondary)'
    }
  }, t.body)))) : /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `repeat(${cols}, minmax(0, 1fr))`,
      gap: 'clamp(16px, 1.8vw, 24px)',
      marginTop: 'clamp(56px, 6vw, 80px)',
      alignItems: 'stretch'
    }
  }, NEURAL_TRAITS.map((t, i) => /*#__PURE__*/React.createElement(Reveal, {
    key: t.title,
    variant: "frame",
    delay: i * 150,
    dur: 600,
    style: {
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement(TraitCard, {
    item: t,
    tone: tone,
    chamferFill: chamferFill
  })))))))));
}

/* Standalone divider · same full-bleed VideoBackdrop device Section 1 (Biological
   Intelligence) uses, but pure background: the wrapper is zero-height and the plate is
   absolutely positioned straddling the seam, so it never pushes Autonomous Solutions and
   Core technology apart — it just shows through their transparent (`field="none"`)
   sections. `edgeFade` dissolves the footage into the canvas colour at its own top/bottom.
   The plate's box is forced SQUARE (width == minHeight, via plateShift) so the shared
   `--plate-feather` ellipse — which follows the box's own aspect ratio — resolves to an
   actual circle instead of stretching into an oval across the wide section. */
function EvoComparisonDivider() {
  const {
    VideoBackdrop
  } = DS();
  const PLATE_H = 'clamp(420px, 42vw, 620px)';
  const mobile = useMaxWidth(640);
  if (mobile) return null;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      height: 0,
      overflow: 'visible',
      pointerEvents: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      top: 0,
      transform: 'translateY(-50%)',
      zIndex: -1
    }
  }, /*#__PURE__*/React.createElement(VideoBackdrop, {
    src: "../../assets/plates/evolutionary-intelligence.mp4",
    scrim: "none",
    circleMask: true,
    pingPong: true,
    minHeight: PLATE_H,
    align: "center",
    edgeFade: false,
    plateAlpha: 0.07,
    plateShift: `clamp(340px, calc(96% - ${PLATE_H}), 860px)`,
    plateSide: "right",
    plateScale: 3,
    plateFit: null,
    plateDrop: "220px",
    bleed: true,
    style: {
      background: 'transparent'
    }
  })));
}

/* Section 4 — Evolutionary Artificial Intelligence */
/* Same two-column device as Autonomous Solutions: a chamfered SectionFrame carries the copy on
   the left: the four pillars, rows of a single accordion on the right, so only one is open at
   a time instead of four long panels competing for attention. */
function EvoAccordionRow({
  item,
  first,
  last,
  open,
  onToggle,
  index = 0
}) {
  const {
    Icon
  } = DS();
  const [hot, setHot] = React.useState(false);
  return /*#__PURE__*/React.createElement(Reveal, {
    variant: "side",
    delay: index * 120,
    dur: 540,
    style: {
      borderTop: first ? 'none' : '1px solid var(--line-hairline)',
      borderBottom: last ? '1px solid var(--line-hairline)' : 'none'
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onToggle,
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      width: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 16,
      padding: 'clamp(18px, 2vw, 24px) 2px',
      background: 'none',
      border: 0,
      cursor: 'pointer',
      textAlign: 'left',
      font: 'inherit'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 16,
      fontWeight: 600,
      letterSpacing: '-0.01em',
      lineHeight: 1.3,
      color: open || hot ? 'var(--vv-ice)' : 'var(--vv-ice-56)',
      textWrap: 'balance',
      transition: 'color 160ms ease'
    }
  }, item.title), /*#__PURE__*/React.createElement(Icon, {
    name: open ? 'angle-down' : 'angle-right',
    size: 26,
    color: hot ? 'var(--vv-amber)' : 'var(--text-secondary)',
    style: {
      flex: 'none',
      transition: 'color 160ms ease'
    }
  })), /*#__PURE__*/React.createElement("div", {
    "aria-hidden": !open,
    style: {
      display: 'grid',
      gridTemplateRows: open ? '1fr' : '0fr',
      transition: `grid-template-rows 420ms ${VV_EASE}, opacity 300ms ${VV_EASE}`,
      opacity: open ? 1 : 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflow: 'hidden',
      minHeight: 0
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: '0 2px clamp(18px, 2vw, 24px)',
      fontSize: 14.5,
      lineHeight: 1.65,
      color: 'var(--text-secondary)',
      textWrap: 'pretty'
    }
  }, item.body))));
}
function EvoAccordion() {
  const [open, setOpen] = React.useState(0);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, EVO_PILLARS.map((p, i) => /*#__PURE__*/React.createElement(EvoAccordionRow, {
    key: p.title,
    index: i,
    item: p,
    first: i === 0,
    last: i === EVO_PILLARS.length - 1,
    open: open === i,
    onToggle: () => setOpen(open === i ? null : i)
  })));
}
function EvolutionarySection({
  onNavigate,
  chamferFill = 'rgba(218,232,242,0.02)'
}) {
  const {
    GradientField,
    Eyebrow,
    SectionFrame
  } = DS();
  const QuietLink = window.QuietLink;
  const stacked = useMaxWidth(900);
  const mobile = useMaxWidth(640);
  const tablet = useMaxWidth(1024) && !mobile;
  const wide = useMinWidth(WIDE_MQ);
  const copy = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Technical Overview",
    speed: 28,
    delay: 260
  })), /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 760,
    delay: 300,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)',
      textWrap: 'balance'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Evolutionary"), " Artificial Intelligence"), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 480,
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      textWrap: 'pretty'
    }
  }, "Our approach utilizes evolutionary algorithms, accelerated via reconfigurable hardware like FPGAs, to develop tailored Dynamic Neural Models for specific hardware platforms. Embedding the models into anything from basic microcontrollers to advanced control systems, Evolutionary AI ensures optimal performance for each unique task and domain."), !mobile ? /*#__PURE__*/React.createElement(Reveal, {
    variant: "side",
    delay: 900,
    dur: 480,
    style: {
      paddingTop: 10
    }
  }, /*#__PURE__*/React.createElement(QuietLink, {
    href: "#/contact",
    label: "Send us your questions",
    onClick: e => {
      e.preventDefault();
      onNavigate && onNavigate('/contact');
    }
  })) : null);
  return /*#__PURE__*/ /* field="none": was "right" — the bloom is off, the field kept for future use. */React.createElement(GradientField, {
    field: "none",
    pad: mobile ? 'clamp(64px, 15vw, 96px) var(--gutter-site)' : 'clamp(40px, 4.2vw, 72px) var(--gutter-site)',
    as: "section",
    id: "evolutionary"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, /*#__PURE__*/React.createElement(RevealGroup, {
    on: stacked
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: stacked ? 'minmax(0, 1fr)' : wide ? '3fr 2fr' : '1fr 1fr',
      columnGap: 'clamp(28px, 2.6vw, 48px)',
      rowGap: 'clamp(36px, 5vw, 56px)',
      alignItems: 'stretch'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      alignSelf: 'center'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: mobile ? 'up' : 'frame',
    dur: 660
  }, /*#__PURE__*/React.createElement(SectionFrame, _extends({
    copyMax: "100%",
    stacked: stacked,
    contentStyle: {
      maxWidth: '100%'
    },
    fill: mobile ? 'transparent' : chamferFill
  }, mobile ? {
    chamfer: 0,
    color: 'transparent',
    blur: 0,
    accent: false,
    padX: '0px',
    padY: '0px'
  } : tablet ? {
    padX: 'clamp(48px, 6vw, 96px)',
    padY: 'clamp(48px, 6vw, 96px)'
  } : null), copy))), /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      alignSelf: 'center'
    }
  }, /*#__PURE__*/React.createElement(RevealSeq, {
    ms: stacked ? 700 : 0
  }, /*#__PURE__*/React.createElement(EvoAccordion, null)))), mobile ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'clamp(24px, 5vw, 36px)'
    }
  }, /*#__PURE__*/React.createElement(RevealSeq, {
    ms: 1450
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "side",
    dur: 480
  }, /*#__PURE__*/React.createElement(QuietLink, {
    href: "#/contact",
    label: "Send us your questions",
    onClick: e => {
      e.preventDefault();
      onNavigate && onNavigate('/contact');
    }
  })))) : null)));
}
Object.assign(window, {
  NeuralSection,
  EvolutionarySection,
  EvoComparisonDivider
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/HomeScience.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/HomeScreen.jsx
try { (() => {
const DS = () => window.VivumAIDesignSystem_b2be15;
const CMP_TINT_HEX_TO_KEY = {
  '#2881B5': 'blue',
  '#EC9A00': 'amber',
  '#DAE8F2': 'ice',
  '#22A66E': 'green'
};
function useMaxWidth(q) {
  const [hit, setHit] = React.useState(false);
  React.useEffect(() => {
    const mq = window.matchMedia(`(max-width: ${q}px)`);
    const on = () => setHit(mq.matches);
    on();
    mq.addEventListener('change', on);
    return () => mq.removeEventListener('change', on);
  }, [q]);
  return hit;
}

// Section geometry now lives in SectionFrame: it owns the padding tokens, the copy cap, and
// the slack math that pulls the hung mark inward. The copy is flush to the frame's left
// padding in every section, so this headline and the Autonomous Solutions headline below it
// start at the same x. What is left here is only what this section contributes: how wide the
// mark is, and where its two dependants read that from.
const BIO_MARK_W = 'clamp(300px, 34vw, 590px)';
const BIO_COPY_CAP = 610;
// Published by the frame, in frame coordinates — the decor layer is inset:0 on the host.
const BIO_MARK_RIGHT = 'var(--sf-decor-right)';
// The corner CTA sits between the frame padding and the mark's right margin — pulled off the
// frame's corner at wide sizes, but not all the way in to the tree's edge.
const BIO_CTA_RIGHT = `max(var(--sf-pad-x), calc(${BIO_MARK_RIGHT} - 40px))`;

/* One tweak store for the whole home screen: useTweaks publishes its key set to the host, so a
   second store on the same screen would fight the first over the panel. */
const HOME_TWEAKS = /*EDITMODE-BEGIN*/{
  "mobileMenuStyle": "fullscreen",
  "bioFill": "stats",
  "bioCaption": false,
  "chamferFill": "transparent",
  "bioCtaPlace": "corner",
  "bioCtaStyle": "arrow",
  "evoCols": "2",
  "evoLede": "beside",
  "evoBody": "trimmed",
  "evoIcons": true,
  "cmpLayout": "ledger",
  "cmpEmphasis": "tint",
  "cmpTintColor": "#DAE8F2",
  "cmpTintOpacity": 0.02,
  "cmpMarker": "stick",
  "cmpSticky": false,
  "ctaFrame": "banner",
  "ctaPlate": true,
  "ctaSecond": false,
  "nmTraits": "side",
  "nmIconTone": "ice",
  "iconWeight": "thin",
  "nmTransition": "fade",
  "seamAnim": "ribbons",
  "seamSize": 480,
  "seamSpeed": 2,
  "seamX": 0,
  "seamY": 0,
  "seamOpacity": 0.2,
  "mFrame": "rise",
  "mSpeed": 1.2,
  "mDelay": 0,
  "mReplay": false
} /*EDITMODE-END*/;
const BIO_TRAITS = [['Adaptive', 'Topologies that rewire themselves as conditions change.'], ['Efficient', 'Inference budgets measured in watts, not racks.'], ['Resilient', 'Degrades gracefully instead of failing closed.']];
function QuietLink({
  href,
  label,
  onClick,
  style
}) {
  const [hot, setHot] = React.useState(false);
  return /*#__PURE__*/React.createElement("a", {
    href: href,
    onClick: onClick,
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 9,
      fontSize: 'clamp(15px, 1.2vw, 17px)',
      fontWeight: 400,
      lineHeight: 1,
      textDecoration: 'none',
      whiteSpace: 'nowrap',
      color: hot ? 'var(--vv-amber)' : 'var(--vv-graphite)',
      transition: 'color 160ms ease',
      ...style
    }
  }, label, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      fontSize: '1.05em',
      lineHeight: 1,
      transform: hot ? 'translateX(3px)' : 'none',
      transition: 'transform 160ms ease'
    }
  }, "\u2192"));
}

/* The About link, in three treatments. Graphite keeps it subordinate to the amber stats;
   hover is the only place amber returns, so the corner stays quiet until it's aimed at. */
function BioCta({
  variant,
  corner,
  placed,
  onNavigate
}) {
  const [hot, setHot] = React.useState(false);
  const base = {
    display: 'inline-flex',
    alignItems: 'center',
    gap: 9,
    fontSize: 'clamp(15px, 1.2vw, 17px)',
    fontWeight: 400,
    letterSpacing: '0.01em',
    lineHeight: 1,
    textDecoration: 'none',
    whiteSpace: 'nowrap',
    color: hot ? 'var(--vv-amber)' : 'var(--vv-graphite)',
    transition: 'color 160ms ease, border-color 160ms ease',
    /* `placed` = an ancestor owns the position (the corner reveal wrapper below): no absolute
       offsets of its own, and no inline margin either. */
    ...(corner ? {
      position: 'absolute',
      right: BIO_CTA_RIGHT,
      bottom: 'clamp(56px, 6.4vw, 94px)'
    } : placed ? null : {
      alignSelf: 'flex-start',
      marginTop: 'clamp(18px, 2vw, 28px)'
    }),
    ...(variant === 'rule' ? {
      paddingBottom: 6,
      borderBottom: `1px solid ${hot ? 'var(--vv-amber)' : 'var(--vv-ice-08)'}`
    } : null),
    ...(variant === 'boxed' ? {
      padding: '11px 18px',
      border: `1px solid ${hot ? 'var(--vv-amber)' : 'var(--vv-ice-08)'}`
    } : null)
  };
  return /*#__PURE__*/React.createElement("a", {
    href: "#/about",
    onClick: e => {
      e.preventDefault();
      onNavigate && onNavigate('/about');
    },
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false),
    style: base
  }, "Read about us", /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      fontSize: '1.05em',
      lineHeight: 1,
      transform: hot ? 'translateX(3px)' : 'none',
      transition: 'transform 160ms ease'
    }
  }, "\u2192"));
}
const BIO_STATS = [['6,000\u00d7', 'less energy'], ['40\u00d7', 'faster'], ['0', 'extra HW']];
function HomeScreen({
  onNavigate
}) {
  const {
    VideoBackdrop,
    GradientField,
    Eyebrow,
    Button,
    Bezel,
    SectionFrame
  } = DS();
  const [t, setTweak] = useTweaks(HOME_TWEAKS);
  /* Push the Motion tweaks into the shared store the Reveal components subscribe to. A layout
     effect so the new values are in place before the next paint; the store's defaults match
     HOME_TWEAKS, so the very first frame is already correct. */
  React.useLayoutEffect(() => {
    window.vvSetMotion({
      frame: t.mFrame,
      speed: t.mSpeed,
      delay: t.mDelay,
      replay: t.mReplay
    });
  }, [t.mFrame, t.mSpeed, t.mDelay, t.mReplay]);
  // Icon weight is a whole-site setting living on the Icon module itself (every <Icon> across
  // every screen subscribes to it), not a prop threaded through each section — this just keeps
  // it in sync with the persisted tweak, including on first load.
  React.useEffect(() => {
    window.vvSetIconWeight && window.vvSetIconWeight(t.iconWeight);
  }, [t.iconWeight]);
  React.useEffect(() => {
    window.vvSetMobileMenuStyle && window.vvSetMobileMenuStyle(t.mobileMenuStyle);
  }, [t.mobileMenuStyle]);
  // The mark and the copy are one pair, so they share a grid track set rather than being
  // pushed to opposite edges — on an ultrawide the slack falls outside the pair, not between.
  // 1024, not 900: between 900 and 1100 the frame is too narrow to carry both a readable
  // measure and the mark — the decoration ends up wider than the copy it decorates.
  const stacked = useMaxWidth(1024);
  const mobile = useMaxWidth(640);
  const tablet = stacked && !mobile;
  // NavBar's own mobile-chrome breakpoint — the hero copy should sit off the bottom edge the
  // way the bar sits off the top. Gating on 640 would leave a 641-760 band where the bar is
  // mobile but the hero still carries its 150px desktop foot.
  const navMobile = useMaxWidth(760);
  const bioMaskImage = `radial-gradient(closest-side, ${navMobile ? 'rgb(0, 0, 0) 0%, rgba(0, 0, 0, 0.88) 50%, rgba(0, 0, 0, 0.42) 70%, rgba(0, 0, 0, 0.12) 94%, rgba(0, 0, 0, 0) 100%' : 'rgb(0, 0, 0) 0%, rgba(0, 0, 0, 0.88) 40%, rgba(0, 0, 0, 0.42) 64%, rgba(0, 0, 0, 0.12) 80%, rgba(0, 0, 0, 0) 100%'})`;
  // Below the stacked breakpoint the mark rides in the flow above the copy instead of being
  // hung past the frame's bottom edge — a crop that reads as intent on a wide frame just
  // reads as a broken image on a phone. Above it the mark holds one size: 36vw until it hits
  // its 540px cap around 1500px, so it never rescales across real desktop widths.
  const bioMark = /*#__PURE__*/React.createElement("span", {
    style: stacked ? {
      display: 'block',
      width: mobile || tablet ? '100%' : 'clamp(150px, 42vw, 260px)',
      aspectRatio: '1',
      margin: '0 auto'
    } : {
      position: 'absolute',
      right: BIO_MARK_RIGHT,
      top: '50%',
      transform: 'translateY(-50%)',
      width: BIO_MARK_W,
      aspectRatio: '1'
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/plates/biological-intelligence-leaf.jpg",
    alt: "",
    style: {
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      display: 'block',
      opacity: 0.44,
      filter: 'grayscale(1)',
      maskImage: bioMaskImage,
      WebkitMaskImage: bioMaskImage
    }
  }));
  /* Mobile only: the mark stops being a figure stacked above the copy and becomes the field the
     copy sits on — oversized past both edges of its container (the section clips horizontally,
     so nothing widens the page) with the copy laid over it. Anchored to the TOP of the box, not
     centred in it: the box's height is driven by the copy, so a centred mark would drift with
     every line of text and the copy's percentage offset would have no fixed image to measure
     against. Pinned to the top, the image occupies a known 0 … min(150vw, 620px) band and the
     copy's 80% padding lands where it says it does. Its own radial mask is what makes an
     overflowing photo read as light rather than a crop. */
  const bioMarkMobile = /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      left: '50%',
      top: 0,
      transform: 'translateX(-50%)',
      width: 'min(150vw, 620px)',
      aspectRatio: '1',
      pointerEvents: 'none',
      zIndex: 0
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/plates/biological-intelligence-leaf.jpg",
    alt: "",
    style: {
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      display: 'block',
      opacity: 0.44,
      filter: 'grayscale(1)',
      maskImage: bioMaskImage,
      WebkitMaskImage: bioMaskImage
    }
  }));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      isolation: 'isolate'
    }
  }, /*#__PURE__*/React.createElement(VideoBackdrop, {
    src: "../../assets/plates/home-banner-video.mp4",
    scrim: "bottom",
    minHeight: "max(620px, 100svh)",
    align: "end",
    edgeFade: false,
    foot: "12%",
    pad: mobile ? '180px var(--gutter-site) clamp(104px, 26vw, 148px)' : navMobile ? '180px var(--gutter-site) 40px' : '180px var(--gutter-site) 150px',
    plateOpacity: 0.4
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site)',
      margin: '0 auto',
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(14px, 1.1vw, 18px)'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    as: "h1",
    variant: "text",
    dur: 880,
    delay: 220,
    style: {
      margin: 0,
      fontSize: 'clamp(30px, 3.2vw, 46px)',
      fontWeight: 700,
      letterSpacing: '-0.03em',
      lineHeight: 1.08,
      color: 'var(--vv-ice)',
      maxWidth: '17.5em',
      textWrap: 'balance'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Legitimate Autonomy"), " for Robotics, Unmanned Vehicles & Edge Systems"), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 620,
    style: {
      margin: 0,
      fontSize: 'clamp(17px, 1.35vw, 20px)',
      lineHeight: 1.5,
      color: 'var(--vv-ice-82)',
      maxWidth: 620
    }
  }, "Using Evolutionary AI to power a new generation of intelligent, self-optimizing autonomous systems across all domains."), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 12,
      marginTop: 'clamp(14px, 1.4vw, 24px)'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "rise",
    delay: 840,
    dur: 520
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    size: "lg",
    onClick: () => onNavigate('/autonomy')
  }, "Explore autonomy")), /*#__PURE__*/React.createElement(Reveal, {
    variant: "rise",
    delay: 940,
    dur: 520
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    size: "lg",
    onClick: () => onNavigate('/contact')
  }, "Contact us")))))), /*#__PURE__*/React.createElement("section", {
    style: {
      padding: 'clamp(32px, 3.6vw, 52px) var(--gutter-site)',
      overflowX: 'clip',
      overflowY: 'visible'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, mobile ? /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      display: 'flex',
      flexDirection: 'column',
      padding: 'calc(min(150vw, 620px) * 0.8) 0 clamp(24px, 3vw, 48px)',
      boxSizing: 'border-box',
      background: 'var(--surface-canvas)'
    }
  }, bioMarkMobile, /*#__PURE__*/React.createElement(RevealGroup, {
    on: mobile
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1
    }
  }, /*#__PURE__*/React.createElement(SectionFrame, {
    copyMax: BIO_COPY_CAP,
    reserve: stacked ? undefined : BIO_MARK_W,
    gap: 18,
    stacked: stacked,
    columnMinHeight: "clamp(300px, 23vw, 380px)",
    decor: stacked ? null : bioMark,
    fill: "transparent",
    chamfer: 0,
    color: "transparent",
    blur: 0,
    accent: false,
    padX: "0px",
    padY: "0px",
    before: null,
    after: /*#__PURE__*/React.createElement(React.Fragment, null, t.bioCtaPlace === 'corner' && !stacked ? /*#__PURE__*/React.createElement(Reveal, {
      variant: "side",
      delay: 820,
      dur: 480,
      style: {
        position: 'absolute',
        right: BIO_CTA_RIGHT,
        bottom: 'clamp(56px, 6.4vw, 94px)'
      }
    }, /*#__PURE__*/React.createElement(BioCta, {
      variant: t.bioCtaStyle,
      placed: true,
      onNavigate: onNavigate
    })) : null)
  }, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Vivum AI",
    speed: 34
  })), /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 760,
    delay: 90,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Biological Intelligence"), " Inspired by Nature and Built for Tomorrow"), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 300,
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)'
    }
  }, "We create AI that mirrors the adaptability, efficiency, and resilience of Biological Systems. Our purpose is to propagate a computing paradigm that benefits all life on Earth."), t.bioFill === 'traits' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gap: 'clamp(8px, 0.9vw, 12px)',
      marginTop: 'clamp(16px, 1.9vw, 28px)',
      paddingTop: 'clamp(16px, 1.8vw, 24px)',
      borderTop: '1px solid var(--vv-ice-08)'
    }
  }, BIO_TRAITS.map(([k, v]) => /*#__PURE__*/React.createElement(Reveal, {
    key: k,
    variant: "side",
    delay: 120 * BIO_TRAITS.findIndex(r => r[0] === k),
    dur: 480,
    style: {
      display: 'flex',
      gap: 'clamp(12px, 1.2vw, 18px)',
      alignItems: 'baseline'
    }
  }, /*#__PURE__*/React.createElement(HudText, {
    text: k,
    speed: 22,
    delay: 120 * BIO_TRAITS.findIndex(r => r[0] === k),
    style: {
      flex: '0 0 auto',
      minWidth: 84,
      fontSize: 12.5,
      fontWeight: 600,
      letterSpacing: '0.09em',
      textTransform: 'uppercase',
      color: 'var(--vv-amber)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'clamp(14px, 1.05vw, 15px)',
      lineHeight: 1.5,
      color: 'var(--vv-ice-82)'
    }
  }, v)))) : null, t.bioFill === 'stats' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 'clamp(22px, 2.8vw, 44px)',
      marginTop: 'clamp(16px, 1.9vw, 28px)',
      paddingTop: 'clamp(16px, 1.8vw, 24px)',
      borderTop: '1px solid var(--vv-ice-08)',
      alignItems: 'flex-end'
    }
  }, BIO_STATS.map(([n, l]) => /*#__PURE__*/React.createElement(Reveal, {
    key: n,
    variant: "up",
    delay: 140 * BIO_STATS.findIndex(r => r[0] === n),
    dur: 520,
    style: {
      display: 'grid',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement(HudNumber, {
    value: n,
    delay: 200 + 140 * BIO_STATS.findIndex(r => r[0] === n),
    style: {
      fontSize: 'clamp(22px, 1.9vw, 28px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1,
      color: 'var(--vv-ice)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      letterSpacing: '0.07em',
      textTransform: 'uppercase',
      lineHeight: 1.4,
      color: 'var(--vv-ice-82)',
      maxWidth: 132
    }
  }, l)))) : null, t.bioCtaPlace === 'inline' || stacked ? mobile ? /*#__PURE__*/React.createElement(RevealSeq, {
    ms: 950
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "side",
    dur: 480
  }, /*#__PURE__*/React.createElement(BioCta, {
    variant: t.bioCtaStyle,
    corner: false,
    onNavigate: onNavigate
  }))) : /*#__PURE__*/React.createElement(Reveal, {
    variant: "side",
    delay: 820,
    dur: 480
  }, /*#__PURE__*/React.createElement(BioCta, {
    variant: t.bioCtaStyle,
    corner: false,
    onNavigate: onNavigate
  })) : null, t.bioCaption ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 9,
      marginTop: 'clamp(14px, 1.6vw, 22px)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 99,
      background: 'var(--vv-amber)',
      flex: '0 0 auto'
    }
  }), /*#__PURE__*/React.createElement(HudText, {
    text: "Fig. 01 \u2014 Evolved topology, generation 4,096",
    speed: 16,
    delay: 260,
    style: {
      fontSize: 11.5,
      letterSpacing: '0.11em',
      textTransform: 'uppercase',
      color: 'var(--vv-ice-82)'
    }
  })) : null)))) : /*#__PURE__*/React.createElement(VideoBackdrop, {
    src: "../../assets/plates/biological-intelligence.mp4",
    scrim: "none",
    circleMask: true,
    minHeight: "clamp(440px, 44vw, 620px)",
    align: "center",
    edgeFade: false,
    pad: "clamp(24px, 3vw, 48px) 0",
    plateOpacity: 0.1,
    plateShift: "clamp(24px, 12vw, 200px)",
    plateSide: "center",
    plateScale: 1.5,
    plateDrop: "clamp(116px, 15vw, 260px)",
    bleed: true,
    style: {
      background: 'transparent'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "frame",
    dur: 660
  }, /*#__PURE__*/React.createElement(SectionFrame, {
    copyMax: BIO_COPY_CAP,
    reserve: stacked ? undefined : BIO_MARK_W,
    gap: 18,
    stacked: stacked,
    columnMinHeight: "clamp(300px, 23vw, 380px)",
    decor: stacked ? null : bioMark,
    fill: t.chamferFill,
    padX: tablet ? 'clamp(48px, 6vw, 96px)' : undefined,
    padY: tablet ? 'clamp(48px, 6vw, 96px)' : undefined,
    before: stacked ? /*#__PURE__*/React.createElement("div", {
      style: {
        marginBottom: 'clamp(24px, 5vw, 40px)'
      }
    }, bioMark) : null,
    after: /*#__PURE__*/React.createElement(React.Fragment, null, t.bioCtaPlace === 'corner' && !stacked ? /*#__PURE__*/React.createElement(Reveal, {
      variant: "side",
      delay: 820,
      dur: 480,
      style: {
        position: 'absolute',
        right: BIO_CTA_RIGHT,
        bottom: 'clamp(56px, 6.4vw, 94px)'
      }
    }, /*#__PURE__*/React.createElement(BioCta, {
      variant: t.bioCtaStyle,
      placed: true,
      onNavigate: onNavigate
    })) : null)
  }, /*#__PURE__*/React.createElement(Eyebrow, null, /*#__PURE__*/React.createElement(HudText, {
    text: "Vivum AI",
    speed: 34
  })), /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 760,
    delay: 90,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Biological Intelligence"), " Inspired by Nature and Built for Tomorrow"), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 300,
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)'
    }
  }, "We create AI that mirrors the adaptability, efficiency, and resilience of Biological Systems. Our purpose is to propagate a computing paradigm that benefits all life on Earth."), t.bioFill === 'traits' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gap: 'clamp(8px, 0.9vw, 12px)',
      marginTop: 'clamp(16px, 1.9vw, 28px)',
      paddingTop: 'clamp(16px, 1.8vw, 24px)',
      borderTop: '1px solid var(--vv-ice-08)'
    }
  }, BIO_TRAITS.map(([k, v]) => /*#__PURE__*/React.createElement(Reveal, {
    key: k,
    variant: "side",
    delay: 120 * BIO_TRAITS.findIndex(r => r[0] === k),
    dur: 480,
    style: {
      display: 'flex',
      gap: 'clamp(12px, 1.2vw, 18px)',
      alignItems: 'baseline'
    }
  }, /*#__PURE__*/React.createElement(HudText, {
    text: k,
    speed: 22,
    delay: 120 * BIO_TRAITS.findIndex(r => r[0] === k),
    style: {
      flex: '0 0 auto',
      minWidth: 84,
      fontSize: 12.5,
      fontWeight: 600,
      letterSpacing: '0.09em',
      textTransform: 'uppercase',
      color: 'var(--vv-amber)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'clamp(14px, 1.05vw, 15px)',
      lineHeight: 1.5,
      color: 'var(--vv-ice-82)'
    }
  }, v)))) : null, t.bioFill === 'stats' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 'clamp(22px, 2.8vw, 44px)',
      marginTop: 'clamp(16px, 1.9vw, 28px)',
      paddingTop: 'clamp(16px, 1.8vw, 24px)',
      borderTop: '1px solid var(--vv-ice-08)',
      alignItems: 'flex-end'
    }
  }, BIO_STATS.map(([n, l]) => /*#__PURE__*/React.createElement(Reveal, {
    key: n,
    variant: "up",
    delay: 140 * BIO_STATS.findIndex(r => r[0] === n),
    dur: 520,
    style: {
      display: 'grid',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement(HudNumber, {
    value: n,
    delay: 200 + 140 * BIO_STATS.findIndex(r => r[0] === n),
    style: {
      fontSize: 'clamp(22px, 1.9vw, 28px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1,
      color: 'var(--vv-ice)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      letterSpacing: '0.07em',
      textTransform: 'uppercase',
      lineHeight: 1.4,
      color: 'var(--vv-ice-82)',
      maxWidth: 132
    }
  }, l)))) : null, t.bioCtaPlace === 'inline' || stacked ? mobile ? /*#__PURE__*/React.createElement(RevealSeq, {
    ms: 950
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "side",
    dur: 480
  }, /*#__PURE__*/React.createElement(BioCta, {
    variant: t.bioCtaStyle,
    corner: false,
    onNavigate: onNavigate
  }))) : /*#__PURE__*/React.createElement(Reveal, {
    variant: "side",
    delay: 820,
    dur: 480
  }, /*#__PURE__*/React.createElement(BioCta, {
    variant: t.bioCtaStyle,
    corner: false,
    onNavigate: onNavigate
  })) : null, t.bioCaption ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 9,
      marginTop: 'clamp(14px, 1.6vw, 22px)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 99,
      background: 'var(--vv-amber)',
      flex: '0 0 auto'
    }
  }), /*#__PURE__*/React.createElement(HudText, {
    text: "Fig. 01 \u2014 Evolved topology, generation 4,096",
    speed: 16,
    delay: 260,
    style: {
      fontSize: 11.5,
      letterSpacing: '0.11em',
      textTransform: 'uppercase',
      color: 'var(--vv-ice-82)'
    }
  })) : null))))), /*#__PURE__*/React.createElement(CapabilitiesSection, {
    onNavigate: onNavigate,
    chamferFill: t.chamferFill,
    extraTweaks: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TweakSection, {
      label: "Motion"
    }), /*#__PURE__*/React.createElement(TweakSelect, {
      label: "Chamfered frames",
      value: t.mFrame,
      options: window.VV_FRAME_OPTIONS,
      onChange: v => setTweak('mFrame', v)
    }), /*#__PURE__*/React.createElement(TweakSlider, {
      label: "Speed",
      value: t.mSpeed,
      min: 0.25,
      max: 2.5,
      step: 0.05,
      unit: "\xD7",
      onChange: v => setTweak('mSpeed', v)
    }), /*#__PURE__*/React.createElement(TweakSlider, {
      label: "Delay",
      value: t.mDelay,
      min: 0,
      max: 800,
      step: 20,
      unit: "ms",
      onChange: v => setTweak('mDelay', v)
    }), /*#__PURE__*/React.createElement(TweakToggle, {
      label: "Replay on re-entry",
      value: t.mReplay,
      onChange: v => setTweak('mReplay', v)
    }), /*#__PURE__*/React.createElement(TweakSection, {
      label: "Seam background"
    }), /*#__PURE__*/React.createElement(TweakSelect, {
      label: "Animation",
      value: t.seamAnim,
      options: [{
        value: 'texture',
        label: 'Texture — film grain'
      }, {
        value: 'nebula',
        label: 'Nebula — turbulence cloud'
      }, {
        value: 'ash',
        label: 'Ash — falling grit'
      }, {
        value: 'sheen',
        label: 'Sheen — satin sweep'
      }, {
        value: 'weave',
        label: 'Weave — combed fibre'
      }, {
        value: 'smoke',
        label: 'Smoke — climbing plumes'
      }, {
        value: 'marble',
        label: 'Marble — breathing veins'
      }, {
        value: 'static',
        label: 'Static — signal loss'
      }, {
        value: 'ribbons',
        label: 'Ribbons — sliding bands'
      }, {
        value: 'starfield',
        label: 'Starfield — twinkling depths'
      }, {
        value: 'drift',
        label: 'Drift — crossing blooms'
      }, {
        value: 'scan',
        label: 'Scan — sweeping hairlines'
      }, {
        value: 'pulse',
        label: 'Pulse — propagating rings'
      }, {
        value: 'lattice',
        label: 'Lattice — drifting mesh'
      }, {
        value: 'orbit',
        label: 'Orbit — tracking dots'
      }, {
        value: 'contours',
        label: 'Contours — breathing terrain'
      }, {
        value: 'motes',
        label: 'Motes — rising particles'
      }, {
        value: 'waves',
        label: 'Waves — swaying crests'
      }, {
        value: 'shafts',
        label: 'Shafts — angled light'
      }, {
        value: 'off',
        label: 'Off'
      }],
      onChange: v => setTweak('seamAnim', v)
    }), t.seamAnim !== 'off' && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TweakSlider, {
      label: "Size",
      value: t.seamSize,
      min: 120,
      max: 640,
      step: 10,
      unit: "px",
      onChange: v => setTweak('seamSize', v)
    }), /*#__PURE__*/React.createElement(TweakSlider, {
      label: "Speed",
      value: t.seamSpeed,
      min: 0.25,
      max: 3,
      step: 0.05,
      unit: "\xD7",
      onChange: v => setTweak('seamSpeed', v)
    }), /*#__PURE__*/React.createElement(TweakSlider, {
      label: "X position",
      value: t.seamX,
      min: -50,
      max: 50,
      step: 1,
      unit: "%",
      onChange: v => setTweak('seamX', v)
    }), /*#__PURE__*/React.createElement(TweakSlider, {
      label: "Y position",
      value: t.seamY,
      min: -100,
      max: 100,
      step: 2,
      unit: "%",
      onChange: v => setTweak('seamY', v)
    }), /*#__PURE__*/React.createElement(TweakSlider, {
      label: "Opacity",
      value: t.seamOpacity,
      min: 0.02,
      max: 1,
      step: 0.01,
      onChange: v => setTweak('seamOpacity', v)
    })))
  }), /*#__PURE__*/React.createElement(SeamAnimation, {
    variant: t.seamAnim,
    size: t.seamSize,
    speed: t.seamSpeed,
    x: t.seamX,
    y: t.seamY,
    opacity: t.seamOpacity
  }), /*#__PURE__*/React.createElement(NeuralSection, {
    traits: t.nmTraits,
    tone: t.nmIconTone === 'amber' ? 'var(--vv-amber)' : 'var(--vv-ice)',
    transition: t.nmTransition,
    chamferFill: t.chamferFill
  }), /*#__PURE__*/React.createElement(EvolutionarySection, {
    onNavigate: onNavigate,
    chamferFill: t.chamferFill
  }), /*#__PURE__*/React.createElement(EvoComparisonDivider, null), /*#__PURE__*/React.createElement(ComparisonSection, {
    layout: t.cmpLayout,
    emphasis: t.cmpEmphasis,
    tintColor: CMP_TINT_HEX_TO_KEY[t.cmpTintColor] || 'blue',
    tintOpacity: t.cmpTintOpacity,
    marker: t.cmpMarker,
    sticky: t.cmpSticky === true,
    chamferFill: t.chamferFill
  }), /*#__PURE__*/React.createElement(GradientField, {
    field: "none",
    pad: mobile ? 'clamp(64px, 15vw, 96px) var(--gutter-site) clamp(96px, 22vw, 140px)' : 'clamp(40px, 4.2vw, 72px) var(--gutter-site) clamp(48px, 5vw, 88px)',
    as: "section"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site-narrow)',
      margin: '0 auto'
    }
  }, t.ctaFrame === 'bare' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 64,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 800,
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)',
      maxWidth: 780,
      textWrap: tablet ? 'wrap' : 'balance'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Legitimate Autonomy"), " On The Hardware You Already Field"), /*#__PURE__*/React.createElement(Reveal, {
    variant: "rise",
    delay: 280,
    dur: 520
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    size: "lg",
    onClick: () => onNavigate('/contact')
  }, "Contact us"))) :
  /*#__PURE__*/
  /* Same frame as the two copy sections — it shares their 44px chamfer, so it has to
     share their padding and left edge too. `contentStyle` lifts the copy cap because
     this frame's content is a full-width grid (copy left, button right); the inner
     copy column keeps its own measure. */
  React.createElement(Reveal, {
    variant: "frame",
    dur: 760
  }, /*#__PURE__*/React.createElement(SectionFrame, {
    accent: true,
    chamfer: 44,
    weight: 1,
    color: "var(--vv-ice-14)",
    stacked: stacked,
    contentStyle: {
      maxWidth: '100%'
    },
    padX: "clamp(64px, 7vw, 120px)",
    plateSrc: t.ctaPlate ? '../../assets/plates/home-banner-video.mp4' : undefined,
    plateOpacity: 0.2,
    plateShift: "clamp(30px, 20vw, 300px)",
    plateScale: 1.7,
    fill: "rgba(218,232,242,0.02)"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: stacked ? 'column' : 'row',
      justifyContent: 'center',
      columnGap: 'clamp(40px, 6vw, 96px)',
      rowGap: 'clamp(28px, 3.4vw, 40px)',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'flex-start',
      gap: 16,
      maxWidth: tablet ? '100%' : 680,
      flex: '0 1 auto'
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    as: "h2",
    variant: "text",
    dur: 800,
    delay: 280,
    style: {
      margin: 0,
      fontSize: mobile ? 'clamp(26px, 2.5vw, 36px)' : 'clamp(30px, 3.2vw, 46px)',
      fontWeight: 700,
      letterSpacing: mobile ? '-0.02em' : '-0.03em',
      lineHeight: mobile ? 1.15 : 1.08,
      color: 'var(--vv-ice)',
      maxWidth: tablet ? '100%' : 680,
      textWrap: tablet ? 'wrap' : 'balance'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)'
    }
  }, "Legitimate Autonomy"), " On Hardware You Already Field"), /*#__PURE__*/React.createElement(Reveal, {
    as: "p",
    variant: "up",
    delay: 460,
    style: {
      margin: 0,
      fontSize: 'clamp(17px, 1.35vw, 20px)',
      lineHeight: 1.5,
      color: 'var(--vv-ice-82)',
      maxWidth: tablet ? '100%' : 560,
      textWrap: tablet ? 'wrap' : 'balance'
    }
  }, "Tell us your platform and your hardest constraint. We\u2019ll show you what an evolved model does with it."), t.ctaSecond !== false ? /*#__PURE__*/React.createElement(Reveal, {
    variant: "side",
    delay: 900,
    dur: 480
  }, /*#__PURE__*/React.createElement(QuietLink, {
    href: "#evolutionary",
    label: "Read the technical overview"
  })) : null), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'center',
      flex: mobile || tablet ? '1 1 100%' : '0 0 auto',
      width: mobile || tablet ? '100%' : undefined,
      maxWidth: mobile && !tablet ? 680 : undefined
    }
  }, /*#__PURE__*/React.createElement(Reveal, {
    variant: "rise",
    delay: 740,
    dur: 520,
    style: {
      width: mobile || tablet ? '100%' : undefined
    }
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    size: "lg",
    onClick: () => onNavigate('/contact'),
    style: {
      height: 58,
      padding: '0 38px',
      fontSize: 17,
      ...(mobile || tablet ? {
        width: '100%'
      } : null)
    }
  }, "Contact us")))))))));
}
Object.assign(window, {
  HomeScreen,
  QuietLink
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/HomeScreen.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/PlatformScreen.jsx
try { (() => {
const DS = () => window.VivumAIDesignSystem_b2be15;
function PlatformScreen({
  onNavigate
}) {
  const {
    GradientField,
    Eyebrow,
    Panel,
    DataTable,
    Stat,
    Divider,
    Icon,
    Button,
    Badge,
    Switch
  } = DS();
  const [compare, setCompare] = React.useState(false);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(GradientField, {
    field: "crest",
    pad: "150px var(--gutter-site) 110px",
    as: "section"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site)',
      margin: '0 auto',
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Platform"), /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontSize: 'clamp(30px, 3.2vw, 46px)',
      fontWeight: 700,
      letterSpacing: '-0.03em',
      lineHeight: 1.08,
      color: 'var(--vv-ice)',
      maxWidth: 900
    }
  }, "Adaptive Systems That Surpass Trained Control"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      maxWidth: 720
    }
  }, "Three parts: a runtime that fits the hardware you already fly, an adaptation loop that keeps improving in the field, and a ground view that stays out of the control path."))), /*#__PURE__*/React.createElement("section", {
    style: {
      padding: '96px var(--gutter-site)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site)',
      margin: '0 auto'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(3, 1fr)',
      gap: 1,
      background: 'var(--line-hairline)'
    }
  }, [{
    icon: 'memory',
    title: 'Edge Runtime',
    body: 'INT8 inference on existing compute. No accelerator dependency, no cloud round-trip, deterministic scheduling.',
    tag: 'v2.14.0'
  }, {
    icon: 'account_tree',
    title: 'Adaptation Loop',
    body: 'On-device evolution inside a bounded envelope. Every change is versioned, reversible, and auditable.',
    tag: 'bounded'
  }, {
    icon: 'monitoring',
    title: 'Ground View',
    body: 'Read-only telemetry, model provenance, and fleet health. Operators observe; the vehicle decides.',
    tag: 'read-only'
  }].map(c => /*#__PURE__*/React.createElement("div", {
    key: c.title,
    style: {
      background: 'var(--surface-canvas)',
      padding: '32px 30px',
      display: 'flex',
      flexDirection: 'column',
      gap: 18,
      minHeight: 260
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: c.icon,
    size: 26,
    color: "var(--vv-amber)"
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 24,
      fontWeight: 600,
      letterSpacing: '-0.015em',
      color: 'var(--vv-ice)'
    }
  }, c.title), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 14.5,
      lineHeight: 1.55,
      color: 'var(--text-secondary)',
      flex: 1
    }
  }, c.body), /*#__PURE__*/React.createElement(Badge, {
    tone: "graphite",
    mono: true,
    style: {
      alignSelf: 'flex-start'
    }
  }, c.tag)))))), /*#__PURE__*/React.createElement(GradientField, {
    field: "core",
    pad: "130px var(--gutter-site)",
    as: "section"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site)',
      margin: '0 auto',
      display: 'flex',
      flexDirection: 'column',
      gap: 40
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-end',
      justifyContent: 'space-between',
      gap: 40,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Specifications"), /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontSize: 'clamp(26px, 2.5vw, 36px)',
      fontWeight: 700,
      letterSpacing: '-0.02em',
      lineHeight: 1.15,
      color: 'var(--vv-ice)'
    }
  }, "Reference Envelope")), /*#__PURE__*/React.createElement(Switch, {
    label: "Compare against baseline",
    checked: compare,
    onChange: setCompare,
    style: {
      minWidth: 320
    }
  })), /*#__PURE__*/React.createElement(Panel, {
    pad: 0
  }, /*#__PURE__*/React.createElement(DataTable, {
    columns: [{
      key: 'k',
      label: 'Metric'
    }, {
      key: 'air',
      label: 'Air',
      mono: true,
      align: 'right',
      tone: 'data'
    }, {
      key: 'land',
      label: 'Land',
      mono: true,
      align: 'right',
      tone: 'data'
    }, {
      key: 'sea',
      label: 'Sea',
      mono: true,
      align: 'right',
      tone: 'data'
    }, {
      key: 'edge',
      label: 'Edge',
      mono: true,
      align: 'right',
      tone: 'data'
    }, ...(compare ? [{
      key: 'base',
      label: 'Baseline',
      mono: true,
      align: 'right',
      tone: 'muted'
    }] : [])],
    rows: [{
      k: 'Inference latency',
      air: '4.2 ms',
      land: '4.6 ms',
      sea: '5.1 ms',
      edge: '3.8 ms',
      base: '11.9 ms'
    }, {
      k: 'Power draw, peak',
      air: '11.4 W',
      land: '14.0 W',
      sea: '9.8 W',
      edge: '6.2 W',
      base: '28.5 W'
    }, {
      k: 'Adaptation window',
      air: '90 s',
      land: '120 s',
      sea: '240 s',
      edge: '60 s',
      base: 'offline'
    }, {
      k: 'Uplink required',
      air: '0 kbps',
      land: '0 kbps',
      sea: '0 kbps',
      edge: '0 kbps',
      base: '2 Mbps'
    }]
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(4, 1fr)',
      gap: 32,
      paddingTop: 8
    }
  }, /*#__PURE__*/React.createElement(Stat, {
    value: "3\xD7",
    label: "Faster than trained control"
  }), /*#__PURE__*/React.createElement(Stat, {
    value: "2.4\xD7",
    label: "Lower power envelope"
  }), /*#__PURE__*/React.createElement(Stat, {
    value: "118",
    label: "Units in field trial"
  }), /*#__PURE__*/React.createElement(Stat, {
    value: "24 h",
    label: "Rollback window"
  })))), /*#__PURE__*/React.createElement("section", {
    style: {
      padding: '88px var(--gutter-site)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site)',
      margin: '0 auto',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 40,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 24,
      fontWeight: 600,
      letterSpacing: '-0.015em',
      color: 'var(--vv-ice)',
      maxWidth: 700
    }
  }, "Integration starts with your hardware, not ours."), /*#__PURE__*/React.createElement(Button, {
    variant: "primary",
    icon: "arrow_forward",
    onClick: () => onNavigate('/briefing')
  }, "Request a briefing"))));
}
Object.assign(window, {
  PlatformScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/PlatformScreen.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/Preloader.jsx
try { (() => {
/* Boot gate, stage two. The cover itself is static markup in index.html so it is on screen
   from the first frame, before React, Babel or the design-system bundle have loaded; this file
   takes that cover over as soon as it exists and reports REAL asset progress into it.

   The homepage is built on three video plates and eight stills; served cold they
   arrive over several seconds and the page assembles in public — plates popping in behind copy
   that has already revealed, the hero rising against an empty rectangle. This holds a single
   acquire screen until every one of them is decoded, then hands over to a site that paints
   complete on its first frame.

   Progress is REAL: each asset settles its own weight, so the rail tracks the network rather
   than a scripted animation. Videos carry the most weight because they are the bytes that
   actually matter — a fake even ramp would sit at 90% for the whole hero download. */

/* Everything the page paints above the fold or scrolls into. `w` is relative byte weight,
   not a guess at time: it decides how much of the rail each asset is worth.

   A page with a different plate set declares its own list on `window.__vvBootAssets` before
   this file runs (see autonomy.html) — one preloader, one rail, per-page manifest. */
const VV_BOOT_ASSETS = typeof window !== 'undefined' && Array.isArray(window.__vvBootAssets) && window.__vvBootAssets.length ? window.__vvBootAssets : [{
  kind: 'image',
  src: '../../assets/vivum-logo-gray.svg',
  w: 1
}, {
  kind: 'image',
  src: '../../assets/plates/biological-intelligence-leaf.jpg',
  w: 3
}, {
  kind: 'image',
  src: '../../assets/plates/dynamic-neural-brain.jpg',
  w: 3
}, {
  kind: 'image',
  src: '../../assets/plates/core-tech-adaptive-learning.png',
  w: 2
}, {
  kind: 'image',
  src: '../../assets/plates/core-tech-temporal-awareness.png',
  w: 2
}, {
  kind: 'image',
  src: '../../assets/plates/core-tech-contextual-understanding.png',
  w: 2
}, {
  kind: 'image',
  src: '../../assets/plates/core-tech-efficient-computation.png',
  w: 2
}, {
  kind: 'video',
  src: '../../assets/plates/home-banner-video.mp4',
  w: 10
}, {
  kind: 'video',
  src: '../../assets/plates/biological-intelligence.mp4',
  w: 7
}, {
  kind: 'video',
  src: '../../assets/plates/evolutionary-intelligence.mp4',
  w: 7
}];
const VV_BOOT_FONT_W = 3;
/* A cold cache on a bad connection must not trap anyone on a loading screen: past this the
   gate opens regardless and the remaining plates finish arriving behind the live page — the
   same behaviour the site had before, but only as a failure mode instead of the default. */
const VV_BOOT_CAP_MS = 10000;
const VV_BOOT_ITEM_MS = 6000;
/* Enough to read as a deliberate hand-off rather than a flash of dark. Warm caches settle in
   ~80ms, and a 90ms overlay looks like a rendering fault. */
const VV_BOOT_MIN_MS = 900;
const vvBootUrl = p => typeof window.__vvResolveAsset === 'function' ? window.__vvResolveAsset(p) : p;
function vvBootLoadImage(url) {
  return new Promise(done => {
    const img = new Image();
    const settle = () => done();
    img.onload = () => {
      if (img.decode) img.decode().then(settle, settle);else settle();
    };
    img.onerror = settle;
    img.src = url;
  });
}

/* `canplaythrough` is the honest signal — the browser believes it can play the whole thing
   without stalling — but it is also the one a browser may simply never fire (autoplay policy,
   metered connection, a codec it deprioritises). `loadeddata` is accepted as the fallback so a
   plate that is merely buffering does not hold the whole site hostage. */
function vvBootLoadVideo(url) {
  return new Promise(done => {
    const v = document.createElement('video');
    let settled = false;
    const settle = () => {
      if (settled) return;
      settled = true;
      clearTimeout(soft);
      clearTimeout(hard);
      v.removeAttribute('src');
      done();
    };
    const soft = setTimeout(() => {
      if (v.readyState >= 2) settle();
    }, 2500);
    const hard = setTimeout(settle, VV_BOOT_ITEM_MS);
    v.muted = true;
    v.defaultMuted = true;
    v.playsInline = true;
    v.preload = 'auto';
    v.addEventListener('canplaythrough', settle);
    v.addEventListener('error', settle);
    v.src = url;
    v.load();
  });
}

/* Drives the whole gate: total weight in, settled weight out, plus a `done` flag that only
   flips once the minimum display time has also elapsed. */
function useVvBootProgress(t0In) {
  const total = VV_BOOT_ASSETS.reduce((s, a) => s + a.w, 0) + VV_BOOT_FONT_W;
  const [loaded, setLoaded] = React.useState(0);
  const [done, setDone] = React.useState(false);
  React.useEffect(() => {
    let live = true;
    /* Minimum display time is measured from when the COVER appeared, not from when this
       component mounted — by then it has already been up for the whole script boot. */
    const t0 = t0In || Date.now();
    let settled = 0;
    const credit = w => {
      if (!live) return;
      settled += w;
      setLoaded(settled);
    };
    const finish = () => {
      if (!live) return;
      const wait = Math.max(0, VV_BOOT_MIN_MS - (Date.now() - t0));
      setTimeout(() => {
        if (live) {
          setLoaded(total);
          setDone(true);
        }
      }, wait);
    };
    /* A tab that is not visible has its media loading deprioritised to the point where
       canplaythrough may never fire, so blocking on footage there buys nothing and costs the
       full cap. The fetch is still started — it warms the cache for when the tab is looked
       at — but the gate does not wait on it. */
    const hidden = typeof document !== 'undefined' && document.visibilityState === 'hidden';
    const jobs = VV_BOOT_ASSETS.map(a => {
      const url = vvBootUrl(a.src);
      /* The self-contained build swaps footage for a still, so trust the resolved URL's own
         extension rather than the manifest's declared kind. */
      const isVideo = a.kind === 'video' && /\.mp4($|\?)/i.test(String(url));
      if (isVideo && hidden) {
        vvBootLoadVideo(url);
        return Promise.resolve().then(() => credit(a.w));
      }
      return (isVideo ? vvBootLoadVideo(url) : vvBootLoadImage(url)).then(() => credit(a.w));
    });
    const fonts = (document.fonts && document.fonts.ready ? document.fonts.ready : Promise.resolve()).then(() => credit(VV_BOOT_FONT_W), () => credit(VV_BOOT_FONT_W));
    Promise.all(jobs.concat([fonts])).then(finish);
    const cap = setTimeout(finish, VV_BOOT_CAP_MS);
    return () => {
      live = false;
      clearTimeout(cap);
    };
  }, [total, t0In]);
  return [total ? loaded / total : 1, done];
}

/* The rail must never sit still while bytes are moving: a big plate settles in one step, so raw
   progress jumps 0 → 34% → 71%. This eases the DISPLAYED value toward the real one, which reads
   as continuous acquisition without inventing progress that hasn't happened — it can only ever
   approach the true figure, never overtake it.

   The current value lives in a ref, not a local: the effect re-runs on every progress tick, and
   a local would reset to 0 there — the counter would fall back to zero each time an asset
   landed. An interval rather than rAF because rAF is not serviced in a hidden tab, and a frozen
   00% is the one state a loading screen must never show. */
function useVvBootEased(target, done, active = true) {
  const [shown, setShown] = React.useState(0);
  const cur = React.useRef(0);
  React.useEffect(() => {
    /* Inactive when the static cover is driving the rail: it runs this same easing itself, and
       two eased stages in series would make the counter lag the network by half a second. */
    if (!active) return undefined;
    if (vvReduced()) {
      cur.current = target;
      setShown(target);
      return undefined;
    }
    const id = setInterval(() => {
      cur.current += (target - cur.current) * (done ? 0.3 : 0.12);
      if (target - cur.current < 0.004) cur.current = target;
      setShown(cur.current);
      if (cur.current === target) clearInterval(id);
    }, 40);
    return () => clearInterval(id);
  }, [target, done, active]);
  return active ? shown : target;
}
const VV_BOOT_CSS = `
@keyframes vvBootScan{0%{transform:translateX(-120%)}100%{transform:translateX(420%)}}
@keyframes vvBootPulse{0%,100%{opacity:.34}50%{opacity:1}}
@media (prefers-reduced-motion:reduce){[data-vv-preload] *{animation:none!important}}
`;

/* Renders over everything until `onReady` has fired and its own fade has finished. Owns the
   scroll lock: the site is not mounted yet, so nothing else can. */
function Preloader({
  onReady
}) {
  /* index.html paints a cover on the first frame — before this file, or React itself, has
     arrived. Where that exists it stays ON SCREEN and this component only feeds it: one cover
     for the whole boot, so the rail never restarts at the hand-off and there is no second
     overlay to cross-fade. The React overlay below is the fallback for any page that mounts
     the site without one. */
  const bridge = React.useMemo(() => (typeof window !== 'undefined' ? window.__vvBoot : null) || null, []);
  const [raw, loaded] = useVvBootProgress(bridge ? bridge.t0 : 0);
  const pct = useVvBootEased(raw, loaded, !bridge);
  React.useEffect(() => {
    if (!bridge) return;
    bridge.set(bridge.base + (1 - bridge.base) * raw, loaded ? 'Ready' : 'Loading data');
  }, [bridge, raw, loaded]);
  const [phase, setPhase] = React.useState('load');
  const fired = React.useRef(false);
  React.useEffect(() => {
    if (!loaded || fired.current) return undefined;
    fired.current = true;
    /* One frame between "the site may mount" and "the cover starts lifting", so the hero's own
       entrance begins under the overlay rather than after it — the two motions overlap the same
       way a chamfered frame overlaps its content. */
    if (onReady) onReady();
    const t = setTimeout(() => {
      setPhase('exit');
      if (bridge) bridge.exit();
    }, 90);
    const g = setTimeout(() => setPhase('gone'), 90 + 760);
    return () => {
      clearTimeout(t);
      clearTimeout(g);
    };
  }, [loaded, onReady, bridge]);
  React.useEffect(() => {
    /* The static cover owns the lock itself (html:has(#vv-boot) in index.html), so taking a
       second one here would restore `hidden` as this effect's "previous" value. */
    if (bridge || phase === 'gone') return undefined;
    const html = document.documentElement;
    const prev = html.style.overflow;
    html.style.overflow = 'hidden';
    return () => {
      html.style.overflow = prev;
    };
  }, [phase, bridge]);
  if (bridge || phase === 'gone') return null;
  const out = phase === 'exit';
  const shown = Math.round(pct * 100);
  const reduced = vvReduced();
  return /*#__PURE__*/React.createElement("div", {
    "data-vv-preload": "",
    role: "status",
    "aria-live": "polite",
    "aria-label": `Loading, ${shown} percent`,
    style: {
      position: 'fixed',
      inset: 0,
      zIndex: 200,
      display: 'grid',
      placeItems: 'center',
      background: 'var(--surface-canvas, #001121)',
      padding: '24px',
      opacity: out ? 0 : 1,
      /* The cover scales up a hair as it goes, so it reads as lifting off the page rather
         than dissolving in place. */
      transform: out && !reduced ? 'scale(1.03)' : 'none',
      transition: `opacity 700ms ${VV_EASE}, transform 700ms ${VV_EASE}`,
      pointerEvents: out ? 'none' : 'auto'
    }
  }, /*#__PURE__*/React.createElement("style", null, VV_BOOT_CSS), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: 'min(340px, 78vw)',
      display: 'flex',
      flexDirection: 'column',
      gap: 'clamp(28px, 6vw, 40px)'
    }
  }, [['top', 'left'], ['top', 'right'], ['bottom', 'left'], ['bottom', 'right']].map(([v, h]) => /*#__PURE__*/React.createElement("span", {
    key: `${v}${h}`,
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      [v]: -18,
      [h]: -18,
      width: 13,
      height: 13,
      [`border${v === 'top' ? 'Top' : 'Bottom'}`]: '1px solid var(--vv-amber)',
      [`border${h === 'left' ? 'Left' : 'Right'}`]: '1px solid var(--vv-amber)',
      opacity: 0.5
    }
  })), /*#__PURE__*/React.createElement("img", {
    src: vvBootUrl('../../assets/vivum-logo-gray.svg'),
    alt: "Vivum AI",
    style: {
      height: 34,
      width: 'auto',
      display: 'block',
      margin: '0 auto',
      opacity: 0.92
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      height: 2,
      background: 'var(--vv-ice-14)',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '0 auto 0 0',
      width: `${Math.max(2, shown)}%`,
      background: 'var(--vv-amber)',
      transition: 'width 180ms linear'
    }
  }), !reduced && !out ? /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: 0,
      width: '26%',
      background: 'linear-gradient(90deg, rgba(236,154,0,0) 0%, rgba(236,154,0,0.55) 55%, rgba(236,154,0,0) 100%)',
      animation: `vvBootScan 2200ms ${VV_EASE} infinite`
    }
  }) : null), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 16,
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      letterSpacing: '0.16em',
      textTransform: 'uppercase'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-ice-56)',
      animation: reduced || out ? undefined : 'vvBootPulse 1800ms ease-in-out infinite'
    }
  }, loaded ? 'Ready' : 'Loading data'), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--vv-amber)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, String(shown).padStart(2, '0'), "%")))));
}
Object.assign(window, {
  Preloader
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/Preloader.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/ResearchScreen.jsx
try { (() => {
const DS = () => window.VivumAIDesignSystem_b2be15;
const NOTES = [{
  id: 1,
  date: '2026-07-28',
  kind: 'Paper',
  title: 'Bounded On-Device Evolution Under Fixed Power Envelopes',
  tag: 'Edge',
  read: '18 min'
}, {
  id: 2,
  date: '2026-06-11',
  kind: 'Field note',
  title: 'Mojave Trial: 118 Units, 90-Second Adaptation Windows',
  tag: 'Land',
  read: '7 min'
}, {
  id: 3,
  date: '2026-05-02',
  kind: 'Paper',
  title: 'Provenance for Models That Change After Deployment',
  tag: 'Governance',
  read: '22 min'
}, {
  id: 4,
  date: '2026-03-19',
  kind: 'Field note',
  title: 'Sea State 5: Perception Without a Horizon Reference',
  tag: 'Sea',
  read: '9 min'
}, {
  id: 5,
  date: '2026-02-04',
  kind: 'Paper',
  title: 'Why Trained Control Fails Outside Its Distribution',
  tag: 'Theory',
  read: '26 min'
}];
function ResearchScreen({
  onToast
}) {
  const {
    GradientField,
    Eyebrow,
    Badge,
    Divider,
    Button,
    Select,
    Input,
    EmptyState,
    Icon,
    Sheet
  } = DS();
  const [kind, setKind] = React.useState('All');
  const [q, setQ] = React.useState('');
  const rows = NOTES.filter(n => (kind === 'All' || n.kind === kind) && n.title.toLowerCase().includes(q.toLowerCase()));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(GradientField, {
    field: "data",
    pad: "150px var(--gutter-site) 100px",
    as: "section"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site)',
      margin: '0 auto',
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Research"), /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontSize: 'clamp(30px, 3.2vw, 46px)',
      fontWeight: 700,
      letterSpacing: '-0.03em',
      lineHeight: 1.08,
      color: 'var(--vv-ice)',
      maxWidth: 860
    }
  }, "Papers and Field Notes"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 'clamp(16px, 1.3vw, 18px)',
      lineHeight: 1.6,
      color: 'var(--vv-ice-82)',
      maxWidth: 640
    }
  }, "What we learned, in the order we learned it. Field notes are written the week the trial ends."))), /*#__PURE__*/React.createElement("section", {
    style: {
      padding: '72px var(--gutter-site) 96px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site)',
      margin: '0 auto'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 16,
      alignItems: 'flex-end',
      justifyContent: 'space-between',
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(Input, {
    label: "Search",
    icon: "search",
    placeholder: "Adaptation, provenance\u2026",
    value: q,
    onChange: e => setQ(e.target.value),
    style: {
      width: 300
    }
  }), /*#__PURE__*/React.createElement(Select, {
    label: "Type",
    options: ['All', 'Paper', 'Field note'],
    value: kind,
    onChange: e => setKind(e.target.value),
    style: {
      width: 190
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 12,
      color: 'var(--text-caption)'
    }
  }, rows.length, " of ", NOTES.length, " entries")), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 40
    }
  }, rows.length === 0 ? /*#__PURE__*/React.createElement(EmptyState, {
    icon: "search_off",
    title: "Nothing matches that filter",
    action: /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      size: "sm",
      onClick: () => {
        setQ('');
        setKind('All');
      }
    }, "Clear filters")
  }, "Try a broader term, or clear the filters to see all entries.") : rows.map(n => /*#__PURE__*/React.createElement("div", {
    key: n.id
  }, /*#__PURE__*/React.createElement("a", {
    href: "#",
    onClick: e => {
      e.preventDefault();
      onToast({
        title: 'Opening entry',
        body: n.title,
        tone: 'live'
      });
    },
    style: {
      display: 'grid',
      gridTemplateColumns: '132px 1fr 130px 92px 28px',
      gap: 28,
      alignItems: 'center',
      padding: '22px 0',
      color: 'inherit'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 12.5,
      color: 'var(--text-caption)'
    }
  }, n.date), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 19,
      fontWeight: 500,
      letterSpacing: '-0.01em',
      color: 'var(--vv-ice)'
    }
  }, n.title), /*#__PURE__*/React.createElement(Badge, {
    tone: n.kind === 'Paper' ? 'blue' : 'graphite'
  }, n.kind), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 12,
      color: 'var(--text-caption)'
    }
  }, n.read), /*#__PURE__*/React.createElement(Icon, {
    name: "north_east",
    size: 17,
    color: "var(--vv-graphite)"
  })), /*#__PURE__*/React.createElement(Divider, null)))))), /*#__PURE__*/React.createElement("section", {
    style: {
      padding: '0 var(--gutter-site) 104px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--container-site)',
      margin: '0 auto'
    }
  }, /*#__PURE__*/React.createElement(Sheet, {
    pad: 56,
    maxWidth: "100%"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 56,
      flexWrap: 'wrap',
      alignItems: 'flex-start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 320,
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      fontWeight: 600,
      letterSpacing: 'var(--track-label)',
      textTransform: 'uppercase',
      color: 'var(--text-caption-on-light)'
    }
  }, "White paper"), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: 30,
      fontWeight: 700,
      letterSpacing: '-0.02em',
      color: 'var(--text-heading-on-light)'
    }
  }, "Bounded On-Device Evolution Under Fixed Power Envelopes"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 15,
      lineHeight: 1.6,
      color: 'var(--text-body-on-light)',
      maxWidth: 640
    }
  }, "How an adaptation loop can improve a deployed model without exceeding the power, thermal, and latency budget it shipped with \u2014 and how each change stays reversible."), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 12,
      marginTop: 8
    }
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "onLight",
    size: "md",
    iconAfter: "north_east"
  }, "Read the paper"), /*#__PURE__*/React.createElement(Button, {
    variant: "quiet",
    size: "md",
    style: {
      color: 'var(--vv-blue)'
    }
  }, "Cite"))), /*#__PURE__*/React.createElement("div", {
    style: {
      width: 260,
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, [['Published', '28 Jul 2026'], ['Authors', 'Vivum Research'], ['Pages', '18'], ['DOI', '10.0000/vivum.2026.07']].map(([k, v]) => /*#__PURE__*/React.createElement("div", {
    key: k,
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      gap: 16,
      paddingBottom: 10,
      borderBottom: '1px solid var(--line-on-light)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--text-caption-on-light)'
    }
  }, k), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-mono)',
      fontSize: 12,
      color: 'var(--text-body-on-light)'
    }
  }, v)))))))));
}
Object.assign(window, {
  ResearchScreen
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/ResearchScreen.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/SeamAnimation.jsx
try { (() => {
/* Full-bleed animated background sitting on the Autonomous Solutions → Core technology seam.
   Replaces the retired NeuralDivider video plate: same job (a breathing full-browser-width
   background between two transparent sections) with no 20MB asset. Six variants, all pure CSS.

   Geometry: PURE BACKGROUND — the outer wrapper is zero-height with overflow visible, and the
   band itself is absolutely positioned and pulled up half its own height, so it straddles the
   seam without ever contributing a single pixel of layout. Nothing above or below it moves when
   the size/position tweaks change. Its own top/bottom mask dissolves it into the canvas colour
   the way the video plates do. */

const SEAM_KEYFRAMES = `
@keyframes vvSeamDrift1{0%{transform:translate3d(-8%,-6%,0) scale(1)}50%{transform:translate3d(10%,6%,0) scale(1.14)}100%{transform:translate3d(-8%,-6%,0) scale(1)}}
@keyframes vvSeamDrift2{0%{transform:translate3d(12%,8%,0) scale(1.08)}50%{transform:translate3d(-10%,-8%,0) scale(0.94)}100%{transform:translate3d(12%,8%,0) scale(1.08)}}
@keyframes vvSeamDrift3{0%{transform:translate3d(0,4%,0) scale(0.96)}50%{transform:translate3d(-14%,-4%,0) scale(1.2)}100%{transform:translate3d(0,4%,0) scale(0.96)}}
@keyframes vvSeamSweep{0%{transform:translateX(-60%)}100%{transform:translateX(260%)}}
@keyframes vvSeamRing{0%{transform:scale(0.12);opacity:0}12%{opacity:0.5}100%{transform:scale(2.6);opacity:0}}
@keyframes vvSeamLattice{0%{background-position:0 0,0 0}100%{background-position:220px 220px,-220px 220px}}
@keyframes vvSeamNodes{0%{opacity:0.25}50%{opacity:0.7}100%{opacity:0.25}}
@keyframes vvSeamFlowLine{0%{transform:translateX(-100%)}100%{transform:translateX(100%)}}
@keyframes vvSeamGrain{0%{transform:translate3d(0,0,0)}100%{transform:translate3d(-220px,-220px,0)}}
@keyframes vvSeamGrainCoarse{0%{transform:translate3d(0,0,0) scale(1)}50%{transform:translate3d(90px,-60px,0) scale(1.1)}100%{transform:translate3d(0,0,0) scale(1)}}
@keyframes vvSeamFlicker{0%,100%{opacity:0.55}50%{opacity:0.9}}
@keyframes vvSeamSpin{to{transform:rotate(360deg)}}
@keyframes vvSeamBreathe{0%{transform:scale(1)}50%{transform:scale(1.16)}100%{transform:scale(1)}}
@keyframes vvSeamSlowSpin{0%{transform:rotate(0)}100%{transform:rotate(360deg)}}
@keyframes vvSeamRise{0%{background-position:0 0}100%{background-position:-60px -420px}}
@keyframes vvSeamSway{0%{transform:translateX(-6%)}50%{transform:translateX(6%)}100%{transform:translateX(-6%)}}
@keyframes vvSeamShaft{0%{transform:translateX(-18%)}100%{transform:translateX(18%)}}
@keyframes vvSeamCloud{0%{transform:translate3d(-4%,-3%,0) scale(1.05)}50%{transform:translate3d(4%,3%,0) scale(1.22)}100%{transform:translate3d(-4%,-3%,0) scale(1.05)}}
@keyframes vvSeamCloudAlt{0%{transform:translate3d(3%,2%,0) scale(1.16)}50%{transform:translate3d(-5%,-2%,0) scale(1)}100%{transform:translate3d(3%,2%,0) scale(1.16)}}
@keyframes vvSeamFall{0%{transform:translate3d(0,-240px,0)}100%{transform:translate3d(-40px,240px,0)}}
@keyframes vvSeamSheen{0%{transform:translateX(-120%)}100%{transform:translateX(220%)}}
@keyframes vvSeamFibre{0%{transform:translate3d(0,0,0)}100%{transform:translate3d(-500px,0,0)}}
@keyframes vvSeamSmoke{0%{transform:translate3d(-3%,12%,0) scale(1.1)}100%{transform:translate3d(3%,-12%,0) scale(1.34)}}
@keyframes vvSeamSmokeAlt{0%{transform:translate3d(4%,-8%,0) scale(1.28)}100%{transform:translate3d(-4%,10%,0) scale(1.06)}}
@keyframes vvSeamVein{0%{transform:rotate(0) scale(1.02)}50%{transform:rotate(4deg) scale(1.14)}100%{transform:rotate(0) scale(1.02)}}
@keyframes vvSeamStatic{0%{transform:translate3d(0,0,0)}100%{transform:translate3d(-110px,-110px,0)}}
@keyframes vvSeamRoll{0%{transform:translateY(-120%)}100%{transform:translateY(320%)}}
@keyframes vvSeamRibbon{0%{transform:translateX(-30%)}50%{transform:translateX(30%)}100%{transform:translateX(-30%)}}
@keyframes vvSeamTwinkle{0%,100%{opacity:0.25}40%{opacity:0.85}70%{opacity:0.4}}
@media (prefers-reduced-motion: reduce){[data-vv-seam] *{animation:none!important}}`;
const SEAM_VARIANTS = ['drift', 'scan', 'pulse', 'lattice', 'texture', 'nebula', 'ash', 'sheen', 'weave', 'smoke', 'marble', 'static', 'ribbons', 'starfield', 'orbit', 'contours', 'motes', 'waves', 'shafts', 'off'];

/* Two feTurbulence tiles carry the grain. Inline SVG data URIs, not image files, so the texture
   costs no request and scales with the band. */
const GRAIN_FINE = "url(\"data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='220' height='220'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='3' stitchTiles='stitch'/></filter><rect width='220' height='220' filter='url(%23n)' opacity='0.55'/></svg>\")";
const GRAIN_COARSE = "url(\"data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='420' height='420'><filter id='m'><feTurbulence type='fractalNoise' baseFrequency='0.14' numOctaves='2' stitchTiles='stitch'/></filter><rect width='420' height='420' filter='url(%23m)' opacity='0.6'/></svg>\")";
const GRAIN_CLOUD = "url(\"data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='700' height='700'><filter id='c'><feTurbulence type='fractalNoise' baseFrequency='0.012 0.03' numOctaves='4' stitchTiles='stitch'/><feColorMatrix type='saturate' values='0'/></filter><rect width='700' height='700' filter='url(%23c)' opacity='0.7'/></svg>\")";
const GRAIN_FIBRE = "url(\"data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='500' height='500'><filter id='f'><feTurbulence type='fractalNoise' baseFrequency='0.006 0.42' numOctaves='3' stitchTiles='stitch'/><feColorMatrix type='saturate' values='0'/></filter><rect width='500' height='500' filter='url(%23f)' opacity='0.75'/></svg>\")";
const GRAIN_VEIN = "url(\"data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='600' height='600'><filter id='v'><feTurbulence type='turbulence' baseFrequency='0.018 0.05' numOctaves='4' stitchTiles='stitch'/><feColorMatrix type='saturate' values='0'/></filter><rect width='600' height='600' filter='url(%23v)' opacity='0.8'/></svg>\")";

/* Soft amber/blue blooms crossing each other — the quietest option, and the closest read to
   the footage that used to sit here. */
function SeamDrift({
  dur
}) {
  const blob = (color, size, left, top, anim, base, delay) => ({
    position: 'absolute',
    left,
    top,
    width: size,
    height: size,
    borderRadius: '50%',
    background: `radial-gradient(closest-side, ${color}, rgba(0,17,33,0) 72%)`,
    filter: 'blur(30px)',
    animation: `${anim} ${dur(base)} ease-in-out ${dur(delay)} infinite`,
    willChange: 'transform'
  });
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: blob('rgba(236,154,0,0.15)', 'clamp(300px, 42vw, 620px)', '4%', '-38%', 'vvSeamDrift1', 26, 0)
  }), /*#__PURE__*/React.createElement("span", {
    style: blob('rgba(40,129,181,0.18)', 'clamp(340px, 48vw, 720px)', '46%', '-24%', 'vvSeamDrift2', 32, -6)
  }), /*#__PURE__*/React.createElement("span", {
    style: blob('rgba(218,232,242,0.07)', 'clamp(240px, 30vw, 460px)', '74%', '-10%', 'vvSeamDrift3', 38, -14)
  }));
}

/* Hairline field with an amber bar sweeping through it — the sensor read of the set. */
function SeamScan({
  dur
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      backgroundImage: 'repeating-linear-gradient(to right, rgba(218,232,242,0.07) 0 1px, transparent 1px 72px), repeating-linear-gradient(to bottom, rgba(218,232,242,0.05) 0 1px, transparent 1px 72px)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: 0,
      width: '34%',
      background: 'linear-gradient(90deg, rgba(236,154,0,0) 0%, rgba(236,154,0,0.08) 42%, rgba(236,154,0,0.20) 50%, rgba(236,154,0,0.08) 58%, rgba(236,154,0,0) 100%)',
      filter: 'blur(14px)',
      animation: `vvSeamSweep ${dur(9)} linear infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      top: '50%',
      height: 1,
      background: 'linear-gradient(90deg, rgba(218,232,242,0) 0%, rgba(218,232,242,0.22) 50%, rgba(218,232,242,0) 100%)'
    }
  }));
}

/* Concentric rings propagating out of the band's centre — signal, not scenery. */
function SeamPulse({
  dur,
  size
}) {
  const RINGS = [0, 2.2, 4.4, 6.6];
  const r = Math.round(size * 0.95);
  return /*#__PURE__*/React.createElement(React.Fragment, null, RINGS.map((d, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      position: 'absolute',
      left: '50%',
      top: '50%',
      width: r,
      height: r,
      translate: '-50% -50%',
      borderRadius: '50%',
      border: `1px solid ${i % 2 ? 'rgba(236,154,0,0.55)' : 'rgba(218,232,242,0.45)'}`,
      opacity: 0,
      animation: `vvSeamRing ${dur(8.8)} ${dur(d)} ease-out infinite`,
      willChange: 'transform, opacity'
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: '50%',
      top: '50%',
      width: 7,
      height: 7,
      marginLeft: -3.5,
      marginTop: -3.5,
      borderRadius: '50%',
      background: 'var(--vv-amber)',
      boxShadow: '0 0 22px 6px rgba(236,154,0,0.35)'
    }
  }));
}

/* Diagonal mesh drifting under a pulsing node grid — the topology read. */
function SeamLattice({
  dur
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-20%',
      backgroundImage: 'repeating-linear-gradient(45deg, rgba(218,232,242,0.08) 0 1px, transparent 1px 110px), repeating-linear-gradient(-45deg, rgba(40,129,181,0.14) 0 1px, transparent 1px 110px)',
      animation: `vvSeamLattice ${dur(30)} linear infinite`,
      willChange: 'background-position'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      backgroundImage: 'radial-gradient(circle, rgba(236,154,0,0.5) 0 1.6px, transparent 1.7px)',
      backgroundSize: '110px 110px',
      animation: `vvSeamNodes ${dur(6)} ease-in-out infinite`
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      top: '38%',
      height: 1,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      width: '55%',
      height: '100%',
      background: 'linear-gradient(90deg, rgba(236,154,0,0) 0%, rgba(236,154,0,0.6) 50%, rgba(236,154,0,0) 100%)',
      animation: `vvSeamFlowLine ${dur(11)} linear infinite`
    }
  })));
}

/* Film-grain texture: fine noise crawling over a slow coarse cloud, warmed by an amber wash.
   Reads as material rather than graphic — the analogue option in the set. */
function SeamTexture({
  dur
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'linear-gradient(100deg, rgba(236,154,0,0.07) 0%, rgba(40,129,181,0.08) 55%, rgba(218,232,242,0.035) 100%)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-220px',
      backgroundImage: GRAIN_COARSE,
      backgroundSize: '420px 420px',
      mixBlendMode: 'overlay',
      opacity: 0.32,
      animation: `vvSeamGrainCoarse ${dur(34)} ease-in-out infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-260px',
      backgroundImage: GRAIN_FINE,
      backgroundSize: '220px 220px',
      mixBlendMode: 'soft-light',
      opacity: 0.35,
      animation: `vvSeamGrain ${dur(2.6)} steps(6) infinite, vvSeamFlicker ${dur(5)} ease-in-out infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      backgroundImage: 'repeating-linear-gradient(to bottom, rgba(0,17,33,0.22) 0 1px, transparent 1px 4px)',
      opacity: 0.4
    }
  }));
}

/* Dots tracking elliptical orbits around a shared amber core. */
function SeamOrbit({
  dur,
  size
}) {
  const PATHS = [[0.95, 22, 'rgba(236,154,0,0.9)'], [0.66, 15, 'rgba(218,232,242,0.75)'], [1.28, 34, 'rgba(40,129,181,0.95)']];
  return /*#__PURE__*/React.createElement(React.Fragment, null, PATHS.map(([f, base, color], i) => {
    const w = Math.round(size * f);
    return /*#__PURE__*/React.createElement("span", {
      key: i,
      style: {
        position: 'absolute',
        left: '50%',
        top: '50%',
        width: w,
        height: w,
        translate: '-50% -50%',
        scale: '1 0.42'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'absolute',
        inset: 0,
        borderRadius: '50%',
        border: '1px solid rgba(218,232,242,0.10)'
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'absolute',
        inset: 0,
        animation: `vvSeamSpin ${dur(base)} linear infinite`,
        animationDirection: i === 1 ? 'reverse' : 'normal',
        willChange: 'transform'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'absolute',
        left: -3,
        top: '50%',
        width: 6,
        height: 6,
        marginTop: -3,
        borderRadius: '50%',
        background: color,
        boxShadow: `0 0 8px 2px ${color}`,
        scale: '1 2.38'
      }
    })));
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: '50%',
      top: '50%',
      width: 'clamp(120px, 16vw, 220px)',
      aspectRatio: '1',
      translate: '-50% -50%',
      borderRadius: '50%',
      background: 'radial-gradient(closest-side, rgba(236,154,0,0.18), rgba(0,17,33,0) 70%)',
      filter: 'blur(14px)'
    }
  }));
}

/* Topographic contour rings breathing against a slowly turning counter-set — terrain, read as
   pure line. */
function SeamContours({
  dur
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-40%',
      backgroundImage: 'repeating-radial-gradient(circle at 50% 50%, rgba(218,232,242,0.10) 0 1px, transparent 1px 26px)',
      animation: `vvSeamBreathe ${dur(18)} ease-in-out infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-60%',
      backgroundImage: 'repeating-radial-gradient(circle at 32% 50%, rgba(40,129,181,0.14) 0 1px, transparent 1px 34px)',
      animation: `vvSeamSlowSpin ${dur(160)} linear infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'radial-gradient(closest-side at 50% 50%, rgba(236,154,0,0.10), rgba(0,17,33,0) 70%)'
    }
  }));
}

/* Fine motes rising through the band at three depths — the softest, most atmospheric option. */
function SeamMotes({
  dur
}) {
  const layer = (color, dot, gap, base, op) => ({
    position: 'absolute',
    inset: 0,
    backgroundImage: `radial-gradient(circle, ${color} 0 ${dot}px, transparent ${dot + 0.6}px)`,
    backgroundSize: `${gap}px ${gap}px`,
    opacity: op,
    animation: `vvSeamRise ${dur(base)} linear infinite`,
    willChange: 'background-position'
  });
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'linear-gradient(90deg, rgba(40,129,181,0.10) 0%, rgba(0,17,33,0) 60%)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: layer('rgba(218,232,242,0.75)', 1, 54, 26, 0.6)
  }), /*#__PURE__*/React.createElement("span", {
    style: layer('rgba(236,154,0,0.8)', 1.4, 92, 18, 0.6)
  }), /*#__PURE__*/React.createElement("span", {
    style: layer('rgba(218,232,242,0.5)', 2.2, 150, 12, 0.45)
  }));
}

/* Four crests swaying past each other — the band as a slow tide line. */
function SeamWaves({
  dur
}) {
  const CRESTS = [[0, 'rgba(218,232,242,0.20)', 22], [18, 'rgba(236,154,0,0.26)', 28], [38, 'rgba(40,129,181,0.34)', 34], [58, 'rgba(218,232,242,0.12)', 42]];
  return /*#__PURE__*/React.createElement(React.Fragment, null, CRESTS.map(([top, color, base], i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      position: 'absolute',
      left: '-30%',
      width: '160%',
      top: `${top}%`,
      height: '150%',
      borderRadius: '50%',
      borderTop: `1px solid ${color}`,
      animation: `vvSeamSway ${dur(base)} ease-in-out ${dur(-i * 3)} infinite`,
      willChange: 'transform'
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'linear-gradient(to top, rgba(40,129,181,0.10), rgba(0,17,33,0) 70%)'
    }
  }));
}

/* Angled light shafts drifting across each other — the most directional option in the set. */
function SeamShafts({
  dur
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-50%',
      backgroundImage: 'repeating-linear-gradient(102deg, rgba(236,154,0,0.10) 0 2px, rgba(0,17,33,0) 2px 78px)',
      filter: 'blur(3px)',
      animation: `vvSeamShaft ${dur(24)} ease-in-out infinite alternate`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-50%',
      backgroundImage: 'repeating-linear-gradient(96deg, rgba(218,232,242,0.07) 0 3px, rgba(0,17,33,0) 3px 124px)',
      filter: 'blur(6px)',
      animation: `vvSeamShaft ${dur(38)} ease-in-out infinite alternate-reverse`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'radial-gradient(120% 90% at 20% 0%, rgba(40,129,181,0.16), rgba(0,17,33,0) 70%)'
    }
  }));
}

/* ── Texture family ─────────────────────────────────────────────────────────────────────────
   All four build on the same device as Texture: tinted wash + blended feTurbulence tiles in
   motion. They differ in the noise's frequency, blend mode and direction of travel, which is
   what separates cloud from grit from satin from fibre. */

/* Low-frequency turbulence clouds crossing each other — the softest of the family. */
function SeamNebula({
  dur
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'linear-gradient(112deg, rgba(40,129,181,0.16) 0%, rgba(236,154,0,0.10) 62%, rgba(0,17,33,0) 100%)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-45%',
      backgroundImage: GRAIN_CLOUD,
      backgroundSize: '700px 700px',
      mixBlendMode: 'overlay',
      opacity: 0.34,
      animation: `vvSeamCloud ${dur(46)} ease-in-out infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-45%',
      backgroundImage: GRAIN_CLOUD,
      backgroundSize: '1100px 1100px',
      mixBlendMode: 'soft-light',
      opacity: 0.45,
      animation: `vvSeamCloudAlt ${dur(64)} ease-in-out infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'radial-gradient(90% 120% at 74% 30%, rgba(236,154,0,0.09), rgba(0,17,33,0) 68%)'
    }
  }));
}

/* Dense grain falling through a cold vignette — the grittiest read of the set. */
function SeamAsh({
  dur
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'linear-gradient(to bottom, rgba(218,232,242,0.05) 0%, rgba(40,129,181,0.10) 100%)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-280px',
      backgroundImage: GRAIN_FINE,
      backgroundSize: '160px 160px',
      mixBlendMode: 'overlay',
      opacity: 0.36,
      animation: `vvSeamFall ${dur(9)} linear infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-280px',
      backgroundImage: GRAIN_COARSE,
      backgroundSize: '520px 520px',
      mixBlendMode: 'soft-light',
      opacity: 0.4,
      animation: `vvSeamFall ${dur(26)} linear infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'radial-gradient(120% 120% at 50% 50%, rgba(0,17,33,0) 40%, rgba(0,17,33,0.55) 100%)'
    }
  }));
}

/* Satin sheen: a still noise ground with a brighter grain band sweeping across it. */
function SeamSheen({
  dur
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'linear-gradient(100deg, rgba(218,232,242,0.045) 0%, rgba(40,129,181,0.09) 100%)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      backgroundImage: GRAIN_COARSE,
      backgroundSize: '360px 360px',
      mixBlendMode: 'soft-light',
      opacity: 0.35
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: 0,
      width: '46%',
      backgroundImage: GRAIN_FINE,
      backgroundSize: '200px 200px',
      mixBlendMode: 'overlay',
      opacity: 0.85,
      maskImage: 'linear-gradient(100deg, rgba(0,0,0,0) 0%, rgb(0,0,0) 50%, rgba(0,0,0,0) 100%)',
      WebkitMaskImage: 'linear-gradient(100deg, rgba(0,0,0,0) 0%, rgb(0,0,0) 50%, rgba(0,0,0,0) 100%)',
      animation: `vvSeamSheen ${dur(14)} ease-in-out infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: 0,
      width: '30%',
      background: 'linear-gradient(90deg, rgba(236,154,0,0) 0%, rgba(236,154,0,0.10) 50%, rgba(236,154,0,0) 100%)',
      filter: 'blur(18px)',
      animation: `vvSeamSheen ${dur(14)} ease-in-out infinite`,
      willChange: 'transform'
    }
  }));
}

/* Stretched turbulence combed into horizontal fibres, tracking sideways — woven material. */
function SeamWeave({
  dur
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'linear-gradient(96deg, rgba(236,154,0,0.09) 0%, rgba(40,129,181,0.11) 70%, rgba(0,17,33,0) 100%)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-30px -520px',
      backgroundImage: GRAIN_FIBRE,
      backgroundSize: '500px 500px',
      mixBlendMode: 'overlay',
      opacity: 0.5,
      animation: `vvSeamFibre ${dur(22)} linear infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-30px -520px',
      backgroundImage: GRAIN_FIBRE,
      backgroundSize: '500px 260px',
      mixBlendMode: 'soft-light',
      opacity: 0.4,
      animation: `vvSeamFibre ${dur(38)} linear infinite reverse`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      backgroundImage: 'repeating-linear-gradient(to right, rgba(0,17,33,0.20) 0 1px, transparent 1px 5px)',
      opacity: 0.45
    }
  }));
}

/* Blurred turbulence plumes climbing through each other — the loosest of the family. */
function SeamSmoke({
  dur
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'linear-gradient(to top, rgba(40,129,181,0.14) 0%, rgba(0,17,33,0) 80%)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-55%',
      backgroundImage: GRAIN_CLOUD,
      backgroundSize: '820px 820px',
      filter: 'blur(16px)',
      mixBlendMode: 'screen',
      opacity: 0.42,
      animation: `vvSeamSmoke ${dur(30)} ease-in-out infinite alternate`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-55%',
      backgroundImage: GRAIN_CLOUD,
      backgroundSize: '1300px 1300px',
      filter: 'blur(28px)',
      mixBlendMode: 'overlay',
      opacity: 0.55,
      animation: `vvSeamSmokeAlt ${dur(52)} ease-in-out infinite alternate`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'radial-gradient(80% 100% at 30% 100%, rgba(236,154,0,0.10), rgba(0,17,33,0) 70%)'
    }
  }));
}

/* Turbulence veining, breathing and turning under itself — stone rather than film. */
function SeamMarble({
  dur
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'linear-gradient(104deg, rgba(218,232,242,0.05) 0%, rgba(40,129,181,0.10) 100%)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-45%',
      backgroundImage: GRAIN_VEIN,
      backgroundSize: '600px 600px',
      filter: 'contrast(1.7)',
      mixBlendMode: 'overlay',
      opacity: 0.42,
      animation: `vvSeamVein ${dur(44)} ease-in-out infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-45%',
      backgroundImage: GRAIN_VEIN,
      backgroundSize: '1250px 1250px',
      mixBlendMode: 'soft-light',
      opacity: 0.5,
      animation: `vvSeamVein ${dur(70)} ease-in-out infinite reverse`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'radial-gradient(70% 110% at 78% 40%, rgba(236,154,0,0.08), rgba(0,17,33,0) 70%)'
    }
  }));
}

/* Hard fine noise stepping frame to frame under a slow roll bar — signal loss, CRT read. */
function SeamStatic({
  dur
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'linear-gradient(to bottom, rgba(40,129,181,0.10), rgba(0,17,33,0) 100%)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: '-140px',
      backgroundImage: GRAIN_FINE,
      backgroundSize: '110px 110px',
      mixBlendMode: 'overlay',
      opacity: 0.5,
      animation: `vvSeamStatic ${dur(0.5)} steps(4) infinite`,
      willChange: 'transform'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      backgroundImage: 'repeating-linear-gradient(to bottom, rgba(0,17,33,0.30) 0 1px, transparent 1px 3px)',
      opacity: 0.6
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      top: 0,
      height: '22%',
      background: 'linear-gradient(to bottom, rgba(218,232,242,0) 0%, rgba(218,232,242,0.10) 50%, rgba(218,232,242,0) 100%)',
      filter: 'blur(6px)',
      animation: `vvSeamRoll ${dur(7)} linear infinite`,
      willChange: 'transform'
    }
  }));
}

/* Wide soft gradient ribbons sliding past each other at a shallow angle. */
function SeamRibbons({
  dur
}) {
  const BANDS = [['rgba(236,154,0,0.16)', -7, '46%', '6%', 26], ['rgba(40,129,181,0.22)', -4, '52%', '34%', 34], ['rgba(218,232,242,0.09)', -10, '30%', '62%', 44]];
  return /*#__PURE__*/React.createElement(React.Fragment, null, BANDS.map(([color, rot, h, top, base], i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      position: 'absolute',
      left: '-25%',
      width: '150%',
      top,
      height: h,
      background: `linear-gradient(90deg, rgba(0,17,33,0) 0%, ${color} 45%, ${color} 55%, rgba(0,17,33,0) 100%)`,
      rotate: `${rot}deg`,
      filter: 'blur(26px)',
      animation: `vvSeamRibbon ${dur(base)} ease-in-out infinite`,
      willChange: 'transform'
    }
  })));
}

/* Three depths of stars twinkling out of phase and drifting sideways as parallax. Each layer
   stacks four dot tilings at coprime sizes, so the scatter never resolves into the visible
   grid a single tiling gives. */
function SeamStarfield({
  dur
}) {
  const TILES = [89, 137, 191, 233];
  const layer = (color, dot, scale, driftBase, twinkleBase, op, dir) => {
    const dots = TILES.map((tile, i) => ({
      image: `radial-gradient(circle at ${18 + i * 21}% ${26 + i * 17}%, ${color} 0 ${dot}px, transparent ${dot + 0.8}px)`,
      size: `${Math.round(tile * scale)}px ${Math.round(tile * scale * 0.78)}px`
    }));
    return {
      position: 'absolute',
      inset: '-12%',
      backgroundImage: dots.map(d => d.image).join(', '),
      backgroundSize: dots.map(d => d.size).join(', '),
      opacity: op,
      animation: `vvSeamRibbon ${dur(driftBase)} ease-in-out ${dir} infinite, vvSeamTwinkle ${dur(twinkleBase)} ease-in-out infinite`,
      willChange: 'transform, opacity'
    };
  };
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      inset: 0,
      background: 'radial-gradient(100% 130% at 50% 120%, rgba(40,129,181,0.16), rgba(0,17,33,0) 70%)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: layer('rgba(218,232,242,0.9)', 1, 1, 90, 4.5, 0.6, 'normal')
  }), /*#__PURE__*/React.createElement("span", {
    style: layer('rgba(236,154,0,0.95)', 1.5, 1.9, 130, 6.5, 0.6, 'reverse')
  }), /*#__PURE__*/React.createElement("span", {
    style: layer('rgba(218,232,242,0.7)', 2.2, 3.1, 170, 9, 0.5, 'normal')
  }));
}
const SEAM_RENDERERS = {
  drift: SeamDrift,
  scan: SeamScan,
  pulse: SeamPulse,
  lattice: SeamLattice,
  texture: SeamTexture,
  nebula: SeamNebula,
  ash: SeamAsh,
  sheen: SeamSheen,
  weave: SeamWeave,
  smoke: SeamSmoke,
  marble: SeamMarble,
  static: SeamStatic,
  ribbons: SeamRibbons,
  starfield: SeamStarfield,
  orbit: SeamOrbit,
  contours: SeamContours,
  motes: SeamMotes,
  waves: SeamWaves,
  shafts: SeamShafts
};
function SeamAnimation({
  variant = 'drift',
  size = 240,
  speed = 1,
  x = 0,
  y = 0,
  opacity = 1
}) {
  const Body = SEAM_RENDERERS[variant];
  if (!Body) return null;
  const s = Math.max(0.1, Number(speed) || 1);
  const dur = base => `${(base / s).toFixed(2)}s`;
  return /*#__PURE__*/ /* Zero-height host: the band hangs off it, so the sections around it never shift. */React.createElement("div", {
    style: {
      position: 'relative',
      height: 0,
      overflow: 'visible',
      pointerEvents: 'none'
    },
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("div", {
    "data-vv-seam": variant,
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      top: 0,
      height: size,
      transform: `translate(${x}%, calc(-50% + ${y}%))`,
      opacity,
      overflow: 'hidden',
      /* Dissolves into the canvas at both edges so neither section gets a visible seam line. */
      maskImage: 'linear-gradient(to bottom, rgba(0,0,0,0) 0%, rgb(0,0,0) 26%, rgb(0,0,0) 74%, rgba(0,0,0,0) 100%)',
      WebkitMaskImage: 'linear-gradient(to bottom, rgba(0,0,0,0) 0%, rgb(0,0,0) 26%, rgb(0,0,0) 74%, rgba(0,0,0,0) 100%)'
    }
  }, /*#__PURE__*/React.createElement("style", null, SEAM_KEYFRAMES), /*#__PURE__*/React.createElement(Body, {
    dur: dur,
    size: size
  })));
}
Object.assign(window, {
  SeamAnimation,
  SEAM_VARIANTS
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/SeamAnimation.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/Site.jsx
try { (() => {
const DS = () => window.VivumAIDesignSystem_b2be15;

/* The site is not mounted until every plate and still is decoded, so its first frame is the
   finished page — and, just as importantly, so the hero's reveal ramp starts when the user can
   actually see it. Rendering behind the cover instead would burn the entrance during loading.

   __vvBooted is a one-shot: an editor hot-reload remounts Site, and replaying the boot screen
   on every code change would make the page unworkable to author. A real page load clears it. */
/* The screens that exist as real files. Anything not in here stays inert: the link is
   still visible (a marketing page with half its nav removed is not the design), it just does
   not go anywhere yet. */
const SITE_PAGES = {
  '/': 'index.html',
  '/autonomy': 'autonomy.html',
  '/about': 'about.html',
  '/contact': 'contact.html'
};
const SITE_SCREENS = {
  '/': 'HomeScreen',
  '/autonomy': 'AutonomyScreen',
  '/about': 'AboutScreen',
  '/contact': 'ContactScreen'
};
/* Routes whose first element is a full-bleed video plate — the bar floats over it. */
const SITE_HERO_ROUTES = ['/', '/autonomy', '/about'];
function Site({
  route = '/'
}) {
  const [booted, setBooted] = React.useState(() => window.__vvBooted === true);
  const onReady = React.useCallback(() => {
    window.__vvBooted = true;
    setBooted(true);
  }, []);
  const {
    NavBar,
    Footer,
    Toast
  } = DS();
  /* index.html paints a static boot cover before any of this code exists; Preloader lifts it
     on the normal path. This is the other path: an editor hot-reload that remounts Site with
     __vvBooted already set never mounts a Preloader, so the cover would have nothing to
     dismiss it. */
  React.useEffect(() => {
    if (booted && window.__vvBoot) window.__vvBoot.exit();
  }, [booted]);
  const [toast, setToast] = React.useState(null);
  /* Navigation is a real page load between the two built pages — the kit is click-through, not
     a single-file router. Unbuilt routes resolve to nothing rather than to a blank screen. */
  const go = React.useCallback(href => {
    const path = String(href || '').replace(/^#/, '');
    const file = SITE_PAGES[path];
    if (file && path !== route) window.location.href = file;
  }, [route]);
  React.useEffect(() => {
    if (!toast) return;
    const t = setTimeout(() => setToast(null), 4200);
    return () => clearTimeout(t);
  }, [toast]);
  // Nav links, footer nav, legal links and CTAs stay visible but inert: their own handlers
  // already call preventDefault() before onNavigate, so a no-op onNavigate is enough for
  // anything the screens own. This guard catches links the screens don't own (footer nav,
  // Terms, Privacy) — mailto:, tel:, in-page #anchors, absolute URLs and anything aimed at a
  // new tab are deliberately left alone.
  React.useEffect(() => {
    const swallow = e => {
      const a = e.target && e.target.closest && e.target.closest('a[href]');
      if (!a) return;
      const href = a.getAttribute('href') || '';
      if (/^(#|mailto:|tel:|https?:\/\/)/.test(href)) return;
      if (a.target === '_blank') return;
      e.preventDefault();
      go(href);
    };
    document.addEventListener('click', swallow, true);
    return () => document.removeEventListener('click', swallow, true);
  }, [go]);
  const links = [{
    href: '/autonomy',
    label: 'Autonomy'
  }, {
    href: '/about',
    label: 'About'
  }];
  /* Resolved off `window` rather than by identifier: each page loads only the screen files it
     needs, so naming the others directly would be a ReferenceError at render. */
  const Screen = window[SITE_SCREENS[route]] || window.HomeScreen || window.AutonomyScreen;
  const heroRoute = SITE_HERO_ROUTES.indexOf(route) > -1;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      minHeight: '100%',
      background: 'var(--surface-canvas)'
    }
  }, booted ? null : /*#__PURE__*/React.createElement(Preloader, {
    onReady: onReady
  }), booted ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'sticky',
      top: 0,
      zIndex: 20,
      height: heroRoute ? 0 : undefined
    }
  }, /*#__PURE__*/React.createElement(NavBar, {
    logoSrc: "../../assets/vivum-logo-gray.svg",
    links: links,
    activeHref: route,
    transparent: heroRoute,
    onNavigate: go,
    cta: {
      label: 'Contact us',
      onClick: () => go('/contact')
    }
  })), /*#__PURE__*/React.createElement(Screen, {
    onNavigate: go,
    onToast: setToast
  }), /*#__PURE__*/React.createElement(Footer, {
    markSrc: "../../assets/vivum-logo-gray.svg",
    tagline: "Evolutionary AI for air, land, sea, space, and the edge.",
    nav: [{
      label: 'Autonomy',
      href: '/autonomy'
    }, {
      label: 'About',
      href: '/about'
    }, {
      label: 'Contact',
      href: '/contact'
    }],
    location: ['South San Francisco, CA', '611 Gateway Blvd (Suite 200)', 'San Francisco, CA 94080'],
    emails: [{
      label: 'Commercial partnerships',
      email: 'partnerships@vivum.ai'
    }, {
      label: 'Defense applications',
      email: 'teaming@vivum.ai'
    }],
    social: [{
      name: 'x',
      href: '#'
    }, {
      name: 'linkedin',
      href: '#'
    }, {
      name: 'facebook',
      href: '#'
    }],
    legalLinks: [{
      label: 'Terms of use',
      href: '/terms'
    }, {
      label: 'Privacy policy',
      href: '/privacy'
    }]
  }), toast ? /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'fixed',
      right: 24,
      bottom: 24,
      zIndex: 60
    }
  }, /*#__PURE__*/React.createElement(Toast, {
    title: toast.title,
    tone: toast.tone || 'live',
    onClose: () => setToast(null)
  }, toast.body)) : null) : null);
}
Object.assign(window, {
  Site
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/Site.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/contact-countries.js
try { (() => {
/* Dial codes for the phone field's country picker. A curated list, not the ISO 249 — a
   contact form's job is to accept a number, not to be a reference table, and a shorter list
   is faster to scan than it is to search. [iso, name, dial, flag].

   Flags are regional-indicator pairs, so they need no asset and no request. Where the
   platform has no flag font (Windows, most notably) the pair falls back to the two ISO
   letters, which is the right degradation: still an identifier, still legible, no box glyph.
   United States sits first because it is the default; everything after it is alphabetical. */
window.VV_COUNTRIES = [['US', 'United States', '+1', '\uD83C\uDDFA\uD83C\uDDF8'], ['AU', 'Australia', '+61', '\uD83C\uDDE6\uD83C\uDDFA'], ['AT', 'Austria', '+43', '\uD83C\uDDE6\uD83C\uDDF9'], ['BE', 'Belgium', '+32', '\uD83C\uDDE7\uD83C\uDDEA'], ['BR', 'Brazil', '+55', '\uD83C\uDDE7\uD83C\uDDF7'], ['CA', 'Canada', '+1', '\uD83C\uDDE8\uD83C\uDDE6'], ['CL', 'Chile', '+56', '\uD83C\uDDE8\uD83C\uDDF1'], ['CN', 'China', '+86', '\uD83C\uDDE8\uD83C\uDDF3'], ['CZ', 'Czechia', '+420', '\uD83C\uDDE8\uD83C\uDDFF'], ['DK', 'Denmark', '+45', '\uD83C\uDDE9\uD83C\uDDF0'], ['EE', 'Estonia', '+372', '\uD83C\uDDEA\uD83C\uDDEA'], ['FI', 'Finland', '+358', '\uD83C\uDDEB\uD83C\uDDEE'], ['FR', 'France', '+33', '\uD83C\uDDEB\uD83C\uDDF7'], ['DE', 'Germany', '+49', '\uD83C\uDDE9\uD83C\uDDEA'], ['GR', 'Greece', '+30', '\uD83C\uDDEC\uD83C\uDDF7'], ['IN', 'India', '+91', '\uD83C\uDDEE\uD83C\uDDF3'], ['ID', 'Indonesia', '+62', '\uD83C\uDDEE\uD83C\uDDE9'], ['IE', 'Ireland', '+353', '\uD83C\uDDEE\uD83C\uDDEA'], ['IL', 'Israel', '+972', '\uD83C\uDDEE\uD83C\uDDF1'], ['IT', 'Italy', '+39', '\uD83C\uDDEE\uD83C\uDDF9'], ['JP', 'Japan', '+81', '\uD83C\uDDEF\uD83C\uDDF5'], ['MX', 'Mexico', '+52', '\uD83C\uDDF2\uD83C\uDDFD'], ['NL', 'Netherlands', '+31', '\uD83C\uDDF3\uD83C\uDDF1'], ['NZ', 'New Zealand', '+64', '\uD83C\uDDF3\uD83C\uDDFF'], ['NO', 'Norway', '+47', '\uD83C\uDDF3\uD83C\uDDF4'], ['PH', 'Philippines', '+63', '\uD83C\uDDF5\uD83C\uDDED'], ['PL', 'Poland', '+48', '\uD83C\uDDF5\uD83C\uDDF1'], ['PT', 'Portugal', '+351', '\uD83C\uDDF5\uD83C\uDDF9'], ['SA', 'Saudi Arabia', '+966', '\uD83C\uDDF8\uD83C\uDDE6'], ['SG', 'Singapore', '+65', '\uD83C\uDDF8\uD83C\uDDEC'], ['ZA', 'South Africa', '+27', '\uD83C\uDDFF\uD83C\uDDE6'], ['KR', 'South Korea', '+82', '\uD83C\uDDF0\uD83C\uDDF7'], ['ES', 'Spain', '+34', '\uD83C\uDDEA\uD83C\uDDF8'], ['SE', 'Sweden', '+46', '\uD83C\uDDF8\uD83C\uDDEA'], ['CH', 'Switzerland', '+41', '\uD83C\uDDE8\uD83C\uDDED'], ['TW', 'Taiwan', '+886', '\uD83C\uDDF9\uD83C\uDDFC'], ['TR', 'Türkiye', '+90', '\uD83C\uDDF9\uD83C\uDDF7'], ['AE', 'United Arab Emirates', '+971', '\uD83C\uDDE6\uD83C\uDDEA'], ['GB', 'United Kingdom', '+44', '\uD83C\uDDEC\uD83C\uDDE7'], ['UA', 'Ukraine', '+380', '\uD83C\uDDFA\uD83C\uDDE6'], ['VN', 'Vietnam', '+84', '\uD83C\uDDFB\uD83C\uDDF3']];
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/contact-countries.js", error: String((e && e.message) || e) }); }

// ui_kits/website/tweaks-panel.jsx
try { (() => {
// @ds-adherence-ignore -- omelette starter scaffold (raw elements/hex/px by design)
// Copied omelette starter. Re-running copy_starter_component with this kind overwrites this file with the latest version (page content is unaffected).

/* BEGIN USAGE */
// tweaks-panel.jsx
// Reusable Tweaks shell + form-control helpers.
// Exports (to window): useTweaks, TweaksPanel, TweakSection, TweakRow, TweakSlider,
//   TweakToggle, TweakRadio, TweakSelect, TweakText, TweakNumber, TweakColor, TweakButton.
//
// Owns the host protocol (listens for __activate_edit_mode / __deactivate_edit_mode,
// posts __edit_mode_available / __edit_mode_set_keys / __edit_mode_dismissed) so
// individual prototypes don't re-roll it. Ships a consistent set of controls so you
// don't hand-draw <input type="range">, segmented radios, steppers, etc.
//
// Usage (in an HTML file that loads React + Babel):
//
//   const TWEAK_DEFAULTS = /*EDITMODE-BEGIN*/{
//     "primaryColor": "#D97757",
//     "palette": ["#D97757", "#29261b", "#f6f4ef"],
//     "fontSize": 16,
//     "density": "regular",
//     "dark": false
//   }/*EDITMODE-END*/;
//
//   function App() {
//     const [t, setTweak] = useTweaks(TWEAK_DEFAULTS);
//     return (
//       <div style={{ fontSize: t.fontSize, color: t.primaryColor }}>
//         Hello
//         <TweaksPanel>
//           <TweakSection label="Typography" />
//           <TweakSlider label="Font size" value={t.fontSize} min={10} max={32} unit="px"
//                        onChange={(v) => setTweak('fontSize', v)} />
//           <TweakRadio  label="Density" value={t.density}
//                        options={['compact', 'regular', 'comfy']}
//                        onChange={(v) => setTweak('density', v)} />
//           <TweakSection label="Theme" />
//           <TweakColor  label="Primary" value={t.primaryColor}
//                        options={['#D97757', '#2A6FDB', '#1F8A5B', '#7A5AE0']}
//                        onChange={(v) => setTweak('primaryColor', v)} />
//           <TweakColor  label="Palette" value={t.palette}
//                        options={[['#D97757', '#29261b', '#f6f4ef'],
//                                  ['#475569', '#0f172a', '#f1f5f9']]}
//                        onChange={(v) => setTweak('palette', v)} />
//           <TweakToggle label="Dark mode" value={t.dark}
//                        onChange={(v) => setTweak('dark', v)} />
//         </TweaksPanel>
//       </div>
//     );
//   }
//
// TweakRadio is the segmented control for 2–3 short options (auto-falls-back to
// TweakSelect past ~16/~10 chars per label); reach for TweakSelect directly when
// options are many or long. For color tweaks always curate 3-4 options rather than
// a free picker; an option can also be a whole 2–5 color palette (the stored value
// is the array). The Tweak* controls are a floor, not a ceiling — build custom
// controls inside the panel if a tweak calls for UI they don't cover.
/* END USAGE */
// ─────────────────────────────────────────────────────────────────────────────

const __TWEAKS_STYLE = `
  .twk-panel{position:fixed;right:16px;bottom:16px;z-index:2147483646;width:280px;
    max-height:calc(100vh - 32px);display:flex;flex-direction:column;
    transform:scale(var(--dc-inv-zoom,1));transform-origin:bottom right;
    background:rgba(250,249,247,.78);color:#29261b;
    -webkit-backdrop-filter:blur(24px) saturate(160%);backdrop-filter:blur(24px) saturate(160%);
    border:.5px solid rgba(255,255,255,.6);border-radius:14px;
    box-shadow:0 1px 0 rgba(255,255,255,.5) inset,0 12px 40px rgba(0,0,0,.18);
    font:11.5px/1.4 ui-sans-serif,system-ui,-apple-system,sans-serif;overflow:hidden}
  .twk-hd{display:flex;align-items:center;justify-content:space-between;
    padding:10px 8px 10px 14px;cursor:move;user-select:none}
  .twk-hd b{font-size:12px;font-weight:600;letter-spacing:.01em}
  .twk-x{appearance:none;border:0;background:transparent;color:rgba(41,38,27,.55);
    width:22px;height:22px;border-radius:6px;cursor:default;font-size:13px;line-height:1}
  .twk-x:hover{background:rgba(0,0,0,.06);color:#29261b}
  .twk-body{padding:2px 14px 14px;display:flex;flex-direction:column;gap:10px;
    overflow-y:auto;overflow-x:hidden;min-height:0;
    scrollbar-width:thin;scrollbar-color:rgba(0,0,0,.15) transparent}
  .twk-body::-webkit-scrollbar{width:8px}
  .twk-body::-webkit-scrollbar-track{background:transparent;margin:2px}
  .twk-body::-webkit-scrollbar-thumb{background:rgba(0,0,0,.15);border-radius:4px;
    border:2px solid transparent;background-clip:content-box}
  .twk-body::-webkit-scrollbar-thumb:hover{background:rgba(0,0,0,.25);
    border:2px solid transparent;background-clip:content-box}
  .twk-row{display:flex;flex-direction:column;gap:5px}
  .twk-row-h{flex-direction:row;align-items:center;justify-content:space-between;gap:10px}
  .twk-lbl{display:flex;justify-content:space-between;align-items:baseline;
    color:rgba(41,38,27,.72)}
  .twk-lbl>span:first-child{font-weight:500}
  .twk-val{color:rgba(41,38,27,.5);font-variant-numeric:tabular-nums}

  .twk-sect{font-size:10px;font-weight:600;letter-spacing:.06em;text-transform:uppercase;
    color:rgba(41,38,27,.45);padding:10px 0 0}
  .twk-sect:first-child{padding-top:0}

  .twk-field{appearance:none;box-sizing:border-box;width:100%;min-width:0;height:26px;padding:0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;
    background:rgba(255,255,255,.6);color:inherit;font:inherit;outline:none}
  .twk-field:focus{border-color:rgba(0,0,0,.25);background:rgba(255,255,255,.85)}
  select.twk-field{padding-right:22px;
    background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='10' height='6' viewBox='0 0 10 6'><path fill='rgba(0,0,0,.5)' d='M0 0h10L5 6z'/></svg>");
    background-repeat:no-repeat;background-position:right 8px center}

  .twk-slider{appearance:none;-webkit-appearance:none;width:100%;height:4px;margin:6px 0;
    border-radius:999px;background:rgba(0,0,0,.12);outline:none}
  .twk-slider::-webkit-slider-thumb{-webkit-appearance:none;appearance:none;
    width:14px;height:14px;border-radius:50%;background:#fff;
    border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}
  .twk-slider::-moz-range-thumb{width:14px;height:14px;border-radius:50%;
    background:#fff;border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}

  .twk-seg{position:relative;display:flex;padding:2px;border-radius:8px;
    background:rgba(0,0,0,.06);user-select:none}
  .twk-seg-thumb{position:absolute;top:2px;bottom:2px;border-radius:6px;
    background:rgba(255,255,255,.9);box-shadow:0 1px 2px rgba(0,0,0,.12);
    transition:left .15s cubic-bezier(.3,.7,.4,1),width .15s}
  .twk-seg.dragging .twk-seg-thumb{transition:none}
  .twk-seg button{appearance:none;position:relative;z-index:1;flex:1;border:0;
    background:transparent;color:inherit;font:inherit;font-weight:500;min-height:22px;
    border-radius:6px;cursor:default;padding:4px 6px;line-height:1.2;
    overflow-wrap:anywhere}

  .twk-toggle{position:relative;width:32px;height:18px;border:0;border-radius:999px;
    background:rgba(0,0,0,.15);transition:background .15s;cursor:default;padding:0}
  .twk-toggle[data-on="1"]{background:#34c759}
  .twk-toggle i{position:absolute;top:2px;left:2px;width:14px;height:14px;border-radius:50%;
    background:#fff;box-shadow:0 1px 2px rgba(0,0,0,.25);transition:transform .15s}
  .twk-toggle[data-on="1"] i{transform:translateX(14px)}

  .twk-num{display:flex;align-items:center;box-sizing:border-box;min-width:0;height:26px;padding:0 0 0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;background:rgba(255,255,255,.6)}
  .twk-num-lbl{font-weight:500;color:rgba(41,38,27,.6);cursor:ew-resize;
    user-select:none;padding-right:8px}
  .twk-num input{flex:1;min-width:0;height:100%;border:0;background:transparent;
    font:inherit;font-variant-numeric:tabular-nums;text-align:right;padding:0 8px 0 0;
    outline:none;color:inherit;-moz-appearance:textfield}
  .twk-num input::-webkit-inner-spin-button,.twk-num input::-webkit-outer-spin-button{
    -webkit-appearance:none;margin:0}
  .twk-num-unit{padding-right:8px;color:rgba(41,38,27,.45)}

  .twk-btn{appearance:none;height:26px;padding:0 12px;border:0;border-radius:7px;
    background:rgba(0,0,0,.78);color:#fff;font:inherit;font-weight:500;cursor:default}
  .twk-btn:hover{background:rgba(0,0,0,.88)}
  .twk-btn.secondary{background:rgba(0,0,0,.06);color:inherit}
  .twk-btn.secondary:hover{background:rgba(0,0,0,.1)}

  .twk-swatch{appearance:none;-webkit-appearance:none;width:56px;height:22px;
    border:.5px solid rgba(0,0,0,.1);border-radius:6px;padding:0;cursor:default;
    background:transparent;flex-shrink:0}
  .twk-swatch::-webkit-color-swatch-wrapper{padding:0}
  .twk-swatch::-webkit-color-swatch{border:0;border-radius:5.5px}
  .twk-swatch::-moz-color-swatch{border:0;border-radius:5.5px}

  .twk-chips{display:flex;gap:6px}
  .twk-chip{position:relative;appearance:none;flex:1;min-width:0;height:46px;
    padding:0;border:0;border-radius:6px;overflow:hidden;cursor:default;
    box-shadow:0 0 0 .5px rgba(0,0,0,.12),0 1px 2px rgba(0,0,0,.06);
    transition:transform .12s cubic-bezier(.3,.7,.4,1),box-shadow .12s}
  .twk-chip:hover{transform:translateY(-1px);
    box-shadow:0 0 0 .5px rgba(0,0,0,.18),0 4px 10px rgba(0,0,0,.12)}
  .twk-chip[data-on="1"]{box-shadow:0 0 0 1.5px rgba(0,0,0,.85),
    0 2px 6px rgba(0,0,0,.15)}
  .twk-chip>span{position:absolute;top:0;bottom:0;right:0;width:34%;
    display:flex;flex-direction:column;box-shadow:-1px 0 0 rgba(0,0,0,.1)}
  .twk-chip>span>i{flex:1;box-shadow:0 -1px 0 rgba(0,0,0,.1)}
  .twk-chip>span>i:first-child{box-shadow:none}
  .twk-chip svg{position:absolute;top:6px;left:6px;width:13px;height:13px;
    filter:drop-shadow(0 1px 1px rgba(0,0,0,.3))}
`;

// ── useTweaks ───────────────────────────────────────────────────────────────
// Single source of truth for tweak values. setTweak persists via the host
// (__edit_mode_set_keys → host rewrites the EDITMODE block on disk).
function useTweaks(defaults) {
  const [values, setValues] = React.useState(defaults);
  // Accepts either setTweak('key', value) or setTweak({ key: value, ... }) so a
  // useState-style call doesn't write a "[object Object]" key into the persisted
  // JSON block.
  const setTweak = React.useCallback((keyOrEdits, val) => {
    const edits = typeof keyOrEdits === 'object' && keyOrEdits !== null ? keyOrEdits : {
      [keyOrEdits]: val
    };
    setValues(prev => ({
      ...prev,
      ...edits
    }));
    window.parent.postMessage({
      type: '__edit_mode_set_keys',
      edits
    }, '*');
    // Same-window signal so in-page listeners (deck-stage rail thumbnails)
    // can react — the parent message only reaches the host, not peers.
    window.dispatchEvent(new CustomEvent('tweakchange', {
      detail: edits
    }));
  }, []);
  return [values, setTweak];
}

// ── TweaksPanel ─────────────────────────────────────────────────────────────
// Floating shell. Registers the protocol listener BEFORE announcing
// availability — if the announce ran first, the host's activate could land
// before our handler exists and the toolbar toggle would silently no-op.
// The close button posts __edit_mode_dismissed so the host's toolbar toggle
// flips off in lockstep; the host echoes __deactivate_edit_mode back which
// is what actually hides the panel.
function TweaksPanel({
  title = 'Tweaks',
  children
}) {
  const [open, setOpen] = React.useState(false);
  const dragRef = React.useRef(null);
  const offsetRef = React.useRef({
    x: 16,
    y: 16
  });
  const PAD = 16;
  const clampToViewport = React.useCallback(() => {
    const panel = dragRef.current;
    if (!panel) return;
    const w = panel.offsetWidth,
      h = panel.offsetHeight;
    const maxRight = Math.max(PAD, window.innerWidth - w - PAD);
    const maxBottom = Math.max(PAD, window.innerHeight - h - PAD);
    offsetRef.current = {
      x: Math.min(maxRight, Math.max(PAD, offsetRef.current.x)),
      y: Math.min(maxBottom, Math.max(PAD, offsetRef.current.y))
    };
    panel.style.right = offsetRef.current.x + 'px';
    panel.style.bottom = offsetRef.current.y + 'px';
  }, []);
  React.useEffect(() => {
    if (!open) return;
    clampToViewport();
    if (typeof ResizeObserver === 'undefined') {
      window.addEventListener('resize', clampToViewport);
      return () => window.removeEventListener('resize', clampToViewport);
    }
    const ro = new ResizeObserver(clampToViewport);
    ro.observe(document.documentElement);
    return () => ro.disconnect();
  }, [open, clampToViewport]);
  React.useEffect(() => {
    const onMsg = e => {
      const t = e?.data?.type;
      if (t === '__activate_edit_mode') setOpen(true);else if (t === '__deactivate_edit_mode') setOpen(false);
    };
    window.addEventListener('message', onMsg);
    window.parent.postMessage({
      type: '__edit_mode_available'
    }, '*');
    return () => window.removeEventListener('message', onMsg);
  }, []);
  const dismiss = () => {
    setOpen(false);
    window.parent.postMessage({
      type: '__edit_mode_dismissed'
    }, '*');
  };
  const onDragStart = e => {
    const panel = dragRef.current;
    if (!panel) return;
    const r = panel.getBoundingClientRect();
    const sx = e.clientX,
      sy = e.clientY;
    const startRight = window.innerWidth - r.right;
    const startBottom = window.innerHeight - r.bottom;
    const move = ev => {
      offsetRef.current = {
        x: startRight - (ev.clientX - sx),
        y: startBottom - (ev.clientY - sy)
      };
      clampToViewport();
    };
    const up = () => {
      window.removeEventListener('mousemove', move);
      window.removeEventListener('mouseup', up);
    };
    window.addEventListener('mousemove', move);
    window.addEventListener('mouseup', up);
  };

  // data-om-starter: inert presence marker — Claude Design's starter-usage
  // probe reads it. The closed panel renders nothing, so the marker rides
  // the <html> element as an attribute instead of a rendered node — zero
  // elements added, so page CSS (even structural selectors like
  // :nth-child) can never observe it. It records that the page WIRES a
  // tweaks panel, whether or not the panel is open. Keep this effect.
  React.useEffect(() => {
    document.documentElement.setAttribute('data-om-starter', 'tweaks-panel');
    return () => document.documentElement.removeAttribute('data-om-starter');
  }, []);
  if (!open) return null;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("style", null, __TWEAKS_STYLE), /*#__PURE__*/React.createElement("div", {
    ref: dragRef,
    className: "twk-panel",
    "data-omelette-chrome": "",
    style: {
      right: offsetRef.current.x,
      bottom: offsetRef.current.y
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-hd",
    onMouseDown: onDragStart
  }, /*#__PURE__*/React.createElement("b", null, title), /*#__PURE__*/React.createElement("button", {
    className: "twk-x",
    "aria-label": "Close tweaks",
    onMouseDown: e => e.stopPropagation(),
    onClick: dismiss
  }, "\u2715")), /*#__PURE__*/React.createElement("div", {
    className: "twk-body"
  }, children)));
}

// ── Layout helpers ──────────────────────────────────────────────────────────

function TweakSection({
  label,
  children
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "twk-sect"
  }, label), children);
}
function TweakRow({
  label,
  value,
  children,
  inline = false
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: inline ? 'twk-row twk-row-h' : 'twk-row'
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-lbl"
  }, /*#__PURE__*/React.createElement("span", null, label), value != null && /*#__PURE__*/React.createElement("span", {
    className: "twk-val"
  }, value)), children);
}

// ── Controls ────────────────────────────────────────────────────────────────

function TweakSlider({
  label,
  value,
  min = 0,
  max = 100,
  step = 1,
  unit = '',
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label,
    value: `${value}${unit}`
  }, /*#__PURE__*/React.createElement("input", {
    type: "range",
    className: "twk-slider",
    min: min,
    max: max,
    step: step,
    value: value,
    onChange: e => onChange(Number(e.target.value))
  }));
}
function TweakToggle({
  label,
  value,
  onChange
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "twk-row twk-row-h"
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-lbl"
  }, /*#__PURE__*/React.createElement("span", null, label)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "twk-toggle",
    "data-on": value ? '1' : '0',
    role: "switch",
    "aria-checked": !!value,
    onClick: () => onChange(!value)
  }, /*#__PURE__*/React.createElement("i", null)));
}
function TweakRadio({
  label,
  value,
  options,
  onChange
}) {
  const trackRef = React.useRef(null);
  const [dragging, setDragging] = React.useState(false);
  // The active value is read by pointer-move handlers attached for the lifetime
  // of a drag — ref it so a stale closure doesn't fire onChange for every move.
  const valueRef = React.useRef(value);
  valueRef.current = value;

  // Segments wrap mid-word once per-segment width runs out. The track is
  // ~248px (280 panel − 28 body pad − 4 seg pad), each button loses 12px
  // to its own padding, and 11.5px system-ui averages ~6.3px/char — so 2
  // options fit ~16 chars each, 3 fit ~10. Past that (or >3 options), fall
  // back to a dropdown rather than wrap.
  const labelLen = o => String(typeof o === 'object' ? o.label : o).length;
  const maxLen = options.reduce((m, o) => Math.max(m, labelLen(o)), 0);
  const fitsAsSegments = maxLen <= ({
    2: 16,
    3: 10
  }[options.length] ?? 0);
  if (!fitsAsSegments) {
    // <select> emits strings — map back to the original option value so the
    // fallback stays type-preserving (numbers, booleans) like the segment path.
    const resolve = s => {
      const m = options.find(o => String(typeof o === 'object' ? o.value : o) === s);
      return m === undefined ? s : typeof m === 'object' ? m.value : m;
    };
    return /*#__PURE__*/React.createElement(TweakSelect, {
      label: label,
      value: value,
      options: options,
      onChange: s => onChange(resolve(s))
    });
  }
  const opts = options.map(o => typeof o === 'object' ? o : {
    value: o,
    label: o
  });
  const idx = Math.max(0, opts.findIndex(o => o.value === value));
  const n = opts.length;
  const segAt = clientX => {
    const r = trackRef.current.getBoundingClientRect();
    const inner = r.width - 4;
    const i = Math.floor((clientX - r.left - 2) / inner * n);
    return opts[Math.max(0, Math.min(n - 1, i))].value;
  };
  const onPointerDown = e => {
    setDragging(true);
    const v0 = segAt(e.clientX);
    if (v0 !== valueRef.current) onChange(v0);
    const move = ev => {
      if (!trackRef.current) return;
      const v = segAt(ev.clientX);
      if (v !== valueRef.current) onChange(v);
    };
    const up = () => {
      setDragging(false);
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("div", {
    ref: trackRef,
    role: "radiogroup",
    onPointerDown: onPointerDown,
    className: dragging ? 'twk-seg dragging' : 'twk-seg'
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-seg-thumb",
    style: {
      left: `calc(2px + ${idx} * (100% - 4px) / ${n})`,
      width: `calc((100% - 4px) / ${n})`
    }
  }), opts.map(o => /*#__PURE__*/React.createElement("button", {
    key: o.value,
    type: "button",
    role: "radio",
    "aria-checked": o.value === value
  }, o.label))));
}
function TweakSelect({
  label,
  value,
  options,
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("select", {
    className: "twk-field",
    value: value,
    onChange: e => onChange(e.target.value)
  }, options.map(o => {
    const v = typeof o === 'object' ? o.value : o;
    const l = typeof o === 'object' ? o.label : o;
    return /*#__PURE__*/React.createElement("option", {
      key: v,
      value: v
    }, l);
  })));
}
function TweakText({
  label,
  value,
  placeholder,
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("input", {
    className: "twk-field",
    type: "text",
    value: value,
    placeholder: placeholder,
    onChange: e => onChange(e.target.value)
  }));
}
function TweakNumber({
  label,
  value,
  min,
  max,
  step = 1,
  unit = '',
  onChange
}) {
  const clamp = n => {
    if (min != null && n < min) return min;
    if (max != null && n > max) return max;
    return n;
  };
  const startRef = React.useRef({
    x: 0,
    val: 0
  });
  const onScrubStart = e => {
    e.preventDefault();
    startRef.current = {
      x: e.clientX,
      val: value
    };
    const decimals = (String(step).split('.')[1] || '').length;
    const move = ev => {
      const dx = ev.clientX - startRef.current.x;
      const raw = startRef.current.val + dx * step;
      const snapped = Math.round(raw / step) * step;
      onChange(clamp(Number(snapped.toFixed(decimals))));
    };
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: "twk-num"
  }, /*#__PURE__*/React.createElement("span", {
    className: "twk-num-lbl",
    onPointerDown: onScrubStart
  }, label), /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: value,
    min: min,
    max: max,
    step: step,
    onChange: e => onChange(clamp(Number(e.target.value)))
  }), unit && /*#__PURE__*/React.createElement("span", {
    className: "twk-num-unit"
  }, unit));
}

// Relative-luminance contrast pick — checkmarks drawn over a swatch need to
// read on both #111 and #fafafa without per-option configuration. Hex input
// only (#rgb / #rrggbb); named or rgb()/hsl() colors fall through to "light".
function __twkIsLight(hex) {
  const h = String(hex).replace('#', '');
  const x = h.length === 3 ? h.replace(/./g, c => c + c) : h.padEnd(6, '0');
  const n = parseInt(x.slice(0, 6), 16);
  if (Number.isNaN(n)) return true;
  const r = n >> 16 & 255,
    g = n >> 8 & 255,
    b = n & 255;
  return r * 299 + g * 587 + b * 114 > 148000;
}
const __TwkCheck = ({
  light
}) => /*#__PURE__*/React.createElement("svg", {
  viewBox: "0 0 14 14",
  "aria-hidden": "true"
}, /*#__PURE__*/React.createElement("path", {
  d: "M3 7.2 5.8 10 11 4.2",
  fill: "none",
  strokeWidth: "2.2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  stroke: light ? 'rgba(0,0,0,.78)' : '#fff'
}));

// TweakColor — curated color/palette picker. Each option is either a single
// hex string or an array of 1-5 hex strings; the card adapts — a lone color
// renders solid, a palette renders colors[0] as the hero (left ~2/3) with the
// rest stacked in a sharp column on the right. onChange emits the
// option in the shape it was passed (string stays string, array stays array).
// Without options it falls back to the native color input for back-compat.
function TweakColor({
  label,
  value,
  options,
  onChange
}) {
  if (!options || !options.length) {
    return /*#__PURE__*/React.createElement("div", {
      className: "twk-row twk-row-h"
    }, /*#__PURE__*/React.createElement("div", {
      className: "twk-lbl"
    }, /*#__PURE__*/React.createElement("span", null, label)), /*#__PURE__*/React.createElement("input", {
      type: "color",
      className: "twk-swatch",
      value: value,
      onChange: e => onChange(e.target.value)
    }));
  }
  // Native <input type=color> emits lowercase hex per the HTML spec, so
  // compare case-insensitively. String() guards JSON.stringify(undefined),
  // which returns the primitive undefined (no .toLowerCase).
  const key = o => String(JSON.stringify(o)).toLowerCase();
  const cur = key(value);
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-chips",
    role: "radiogroup"
  }, options.map((o, i) => {
    const colors = Array.isArray(o) ? o : [o];
    const [hero, ...rest] = colors;
    const sup = rest.slice(0, 4);
    const on = key(o) === cur;
    return /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      className: "twk-chip",
      role: "radio",
      "aria-checked": on,
      "data-on": on ? '1' : '0',
      "aria-label": colors.join(', '),
      title: colors.join(' · '),
      style: {
        background: hero
      },
      onClick: () => onChange(o)
    }, sup.length > 0 && /*#__PURE__*/React.createElement("span", null, sup.map((c, j) => /*#__PURE__*/React.createElement("i", {
      key: j,
      style: {
        background: c
      }
    }))), on && /*#__PURE__*/React.createElement(__TwkCheck, {
      light: __twkIsLight(hero)
    }));
  })));
}
function TweakButton({
  label,
  onClick,
  secondary = false
}) {
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: secondary ? 'twk-btn secondary' : 'twk-btn',
    onClick: onClick
  }, label);
}
Object.assign(window, {
  useTweaks,
  TweaksPanel,
  TweakSection,
  TweakRow,
  TweakSlider,
  TweakToggle,
  TweakRadio,
  TweakSelect,
  TweakText,
  TweakNumber,
  TweakColor,
  TweakButton
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/tweaks-panel.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/vv-motion.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* ── Vivum HUD motion kit ────────────────────────────────────────────────────────────────
   One shared vocabulary for every entrance on the site, built to read like a targeting
   system acquiring a contact rather than a web page fading in.

   Non-negotiable rule this file is designed around: THE RESTING STATE IS THE CURRENT DESIGN.
   Every reveal ends at opacity 1 / transform none / filter none / clip-path none, and the
   transient HUD furniture (corner brackets, scan sweep) UNMOUNTS when the animation finishes,
   so nothing is added to the design permanently — it is only visible while arriving.

   Vocabulary
   · acquire   — the signature: 4 corner brackets snap in, a scan line sweeps the box, the
                 whole plate flickers like a CRT settling, then content resolves. For chamfered
                 Bezel containers.
   · wipe      — clip-path unfurl from the top edge. For panels and images.
   · up        — 14px rise + 6px defocus. For copy blocks and list rows.
   · line      — 1px rules draw out from their origin. For hairlines and dividers.
   · text      — per-element mask wipe with a faint chromatic offset.
   Shared easing is expo-out: machines settle, they do not bounce. */

const VV_EASE = 'cubic-bezier(.16,1,.3,1)';
const VV_HUD_CSS = `
@keyframes vvFlicker{0%{opacity:0}8%{opacity:.55}12%{opacity:.08}20%{opacity:.82}26%{opacity:.34}34%{opacity:.95}40%{opacity:.6}52%{opacity:1}58%{opacity:.86}100%{opacity:1}}
@keyframes vvScanSweep{0%{transform:translateY(-102%);opacity:0}10%{opacity:1}90%{opacity:1}100%{transform:translateY(102%);opacity:0}}
@keyframes vvBracket{0%{opacity:0;transform:scale(1.5)}30%{opacity:1}70%{opacity:1;transform:scale(1)}100%{opacity:0;transform:scale(1)}}
@keyframes vvTick{0%{opacity:0}50%{opacity:1}100%{opacity:0}}
@keyframes vvCaret{0%,49%{opacity:1}50%,100%{opacity:0}}
@keyframes vvHudPulse{0%{opacity:.25}50%{opacity:.7}100%{opacity:.25}}
@keyframes vvGlitchIn{0%{clip-path:inset(0 0 64% 0);transform:translate3d(-8px,0,0)}14%{clip-path:inset(46% 0 20% 0);transform:translate3d(7px,0,0)}26%{clip-path:inset(12% 0 57% 0);transform:translate3d(-5px,0,0)}38%{clip-path:inset(68% 0 6% 0);transform:translate3d(4px,0,0)}50%{clip-path:inset(24% 0 34% 0);transform:translate3d(-2px,0,0)}62%{clip-path:inset(0 0 0 0);transform:none}100%{clip-path:inset(0 0 0 0);transform:none}}
@keyframes vvScanLine{0%{transform:translateY(-4%);opacity:0}12%{opacity:1}88%{opacity:1}100%{transform:translateY(104%);opacity:0}}
/* The chamfered outline draws ITSELF. Bezel renders that silhouette as one continuous SVG
   path, which now carries pathLength="1", so a normalised dash can walk the real shape — cut
   corners and all — rather than a rectangle faked over the top of it. The fill mode holds the
   end state (offset 0 = the complete stroke) and the wrapper drops the hook at rest, so the
   outline returns to precisely the path the design draws.
   NB: nothing inside this literal may contain a backtick or a dollar-brace. */
@keyframes vvStrokeDraw{from{stroke-dashoffset:1}to{stroke-dashoffset:0}}
[data-vv-stroke] svg path[stroke]{stroke-dasharray:1;animation:vvStrokeDraw var(--vv-stroke-dur,760ms) cubic-bezier(.16,1,.3,1) both}
html:not(.vv-motion-live) [data-vv-stroke] svg path[stroke]{stroke-dasharray:none!important;stroke-dashoffset:0!important;animation:none!important}
@media (prefers-reduced-motion: reduce){[data-vv-stroke] svg path[stroke]{stroke-dasharray:none!important;stroke-dashoffset:0!important;animation:none!important}}
/* MASTER SAFETY GATE. The pre-reveal offsets are only ever allowed to apply once the frame
   loop has proven itself alive (see vvProveMotion below, which adds .vv-motion-live from
   inside a requestAnimationFrame that actually ran). Anywhere rAF/IntersectionObserver are
   throttled or never serviced — background tab, print and PDF export, html-to-image capture,
   headless screenshots, embedded webviews — the class is never added and every animated node
   renders at its resting design values. Hidden is additive; visible is the default. */
html:not(.vv-motion-live) [data-vv-reveal]{opacity:1!important;transform:none!important;filter:none!important;clip-path:none!important;animation:none!important;transition:none!important}
html:not(.vv-motion-live) [data-vv-hud-fx]{display:none!important}
@media (prefers-reduced-motion: reduce){[data-vv-reveal]{animation:none!important;transition:none!important;opacity:1!important;transform:none!important;filter:none!important;clip-path:none!important}[data-vv-hud-fx]{display:none!important}}`;
function vvInjectCss() {
  if (typeof document === 'undefined' || document.getElementById('vv-hud-motion')) return;
  const el = document.createElement('style');
  el.id = 'vv-hud-motion';
  el.textContent = VV_HUD_CSS;
  document.head.appendChild(el);
}
vvInjectCss();

/* Proof of life for the animation pipeline, and the one thing that must never latch shut.
   A single rAF at module load is not enough: any load that begins while the document is not
   painting (background tab, an iframe that starts hidden, session restore, a preview pane)
   never services that frame, and the CSS gate would then keep every animation disabled for
   the rest of the session. So the probe RE-ARMS — on visibilitychange, and on the first real
   user input — until a frame is actually served. `vvMotionLive` also gates the JS-side
   scramble/count-up effects, which cannot be expressed in CSS. */
let _vvLive = false;
const vvMotionLive = () => _vvLive;
const _vvLiveSubs = new Set();
(function vvProveMotion() {
  if (typeof window === 'undefined' || typeof document === 'undefined') return;
  if (typeof requestAnimationFrame !== 'function') return;
  let pending = false;
  const settle = () => {
    if (_vvLive) return;
    _vvLive = true;
    document.documentElement.classList.add('vv-motion-live');
    /* The gate lifting is a visibility change for anything still in phase 'idle', so make
       every position test re-run in the same tick: a synthetic scroll event wakes the
       listeners, and the subscribers re-render the components whose effects read the flag.
       Without this, an element that is already past the trigger line would blink out until
       the user's next scroll. */
    window.dispatchEvent(new Event('scroll'));
    _vvLiveSubs.forEach(fn => fn());
    detach();
  };
  const arm = () => {
    if (_vvLive || pending) return;
    pending = true;
    requestAnimationFrame(() => {
      pending = false;
      settle();
    });
  };
  const onVis = () => {
    if (!document.hidden) arm();
  };
  function detach() {
    document.removeEventListener('visibilitychange', onVis);
    window.removeEventListener('scroll', arm, true);
    window.removeEventListener('pointerdown', arm, true);
    window.removeEventListener('pointermove', arm, true);
    window.removeEventListener('keydown', arm, true);
    window.removeEventListener('focus', arm);
  }
  document.addEventListener('visibilitychange', onVis);
  window.addEventListener('scroll', arm, {
    passive: true,
    capture: true
  });
  window.addEventListener('pointerdown', arm, {
    passive: true,
    capture: true
  });
  window.addEventListener('pointermove', arm, {
    passive: true,
    capture: true
  });
  window.addEventListener('keydown', arm, {
    capture: true
  });
  window.addEventListener('focus', arm);
  arm();
})();

/* Subscribe to the live flag. Used by the readout components, whose effects must re-run if
   the pipeline only wakes up after they mounted. */
function useVvLive() {
  const [live, setLive] = React.useState(_vvLive);
  React.useEffect(() => {
    if (_vvLive) {
      setLive(true);
      return undefined;
    }
    const fn = () => setLive(true);
    _vvLiveSubs.add(fn);
    return () => {
      _vvLiveSubs.delete(fn);
    };
  }, []);
  return live;
}
const vvReduced = () => typeof window !== 'undefined' && window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;

/* ── Global motion settings ───────────────────────────────────────────────────────────────
   Site-wide, not per-call-site: the chamfered-frame treatment, the global speed and delay,
   and whether a reveal re-arms when it leaves the viewport are one editorial decision each,
   so they live in a small subscribable store the Tweaks panel writes to — the same device
   NavBar uses for its mobile-menu style. Reveal call sites keep saying WHAT they are (a frame,
   a paragraph, a row); the store says HOW frames behave. */
const VV_MOTION_DEFAULTS = {
  frame: 'rise',
  speed: 1.2,
  delay: 0,
  replay: false
};
let _vvOpts = {
  ...VV_MOTION_DEFAULTS,
  ...(typeof window !== 'undefined' && window.__vvMotion || {})
};
const _vvSubs = new Set();
function vvSetMotion(patch) {
  _vvOpts = {
    ..._vvOpts,
    ...patch
  };
  if (typeof window !== 'undefined') window.__vvMotion = _vvOpts;
  _vvSubs.forEach(fn => fn());
}
function useVvMotion() {
  const [, force] = React.useState(0);
  React.useEffect(() => {
    const fn = () => force(x => x + 1);
    _vvSubs.add(fn);
    return () => {
      _vvSubs.delete(fn);
    };
  }, []);
  return _vvOpts;
}

/* Reveal trigger, in order of reliability: a SYNCHRONOUS rect test first (so anything already
   on screen never waits on a callback at all), then IntersectionObserver, then a plain scroll
   listener as an independent second path. All three are position-correct — there is
   deliberately NO unconditional timer here: a timer would fire sections that are still far
   below the fold, which collapses the whole page's choreography into the first second after
   load. The "nothing ever fires" case is handled entirely by the CSS gate above, which shows
   the resting design instead of guessing at a reveal time.

   The rootMargin fires slightly before the box is fully in view — a reveal that starts exactly
   at the viewport edge always reads as late. */
function vvWhenVisible(node, cb) {
  if (!node || typeof window === 'undefined') {
    cb();
    return () => {};
  }
  let fired = false;
  let io = null;
  const inView = () => {
    const r = node.getBoundingClientRect();
    const h = window.innerHeight || document.documentElement.clientHeight || 0;
    /* One-directional on purpose: true while the box is in view AND once it has been scrolled
       PAST. A two-sided test (`r.bottom > 0 && …`) strands anything a single scroll jump
       clears — fling, scrollbar drag, an in-page anchor like #models, or reload scroll
       restoration — because it is never in view on any sampled frame. Nothing fires early:
       below-fold elements still have r.top > h at load. */
    return r.top < h * 0.9;
  };
  const cleanup = () => {
    if (io) {
      io.disconnect();
      io = null;
    }
    window.removeEventListener('scroll', onScroll, true);
    window.removeEventListener('resize', onScroll);
  };
  const fire = () => {
    if (fired) return;
    fired = true;
    cleanup();
    cb();
  };
  function onScroll() {
    if (inView()) fire();
  }
  if (inView()) {
    fire();
    return cleanup;
  }
  if (typeof IntersectionObserver !== 'undefined') {
    io = new IntersectionObserver(es => {
      if (es.some(e => e.isIntersecting)) fire();
    }, {
      rootMargin: '0px 0px -10% 0px',
      threshold: 0.05
    });
    io.observe(node);
  }
  window.addEventListener('scroll', onScroll, {
    passive: true,
    capture: true
  });
  window.addEventListener('resize', onScroll);
  return cleanup;
}

/* What a chamfered frame tells the content inside it. The frame animation is the section's
   opening gesture and the content OVERLAPS it: children add `offset` — the frame's own delay
   plus a fraction of its duration (already speed-scaled) — so the first content is moving
   while the shape is still settling. Waiting for the shape to finish reads as two separate
   animations and pushes everything downstream by a full frame duration, which is expensive on
   a long section where the content itself is a long stagger.

   `group` is for sections too tall to reveal by position — the comparison table above all.
   Its rows are spread over several screens, so triggering each on its own position means the
   user scrolls a long way to see the section assemble. A grouped frame instead hands its own
   trigger down: every descendant starts when the FRAME was reached, staggered by delay alone. */
const VV_FRAME_OVERLAP = 0.28;
const VvFrameCtx = React.createContext(null);

/* Three-state machine, not a boolean: 'idle' (pre-reveal offsets applied) → 'run'
   (transition to rest + HUD furniture mounted) → 'done' (all motion styles dropped so the
   node's computed style is byte-identical to the un-animated design, and hover transitions
   the element owns are no longer competing with an entrance transition).

   With `replay` on the machine is cyclic: leaving the viewport re-arms it back to 'idle', so
   the reveal plays again on re-entry. That needs a TWO-sided test — the opposite of the
   one-directional default — and a live observer that is never torn down, which is why the two
   modes take different paths here rather than sharing vvWhenVisible. */
function useVvReveal(delayIn = 0, durIn = 560, inherit = null, eager = false) {
  const ref = React.useRef(null);
  const opts = useVvMotion();
  const sp = opts.speed > 0 ? opts.speed : 1;
  const dur = Math.max(60, Math.round(durIn / sp));
  /* An inherited offset is already speed-scaled and already carries the panel's global delay,
     so it replaces that term rather than stacking a second copy of it. */
  const offset = inherit ? Math.round(inherit.offset || 0) : 0;
  const delay = Math.max(0, Math.round(delayIn / sp) + (offset ? 0 : Math.round(opts.delay || 0)) + offset);
  const [phase, setPhase] = React.useState(() => vvReduced() ? 'done' : 'idle');
  const replay = !!opts.replay;
  /* null = trigger on my own position. true/false = follow the grouped ancestor frame. */
  const follow = inherit && inherit.group ? !!inherit.started : null;
  React.useEffect(() => {
    if (vvReduced()) {
      setPhase('done');
      return undefined;
    }
    const node = ref.current;
    if (!node || typeof window === 'undefined') {
      setPhase('done');
      return undefined;
    }
    let t1 = 0,
      t2 = 0,
      io = null,
      running = false,
      torn = false;
    const vh = () => window.innerHeight || document.documentElement.clientHeight || 0;
    const rect = () => node.getBoundingClientRect();
    /* A grouped frame fires the whole section on a timer, so it must not wait for the box to
       be comfortably in view the way a single element does: its content can be several screens
       tall, and the stagger needs to be already running by the time the user has scrolled to
       the far end of it. Its trigger line therefore sits at the very bottom edge of the
       viewport — the moment the top of the section appears, the section starts. */
    const line = () => vh() * (eager ? 1 : 0.9);
    const past = () => rect().top < line();
    const within = () => {
      const r = rect();
      return r.bottom > 0 && r.top < line();
    };
    const clearTimers = () => {
      clearTimeout(t1);
      clearTimeout(t2);
    };
    const teardown = () => {
      if (torn) return;
      torn = true;
      if (io) {
        io.disconnect();
        io = null;
      }
      window.removeEventListener('scroll', evaluate, true);
      window.removeEventListener('resize', evaluate);
    };
    const start = () => {
      if (running) return;
      running = true;
      clearTimers();
      t1 = setTimeout(() => {
        setPhase('run');
        t2 = setTimeout(() => setPhase('done'), dur + 420);
      }, delay);
    };
    const rearm = () => {
      if (!running) return;
      running = false;
      clearTimers();
      setPhase('idle');
    };
    function evaluate() {
      if (follow !== null) {
        if (follow) start();else rearm();
        return;
      }
      if (replay) {
        if (within()) start();else rearm();
        return;
      }
      if (past()) {
        start();
        teardown();
      }
    }
    /* A grouped child needs no observer of its own — the frame is its trigger. */
    if (follow !== null) {
      evaluate();
      return () => {
        teardown();
        clearTimers();
      };
    }
    if (typeof IntersectionObserver !== 'undefined') {
      /* threshold is a fraction of the OBSERVED box, so the default 5% is a whole screenful on
         a section three screens tall — eager sections take any intersection at all. */
      io = new IntersectionObserver(evaluate, eager ? {
        threshold: 0
      } : {
        rootMargin: '0px 0px -10% 0px',
        threshold: 0.05
      });
      io.observe(node);
    }
    window.addEventListener('scroll', evaluate, {
      passive: true,
      capture: true
    });
    window.addEventListener('resize', evaluate);
    evaluate();
    return () => {
      teardown();
      clearTimers();
    };
  }, [delay, dur, replay, follow, eager]);
  return [ref, phase, dur, delay];
}
const VV_FROM = {
  acquire: {
    opacity: 0,
    transform: 'scale(0.985)'
  },
  wipe: {
    opacity: 0,
    clipPath: 'inset(0 0 100% 0)'
  },
  up: {
    opacity: 0,
    transform: 'translate3d(0,16px,0)',
    filter: 'blur(6px)'
  },
  side: {
    opacity: 0,
    transform: 'translate3d(-18px,0,0)',
    filter: 'blur(4px)'
  },
  text: {
    opacity: 0,
    transform: 'translate3d(0,10px,0)',
    clipPath: 'inset(0 0 100% 0)'
  },
  line: {
    opacity: 0,
    transform: 'scaleX(0)'
  },
  rise: {
    opacity: 0,
    transform: 'translate3d(0,28px,0) scale(0.97)'
  },
  fade: {
    opacity: 0
  },
  /* Chamfered-frame treatments, all selectable from the Tweaks panel. */
  corners: {
    opacity: 0
  },
  scanwipe: {
    opacity: 0,
    clipPath: 'inset(0 0 100% 0)'
  },
  crt: {
    opacity: 0,
    transform: 'scaleY(0.05)'
  },
  glitch: {
    opacity: 0
  },
  expand: {
    opacity: 0,
    transform: 'scale(0.94)',
    filter: 'blur(8px)'
  },
  stroke: {
    opacity: 0
  },
  strokecrt: {
    opacity: 0,
    transform: 'scaleY(0.05)'
  }
};
/* Which transient HUD furniture each frame treatment mounts while it runs. Anything not
   listed animates on transforms alone. */
const VV_FRAME_FX = {
  acquire: {
    brackets: true,
    scan: true,
    flicker: true
  },
  corners: {
    brackets: true
  },
  scanwipe: {
    scan: true
  },
  crt: {
    flicker: true
  },
  strokecrt: {
    flicker: true
  }
};
/* The chamfered-container options offered in the Tweaks panel, in menu order. */
const VV_FRAME_OPTIONS = [{
  value: 'acquire',
  label: 'Acquire — brackets + scan + flicker'
}, {
  value: 'corners',
  label: 'Corner brackets — snap in, no flicker'
}, {
  value: 'scanwipe',
  label: 'Scan wipe — line reveals as it travels'
}, {
  value: 'crt',
  label: 'CRT boot — unfolds from a line'
}, {
  value: 'stroke',
  label: 'Stroke draw — the outline draws itself'
}, {
  value: 'strokecrt',
  label: 'Stroke + CRT — unfolds as the outline draws'
}, {
  value: 'glitch',
  label: 'Glitch — sliced displacement, then settles'
}, {
  value: 'expand',
  label: 'Expand — defocused scale-up'
}, {
  value: 'wipe',
  label: 'Wipe — unfurl from the top edge'
}, {
  value: 'rise',
  label: 'Rise — quiet lift + fade'
}, {
  value: 'fade',
  label: 'Fade — opacity only'
}, {
  value: 'off',
  label: 'Off — no frame animation'
}];
/* clip-path cannot interpolate from inset() to `none`, so the wiping variants land on an
   OVERSIZED inset (clips nothing) during the transition; the `done` phase then drops the
   property entirely, leaving the element's computed style identical to the design. */
const VV_TO_CLIP = 'inset(-24% -8% -24% -8%)';
const VV_TO = {
  opacity: 1,
  transform: 'none',
  filter: 'none',
  clipPath: 'none'
};
const VV_PROPS = 'opacity,transform,filter,clip-path';

/* Transient HUD furniture. Mounted only for the duration of `run`, then gone — so the
   design at rest never carries brackets or a scan line it did not have before. */
function VvAcquireFx({
  dur,
  tone,
  brackets,
  scan
}) {
  const b = {
    position: 'absolute',
    width: 13,
    height: 13,
    borderColor: tone,
    borderStyle: 'solid',
    borderWidth: 0,
    animation: `vvBracket ${dur + 260}ms ${VV_EASE} both`
  };
  return /*#__PURE__*/React.createElement("span", {
    "data-vv-hud-fx": "",
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      inset: 0,
      pointerEvents: 'none',
      overflow: 'hidden',
      zIndex: 2
    }
  }, brackets ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      ...b,
      top: -1,
      left: -1,
      borderTopWidth: 1,
      borderLeftWidth: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      ...b,
      top: -1,
      right: -1,
      borderTopWidth: 1,
      borderRightWidth: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      ...b,
      bottom: -1,
      left: -1,
      borderBottomWidth: 1,
      borderLeftWidth: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      ...b,
      bottom: -1,
      right: -1,
      borderBottomWidth: 1,
      borderRightWidth: 1
    }
  })) : null, scan ? /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      top: 0,
      height: '42%',
      background: `linear-gradient(180deg, rgba(236,154,0,0) 0%, ${tone === 'var(--vv-amber)' ? 'rgba(236,154,0,0.10)' : 'rgba(218,232,242,0.09)'} 82%, ${tone} 100%)`,
      opacity: 0.9,
      animation: `vvScanSweep ${dur + 200}ms ${VV_EASE} both`
    }
  }) : null);
}

/* One wrapper element, one transition. `as` keeps the wrapper semantic where it matters
   (section/li/h2) so nothing extra lands in the accessibility tree.

   variant="frame" is indirect on purpose: every chamfered container asks for "the frame
   treatment" and the Tweaks panel decides which one that is, so one control restyles the
   whole page instead of nine call sites. */
function Reveal({
  variant = 'up',
  delay = 0,
  dur = 560,
  as = 'div',
  tone = 'var(--vv-amber)',
  group = false,
  style,
  children,
  ...rest
}) {
  const opts = useVvMotion();
  const inherit = React.useContext(VvFrameCtx);
  const named = variant === 'frame' ? opts.frame || 'acquire' : variant;
  const off = named === 'off';
  const [ref, rawPhase, runDur, runDelay] = useVvReveal(off ? 0 : delay, dur, inherit, group && !inherit);
  const phase = off ? 'done' : rawPhase;
  /* A frame hands the content inside it a start time PART WAY into its own animation, so the
     two overlap. With `off` there is nothing to wait for, so the offset collapses to whatever
     an outer frame already imposed. */
  const isFrame = variant === 'frame' || group;
  const frameCtx = React.useMemo(() => isFrame ? {
    offset: off ? inherit ? inherit.offset : 0 : runDelay + Math.round(runDur * VV_FRAME_OVERLAP),
    group: group || !!(inherit && inherit.group),
    started: off ? true : rawPhase !== 'idle'
  } : null, [isFrame, off, runDelay, runDur, group, rawPhase, inherit]);
  const Tag = as;
  const from = VV_FROM[named] || VV_FROM.up;
  const fx = VV_FRAME_FX[named] || {};
  const anim = named === 'glitch' ? `vvGlitchIn ${Math.min(runDur, 640)}ms steps(1,end) both` : fx.flicker ? `vvFlicker ${Math.min(runDur, 520)}ms steps(1,end) both` : undefined;
  const motion = phase === 'done' ? null : phase === 'idle' ? {
    ...from,
    willChange: VV_PROPS
  } : {
    ...VV_TO,
    ...(from.clipPath ? {
      clipPath: VV_TO_CLIP
    } : null),
    transition: VV_PROPS.split(',').map(p => `${p} ${runDur}ms ${VV_EASE}`).join(', '),
    willChange: VV_PROPS,
    animation: anim
  };
  const needsHost = (fx.brackets || fx.scan) && phase === 'run';
  /* The two stroke treatments reach DOWN into the chamfered outline rather than mounting
     anything: this hook is what the [data-vv-stroke] rule keys off, and the drawing duration
     rides the same speed control as everything else. */
  const drawsStroke = (named === 'stroke' || named === 'strokecrt') && phase === 'run';
  return /*#__PURE__*/React.createElement(Tag, _extends({
    ref: ref,
    "data-vv-reveal": named
  }, drawsStroke ? {
    'data-vv-stroke': ''
  } : null, {
    style: {
      ...style,
      ...motion,
      ...(drawsStroke ? {
        '--vv-stroke-dur': `${runDur + 220}ms`
      } : null),
      ...(needsHost && !(style && style.position) ? {
        position: 'relative'
      } : null),
      ...(named === 'line' && phase !== 'done' ? {
        transformOrigin: 'left center'
      } : null)
    }
  }, rest), frameCtx ? /*#__PURE__*/React.createElement(VvFrameCtx.Provider, {
    value: frameCtx
  }, children) : children, needsHost ? /*#__PURE__*/React.createElement(VvAcquireFx, {
    dur: runDur,
    tone: tone,
    brackets: fx.brackets,
    scan: fx.scan
  }) : null);
}

/* The trigger half of stacked sequencing. RevealSeq shifts a subtree's CLOCK, but on its own
   that is not sequencing: its children still trigger on their own position, so an element a
   screen further down starts its offset only once the user has already scrolled to it — the
   offset becomes a wait in front of content that is on screen and blank, which is exactly the
   failure this pass was meant to remove.

   RevealGroup supplies the missing half. It animates nothing itself: it watches its own box
   with the eager trigger (the moment its top edge appears) and hands every descendant a
   grouped context, so the whole section runs off ONE start time and the RevealSeq offsets
   become real ordering. Grouped and shifted together, a stacked section assembles top to
   bottom from the instant its top edge is visible, which is why the offsets below are sized to
   the ramp above them rather than to how far the user has scrolled.

   `on` is the layout switch: side by side there is nothing to sequence, so it passes straight
   through and the section keeps per-element position triggers. */
function RevealGroup({
  on = true,
  style,
  children
}) {
  const opts = useVvMotion();
  const inherit = React.useContext(VvFrameCtx);
  const [ref, phase] = useVvReveal(0, 60, null, true);
  const value = React.useMemo(() => ({
    offset: inherit ? inherit.offset || 0 : Math.round(opts.delay || 0),
    group: true,
    started: phase !== 'idle'
  }), [inherit, opts.delay, phase]);
  return /*#__PURE__*/React.createElement("div", {
    ref: ref,
    style: style
  }, on ? /*#__PURE__*/React.createElement(VvFrameCtx.Provider, {
    value: value
  }, children) : children);
}

/* Column ordering once a two-column section stacks. Side by side, the two columns run their
   delay ramps in PARALLEL on purpose — the eye takes both in at once, and making the right
   column wait for the left would leave half the section visibly dead. Stacked on a phone the
   same two ramps are one vertical sequence, and parallel ramps read as out of order: the right
   column's ramp starts near 0 while the left column's copy is still waiting behind its eyebrow
   and headline, so any scroll that brings both columns into view on one frame reveals the
   lower column first. RevealSeq pushes a subtree's whole ramp past a given point without
   touching the individual delays, so the same markup sequences correctly in either layout.

   It only shifts the clock — the children still trigger on their own position (or on a grouped
   ancestor, which is passed straight through), so a long stacked column still reveals as the
   user reaches it rather than all at once. */
function RevealSeq({
  ms = 0,
  children
}) {
  const inherit = React.useContext(VvFrameCtx);
  const opts = useVvMotion();
  const sp = opts.speed > 0 ? opts.speed : 1;
  /* An inherited offset already carries the global delay; standing alone we add it ourselves,
     because a non-zero offset makes useVvReveal drop its own copy of that term. */
  const value = React.useMemo(() => ({
    offset: Math.round((inherit ? inherit.offset || 0 : Math.round(opts.delay || 0)) + ms / sp),
    group: !!(inherit && inherit.group),
    started: inherit ? inherit.started : true
  }), [inherit, ms, sp, opts.delay]);
  if (!ms) return children;
  return /*#__PURE__*/React.createElement(VvFrameCtx.Provider, {
    value: value
  }, children);
}

/* Character-decode readout — the same idiom as the Core-technology slider's typewriter
   caption, generalised. Glyphs land left to right; unresolved ones churn through a mono
   scramble set, so a headline arrives the way a callsign resolves on a radar display.
   Ends on the exact source string, and the DOM node is plain text at rest.

   The resting string is the INITIAL state, not the end state: if the frame loop is dead the
   label simply reads correctly and never scrambles. The scramble is entered from a layout
   effect (before paint) so a live browser never flashes the resolved text first. */
const VV_GLYPHS = '▚▞░▓█/\\|<>_=+*#0123456789ABCDEFGHJKLMNPQRSTUVWXYZ';
const vvLayout = typeof window === 'undefined' ? React.useEffect : React.useLayoutEffect;
function HudText({
  text,
  delay: delayIn = 0,
  speed = 26,
  as = 'span',
  caret = false,
  style,
  ...rest
}) {
  const str = String(text == null ? '' : text);
  const ref = React.useRef(null);
  const [shown, setShown] = React.useState(str);
  const [live, setLive] = React.useState(false);
  const pipeLive = useVvLive();
  const inherit = React.useContext(VvFrameCtx);
  const delay = delayIn + (inherit ? Math.round(inherit.offset || 0) : 0);
  vvLayout(() => {
    if (vvReduced() || !pipeLive || typeof requestAnimationFrame !== 'function') return undefined;
    let raf = 0,
      t0 = 0,
      start = 0;
    const blank = () => {
      let s = '';
      for (let i = 0; i < str.length; i += 1) s += str[i] === ' ' || str[i] === '\n' ? str[i] : ' ';
      return s;
    };
    const stop = vvWhenVisible(ref.current, () => {
      setShown(blank());
      t0 = setTimeout(() => {
        setLive(true);
        start = performance.now();
        const step = now => {
          const solved = Math.floor((now - start) / speed);
          if (solved >= str.length) {
            setShown(str);
            setLive(false);
            return;
          }
          let out = '';
          for (let i = 0; i < str.length; i += 1) {
            const c = str[i];
            if (i < solved || c === ' ' || c === '\n') out += c;else if (i < solved + 6) out += VV_GLYPHS[Math.random() * VV_GLYPHS.length | 0];else out += ' ';
          }
          setShown(out);
          raf = requestAnimationFrame(step);
        };
        raf = requestAnimationFrame(step);
      }, delay);
    });
    /* Hard floor — the label is correct past this point whatever happened. Bounded by the
       readout's own run length, so it can only ever rescue a scramble that has ALREADY
       started; it never reveals a section the user has not reached. */
    const floor = setTimeout(() => {
      setShown(str);
      setLive(false);
    }, 1200 + delay + str.length * speed);
    return () => {
      stop();
      clearTimeout(t0);
      clearTimeout(floor);
      cancelAnimationFrame(raf);
    };
  }, [str, delay, speed, pipeLive]);
  const Tag = as;
  return /*#__PURE__*/React.createElement(Tag, _extends({
    ref: ref,
    style: style
  }, rest), shown, caret && live ? /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: 'inline-block',
      width: '0.5em',
      height: '1em',
      marginLeft: '0.06em',
      background: 'var(--vv-amber)',
      verticalAlign: '-0.12em',
      animation: 'vvCaret 480ms steps(1,end) infinite'
    }
  }) : null);
}

/* Instrument count-up. Parses the numeric core out of a display string ("99.2%", "3×",
   "< 5 ms") and rolls only that, so units, symbols and spacing are preserved verbatim and
   the resting label is character-identical to the design. */
function HudNumber({
  value,
  delay: delayIn = 0,
  dur = 1100,
  as = 'span',
  style,
  ...rest
}) {
  const str = String(value == null ? '' : value);
  const m = str.match(/-?\d+(?:[.,]\d+)?/);
  const ref = React.useRef(null);
  const [out, setOut] = React.useState(str);
  const pipeLive = useVvLive();
  const inherit = React.useContext(VvFrameCtx);
  const delay = delayIn + (inherit ? Math.round(inherit.offset || 0) : 0);
  vvLayout(() => {
    if (!m || vvReduced() || !pipeLive || typeof requestAnimationFrame !== 'function') return undefined;
    const target = parseFloat(m[0].replace(',', '.'));
    const decimals = (m[0].split(/[.,]/)[1] || '').length;
    let raf = 0,
      t0 = 0,
      start = 0;
    const stop = vvWhenVisible(ref.current, () => {
      setOut(str.replace(m[0], m[0].replace(/\d/g, '0')));
      t0 = setTimeout(() => {
        start = performance.now();
        const step = now => {
          const p = Math.min(1, (now - start) / dur);
          const eased = 1 - Math.pow(1 - p, 3);
          const n = (target * eased).toFixed(decimals);
          setOut(str.replace(m[0], m[0].includes(',') ? n.replace('.', ',') : n));
          if (p < 1) raf = requestAnimationFrame(step);else setOut(str);
        };
        raf = requestAnimationFrame(step);
      }, delay);
    });
    /* Hard floor — the figure reads its real value past this point whatever happened. It only
       ever rescues a roll that has already started: the initial state IS the real value. */
    const floor = setTimeout(() => setOut(str), 1200 + delay + dur);
    return () => {
      stop();
      clearTimeout(t0);
      clearTimeout(floor);
      cancelAnimationFrame(raf);
    };
  }, [str, delay, dur, pipeLive]);
  const Tag = as;
  return /*#__PURE__*/React.createElement(Tag, _extends({
    ref: ref,
    style: {
      fontVariantNumeric: 'tabular-nums',
      ...style
    }
  }, rest), out);
}
Object.assign(window, {
  vvReduced,
  vvWhenVisible,
  Reveal,
  RevealSeq,
  RevealGroup,
  HudText,
  HudNumber,
  useVvReveal,
  useVvMotion,
  useVvLive,
  vvSetMotion,
  vvMotionLive,
  VV_FRAME_OPTIONS,
  VV_EASE,
  VV_HUD_CSS
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/vv-motion.jsx", error: String((e && e.message) || e) }); }

__ds_ns.Badge = __ds_scope.Badge;

__ds_ns.Button = __ds_scope.Button;

__ds_ns.Divider = __ds_scope.Divider;

__ds_ns.Eyebrow = __ds_scope.Eyebrow;

__ds_ns.Icon = __ds_scope.Icon;

__ds_ns.IconButton = __ds_scope.IconButton;

__ds_ns.Panel = __ds_scope.Panel;

__ds_ns.DataTable = __ds_scope.DataTable;

__ds_ns.RatioBar = __ds_scope.RatioBar;

__ds_ns.Stat = __ds_scope.Stat;

__ds_ns.StatusDot = __ds_scope.StatusDot;

__ds_ns.EmptyState = __ds_scope.EmptyState;

__ds_ns.Modal = __ds_scope.Modal;

__ds_ns.Toast = __ds_scope.Toast;

__ds_ns.Checkbox = __ds_scope.Checkbox;

__ds_ns.Input = __ds_scope.Input;

__ds_ns.Select = __ds_scope.Select;

__ds_ns.Switch = __ds_scope.Switch;

__ds_ns.Footer = __ds_scope.Footer;

__ds_ns.NavBar = __ds_scope.NavBar;

__ds_ns.SideNav = __ds_scope.SideNav;

__ds_ns.Bezel = __ds_scope.Bezel;

__ds_ns.GradientField = __ds_scope.GradientField;

__ds_ns.SectionFrame = __ds_scope.SectionFrame;

__ds_ns.Sheet = __ds_scope.Sheet;

__ds_ns.VideoBackdrop = __ds_scope.VideoBackdrop;

__ds_ns.HudOutline = __ds_scope.HudOutline;

})();
