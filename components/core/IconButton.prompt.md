One-line: Icon-only action for toolbars, table rows, and panel headers.

```jsx
<IconButton icon="settings" label="Settings" />
<IconButton icon="pause" label="Pause stream" variant="ghost" active />
```

Use `ghost` inside dense rows so the chrome only appears on hover; `outline` in toolbars where the target must be visible at rest.
