One-line: Big figure + uppercase label — the only way this system presents a metric.

```jsx
<Stat value="4.2" unit="ms" label="Inference latency" delta="-31% vs. baseline" />
<Stat value="118" label="Edge units live" size="lg" />
```

Lay stats out in a grid with hairline dividers between columns; never put each stat in its own bordered card.
