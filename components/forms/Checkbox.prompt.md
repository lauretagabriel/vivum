One-line: Square multi-select control; amber fill when checked.

```jsx
<Checkbox label="Stream telemetry to ground" hint="Adds 40 kbps uplink" defaultChecked />
```

Group with 12px gaps. For a single on/off setting inside a panel, use `Switch` instead.
