One-line: The system's text field — uppercase label, 4% wash, blue focus ring, amber error.

```jsx
<Input label="Fleet ID" placeholder="vv-fleet-000" icon="search" />
<Input label="Endpoint" error="Unreachable from this region" defaultValue="edge-04.vivum.ai" />
```

Amber is the error color here — that use does not count against the one-accent rule, because errors are rare.
