One-line: Dropdown matching Input chrome exactly — use it for 4+ mutually exclusive options.

```jsx
<Select label="Deployment target" options={['Air', 'Land', 'Sea', 'Space', 'Edge']} defaultValue="Edge" />
```

For two or three options prefer a segmented row of `Button variant="secondary"`.
