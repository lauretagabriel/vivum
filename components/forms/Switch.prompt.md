One-line: Immediate-effect on/off control, laid out label-left / switch-right inside settings panels.

```jsx
<Switch label="Autonomous fallback" hint="Run on-device when the link drops" defaultChecked />
```

The track is rectangular, not a pill — the brand keeps 2px corners everywhere. On means blue, never amber.
