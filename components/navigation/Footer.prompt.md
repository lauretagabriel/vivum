One-line: Page footer — a base bloom over the canvas, gray mark, up to four link columns, legal line and the "TECHNOLOGY EVOLVED" tagline.

```jsx
<Footer markSrc="assets/vivum-mark-gray.svg" tagline="Evolutionary AI for the edge."
  columns={[{ label: 'Platform', links: [{ label: 'Overview' }, { label: 'Edge runtime' }] }]} />
```

The tagline lock-up at bottom-right is fixed brand furniture — keep it.
